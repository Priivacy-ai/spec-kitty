{
  "release_train": "3.2.0",
  "authority": "spec-kitty-cli release authority for shared package compatibility",
  "packages": [
    {
      "package": "spec-kitty-events",
      "source_repo": "Priivacy-ai/spec-kitty-events",
      "cli_range": "",
      "locked_version": "8.0.0",
      "contract_surface": "volatile mission/WP families + zeitgeist attrs codecs; sync/legacy/cutover deleted",
      "resolution_note": "8.0.0 is not yet published to an index (EXPERIMENTAL-spec-kitty-planning#31); the CLI pins it via a PEP 508 direct git reference on github.com at a full-SHA rev instead of a version range (PROGRAM.md §2 wheel-installability exception). An empty cli_range matches the direct reference's empty specifier; move back to a bounded range once 8.0.0 is published."
    },
    {
      "package": "spec-kitty-tracker",
      "source_repo": "spec-kitty/EXPERIMENTAL-spec-kitty-tracker",
      "cli_range": "",
      "locked_version": "0.5.2",
      "contract_surface": "tracker discovery and sync SDK",
      "resolution_note": "For the programme, the CLI pins this package via a PEP 508 direct git reference on github.com at a full-SHA rev instead of a version range (PROGRAM.md §2; EXPERIMENTAL-spec-kitty-planning#31). An empty cli_range matches the direct reference's empty specifier."
    }
  ],
  "retired_packages": [
    {
      "package": "spec-kitty-runtime",
      "status": "retired-for-cli-release"
    }
  ],
  "consumers": [
    {
      "consumer": "spec-kitty-saas",
      "contract_path": "contracts/consumer-compatibility.json",
      "required_before_publish": true
    }
  ]
}
