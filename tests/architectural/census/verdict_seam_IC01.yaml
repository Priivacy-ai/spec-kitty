# tests/architectural/census/verdict_seam_IC01.yaml
#
# WP01 (review-cycle-verdict-seam-rebuild-01KZ2W7W) -- the verdict-seam census's
# LITERAL expected-set fixture (FR-020): tests/architectural/test_verdict_seam_census.py
# loads this file at collection time and compares its AST-derived writer/resolver/
# reader sets against exactly the `status: active` rows below, per category. This is
# NOT decorative prose -- editing a row changes the check's pass/fail outcome.
#
# Revised post-review (opus pass 1): F1 tightened the resolver predicate to be
# ARTIFACT-anchored (review-cycle-*.md), not directory-anchored (tasks/<wp>/), per
# ADR 2026-08-03-1 -- dropping implement_capture_baseline / _review_baseline_context_lines
# / review_context_for_repo_root_workspace / build_review_prompt_lines (baseline-tests.json
# and bare WP.md globs are a DIFFERENT artifact kind sharing the same directory). F2 added a
# second scope leg keyed on ReviewResult/ReviewOverride construction, reaching
# orchestrator_api/commands.py's external ingress and migration/backfill_runtime_state.py's
# legacy-frontmatter reconstruction, neither of which mentions review-cycle/review_cycle text.
# F3 added workflow.py::review (an independent cycle-number allocation + mkdir, a genuine
# FR-006 numbering-divergence risk). F4 added a narrow cross-module TRANSLATOR pass (a
# try/except ValueError/OSError wrapping an already-reader-classified call), reaching
# post_merge/review_artifact_consistency.py::find_rejected_review_artifact_conflicts as a
# reader (the merge gate's actual fail-closed polarity site). F5 tightened the writer's
# filename-literal leg to skip print()-only mentions, dropping
# implement_try_render_fix_mode_prompt from writer (its reader row is legitimate and stays).
#
# Every row is `status: active` (this WP retires nothing -- T002's `retire` +
# `retiring_fr` shape is exercised by WP07/WP08/WP14's later fragments, e.g.
# verdict_seam_IC07.yaml / verdict_seam_IC08.yaml / verdict_seam_IC14.yaml, and folded
# into tests/architectural/verdict_seam_census.yaml by WP16/IC-12 -- see
# test_verdict_seam_census.py's module docstring).
#
# Schema: category (writer|resolver|reader), module (repo-relative path), function
# (bare name, or Class.method), status (active|retire), retiring_fr (required, and
# must name a landed FR, only when status is retire).

- category: writer
  module: src/specify_cli/cli/commands/agent/tasks_materialization.py
  function: _persist_review_artifact_override
  status: active
- category: writer
  module: src/specify_cli/cli/commands/agent/tasks_move_task.py
  function: _mt_emit_transitions
  status: active
- category: writer
  module: src/specify_cli/cli/commands/agent/tasks_move_task.py
  function: _mt_finalize_plan
  status: active
- category: writer
  module: src/specify_cli/cli/commands/agent/tasks_move_task.py
  function: _mt_hop_review_result
  status: active
- category: writer
  module: src/specify_cli/cli/commands/agent/tasks_move_task.py
  function: _mt_plan_review_result
  status: active
- category: writer
  # WP06 (review-cycle-verdict-seam-rebuild-01KZ2W7W): the OLD-timing
  # review-artifact override write moved here from ``tasks_move_task.py``'s
  # ``_mt_fire_override_persist``, which is now a pure forwarder with no
  # write-shaped call in its own body -- correctly no longer writer-classified
  # (its row, and ``_mt_run_decision``'s row -- promoted only via the
  # same-module one-hop closure through ``_mt_fire_override_persist`` -- are
  # both removed from this fixture in the same change).
  module: src/specify_cli/cli/commands/agent/tasks_verdict_persistence.py
  function: persist_review_override_before_guard
  status: active
- category: writer
  # WP11 (review-cycle-verdict-seam-rebuild-01KZ2W7W, T048, DM-01KZ6JE62Q6CQ24DMBX8KZZ5R9):
  # the revert-commit compensator -- deletes an already-committed review-cycle
  # artifact and commits the deletion (via safe_commit) when a LATER transition-
  # emit failure means the write it undoes should never have survived (FR-002).
  # Genuinely a writer (a real git-commit call site), not a duplicate of WP10's
  # compensator in review/cycle.py (which unlinks an UNCOMMITTED file before any
  # commit exists, from inside the writer's own call -- see this function's own
  # docstring for the full distinction). Disclosed here per the WP01 census's
  # standing cross-WP-edit precedent (WP08/WP10 both added rows the same way).
  module: src/specify_cli/cli/commands/agent/tasks_verdict_persistence.py
  function: revert_committed_verdict_write
  status: active
- category: writer
  module: src/specify_cli/cli/commands/agent/workflow.py
  function: review
  status: active
- category: writer
  module: src/specify_cli/migration/backfill_runtime_state.py
  function: _review_from_frontmatter
  status: active
- category: writer
  module: src/specify_cli/migration/backfill_runtime_state.py
  function: read_legacy_runtime
  status: active
- category: writer
  module: src/specify_cli/orchestrator_api/commands.py
  function: _parse_review_result_json
  status: active
- category: writer
  module: src/specify_cli/orchestrator_api/commands.py
  function: transition
  status: active
# WP12 (review-cycle-verdict-seam-rebuild-01KZ2W7W, T051, FR-009): the
# ``writer`` row for ``review/arbiter.py::_persist_in_artifact`` is REMOVED
# here -- deleted outright, retiring the ``arbiter_override``
# frontmatter-stamping path into the event-sourced ``ReviewOverride`` emit
# (representation #1; verdict_seam_IC08.yaml's own retire row names this
# exact function under FR-009). Row removed, not merely relabelled.
# Disclosed cross-WP edit to a file WP01 owns, per this fixture's own
# WP06/WP10/WP11 precedent (see this WP's final report).
- category: writer
  # WP12: ``persist_arbiter_decision`` STAYS writer-classified (unchanged
  # row) -- its new body calls ``_persist_review_artifact_override(
  # artifact_path, ...)``, whose name contains "persist" and whose first
  # positional arg's display name contains "artifact", so
  # ``_has_artifact_named_arg`` still matches. No shrinkage here; kept for
  # the record so a reviewer sees this row was considered, not overlooked.
  module: src/specify_cli/review/arbiter.py
  function: persist_arbiter_decision
  status: active
- category: writer
  module: src/specify_cli/review/artifacts.py
  function: ReviewCycleArtifact.write
  status: active
- category: writer
  module: src/specify_cli/review/cycle.py
  function: create_rejected_review_cycle
  status: active
- category: writer
  # WP10 (review-cycle-verdict-seam-rebuild-01KZ2W7W, T041/T043): extracted
  # from create_rejected_review_cycle to keep cycle-number allocation, the
  # write, and post-write validation inside ONE `with feature_status_lock`
  # critical section (FR-005/NFR-006) without pushing the parent function
  # past the C901 complexity ceiling. It builds/writes a
  # `review-cycle-{cycle_n}.md`-shaped filename and calls `artifact.write`,
  # so the AST writer predicate correctly detects it as a genuinely new
  # writer -- this is a disclosed row addition (cross-WP edit to a file
  # WP01 owns), not a silent one; see this WP's final report.
  module: src/specify_cli/review/cycle.py
  function: _allocate_and_write_review_cycle_locked
  status: active
- category: resolver
  module: src/specify_cli/cli/commands/agent/tasks_materialization.py
  function: _persist_review_feedback
  status: active
- category: resolver
  module: src/specify_cli/cli/commands/agent/tasks_materialization.py
  function: _resolve_wp_slug
  status: active
- category: resolver
  module: src/specify_cli/cli/commands/agent/workflow.py
  function: review
  status: active
- category: resolver
  module: src/specify_cli/cli/commands/agent/workflow_cores.py
  function: has_prior_rejection
  status: active
- category: resolver
  module: src/specify_cli/cli/commands/agent/workflow_executor.py
  function: implement_try_render_fix_mode_prompt
  status: active
- category: resolver
  module: src/specify_cli/post_merge/review_artifact_consistency.py
  function: _artifact_dirs_for_wp
  status: active
- category: resolver
  module: src/specify_cli/post_merge/review_artifact_consistency.py
  function: find_rejected_review_artifact_conflicts
  status: active
# WP12 (review-cycle-verdict-seam-rebuild-01KZ2W7W, T051-T053, FR-009): the
# ``resolver`` rows for ``review/arbiter.py::_find_review_cycle_artifact``,
# ``get_arbiter_overrides_for_wp``, and ``persist_arbiter_decision`` are
# REMOVED here (three rows). ``_find_review_cycle_artifact`` is deleted
# outright (T053: its bare-``wp_id`` join and lexicographic-sort bugs are
# retired, not fixed in place -- resolution now routes through
# ``_resolve_wp_slug`` + ``review/cycle.py::_review_cycle_wp_dir``, called
# from a DIFFERENT module, so the same-module one-hop closure this census
# uses does not promote the caller). ``get_arbiter_overrides_for_wp`` and
# ``persist_arbiter_decision`` both survive by name (real callers outside
# this WP's ``owned_files`` depend on them) but their NEW bodies no longer
# do a ``tasks``-join or parse frontmatter themselves -- they delegate to
# other-module resolvers/the event-sourced snapshot, so neither still
# matches this category's AST shape. verdict_seam_IC08.yaml's own retire
# rows name all three under FR-009. Disclosed cross-WP edit to a file WP01
# owns, per this fixture's own WP06/WP10/WP11 precedent (see this WP's
# final report).
- category: resolver
  module: src/specify_cli/review/cycle.py
  function: _review_cycle_wp_dir
  status: active
- category: resolver
  module: src/specify_cli/review/cycle.py
  function: create_rejected_review_cycle
  status: active
- category: resolver
  module: src/specify_cli/review/cycle.py
  function: resolve_review_cycle_pointer
  status: active
- category: reader
  module: src/specify_cli/agent_utils/status.py
  function: _get_wp_review_verdict
  status: active
- category: reader
  module: src/specify_cli/agent_utils/status.py
  function: show_kanban_status
  status: active
- category: reader
  module: src/specify_cli/cli/commands/agent/tasks_parsing_validation.py
  function: _apply_review_status_flags
  status: active
- category: reader
  module: src/specify_cli/cli/commands/agent/tasks_parsing_validation.py
  function: _get_latest_review_cycle_verdict
  status: active
- category: reader
  module: src/specify_cli/cli/commands/agent/workflow_cores.py
  function: resolve_review_feedback_pointer
  status: active
- category: reader
  module: src/specify_cli/cli/commands/agent/workflow_executor.py
  function: implement_try_render_fix_mode_prompt
  status: active
- category: reader
  module: src/specify_cli/post_merge/review_artifact_consistency.py
  function: find_rejected_review_artifact_conflicts
  status: active
# WP12 (review-cycle-verdict-seam-rebuild-01KZ2W7W, T051-T052, FR-009): the
# ``reader`` rows for ``review/arbiter.py::_persist_in_artifact``,
# ``get_arbiter_overrides_for_wp``, and ``persist_arbiter_decision`` are
# REMOVED here (three rows) -- same reasoning as the ``resolver`` removal
# above: ``_persist_in_artifact`` is deleted outright, and the surviving
# ``get_arbiter_overrides_for_wp`` / ``persist_arbiter_decision`` no longer
# parse frontmatter/JSON themselves (the manual YAML parse this census's
# reader shape detected was ALSO spec.md User Story 4's uncaught-crash
# defect -- retiring it closes that incidentally, though FR-012/reader
# failure-polarity remains WP14's owned concern, not claimed here).
# verdict_seam_IC08.yaml's own retire rows name all three under FR-009.
# Disclosed cross-WP edit to a file WP01 owns, per this fixture's own
# WP06/WP10/WP11 precedent (see this WP's final report).
- category: reader
  module: src/specify_cli/review/artifacts.py
  function: ReviewCycleArtifact.from_file
  status: active
- category: reader
  module: src/specify_cli/review/artifacts.py
  function: ReviewCycleArtifact.latest
  status: active
- category: reader
  module: src/specify_cli/review/artifacts.py
  function: latest_review_artifact_verdict
  status: active
- category: reader
  module: src/specify_cli/review/artifacts.py
  function: rejected_review_artifact_for_terminal_lane
  status: active
- category: reader
  module: src/specify_cli/review/cycle.py
  function: _guard_feedback_source_provenance
  status: active
- category: reader
  # WP10 (review-cycle-verdict-seam-rebuild-01KZ2W7W, T041): same extraction
  # as the writer row above -- the same-module one-hop closure promotes it
  # to reader because it calls validate_review_artifact_file (already
  # reader-classified) by bare name within cycle.py. Disclosed row addition.
  module: src/specify_cli/review/cycle.py
  function: _allocate_and_write_review_cycle_locked
  status: active
- category: reader
  module: src/specify_cli/review/cycle.py
  function: create_rejected_review_cycle
  status: active
- category: reader
  module: src/specify_cli/review/cycle.py
  function: resolve_review_cycle_pointer
  status: active
- category: reader
  module: src/specify_cli/review/cycle.py
  function: validate_review_artifact_file
  status: active
