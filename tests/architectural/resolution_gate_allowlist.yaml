# Resolution-authority gate allowlist — DESIGN-P (frozen tool-derived comparand).

# Each entry's authoritative comparand is the composite key
# ``(file, qualname, token)``. ``token`` is a FROZEN, tool-derived
# ``code_tokens_by_line`` string (via the WP01 freeze_converter — never typed by
# hand). ``line:`` is a NON-AUTHORITATIVE locator (jump-to / diagnostics only); no
# comparison, set-membership, or count logic reads it.

# FRESHEN PROCEDURE (legitimate site edit moved the token): re-run the gate
# ``pytest tests/architectural/test_resolution_authority_gates.py``; a stale entry
# fails with the evict-or-re-approve message that PRINTS the nearest live token for
# the qualname — copy that token into ``token:`` (and refresh ``line:``). Do NOT
# hand-author tokens; re-run freeze_converter.py if bulk re-derivation is needed.

# COLLISION (speculative surface, zero real users today): if two live sites in one
# function share an identical token and the gate must require an exact occurrence
# count, add ``count: N`` to the entry (default 1 covers any number of matches).
# read-surface-ssot-closeout WP04 (T017/T018): coord_authority allowlist
# shrunk 7 -> 3 (the 4 workflow.py entries drained; see the coord_authority
# header comment below for the routing detail). SHRINK-ONLY: baseline must
# never rise back above the honest live entry count.
canonicalizer_baseline: 3
coord_authority_baseline: 4

canonicalizer:
- qualname: _canonicalize_bare_modern_handle
  line: 453
  rationale: "C-001/FR-011 sanctioned bare probe inside _canonicalize_bare_modern_handle (the FR-004 bare-human-slug fold) - folding canonicalization into the primitive would recurse; PERMANENT fixture, never remove"
  file: src/specify_cli/missions/_read_path_resolver.py
  token: literal_primary = primary_feature_dir_for_mission ( repo_root , handle )
- qualname: read_primary_meta
  line: 819
  rationale: "WP03 SANCTION: seam-internal first-probe in read_primary_meta - callers (resolve_handle_to_read_path:929, _stored_topology_best_effort:1203) pass already-canonicalized handles; the raw first-probe is intentional (try literal dir, then _canonicalize_handle fallback in body); wrapping here would double-fold the composed handle; PERMANENT fixture"
  file: src/specify_cli/missions/_read_path_resolver.py
  token: primary_dir = primary_feature_dir_for_mission ( repo_root , handle )
- qualname: MissionStatus._find_meta_path
  line: 533
  rationale: "WP05 SANCTION: bare_dir_name is returned by resolve_bare_modern_mission_dir_name (called on line 531) which returns meta_path.parent.name -- the actual on-disk composed dir name (e.g. 'demo-feature-01ABCDEF'). This is a feature_dir.name-equivalent; the gate cannot detect it via def-use (bare_dir_name is a local str variable, not a .name attribute read) but the invariant is guaranteed by the callee's implementation and docstring. Already-canonical by provenance."

  file: src/specify_cli/status/aggregate.py
  token: composed_primary = primary_feature_dir_for_mission ( repo_root , bare_dir_name )
coord_authority:
  # WP02 (T008/T014): ``mark_status:1807`` is ROUTED -- its write leg now uses the
  # kind-aware resolve_planning_read_dir(kind=TASKS_INDEX) (closes #2154), so it is
  # no longer a kind-blind resolve_feature_dir_for_mission write and its entry is
  # removed (the NFR-003 staleness twin-guard would otherwise red-fail on it).
  # The remaining tasks.py / implement.py coord-authority entries below are
  # line-drift RE-PINS only (same kind-blind write site, new line after WP edits
  # shifted the file) -- NOT routed, still pre-sweep baseline for WP02-WP07.
  # tasks-py-degod WP09 DRAIN: move_task:1138 & move_task:1396 removed — WP06
  # (03be8db6e) rewrote move_task as a thin orchestrator; merged move_task has zero
  # resolve_feature_dir_for_mission calls (cross-base artifact in header). list_dependents:3568
  # removed — WP08 (2dda443d5) folded its probe to primary; merged list_dependents
  # has zero resolve calls.
  # tasks-py-degod-wave2 WP09 DRAIN: list_tasks:2768 & validate_workflow:3836
  # removed — the wave-2 WP04 render-seam unification routed both bodies' inline
  # json.dumps emission through Render.json_envelope, removing the ``dumps``
  # write-indicator; the surviving kind-blind STATUS-partition probes
  # (tasks.py:727 / :1042) are READ-classified now (header block above).
  # read-surface-ssot-closeout WP04+WP05 COMBINED DRAIN (SHRINK-ONLY): all 5
  # drainable coord_authority sites are removed — the 4 workflow.py entries
  # (implement feedback-context/dossier-sync READS, review baseline READ, review
  # sub-artifact WRITE; WP04) AND implement.py's detect-feature-context READ (WP05)
  # are all routed onto the kind-aware placement seam.
  # read-surface-ssot-closeout WP11 (FR-003 predicate-widen): the write-vs-read
  # predicate was BLIND to two legitimate coord-owned write helpers whose write
  # indicator sits one hop outside this scanner's function-granularity trace
  # (agent_tasks_ports.py returns the dir to a caller-level commit; recovery.py
  # threads it into a TransitionRequest consumed by a differently-named emit
  # helper). Widening ``_COORD_WRITE_BY_DESIGN`` to file-scope for both modules
  # makes them WRITE-classified for the first time — they were previously
  # silently READ-classified and escaped the gate entirely, never routed by
  # WP07/WP08 because they were never seen. This is a census GAIN (2 new
  # legitimate write sites surfaced, not routed), raising honest write count
  # 2 -> 4 and allowlist baseline 2 -> 4.
- qualname: _mission_dir
  line: 71
  rationale: "legitimate coord-owned write that bypasses commit_for_mission by design - coord status authority is at the caller level, not the commit level; PERMANENT fixture"
  file: src/specify_cli/decisions/emit.py
  token: return resolve_feature_dir_for_mission ( repo_root , mission_slug )
- qualname: WidenPendingStore.__init__
  line: 63
  rationale: "legitimate coord-owned write that bypasses commit_for_mission by design - coord status authority is at the caller level, not the commit level; PERMANENT fixture"
  file: src/specify_cli/widen/state.py
  token: self . _path = resolve_feature_dir_for_mission ( repo_root , mission_slug ) /
- qualname: RealCoordCommitRouter.feature_write_dir
  line: 322
  rationale: "WP11 FR-003 predicate-widen SANCTION: legitimate coord-owned write helper that RETURNS the resolved dir to callers which then commit_artifact/commit_status it (same by-design shape as decisions/emit.py) - the caller-level write indicator is outside this scanner's function-granularity trace; PERMANENT fixture, never remove"
  file: src/specify_cli/agent_tasks_ports.py
  token: "write_dir : Path = resolve_feature_dir_for_mission ("
- qualname: reconcile_status
  line: 765
  rationale: "WP11 FR-003 predicate-widen SANCTION: legitimate coord-owned write helper - the resolved feature_dir feeds a STATUS-WRITE leg (emit_status_transition_transactional via TransitionRequest) that MUST stay coord-aware (C-001/#2155 analog per the KEEP-coord-aware comment at the call site); the write indicator lives in the callee, not this function body; PERMANENT fixture, never remove"
  file: src/specify_cli/lanes/recovery.py
  token: "feature_dir = resolve_feature_dir_for_mission ( repo_root , mission_slug )"
