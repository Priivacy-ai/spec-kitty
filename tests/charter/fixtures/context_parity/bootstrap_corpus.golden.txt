Charter Context (Bootstrap):
  - Source: <REPO_ROOT>/.kittify/charter/charter.md
  - This is the first load for this action. Use the summary and follow references as needed.

Policy Summary:
  - Intent: deterministic parity fixture
  - Testing: pytest golden comparison

Action-Critical Charter Sections (implement):

### Terminology Canon
  Run: spec-kitty charter context --include section:terminology-canon
  When you rename or introduce a term in the diff, run this command and apply the returned rule.

### Code Review Checklist
- Confirm the diff matches the reviewed scope.
  Run: spec-kitty charter context --include section:code-review-checklist
  When you prepare a WP for review, run this command and apply the returned rule.

### Regression Vigilance
- Watch for silently reintroduced legacy terminology.
  Run: spec-kitty charter context --include section:regression-vigilance
  When you perform a terminology cutover, run this command and apply the returned rule.

Profile-Cited Directives (parity-fixture-agent):
  - DIRECTIVE_998: Ghost Directive — force a catalog-miss fall-through
    (catalog entry not found for directive:DIRECTIVE_998)
      Cause: missing_artifact
      Suggestion: confirm the artifact exists (check project, org, and built-in layers) or run `spec-kitty doctrine validate` to check for a silent schema validation drop.

Action Doctrine (implement):

Reference Docs:
  - No references manifest found.

# Governance payload: 1 sections substituted with fetch commands (budget=32000).