schema_version: "1.0"
id: delivery-first
name: Delivery-First Doctrine
summary: Bias toward small, frequent, verifiable delivery increments.
# Post-WP02: inline `tactic_refs` removed — relationships live in graph.yaml.
# Post-WP03 (doctrine-tension-edges-01KY1WPC): the contradiction-declaration
# field this fixture used to carry (test-first tension) is retired — tension
# relationships are now hand-authored DRG edges in graph.yaml, not an inline
# artifact field.
