# Scenario 1 — lineage authored as a DRG fragment edge (the canonical form).
# A specialist profile derives from a base profile via a `specializes_from`
# edge, NOT a `specializes-from:` artifact field.
pack_name: lineage-pack
source_kind: local_path
source_ref: tests/doctrine/fixtures/relationship_packs/lineage-pack
layer_index: 1
provenance_marker: org
nodes:
  - id: base-implementer
    kind: agent_profiles
    title: "Base Implementer (fixture)"
  - id: specialist-implementer
    kind: agent_profiles
    title: "Specialist Implementer (fixture)"
edges:
  - source: specialist-implementer
    target: base-implementer
    relation: specializes_from
    reason: "Specialist derives from the base implementer (lineage, not delegation)"
