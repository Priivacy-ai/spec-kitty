{
  "_purpose": "FR-004 (#3115) capture provenance. This is the CONTROL capture: identical journal seed and identical TERM/FORCE_COLOR as capture_width80.txt, but with the render surface pinned exactly the way WP02's tests/conftest.py::_plain_cli_console_seam pins it -- both width AND height set explicitly, so rich's explicit-size early return fires and is_dumb_terminal never gets asked. Proves the substring assertion discriminates (finds the uuid here) rather than being trivially true (mission verification-trust-3115-01KYVYWM, T011's discriminator requirement).",
  "fixture": "capture_pinned.txt",
  "reproduction_script_status": "FROZEN (issue #5/#114): see capture_width80.provenance.json's reproduction_script_status -- this capture's reproduction (capture_width80.txt's script plus reproduction_script_delta below) drove the same now-deleted specify_cli.cli.commands.sync.app and cannot be re-run against current source.",
  "command": "Identical seed and CliRunner().invoke(app, [\"status\"]) as capture_width80.txt's reproduction_script, with `console.size = (240, 50); err_console.size = (240, 50)` executed immediately before runner.invoke() -- the same (width, height) pair tests/conftest.py::_plain_cli_console_seam assigns (_RENDER_WIDTH = 240, _RENDER_HEIGHT = 50).",
  "reproduction_script_delta": "Same reproduction_script as capture_width80.provenance.json, with this inserted immediately before `result = CliRunner().invoke(app, ['status'])`:\nfrom specify_cli.cli.console import console, err_console\nconsole.size = (240, 50)\nerr_console.size = (240, 50)\n",
  "commit": "bb2020fea9",
  "captured_at_worktree_head": "9bddb14705ff524f484a78614d20b6c2fe25f122",
  "captured_at_worktree_head_note": "see capture_width80.provenance.json's identical note -- the rendering mechanics this capture exercises are unchanged between bb2020fea9 and this worktree's HEAD.",
  "env": {
    "TERM": "dumb",
    "FORCE_COLOR": "1",
    "TTY_COMPATIBLE": null,
    "COLUMNS": null
  },
  "env_note": "Same ambient env as capture_width80.txt (TERM=dumb, FORCE_COLOR=1, TTY_COMPATIBLE/COLUMNS unset) -- the only difference between the two captures is the explicit console.size pin, isolating width as the sole variable.",
  "observed_console_size": {
    "width": 240,
    "height": 50
  },
  "observed_console_size_note": "rich.console.Console.size, read immediately before and after CliRunner.invoke(): (240, 50) both times, produced by Console.size's explicit-size early return (`self._width is not None and self._height is not None`), which -- unlike a width-only pin -- bypasses the is_dumb_terminal branch entirely."
}
