"""Regression tests pinned to specific GitHub issues.

Each test in this package corresponds to a single issue and asserts the
exact reproduction sequence from the issue text is now fixed.  These are
guardrail tests — they exist to catch a regression of a specific bug
class, not to test the system broadly.
"""
