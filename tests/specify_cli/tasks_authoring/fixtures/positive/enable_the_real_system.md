---
work_package_id: WPA1
title: Enable the real sync system and confirm it works
---

## Objective

Turn on the real sync system so downstream teams get live data.

## Acceptance Criteria

- Enable the real system in CI once enabled for the org, then confirm the
  first push succeeds after merge to the integration branch.
- The connector is considered done only once merged and observed live.
