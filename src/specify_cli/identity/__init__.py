"""Identity compatibility helpers — leaf package with no dependencies on core or status."""

from specify_cli.identity.aliases import with_tracked_mission_slug_aliases

__all__ = ["with_tracked_mission_slug_aliases"]
