---
work_package_id: WP17
title: '#2088 / #2100 / epic #1716 closeout'
dependencies:
- WP03
- WP11
- WP16
requirement_refs:
- FR-010
tracker_refs: []
planning_base_branch: design/read-surface-ssot-closeout
merge_target_branch: design/read-surface-ssot-closeout
branch_strategy: Planning artifacts for this mission were generated on design/read-surface-ssot-closeout. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into design/read-surface-ssot-closeout unless the human explicitly redirects the landing branch.
subtasks:
- T048
- T049
- T050
agent: "claude:opus:reviewer-renata:reviewer"
shell_pid: "3579519"
history:
- at: '2026-07-08T06:52:05+00:00'
  actor: planner
  action: created
agent_profile: planner-priti
authoritative_surface: tests/specify_cli/
create_intent:
- tests/specify_cli/test_1716_closeout_regression.py
execution_mode: code_change
owned_files:
- tests/specify_cli/test_1716_closeout_regression.py
role: implementer
tags: []
---

## ⚡ Do This First: Load Agent Profile

Load `/ad-hoc-profile-load` for `planner-priti` (tracker/planning rigour). Read `spec.md` (FR-010,
SC-004), `plan.md` (IC-07). **Append to `traces/*.md`.**

## Objective

Close the mission's tracker obligations: verify + close #2088, close #2100, and close epic #1716 —
gated on #2462-merged (GREEN) and a children-enumeration check.

## Context

- **#2088** is ALREADY fixed on main (`69dd1fa46`, verified ancestor of base, 16 tests green) — this
  is a **verify-only** close, independent of the code WPs.
- **#1716** epic-close gate: (a) #2462 merged to `upstream/main` — **now GREEN** (38257f737);
  (b) a plan-time enumeration confirming #1716 has **no open children beyond #2088/#2100** (re-verified
  during re-grounding: open children = {2088, 2100}).
- **Closing-keyword discipline** (memory: PR closing-keyword parsing): verify each `Closes #N` via
  `gh ... closingIssuesReferences` after editing; a keyword adjacent to the wrong issue mis-closes.
  Use `unset GITHUB_TOKEN` before org `gh` commands.
- issue-matrix verdicts must ALL be terminal before the mission merges (`in-mission` is rejected on `done`).

## Subtasks

### T048 — Verify + lock + close #2088
Confirm `69dd1fa46` is an ancestor of the base and its 16 tests are green. Add
`tests/specify_cli/test_1716_closeout_regression.py` — a thin regression lock asserting the #2088
ownership-overlap validator stays dependency/lane-aware (so #2088 cannot silently regress; it
underwrites this mission's own same-lane decomposition). Close #2088 with the
`verified-already-fixed` verdict in the mission's `issue-matrix.md` (updated via the mission lifecycle,
not an owned_file) + a tracker comment naming this mission.

### T049 — Close #2100 + epic #1716
Confirm Thread B routing + the ratchet (WP16) landed. Enumerate #1716's open children
(`gh issue view 1716` + sub-issue check); confirm only {2088, 2100}. Close #2100 and epic #1716 with
correct closing-keyword refs. Verify `closingIssuesReferences` after each edit.

### T050 — issue-matrix terminal + SC-004
Ensure every issue-matrix row has a terminal verdict (fixed / verified-already-fixed /
deferred-with-followup). Record SC-004 confirmation.

## Branch Strategy
Planning + merge target: `design/read-surface-ssot-closeout`. `spec-kitty agent action implement WP17 --agent <name>`.

## Definition of Done
- [ ] #2088 verified + closed; #2100 closed; epic #1716 closed (children enumerated).
- [ ] All closing-keyword refs verified via `closingIssuesReferences`.
- [ ] issue-matrix verdicts all terminal (SC-004); tracer updated.

## Reviewer guidance (opus)
Confirm #1716 children were enumerated (not assumed). Verify closing-keyword refs actually resolved to
the intended issues. Confirm no `in-mission` verdict remains before merge.

## Activity Log

- 2026-07-08T10:34:03Z – claude:sonnet:planner-priti:implementer – shell_pid=3528678 – Assigned agent via action command
- 2026-07-08T10:50:20Z – claude:sonnet:planner-priti:implementer – shell_pid=3528678 – Ready: #1716 closeout regression lock (#2088 dependency-aware validator), #2088 verified 69dd1fa46 ancestor, #1716 children enumerated, terminal verdicts recommended, SC-001 reconciled to floor 4
- 2026-07-08T10:52:42Z – claude:opus:reviewer-renata:reviewer – shell_pid=3579519 – Started review via action command
