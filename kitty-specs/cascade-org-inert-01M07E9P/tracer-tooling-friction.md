# Tracer: Tooling Friction — cascade-org-inert-01M07E9P

| Date | Surface | Evidence | Resolution/status |
|------|---------|----------|-------------------|
| 2026-08-17 | `spec-kitty specify`/`plan` event capture | Every `specify`/`plan --json` invocation this mission ran printed: `Warning: event journal capture failed: project sync store is locked`, `Warning: Event routing failed: project sync store is locked`, and `Warning: Explicit-context event capture failed: machine layout cutover did not publish within the bounded wait; the event is routed to the loud surface rather than dropped to legacy`. Non-blocking (commands still returned `result: success`), but recurring on every invocation. | Not investigated further this mission — plausibly caused by concurrent missions sharing this machine's sync store (this workspace root hosts multiple `SK-missions/<N>` checkouts). Worth a ledger entry if it recurs across missions or starts blocking rather than warning. |
| 2026-08-17 | `.venv` lacks `mypy` | The hand-built `.venv` (pytest 9.0.3 pinned per workspace prep) has no `mypy` module installed — `python -m mypy` fails with `No module named mypy`. | Worked around via `uvx --with-requirements pyproject.toml mypy --strict <files>`, which downloads/runs mypy without touching the hand-built `.venv`. This is the invocation the Gate Set section of `plan.md` records for WPs to reuse. Not filed as a ledger defect — a hand-built venv intentionally omitting dev-only tools is expected, not broken. |
| 2026-08-17 | Mission scaffold branch-context resolution | `spec-kitty specify`/`plan --json` both resolve `current_branch = target_branch = base_branch = planning_base_branch = merge_target_branch = pr/up-cascade-org-inert` — i.e. the tool has no native concept that this branch's true git ancestor is a different open PR (#3520), not `main`. This is not a bug (spec-kitty's branch-context resolver is working as designed for a non-primary feature branch), but it means the base-branch-drift fact has no home except spec.md's own prose — the tooling will not surface it on its own to a reviewer or a later phase. | Documented explicitly in spec.md's Base-Branch Drift section (mandatory-read-first) precisely because the tooling does not carry this fact forward automatically. |
| 2026-08-17 | `spec-kitty agent mission finalize-tasks` requirement-coverage check | Errored with `"unmapped_functional_requirements": ["FR-004"]` even though spec.md's Requirements table has no `FR-004` row (retired mid-mission, replaced with prose explaining why). Root cause traced live: `parse_requirement_ids_from_spec_md` (`src/specify_cli/requirement_mapping.py:104`) regex-scans the WHOLE spec.md document for any `FR-\d+`/`NFR-\d+`/`C-\d+` token, with no markdown-structure awareness and no "retired" concept — so the retirement notice's own prose (which must NAME "FR-004" to be a useful retirement notice at all) gets re-parsed as a still-active, still-uncovered requirement. | Filed as `SPEC-KITTY-LEDGER.md` SK-51 (verified first-hand). Not worked around by deleting the retirement prose — that would trade tooling compliance for spec honesty, which this mission declines to do. Does NOT block this mission's design-phase deliverable: `check-prerequisites --include-tasks` (what `analyze` depends on) returns `valid: true` regardless of `finalize-tasks`'s own success — confirmed live. Will block `lanes.json` generation for whoever implements this mission; flagged in the hand-off report. |
| 2026-08-17 | `spec-kitty agent tasks mark-status <WP02 Txxx> --status done` (lane-b) | Prints `✓ Marked T008 as done` (success) but `git status --short` shows zero file changes afterward — the WP file's subtask checkbox is not actually updated. Stderr carries the real cause, swallowed from the success-looking stdout: a traceback through `specify_cli/sync/body_queue.py::_require_project_destination` ending `RuntimeError: body outbox writes require the project_only layout`. Verified first-hand this lane (WP02), same failure independently reported by sibling lanes (WP01/WP03) on this same mission. | Not worked around by hand-editing WP frontmatter/tasks.md (charter standing order forbids it). Git commits are the evidence of subtask completion for this WP instead; commit messages name the subtask IDs (T008/T009/T010/T011-T016). Worth a `SPEC-KITTY-LEDGER.md` entry if not already filed by a sibling lane. |
| 2026-08-17 | WP03 `owned_files`/`lanes.json` write-scope vs. its own task body | WP03's frontmatter `owned_files` and `lanes.json`'s `lane-c.write_scope` list ONLY `src/charter/activation/context.py` + `tests/charter/test_context_org_chain.py` — but WP03's own task body (T017) explicitly requires editing `src/specify_cli/cli/commands/charter/context.py` (the CLI-level truncation fix), and the WP's own Gates section runs `mypy --strict` over that exact file as part of WP03's DoD. No other WP's `owned_files`/`write_scope` claims that file either (verified: WP01 = dossier/rebaseline only; WP02 = `_layer_roots.py`/`activate.py`/`deactivate.py` only) — it is a genuine orphaned surface in `lanes.json`, not a cross-lane conflict. | Proceeded per the WP body (stated authoritative for scope in the dispatch brief) since omitting T017 would leave WP03 an inert no-op — the exact SPEC-ARCH-002 failure this WP exists to close. The pre-commit ownership guard (`src/specify_cli/policy/commit_guard.py`) defaults to `mode: "warn"` (not `block`), confirmed via `src/specify_cli/policy/config.py`, and this mission's `.kittify/config.yaml` carries no override — so this does not hard-block the commit, only (potentially) warns. Flagged here rather than silently worked around; worth a `lanes.json`/`finalize-tasks` generation-time cross-check against each WP's own task-body file list before this class recurs. |
| 2026-08-17 | `spec-kitty agent tasks mark-status` reports success with no observable write | Ran `spec-kitty agent tasks mark-status T017 T018 T019 T020 T021 --status done --mission cascade-org-inert-01M07E9P` (and separately, `T017` alone with `--json`) in the lane-c worktree. Both invocations printed repeated `Warning: Explicit-context event capture failed: live payload writes require the project_only layout; legacy state is migration input only` / `RuntimeError: body outbox writes require the project_only layout` traces on stderr, then reported success (`"outcome": "updated"`, `"message": "Marked T017 as done (pipe-table row updated)."`). `git status --short` before and after each invocation showed **zero** files changed anywhere in the repo — `tasks.md`'s own "Subtask Index (Reference)" table (the only place T017-T021 appear as table rows) has no status/done column to update in the first place, so "pipe-table row updated" does not correspond to any actual on-disk change. | Not worked around (would require hand-editing spec-kitty state, which is forbidden). Subtask completion for T017-T021 is instead evidenced by this WP's git commits (SHAs in the WP report) and this tracer/status-events record, not by a `mark-status`-driven artifact. Worth investigating upstream: either the command's success message is decoupled from its actual write outcome (a "diagnostic that lied"), or the `project_only` layout precondition silently no-ops every write attempted from a worktree that isn't in that layout — either way an operator/agent has no signal that nothing happened. |
