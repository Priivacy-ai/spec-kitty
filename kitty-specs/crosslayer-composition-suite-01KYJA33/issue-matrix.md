# Issue matrix — crosslayer-composition-suite-01KYJA33

Per FR-037 of the spec-kitty-mission-review skill Gate-4. One row per issue referenced in spec.md.

| Issue | Title | Verdict | Evidence ref |
|-------|-------|---------|--------------|
| #30 | M3 (`doctrine-rule-manifests`) PR — FR-005's rule-inventory dependency; blocked on CI infra, not on review | deferred-with-followup | `https://github.com/MOES-Media/spec-kitty/pull/30` — verified via `gh pr view 30 --repo MOES-Media/spec-kitty --json state,mergedAt,mergeable`: `state: OPEN`, `mergeable: MERGEABLE`, `mergedAt: null` (checked 2026-07-27). Blocked on 4 checks (`Detect Windows-critical changes`, `docs-freshness`, `drift-detector`, `ui-e2e`) queued against `blacksmith-4vcpu-ubuntu-2404` runner labels this fork has none of — confirmed via `gh api repos/MOES-Media/spec-kitty/actions/jobs/<id>`, each showing `status: queued`, `runner_id: 0`, queued since job creation (~4h at check time); operator resolving separately. WP05/FR-005 (`tasks/WP05-rule-survival-cases.md` T023/T024) is hard-blocked on this merging and must not proceed until it does. Follow-up: `MOES-Media/spec-kitty#30`. |
| #12 | Crosslayer static-rubric citation is an in-code string constant, not a published rubric doc — recorded gap, out of scope for this programme | deferred-with-followup | `https://github.com/garrison-hq/muster/issues/60` (M9 `conformance-programme-docs` mission spec, §11 recorded-gaps register): "No rubric for tools/memory/heartbeat/crosslayer (correction #12): … crosslayer cites the in-code string constant `\"muster cross-layer rubric (2026)\"` (`src/crosslayer/contradiction-lint.ts:36`). Recorded as a gap; not this programme's job to fix for non-SK layers." Matches this mission's own Scope Guard (`spec.md`): "this mission consumes `contradiction-lint.ts`'s existing `\"muster cross-layer rubric (2026)\"` citation as-is (correction #12, recorded upstream in muster issue `garrison-hq/muster#60`; not this programme's job to fix for non-SK layers)." Follow-up: `garrison-hq/muster#60`. |

Valid `Verdict` values: `fixed`, `verified-already-fixed`, `deferred-with-followup`, `in-mission` (being fixed by a later WP in this mission; must reach a terminal verdict before mission `done`).
