# Approach Trace

## 2026-07-16 — Planning baseline

Issue #2658 is planned as an authority-chain completion rather than a new template subsystem. The approach projects the doctrine-owned artifact mapping through activated charter context, uses that mapping to select a filename, and delegates actual file choice to the existing five-tier resolver. The implementation will migrate specification and planning readers, prove software-development parity transiently, retain behavioral tests, and stop short of issues #2659–#2661.

### 2026-07-16 — WP05 authority-swap proof and closeout

WP05 verified the templates-as-configuration cutover through four layers: a temporary executable parity scaffold, enduring activated-context integration, subprocess CLI smoke for `mission create` and `setup-plan`, and an AST architectural gate over `create_mission_core` plus `_resolve_plan_template`. The temporary scaffold compared the pre-swap direct software-development selector with `resolve_configured_template` for semantic keys `spec` and `plan`, across package defaults and project overrides, asserting identical winner path, tier, mission, and exact bytes (4 passed in 26.51s). The architectural guard failed while the scaffold existed; the source and generated bytecode were deleted before handoff. Enduring tests cover exact built-in mapping, explicit null mappings for documentation/research/plan, unknown-type non-activation even when template files exist, package content resolution, override precedence through both production readers, actionable fail-closed CLI behavior before mission state creation, and structural reader wiring. Cross-WP proof: 186 passed with 4 pre-existing `DoctrineLayerCollisionWarning` warnings; Ruff and formatting clean; mypy strict clean on five affected production files; diff coverage 100% across 74 changed production lines with 0 missing; determinism and the <100 ms hot-path tests passed. Issue #2658 remains assigned to `robertDouglass`; claim evidence: https://github.com/Priivacy-ai/spec-kitty/issues/2658#issuecomment-4989267695. No implementation deviation from the plan.

### 2026-07-16 — WP05 cycle-1 replay and order-isolation correction

Review cycle 1 reproduced the canonical path-order regression with `git diff --name-only kitty/mission-templates-as-config-01KXMS1G..HEAD | rg '^tests/.*\.py$' | xargs env -u SPEC_KITTY_ENABLE_SAAS_SYNC SPEC_KITTY_TEST_DB_NAME=test_templates_as_config_01KXMS1G_lane_e uv run pytest -q`: before correction, 1 failed and 185 passed because an earlier e2e test materialized a GLOBAL_MISSION winner. The integration test now owns the asserted precedence by setting `SPEC_KITTY_HOME` to an empty temporary home before requiring PACKAGE_DEFAULT. Both repository path order and the original implementer order now pass 189 tests with 4 known warnings (64.76s and 64.44s respectively). Transient parity was replayed with exact command `env -u SPEC_KITTY_ENABLE_SAAS_SYNC SPEC_KITTY_TEST_DB_NAME=test_templates_as_config_01KXMS1G_lane_e uv run pytest -q tests/integration/test_template_resolution_parity_scaffold.py`; exit 0; 4 passed in 28.09s. Node IDs: `test_configured_mapping_matches_shipped_software_development_selection[package-spec]`, `[package-plan]`, `[project-override-spec]`, `[project-override-plan]`. Package and project-override winners matched direct shipped selection vs configured semantic-key selection by path, tier, mission, and exact bytes. The architecture gate failed while both scaffold source and compiled bytecode existed; both were deleted. Final focused owned suite: 25 passed. Aggregate coverage run: 189 passed; diff coverage 100% across 74 changed production lines. Ruff/format, mypy strict, determinism/performance, diff, and no-residue checks passed.

### 2026-07-16 — Post-merge mission review and realistic-boundary convergence

Mission review reproduced and fixed the real split-surface plan bug, strict metadata failure behavior, portable configured filenames, supported legacy `mission` metadata, and stale architecture documentation. Adversarial review distinguished supported states from synthetic threat theater: valid readable metadata symlinks remain accepted; only broken/self-loop `meta.json` links are rejected so they cannot masquerade as absent metadata; manually constructed `ResolvedMissionType` objects are not treated as authenticated, and receive only cheap path-safety validation. An overfitted 309-line AST mutation ratchet was removed while the enduring semantic guard remained. Frozen main HEAD `534bd432` passes 1,014 architectural tests, 293 SaaS-enabled contract tests, the 251-test changed corpus, Ruff, and strict mypy. The sibling E2E drift harness was repaired at `615f1a6`; the dependent-WP scenario was then expanded at `4ab0e9b` into three sequential code WPs plus one planning-artifact WP, with real review, approval, acceptance, lane merge, and simulated PR merge checks. Full E2E now reports five passes and one missing-endpoint xfail. That remaining non-pass is an environmental hard-gate blocker, not an observed #2658 product failure.

Round 6 reverse-speccing found one further normal-workflow evidence loss: the mission's accepted commit remained in branch history, but squash merge had replaced target-newer `meta.json` and removed its acceptance/VCS provenance. The exact accepted fields were restored without changing the newer merge baseline or assigned mission number. This is tracked as TF-066 for a general merge reconciliation fix; the current mission artifact itself is repaired.
