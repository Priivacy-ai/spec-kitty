---
affected_files:
- path: tests/architectural/test_no_parity_scaffold.py
- path: tests/integration/test_mission_type_resolution_integration.py
cycle_number: 1
mission_slug: templates-as-config-01KXMS1G
reproduction_command: "git diff --name-only kitty/mission-templates-as-config-01KXMS1G..HEAD | rg '^tests/.*\\.py$' | xargs uv run pytest -q"
reviewed_at: '2026-07-16T09:34:45Z'
reviewer_agent: codex:gpt-5:reviewer-renata:reviewer
verdict: rejected
wp_id: WP05
---

# WP05 Review — Cycle 1

Reviewer profile: `reviewer-renata` (incremental review, reverse-speccing, mutation resistance, locality of change, and independent quality-gate verification).

## Blocking findings

### 1. The new package-default integration assertion is order-dependent

`tests/integration/test_mission_type_resolution_integration.py:227` asserts that the configured software-development template resolves specifically from `ResolutionTier.PACKAGE_DEFAULT`, but the test does not isolate the global template tier. It passes alone and in the implementer's hand-selected order, yet fails when the same 186 changed tests are supplied in repository path order (where `tests/e2e/` precedes `tests/integration/`):

```text
FAILED test_software_development_mission_resolves_exact_configured_templates
AssertionError: GLOBAL_MISSION is not PACKAGE_DEFAULT
1 failed, 185 passed, 4 warnings
```

Reproduction:

```bash
git diff --name-only kitty/mission-templates-as-config-01KXMS1G..HEAD \
  | rg '^tests/.*\.py$' \
  | xargs uv run pytest -q
```

The enduring test must own/isolate every precedence tier it constrains, or assert a contract that remains valid under the repository's test-home behavior. Re-run both this path-order command and the focused WP05 suite after the correction.

### 2. The typeless-boundary AST gate accepts the inverse condition

`tests/architectural/test_no_parity_scaffold.py:116-118` verifies only that the guard is an `ast.Compare` containing a `None` comparator and a `mission_type` attribute. It never checks the comparison operator or whether the legacy call is in the guarded body. Mutating the production condition from:

```python
if resolved_mission_type.mission_type is None:
```

to:

```python
if resolved_mission_type.mission_type is not None:
```

still satisfies every current assertion (`operator=IsNot`, `current_test_would_pass=True`). That mutation moves the legacy selector onto activated missions, the exact regression T030 says the structural gate must prevent. Assert the exact `is None` predicate and body placement, and add a mutation-oriented test/helper case that proves `is not None` fails.

### 3. The scaffold reappearance scan ignores all compiled scaffold artifacts

`tests/architectural/test_no_parity_scaffold.py:46-47` excludes every path containing `__pycache__`, not only the permanent guard's own generated bytecode. A stale `test_template_resolution_parity_scaffold.*.pyc` would therefore survive undetected even though the module docstring promises to reject any parity-scaffold artifact and T027/T030 require generated scaffold bytecode to be gone. The current tree is clean (the only parity-named `.pyc` belongs to the permanent guard), but the permanent gate is false-negative by construction. Narrow the exception to this guard's own source/compiled artifacts and prove a synthetic scaffold-named bytecode path is detected.

### 4. The transient parity execution record omits the required replay command

The WP Activity Log and coordinator trace record `4 passed in 26.51s` and describe what was compared, but neither records the exact pytest command/node IDs or preserves raw output. The scaffold was correctly deleted, so there is no remaining executable evidence from which the reviewer can reconstruct the run. T027 step 6 requires the command/result, and T031 step 8 requires exact commands/results. Recreate the transient scaffold, run it, record the exact command plus exit code/node count and compared tiers, delete source and generated bytecode again, and obtain an updated coordinator trace acknowledgment/hash.

## Passing evidence retained

- WP05 commit changes exactly its three declared enduring test files.
- The temporary scaffold source and generated bytecode are absent now.
- Focused WP05 tests pass: 22 passed in 54.53s.
- The implementer-ordered cross-WP suite passes: 186 passed, 4 warnings.
- Diff coverage is 100% (75 changed production lines, 0 missing).
- Ruff check/format, mypy strict on five production files, and the determinism/performance nodes pass.
- Issue #2658 remains assigned to `robertDouglass`, the mission claim comment exists, and its issue-matrix verdict is valid for per-WP approval.
- Coordinator commit `81e622626ce22bdc6d9cdf68f5cad15096559d3c` exists; all three recorded SHA-256 values match the committed trace contents.

## Anti-pattern checklist

1. Dead code: PASS — WP05 adds tests only; production seams have live reader callers.
2. Synthetic-fixture test: FAIL — the package-default test leaks external tier state and does not fully own the claimed runtime fixture.
3. Silent empty return: PASS — no WP05 production code added.
4. FR coverage: PASS with the execution-evidence correction above required for FR-007/FR-008 auditability.
5. Frozen surface: PASS.
6. Locked decision: FAIL — the architectural test does not reject the inverse typeless guard.
7. Shared-file ownership: PASS — WP05 commit stays within its owned files.
8. Production fragility: PASS — no WP05 production `raise` added.

Verdict: **REJECT — changes required**.
