# review-refute/v1 — canonical format for refuter-batch verdicts.
schema: review-refute/v1
complete: true
phase: spec
batch: 1
verdicts:
  - id: SPEC-GOV-001
    verdict: confirmed
    reason: >
      Direct read confirms C-002 (spec.md:365) is a bare rule with no "per Decision 2"
      back-reference, while FR-010/FR-011 (lines 345-346) both literally say "per Decision 1";
      FR-013 (line 348) names "Decision 2" but only says to record its rationale as a YAML
      comment, never restating the defeater's substance inline in the table row.

  - id: SPEC-GOV-002
    verdict: confirmed
    reason: >
      plan/mission.yaml:40-42 shows structure→draft has no `conditions:` key at all
      (unconditional), while review→done (lines 48-52) gates on `gate_passed("plan_approved")`;
      spec.md's AS3 text ("no artifact_exists condition on structure→draft or review→done, which
      instead gates on gate_passed(...)") grammatically attributes the gate to both transitions,
      which is inaccurate for structure→draft.

  - id: SPEC-GOV-003
    verdict: confirmed
    reason: >
      runtime_bridge_cores.py:554-566 shows _evaluate_software_dev_guards recognizes 8 distinct
      ids (specify, plan, tasks_outline, tasks_packages, tasks_finalize, tasks [composed, line
      562-563], implement, review), but Decision 1's Q text in spec.md enumerates only 7,
      omitting the composed "tasks" id.

  - id: SPEC-ARCH-001
    verdict: confirmed
    reason: >
      manifest.py:207-215 confirms ManifestRegistry.load_manifest() wraps model_validate in a
      bare `except Exception` that returns None on any ValidationError, and US3's AS1/AS2 in
      spec.md only exercise direct model construction while AS3 only re-confirms the happy path
      of the four shipped files — no acceptance scenario exercises load_manifest() against a
      typo'd real YAML file, matching the claim exactly.

  - id: SPEC-VERIFY-001
    verdict: confirmed
    reason: >
      Direct code read confirms `_evaluate_software_dev_guards` has an explicit
      `if step_id in ("implement", "review")` branch (runtime_bridge_cores.py:564-566) that
      fires before the bare `return []`, `_check_cli_guards` hardcodes
      mission_family="software-dev" (runtime_bridge.py:692) regardless of actual mission type,
      and `_dn_composition_dispatch` is hard-gated to mission=="software-dev"
      (runtime_bridge.py:1541-1544) so plan missions fall through to `_check_cli_guards`; spec.md's
      own Decision 1 text and US2/AS4 both assert "review" (among others) "falls to the bare
      return []", which the code directly contradicts since plan/mission.yaml's state list
      literally includes a state named "review" that collides with software-dev's vocabulary.
