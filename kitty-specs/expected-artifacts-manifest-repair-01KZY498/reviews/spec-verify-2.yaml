# review-verify/v1 — canonical format for fix-verification output.
schema: review-verify/v1
complete: false
phase: spec
round: 2
results:
  - id: SPEC-FRESH-001
    status: resolved
    reason: >
      The false "it touches only manifest.py" claim is gone from both User Story 3's motivation
      paragraph and Decision 3, replaced by a call-graph-verified blast-radius statement (manifest.py
      + one narrow line in sync/namespace.py's resolve_manifest_version(), explicitly NOT
      dossier/indexer.py); every line number and behavior the fix cites (manifest.py:202,207-215,
      indexer.py:123/176/355/407, reconcile.py:151-160, rebaseline.py:162-170,
      dossier_pipeline.py:243-250/344/351/368, namespace.py:90-101) was independently verified against
      current code and is accurate; NFR-002, C-002, and Key Entities were updated with the real
      file/behavior list; FR-016's table row now specifies exact per-call-site handling instead of the
      old vague "handle explicitly"; and AS5/AS6 were added to exercise the indexer-caller path and the
      resolve_manifest_version() path respectively, satisfying the remediation's caller-side-coverage
      requirement.
complete: true
