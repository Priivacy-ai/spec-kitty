# review-verify/v1 — canonical format for fix-verification output.
schema: review-verify/v1
complete: true
phase: spec
results:
  - id: SPEC-GOV-001
    status: resolved
    reason: C-002 now carries an inline parenthetical restating the sync-namespace-rekey defeater and a "per Decision 2" back-reference, and FR-013 independently restates Decision 2's rationale inline — both now match the "per Decision 1" treatment FR-010/FR-011 already had.
  - id: SPEC-GOV-002
    status: resolved
    reason: US2-AS3 (spec.md ~lines 306-309) now correctly states structure→draft carries no conditions key (unconditional) while review→done gates on gate_passed("plan_approved"), verified against packs/built-in/missions/plan/mission.yaml:39-48 which shows exactly that asymmetry.
  - id: SPEC-ARCH-001
    status: resolved
    reason: Editorial call taken (remediation option a) — FR-016 added to change load_manifest()'s exception handling to let ValidationError propagate, with Decision 3 in tracer-design-decisions.md documenting context, chosen option, why in-scope (C-001 only forbids runtime_bridge_* files), and an explicitly rejected alternative (narrowing US3 instead), not a caveat bolted on; current manifest.py:207-215 still has the bare except confirming the finding's premise was accurate pre-fix.
  - id: SPEC-VERIFY-001
    status: resolved
    reason: Decision 1 (tracer + Clarifications Q text), US2-AS3/AS4, and FR-011 are now all mutually consistent — each states review is evaluated by the real _evaluate_wp_iteration_guard branch (not the bare return []) because _check_cli_guards hardcodes mission_family="software-dev", verified directly against runtime_bridge_cores.py:562-566 and runtime_bridge.py:680-698,1531-1544; no location still claims all plan-type steps fall to the bare fallback.
  - id: SPEC-ARCH-003
    status: resolved
    reason: All three locations (Overview Motivation ~line 87, Overview "Corrected risk framing" item 3 ~lines 144-148, and Non-Gate Consumer Notes ~lines 540-541) now correctly name ResolvedMissionType.expected_artifacts (verified as a @cached_property on ResolvedMissionType, not MissionTypeProfile, at src/charter/mission_type_profiles.py:404-409/330), and all three now acknowledge tests/charter/test_resolved_mission_type_context.py:160-162 as a real consumer instead of repeating "no consumer found."
  - id: SPEC-ARCH-004
    status: resolved
    reason: tests/charter/test_resolved_mission_type_context.py is now listed in NFR-003's validation surface, FR-015's audit list, and SC-002's pytest invocation.
  - id: SPEC-ARCH-005
    status: resolved
    reason: Editorial call taken — FR-014 now marks the dead .kittify/overrides copies as deprecated/inert via header comment rather than refreshing content, with Decision 4 in tracer-design-decisions.md documenting the DIRECTIVE_044 tension (verified present in .kittify/charter/charter.md) and weighing two rejected alternatives (refresh, delete) with reasoning, not a caveat appended without resolving the tension.
  - id: SPEC-ARCH-006
    status: resolved
    reason: Non-Gate Consumer Notes' plan-specific sentence now correctly states resolve_manifest_version("plan") already returned "1" pre-mission via the None-fallback branch and continues to via the real field post-FR-010, verified against src/specify_cli/sync/namespace.py:90-101 which matches exactly as described.
  - id: SPEC-ARCH-007
    status: resolved
    reason: The "second-order finding" section now cites two separate functions/ranges — _resolve_asset() (~172-183) and resolve_mission() (~331-334) — verified against src/doctrine/resolver.py where _resolve_asset's override check spans ~171-183 and resolve_mission's spans ~330-334, matching the split citation.
  - id: SPEC-VERIFY-002
    status: resolved
    reason: FR-007 now explicitly requires an inline YAML comment on the tasks_packages entry documenting _evaluate_tasks_packages_guard's requirement_mapping_failures check, verified against runtime_bridge_cores.py:484-489 which confirms the second check exists on that branch; AS5 itself wasn't reworded to name it, but the authoritative FR-007 requirement plus AS7's general "every key carries an inline comment or matches a guard" bar cover the substance the finding raised.
  - id: SPEC-GOV-003
    status: resolved
    reason: Decision 1's Q text now enumerates all 8 step ids including "tasks (composed)", verified against _evaluate_software_dev_guards which recognizes specify, plan, the three _CLI_TASKS_STEP_IDS, tasks (composed), implement, and review — exactly 8 branches matching the corrected enumeration.
