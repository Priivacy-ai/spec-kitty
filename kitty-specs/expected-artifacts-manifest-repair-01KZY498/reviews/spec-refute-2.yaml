# review-refute/v1 — canonical format for refuter-batch verdicts.
schema: review-refute/v1
complete: true
phase: spec
batch: 2
verdicts:
  - id: SPEC-ARCH-003
    verdict: confirmed
    reason: "Code confirms expected_artifacts is a @cached_property on ResolvedMissionType (mission_type_profiles.py:404-409), not on MissionTypeProfile (a separate pydantic BaseModel at line 114 with no such attribute), and tests/charter/test_resolved_mission_type_context.py:160-162 does assert bundle.expected_artifacts is not None, is a dict, and has mission_type==\"software-dev\", contradicting spec.md's repeated \"no consumer beyond its own docstring\" claim."
  - id: SPEC-ARCH-004
    verdict: confirmed
    reason: "spec.md's NFR-003 (line 358), FR-015 (line 350), and SC-002 (lines 395-397) all list tests/dossier/, tests/doctrine/missions/, tests/runtime/ and FR-015's named files but never tests/charter/, even though that directory holds the real software-dev consumer confirmed in SPEC-ARCH-003."
  - id: SPEC-ARCH-005
    verdict: confirmed
    reason: "charter.md's Governing Principles (lines 27-31) literally state \"chase unification, not parity with a dead quirk\" under DIRECTIVE_044, and FR-014/second-order-finding text proposes refreshing the confirmed-dead override copies as hygiene while invoking \"single canonical authority\" but never weighing deletion/deprecation as an explicit alternative."
  - id: SPEC-ARCH-006
    verdict: confirmed
    reason: "namespace.py:90-101 shows resolve_manifest_version() has an explicit `return \"1\"` fallback when load_manifest() returns None, so it already returned \"1\" for plan before FR-010 (via fallback) and continues to after (via the real manifest) — spec.md's framing of plan's value as \"returns '1' for the first time\"/\"a new value where previously there was none\" conflates load_manifest's None with resolve_manifest_version's always-string contract."
  - id: SPEC-ARCH-007
    verdict: confirmed
    reason: "resolver.py confirms _resolve_asset() (def at line 145) contains the templates/command-templates override check at ~172-183, while resolve_mission() (def at line 303) has its own separate mission.yaml override check at exactly 331-334 — outside spec.md's single cited range of 154-181, so one citation is used to describe checks that live in two different functions."
  - id: SPEC-VERIFY-002
    verdict: confirmed
    reason: "runtime_bridge_cores.py:484-489 confirms _evaluate_tasks_packages_guard returns snapshot.status_facts[\"requirement_mapping_failures\"] when the tasks dir is ready, a second non-filesystem check FR-007/Acceptance-Scenario-5 never mention (only the tasks/WP*.md glob), and a grep of spec.md for \"requirement_mapping\" returns zero hits, while the analogous tasks_finalize non-expressible checks are named explicitly elsewhere in the spec."
