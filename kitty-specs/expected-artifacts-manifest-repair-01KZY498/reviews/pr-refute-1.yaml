# review-refute/v1 — canonical format for refuter-batch verdicts.
schema: review-refute/v1
complete: true
phase: pr
batch: 1
verdicts:
  - id: pr-contract-001
    verdict: confirmed
    reason: >-
      Verified line-for-line against the actual diff and code: the shipped docstring
      (manifest.py:192-196) claims malformation-vs-absence in general terms,
      repository.py's except-OSError/UnicodeDecodeError/YAMLError-return-None
      at line 382-383 is real and untouched by this diff (git diff --stat shows
      zero changes to that file), and it is reachable from load_manifest()'s
      config-is-None branch; the fixture test_manifest.py:539-564 uses
      tests/dossier/fixtures/expected_artifacts_typo.yaml, which parses as fully
      valid YAML (verified with yaml.safe_load) containing only a misspelled key,
      so the test named test_load_manifest_raises_on_malformed_yaml never
      exercises a YAML-syntax failure, and a grep across every other "malformed"
      test in the touched test files (test_reconcile.py, test_namespace.py,
      test_rebaseline.py) confirms they all route through the same schema-typo
      fixture, not real YAML-syntax breakage.
  - id: pr-contract-002
    verdict: refuted
    reason: >-
      The finding merely restates the residual risk Decision 2 already names and
      accepts almost word-for-word (that the version field is a sync-namespace
      identity key rather than a content-freshness counter, and that this repo
      cannot verify spec-kitty-saas's blast radius either way), and its own
      remediation — asking for the inline YAML comment to state that
      manifest_version does not signal content stability — is already shipped
      verbatim in this diff: verified that all four expected-artifacts.yaml files
      carry the comment "manifest_version is a sync-namespace identity key (feeds
      NamespaceRef), not a content-freshness counter; do not bump on
      content-only changes" citing Decision 2, so the finding re-litigates a
      settled, documented decision without identifying anything Decision 2 or the
      shipped code does not already cover.
  - id: pr-tests-001
    verdict: refuted
    reason: >-
      The underlying facts check out (kernel floor at ci-quality.yml:1113 covers
      only src/kernel, mission-loader gate at line 1535 covers only
      src/specify_cli/mission_loader, and the critical_paths array at lines
      3491-3514 omits src/specify_cli/dossier/* and src/specify_cli/sync/*, the
      only two src/ files this diff touches per git diff --stat), but the finding
      itself labels this severity-1/informational, explicitly states it is a
      pre-existing CI-topology gap this PR neither introduces nor worsens, and
      requests no action for this PR — that makes it an observation about CI
      infrastructure unrelated to this diff's correctness rather than a finding
      about the PR under review, so it does not survive as a confirmed
      pre-merge-squad finding on this specific diff.
