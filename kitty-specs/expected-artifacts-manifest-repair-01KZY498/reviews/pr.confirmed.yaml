# review-confirmed/v1 - findings surviving R3 refutation. Cites the merged and refute files as evidence.
schema: review-confirmed/v1
complete: true
phase: pr
mission: expected-artifacts-manifest-repair-01KZY498
evidence:
  lens_files:
  - pr.boundary.findings.yaml
  - pr.contract.findings.yaml
  - pr.tests.findings.yaml
  merged_file: pr.merged.yaml
  refute_files:
  - pr-refute-1.yaml
stages_run: R1-R3 only (report-only; code phases run no R4/R5 fix round inside the squad)
severity_counts:
  '5': 0
  '4': 0
  '3': 1
  '2': 0
  '1': 0
blocking: []
refuted:
- id: pr-contract-002
  severity: 3
  title: manifest_version staying "1" across a materially reconciled required_by_step is a reasoned but
    unverified bet on a system this repo cannot see into
  refuter_reason: 'The finding merely restates the residual risk Decision 2 already names and accepts
    almost word-for-word (that the version field is a sync-namespace identity key rather than a content-freshness
    counter, and that this repo cannot verify spec-kitty-saas''s blast radius either way), and its own
    remediation — asking for the inline YAML comment to state that manifest_version does not signal content
    stability — is already shipped verbatim in this diff: verified that all four expected-artifacts.yaml
    files carry the comment "manifest_version is a sync-namespace identity key (feeds NamespaceRef), not
    a content-freshness counter; do not bump on content-only changes" citing Decision 2, so the finding
    re-litigates a settled, documented decision without identifying anything Decision 2 or the shipped
    code does not already cover.'
- id: pr-tests-001
  severity: 1
  title: Named 90% coverage floors do not scope over the two src/ files this diff touches (informational,
    not a defect of this PR)
  refuter_reason: The underlying facts check out (kernel floor at ci-quality.yml:1113 covers only src/kernel,
    mission-loader gate at line 1535 covers only src/specify_cli/mission_loader, and the critical_paths
    array at lines 3491-3514 omits src/specify_cli/dossier/* and src/specify_cli/sync/*, the only two
    src/ files this diff touches per git diff --stat), but the finding itself labels this severity-1/informational,
    explicitly states it is a pre-existing CI-topology gap this PR neither introduces nor worsens, and
    requests no action for this PR — that makes it an observation about CI infrastructure unrelated to
    this diff's correctness rather than a finding about the PR under review, so it does not survive as
    a confirmed pre-merge-squad finding on this specific diff.
findings:
- id: pr-contract-001
  lens: contract
  severity: 3
  title: '"Malformation now raises" is only true for schema-level malformation; YAML-syntax-level malformation
    still silently degrades to the same None path as absence'
  evidence:
  - artifact: manifest.py docstring shipped in this diff
  - code: src/specify_cli/dossier/manifest.py::ManifestRegistry.load_manifest (lines 184-222, esp. 192-196,
      214-217)
  - code: src/doctrine/missions/repository.py::MissionTemplateRepository.get_expected_artifacts (lines
      362-383, catch at 382)
  - code: tests/dossier/test_manifest.py::TestManifestHardening.test_load_manifest_raises_on_malformed_yaml
      (lines 539-564)
  claim: 'FR-016''s fix (this diff) makes `load_manifest()` distinguish "absent" from "malformed" for
    one specific failure mode: a syntactically valid YAML document that violates the Pydantic schema (an
    unrecognized/typo''d key, caught by the new `extra="forbid"`). The new docstring this diff adds states
    the guarantee generally: "Gracefully returns None if the manifest is not found ... but a *found*,
    malformed manifest ... raises ... instead of being silently swallowed ... absence and malformation
    are distinct outcomes, and only absence degrades to None." That claim is broader than what the code
    delivers. `load_manifest()`''s `config is None` branch (manifest.py:214-217) is reached whenever `_doctrine_repository().get_expected_artifacts(mission_type)`
    returns None — and that helper (src/doctrine/missions/repository.py, untouched by this diff) returns
    None not only when the file is absent, but also on `OSError`, `UnicodeDecodeError`, or `YAMLError`
    (repository.py:382), i.e. genuinely broken YAML (bad indentation, unclosed structures, duplicate keys
    ruamel rejects, etc.) — an even more clear-cut case of "found but malformed" than a typo''d key, yet
    it still collapses to the exact same "not found" outcome the whole mission exists to eliminate. The
    one test whose name promises this coverage, `test_load_manifest_raises_on_malformed_yaml`, does not
    actually feed malformed YAML through the loader; it feeds the schema-typo fixture (`expected_artifacts_typo.yaml`,
    still syntactically valid YAML) through a monkeypatched `_doctrine_repository`, so it exercises the
    `extra="forbid"` path, not the YAMLError path its name suggests. No test in this diff constructs an
    actual YAML-syntax error and asserts it raises rather than degrading to None.'
  remediation: Either narrow the docstring/tracer language to say precisely what changed ("schema-validation
    malformation now raises; YAML-syntax malformation is unchanged and still degrades to None, tracked
    separately") so a future reader doesn't assume broader coverage than exists, or extend FR-016's scope
    to also let YAMLError propagate from `get_expected_artifacts`/`load_manifest` (with the same "found
    but broken" semantics as the schema case), and add a test that actually constructs invalid YAML syntax
    (not just an extra key) and asserts it raises. At minimum, rename `test_load_manifest_raises_on_malformed_yaml`
    to name what it actually tests (a typo'd/extra key), since its current name will mislead a future
    reader auditing coverage for this exact gap.
  refuter_reason: 'Verified line-for-line against the actual diff and code: the shipped docstring (manifest.py:192-196)
    claims malformation-vs-absence in general terms, repository.py''s except-OSError/UnicodeDecodeError/YAMLError-return-None
    at line 382-383 is real and untouched by this diff (git diff --stat shows zero changes to that file),
    and it is reachable from load_manifest()''s config-is-None branch; the fixture test_manifest.py:539-564
    uses tests/dossier/fixtures/expected_artifacts_typo.yaml, which parses as fully valid YAML (verified
    with yaml.safe_load) containing only a misspelled key, so the test named test_load_manifest_raises_on_malformed_yaml
    never exercises a YAML-syntax failure, and a grep across every other "malformed" test in the touched
    test files (test_reconcile.py, test_namespace.py, test_rebaseline.py) confirms they all route through
    the same schema-typo fixture, not real YAML-syntax breakage.'
