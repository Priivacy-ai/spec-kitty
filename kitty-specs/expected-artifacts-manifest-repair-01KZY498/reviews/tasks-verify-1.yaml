# review-verify/v1
schema: review-verify/v1
complete: true
phase: tasks
round: 1
results:
  - id: TASKS-DECOMP-001
    status: resolved
    reason: WP03 T017 now has a required idempotency guard (step 1, gh issue list --search before creating) that reuses a prior matching issue instead of filing a duplicate, and the DoD gained an explicit bullet stating T015/T016's code-content may be reviewed and marked complete independently of T017's tracker-side completion.
  - id: TASKS-SEQ-001
    status: resolved
    reason: wps.yaml now declares WP04 dependencies as [WP01], and lanes.json's regenerated lane-d (WP04) carries depends_on_lanes ["lane-a"] with parallel_group 1, strictly after WP01's lane-a at parallel_group 0 — a real structural edge, not prose-only.
  - id: TASKS-SEQ-002/VERIFY-001
    status: resolved
    reason: WP01 T002 step 4 now asserts only the three manifests WP01 owns and explicitly says not to assert on plan, T005's validation note and the DoD bullet were updated to state the test is fully GREEN at WP01's own completion with no carve-out, and WP03 T016's validation text no longer references "closing out the partial check WP01 left open" — all three spots are now internally consistent.
  - id: TASKS-VERIFY-002
    status: unresolved
    remaining_severity: 2
    reason: T006 step 1 itself was correctly reworded to state no ValidationError catch exists in load_manifest(), but the file's own "Reviewer Guidance" section (line 432) was left unchanged and still instructs the reviewer to "Confirm the except Exception -> except pydantic.ValidationError narrowing in manifest.py did not silently swallow a different...exception type" — reintroducing the exact catch-exists framing T006 step 1 was fixed to remove, so a reviewer following this guidance would look for (and could accept) a ValidationError catch that the corrected implementation instructions say must not exist.
  - id: TASKS-VERIFY-003/DECOMP-002
    status: resolved
    reason: WP02 T012 and T014 now state the path_pattern as exactly "tasks/WP*.md" with no residual "reuse the existing tasks/*.md-style pattern" framing, and WP05's T020 step 2 was swept to additionally assert the literal path_pattern string is exactly "tasks/WP*.md" for the software-dev manifest's tasks_packages/tasks_finalize entries, not just step-key presence.
