# review-refute/v1
schema: review-refute/v1
complete: true
phase: plan
batch: 2
verdicts:
  - id: PLAN-VERIFY-003
    verdict: confirmed
    reason: >
      Read .github/workflows/ci-quality.yml directly. The kernel-tests job starts at line 1077
      with `needs: [changes]` at line 1079, and an `if:` block at lines 1080-1082 reads
      `always() && (needs.changes.outputs.kernel == 'true' || github.event_name == 'push')` —
      exactly the gate the finding cites, two lines below the `needs:` line plan.md points at.
      plan.md's own text (verified at lines 443-446) says "no `if:` gate visible restricting it
      to the `kernel` filter group alone — re-verify at implementation time, but treat as
      always-on." That statement is factually wrong: the gate is visible, present, and would
      exclude this mission's PR (a `pull_request` event touching no `src/kernel/**` or
      `tests/kernel/**` paths per this mission's file list). The plan's practical bottom line
      (no coverage-floor risk) still lands right, but for "the job doesn't run," not "it runs
      unconditionally and passes trivially" as stated.
  - id: PLAN-VERIFY-004
    verdict: confirmed
    reason: >
      Read .github/workflows/ci-quality.yml directly. The consumer-compatibility job (line
      4216, "SaaS consumer compatibility") has `if: always() && needs.changes.outputs.release
      == 'true' && needs.build-wheel.result == 'success'` at line 4220 — a real path gate on
      the `release` filter group. That group (defined ~line 279) covers
      `src/specify_cli/release/**`, `tests/release/**`, `pyproject.toml`, `uv.lock`,
      `CHANGELOG.md`, etc. — none of which this mission's file set (dossier/manifest.py,
      sync/namespace.py, tests/dossier, packs/built-in/missions/*.yaml) touches. plan.md's
      "Explicitly NOT included" bullet (verified at lines 511-515) states the job "[r]uns as an
      always-on job regardless... not separately justified as excluded since it has no path
      gate" — contradicted by the gate found at line 4220. The plan's conclusion (job doesn't
      meaningfully affect this PR) is accidentally right for the wrong stated mechanism.
  - id: PLAN-VERIFY-005
    verdict: confirmed
    reason: >
      Read .github/workflows/ci-quality.yml directly. `tests/dossier/**` is at line 376,
      genuinely inside the `core_misc:` filter block (which starts at line 322 and lists
      src/specify_cli/{egress.py,core,coordination,delivery,event_journal,
      doctrine_synthesizer,saas,status,tool_surface}/** plus various tests/** paths — no
      `src/specify_cli/dossier/**` entry anywhere in that block). `src/specify_cli/dossier/**`
      is instead at line 495, inside a separately-named `agent_surface:` block (starts line
      492, alongside orchestrator_api, tracker, bulk_edit, skills). plan.md's claim (verified
      at lines 423-426) that "`tests/dossier/**` and `src/specify_cli/dossier/**` are explicitly
      listed in the `core_misc` path-filter group" is only half right — the src path is in a
      distinct group. Confirmed also that fast-tests-core-misc's `if:` (line 1704) ORs across
      both `core_misc` and `agent_surface` (plus others), so the practical "applies" conclusion
      survives, but the plan's specific group-membership claim for
      `src/specify_cli/dossier/**` is factually wrong.
  - id: PLAN-VERIFY-006
    verdict: confirmed
    reason: >
      Read the Test Strategy Per Acceptance Criterion table (plan.md lines 384-401) directly —
      every row was scanned by FR/AS: FR-001, FR-002/003, FR-004/005, FR-006, FR-007, FR-008,
      SC-001, FR-010, FR-011 (explicit "N/A — tracker-state assertion... verified once at
      mission close, not re-run in CI"), FR-009, SC-003, FR-016 (x3), SC-006, FR-015. FR-014 has
      no row and no explicit N/A note anywhere in the table or the surrounding plan (grepped
      "override" across the whole file — the only FR-014 test-adjacent text is IC-04, lines
      582-597). IC-04's own Risks paragraph (lines 593-596) is "Low. The only failure mode is
      writing a comment vague enough that a future maintainer still believes the override might
      take effect — the comment must name the specific mechanism... and point at the
      follow-up-candidate issue" — no regression test is named, only a one-time authoring-care
      concern. One caveat on the finding's own evidence: its quoted "...the reviewing squad
      should check this comment word-for-word" clause is not actually IC-04's text — that
      sentence belongs to IC-03's risk paragraph (lines 577-580, a different FR's header-comment
      requirement) and the finding's ellipsis spliced it onto IC-04's quote. That's a
      transcription slip, but it doesn't change the substance: FR-014 genuinely has no Test
      Strategy row, no explicit N/A rationale, and no automated revert-fails test in this plan
      as written — the core claim holds independent of the misattributed sentence.
