# review-verify/v1 — canonical format for fix-verification output.
schema: review-verify/v1
complete: false
phase: spec
round: 3
results:
  - id: SPEC-FRESH-002
    status: resolved
    reason: >
      spec.md FR-016 and tracer-design-decisions.md Decision 3 no longer claim the
      sync-pipeline path satisfies "fails loudly ... for free"; a new Decision 5 records
      the chosen remediation (option a: soften + name the gap, not option b: widen scope)
      with rationale tied to Decision 2/C-002, and the residual gap (every real caller of
      trigger_feature_dossier_sync_if_enabled discards DossierSyncResult without reading
      .errors) is stated honestly rather than hand-waved. Spot-checked live code: found
      the same 10 caller sites cited in Decision 5 (merge/executor.py:1300,
      research.py:195, tasks_mark_status.py:399, sync/__init__.py:196,
      workflow_executor.py:877, mission_record_analysis.py:372, mission_finalize.py:1763,
      mission_setup_plan.py:773, migration/backfill_identity.py:290,
      migration/normalize_mission_lifecycle.py:111) and confirmed none reads `.errors`;
      dossier_pipeline.py:243-250, reconcile.py:151-160, rebaseline.py:162-170,
      namespace.py:90-101, and the outer except at dossier_pipeline.py:368 all match
      their cited line ranges/comments exactly.

  - id: SPEC-FRESH-003
    status: resolved
    reason: >
      FR-012's test-addition enumeration now explicitly names "(AS5)" for the
      reconcile.py/rebaseline.py propagation test and "(AS6)" for
      resolve_manifest_version()'s ValidationError fallback test, and SC-003 adds
      matching bullets naming both regression tests by scenario; AS5/AS6 (spec.md
      Acceptance Scenarios 5-6, User Story 3) are therefore now gated by the
      requirements/success-criteria machinery instead of existing only in prose.
complete: true
