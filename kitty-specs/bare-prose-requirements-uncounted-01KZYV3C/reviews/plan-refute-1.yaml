schema: review-refute/v1
complete: true
phase: plan
batch: 1
verdicts:
  - id: PLAN-GOV-001
    verdict: confirmed
    reason: >
      radon confirms cyclomatic complexity 16 for _validate_requirement_mapping
      (mission_finalize.py:621-675), the ceiling of 15 is real (pyproject.toml
      mccabe max-complexity=15, also restated verbatim in plan.md's own text at
      line 602), IC-03 schedules a new branch into this exact function, and
      plan.md's Campsite-Clean Scope section (lines 598-602) explicitly claims
      none of the six touched functions, including this one, is "within reach"
      of the ceiling -- false, since it is already one over.
  - id: PLAN-GOV-002
    verdict: confirmed
    reason: >
      Plan.md's C-008 decision (lines 203-217) frames the missing broadened-
      predicate FP measurement as "not performed or available" without
      acknowledging that IC-07/Story 4 AC4's false-negative sample already
      builds the exact broadened-heading corpus-walk helper that could produce
      the same FP data point at minimal marginal cost, and spec.md's C-008 row
      / Story 4 AC4 (verified directly) require only a false-negative sample,
      never foreclosing a plan-phase FP measurement.
  - id: PLAN-ARCH-001
    verdict: confirmed
    reason: >
      test_bridge_cores.py's _snapshot() base_status_facts dict (lines 238-249)
      confirmed to lack a "bare_prose_requirement_failures" key; plan.md's code
      sketch (lines 340-345) reads snapshot.status_facts["bare_prose_requirement_failures"]
      via unconditional subscript in all four touched guards (plan.md IC-02,
      grep-confirmed for all four guard names); existing tests at lines 279-282
      and 306-308 build snapshots via the unmodified default dict and assert
      exact-equality lists, so they raise KeyError once wired as sketched.
  - id: PLAN-VERIFY-001
    verdict: confirmed
    reason: >
      Re-derived directly: a detector that always returns [] passes assertion 1
      (empty result for every non-fixture spec) trivially and assertion 2
      vacuously (empty set is a subset of any recorded set); the cited precedent
      test_ratchet_baselines.py is confirmed count-based (len() vs. integer
      baseline), not signature-based; charter Standing Order 5 (confirmed text,
      lines 74-79) requires a concrete floor + self-mutation test, and plan.md
      never cites architectural-gate-non-vacuity nor proposes a self-mutation
      test for this ratchet, only frozen-baseline-shrink-only-ratchet.
  - id: PLAN-VERIFY-002
    verdict: confirmed
    reason: >
      ci-quality.yml:3648 `if: always() && (github.event_name == 'schedule' ||
      github.event_name == 'workflow_dispatch')` confirmed verbatim with the
      job's own comment stating "PRs skip Sonar entirely to keep review latency
      low" (line 3588), directly contradicting plan.md's Gate Set section (line
      456) which lists SonarCloud under "Triggered / must pass" as "always
      attempted when SONAR_TOKEN is configured."
  - id: PLAN-VERIFY-003
    verdict: confirmed
    reason: >
      Live `git log --oneline ab15225ea..HEAD` now returns 8 commits with
      HEAD=38a216f92 (a plan(...) commit), while plan.md's own text (lines
      512-514) states HEAD=2e088fbe0, 7 commits, enumerating only spec(...)/
      fix(spec:...)/reviews(spec:...)/meta-add prefixes -- none of which is
      "plan(...)" -- confirming the staleness claim exactly as stated.
