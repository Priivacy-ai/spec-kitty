work_packages:
  - id: WP01
    title: "Activation-time empty-action-sequence gate"
    dependencies: []
    owned_files:
      - "src/charter/mission_type_profiles.py"
      - "src/specify_cli/cli/commands/charter/activate.py"
      - "tests/charter/test_mission_type_profiles.py"
      - "tests/specify_cli/cli/commands/charter/test_charter_activate_commands_core.py"
    # SC-004 is intentionally absent below. `_REF_FIND_PATTERN`
    # (`requirement_mapping.py:16`) matches only `(FR|NFR|C)-\d+`, so any `SC-###`
    # entry would be dropped at the tasks-packages -> WP-frontmatter boundary anyway
    # (upstream defect #2991, cited not re-filed) -- this is a known tooling gap, not
    # an authoring oversight. Do not "clean it up" by inventing a workaround.
    requirement_refs:
      - FR-001
      - FR-002
      - FR-003
      - FR-004
      - FR-005
      - FR-006
      - FR-007
      - NFR-001
      - NFR-002
      - C-001
      - C-002
      - C-003
      - C-004
      - C-005
      - C-006
      - C-007
    subtasks:
      - T001
      - T002
      - T003
      - T004
    prompt_file: "tasks/WP01-activation-empty-action-sequence-gate.md"
