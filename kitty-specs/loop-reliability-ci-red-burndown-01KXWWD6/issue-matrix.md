# Issue matrix — loop-reliability-ci-red-burndown-01KXWWD6

Per FR-037 of the spec-kitty-mission-review skill Gate-4. One row per issue referenced in spec.md.

> ⚠ `finalize-tasks` re-scaffolds MISSING rows only (idempotent). Re-fill verdicts once after finalize.

| Issue | Title | Verdict | Evidence ref |
|-------|-------|---------|--------------|
| #2534 | pre-review gate "gate authorities unavailable" in consumer repos | fixed | FR-002/WP01: landed `fix/2534` (`0153934f9`) byte-identically (+142/−3, 4 files) → consumer-repo `move-task --to for_review` calm-degrades to a non-blocking `no_coverage` warn; spec-kitty-repo path unchanged (NFR-003). 58 tests green; reviewer-verified fidelity. #2598 (epic-blocked under #2535) supersedes by-construction later. |
| #2573 | move-task pre-review gate reads as a hang (no skip flag, sync-daemon deaf to disable env) | deferred-with-followup | FR-003 closes the **#2573b** sub-scope (sync daemon honors `SPEC_KITTY_SYNC_DISABLE`; flips `test_daemon_sync_disable_env` green) in-mission. **#2573 stays OPEN** for the #2573a-deep design axis (non-blocking/streamed gate) — Follow-up: **#2573**. |
| #2807 | CI: 4 untracked charter synthesize/evidence/bundle reds | fixed | FR-004/WP04+WP05: WP04's `isinstance` guard in `orchestrator.py` REAL-fixed the `'str'.get` evidence crash — full `test_charter_epic_golden_path` e2e greens end-to-end (past synthesize), clearing 3 reds; no xfail needed. WP05 seeded the tracked `charter.yaml` fixture (`test_bundle_contract` green) + skip-when-logged-out guard on `test_upgrade`. All reviewer-verified. |
| #2809 | CI: untracked strict-JSON-under-sync-skip red (test_strict_json_stdout) | deferred-with-followup | FR-005/WP03: red-first **disproved** the leaked-toggle premise — `test_strict_json_stdout` contains zero `SPEC_KITTY_SYNC_*` refs and fails with the **#2782** signature (`sync.server_auth_failure … Connection refused` in `final_sync`), not a toggle leak. The FR-001 shared env-reset fixture landed (byte-neutral to this test, valuable for genuinely toggle-sensitive tests). The test stays on the **non-blocking** `regression visibility` gate (`@pytest.mark.regression`) so NFR-001 (blocking jobs) is unaffected; its live red is tracked under #2782. Follow-up: **#2782**. |
| #2812 | CI debt: urn-lane warnings flake + mission-loader-coverage skip anomaly | fixed | FR-006/WP06: urn-lane legacy-warning flake root-fixed. Pre-PR squad found WP06's first attempt (a `__warningregistry__` clear) chased the wrong mechanism — the assertion intermittently saw 0 warnings because `_warn_legacy_asset` SUPPRESSES the DeprecationWarning (emits a one-time stderr nudge) when `_is_global_runtime_configured()` is True, and that predicate reads ambient machine/HOME state under `-n0`/full-suite ordering. Real fix: point `SPEC_KITTY_HOME` (the uncached env seam `get_kittify_home` reads first) at a fresh empty dir so `_is_global_runtime_configured()` is deterministically False and the emit branch runs — env-based, so it is immune to the module-reload/patch-binding ordering that defeated a `patch(...)` approach (verified green in the adverse full-suite ordering). Mission-loader-coverage anomaly fixed in-slice (no split) by adding `platform` to BOTH bound sites in `ci-quality.yml` (job `if:` + `JOB_GROUPS`), parity gate `test_workflow_coherence` 16 passed. Post-merge caveat: the job-actually-runs runtime effect is verified on the first post-merge `mission_loader/**` change (gate-unmask-cannot-self-validate). |
| #2555 | move-task hangs on the sync-daemon fan-out (multi-point) | deferred-with-followup | Point 2 (`ensure_sync_daemon` deaf to the disable env) is the **same fix as #2573b** — closed in-mission via FR-003/IC-02 (per #2573's body: "Ask (b) above is the same fix"). Points 1/3/4/5 stay open under #2017/#2160. Follow-up: **#2555**. |
| #2782 | test_strict_json_stdout regression (sync-skips-ingress) | deferred-with-followup | WP03 red-first CONFIRMED #2782's RCA is the live cause: `test_strict_json_stdout` fails with `sync.server_auth_failure … Connection refused` in `final_sync` (not #2809's leaked-toggle path). This is out of this mission's scope (a real sync-connection question) → **#2782 stays OPEN**. The test is on the non-blocking `regression visibility` gate, so it does not muddy the blocking release gate (NFR-001). Follow-up: **#2782**. |
| #2222 | Merge coord-worktree tool-churn classifier | deferred-with-followup | Context reference only — the existing churn classifier the deferred #2795/#2367-A merge-design mission reuses; not touched here (C-001 out of scope). Follow-up: **#2795**. |
| #2535 | Epic parent of the by-construction #2534 fix (#2598) | deferred-with-followup | Context reference — #2598 is epic-blocked under #2535/#2466; neither is in scope (C-001/C-002). Product P0 parenting is unchanged (C-004). Follow-up: **#2535**. |
| #2598 | By-construction pre-review-gate fix (supersedes #2534 later) | deferred-with-followup | Explicitly OUT of scope (C-001): epic-blocked under #2535 step-3, no PR. #2534 lands via the ready `fix/2534` branch now (FR-002); #2598 supersedes by-construction later. Follow-up: **#2598**. |
| #2800 | Pre-review-gate sync-toggle isolation pattern | verified-already-fixed | Referenced only as the shipped isolation pattern FR-001's shared env-reset fixture mirrors; #2800 already merged. No in-mission action. |

Valid `Verdict` values: `fixed`, `verified-already-fixed`, `deferred-with-followup`, `in-mission` (being fixed by a later WP in this mission; must reach a terminal verdict before mission `done`).

Note: `#2573` resolves to `deferred-with-followup` (2573b closed in-mission; 2573a-deep stays open on #2573). #2807/#2812 may resolve to `deferred-with-followup` if their deferred sub-parts (evidence-red xfail / loader anomaly split) are not fixed in-slice.
