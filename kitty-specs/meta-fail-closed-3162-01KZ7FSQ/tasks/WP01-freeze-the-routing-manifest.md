---
work_package_id: WP01
title: Freeze the routing manifest, both floors, and the budget allocation
dependencies: []
requirement_refs:
- FR-002
- NFR-002
- C-001
- C-003
planning_base_branch: feat/meta-fail-closed-3162
merge_target_branch: feat/meta-fail-closed-3162
branch_strategy: Planning artifacts for this mission were generated on feat/meta-fail-closed-3162. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into feat/meta-fail-closed-3162 unless the human explicitly redirects the landing branch.
subtasks:
- T001
- T002
- T003
- T004
- T005
history: []
agent_profile: debugger-debbie
authoritative_surface: scripts/
create_intent:
- scripts/verify_meta_routing_manifest_3162.py
execution_mode: planning_artifact
owned_files:
- scripts/verify_meta_routing_manifest_3162.py
role: investigator
tags: []
tracker_refs: []
---

# Work Package Prompt: WP01 – Freeze the routing manifest, both floors, and the budget allocation

## ⚡ Do This First: Load Agent Profile

Use the `/ad-hoc-profile-load` skill to load the agent profile specified in the frontmatter, and behave according to its guidance before parsing the rest of this prompt.

- **Profile**: `debugger-debbie`
- **Role**: `investigator`
- **Agent/tool**: `claude`

If no profile is specified, run `spec-kitty agent profile list` and select the best match for this work package's `task_type` and `authoritative_surface`.

---

## Ownership note — read this; the earlier version asserted a constraint that does not exist

**What is true.** `owned_files` may **not** contain paths under `kitty-specs/`: `_is_mission_specs_owned_file`
(`src/specify_cli/cli/commands/agent/mission_parsing.py:153-157`) classifies any such entry as a mission-specs
path and `_invalid_mission_specs_owned_files` (`:207-215`) turns it into a hard `finalize-tasks` error
(`_validate_owned_files_not_in_mission_specs`, `mission_finalize.py:918-937`). So the two `contracts/*.md`
deliverables are written as declared **out-of-map** planning writes with a one-line rationale each, which the
tasks-packages contract permits.

**What was false and is struck.** The earlier note said the `kitty-specs/` prohibition *"forced"* this WP to
own a script. It does not force that, and it is not the reason the script exists. Two corrections:

1. `execution_mode: planning_artifact` is what matters, and it routes this WP to the **repository root**
   (`src/specify_cli/workspace/context.py:752` — *"planning_artifact -> repository root"* — and the branch at
   `:761`). That is the load-bearing fact: the repository root is the only tree with a `.venv`. Nothing
   provisions `.venv` into a git worktree (`.gitignore:31-32` ignores `.venv` / `.venv*/`; there are no venv
   references across `workspace/`, `lanes/` or `git/`). A `code_change` declaration would have given this WP a
   lane worktree in which every `.venv/bin/python` command below is unrunnable.
2. The `scripts/` entry exists because **an ownership manifest requires a non-empty `owned_files`**, not because
   `kitty-specs/` is barred. Measured on this tree: `build_wp_manifests` builds a manifest only when
   `fm.execution_mode and fm.owned_files` are **both** truthy
   (`src/specify_cli/ownership/validation.py:355-357`), and `compute_lanes` then raises
   `LaneComputationError: Executable WP 'WP01' has no ownership manifest` for any WP in the dependency graph
   without one (`src/specify_cli/lanes/compute.py:326-336`). `validate_authoritative_surface`
   (`ownership/validation.py:223-253`) additionally requires `authoritative_surface` to prefix an
   `owned_files` entry. So `owned_files: []` — which `_owned_files_yaml_is_explicit_empty_list`
   (`mission_parsing.py:161-187`) and `mission_finalize.py:796-801` *do* honour at the **inference** layer —
   **fails at the lane layer**. `[UNVERIFIED — upstream gap]`: a `planning_artifact` WP whose only deliverables
   live under `kitty-specs/` has no fully clean declaration today; `_PLANNING_PREFIXES` is
   `("kitty-specs/", "docs/")` (`ownership/validation.py:75`), so a `scripts/` entry earns a **non-fatal**
   consistency warning. File it upstream rather than working around it silently.

**Therefore the script is a real deliverable, not a placeholder.** `scripts/verify_meta_routing_manifest_3162.py`
**must be created** by this WP (T004 owns it; see its Files and Validation lines and DoD item 12). It is the
re-runnable form of this manifest's measurements — every count in this mission's plan was wrong at least once,
so re-runnable evidence beats a prose snapshot, and a manifest whose numbers can be regenerated by one command
is strictly better than one that cannot. An earlier draft named the script **only** in frontmatter while DoD
item 1 forbade creating any other file, so this WP could be marked done with its entire declared write surface
empty; that contradiction is resolved by making the surface load-bearing.

Note `scripts/` is **not** in CI's lint scope (`ruff check src tests`) and is exempted only from `TID251`, so
run `ruff check` on the script explicitly and keep every function at complexity <= 15.

## Objective

Produce the mission's measurement freeze as two contract documents: `contracts/routing-manifest.md`
(the 13 routable call sites with their arms, the 6 `except ValueError` handlers, the 5-read-expression /
6-invocation-site bypass set, and both live floors) and `contracts/headroom-allocation.md` (the
single-allocator routed budget and the `NFR-002`/`SC-013` predicate-symbol pre-list). Every number in both
documents ships with the command that produced it and that command's input count.

This is a **measurement** work package, not a writing one. Nothing downstream is priced correctly if a
count here is right by luck.

## Context

Every other work package in this mission (`WP02`–`WP08`) depends on this one, and each of them is required
to print the live routed count **before and after its own edit** against the command and input count this
manifest records. Without a recorded command the mission cannot tell a mid-mission drift caused by an
unrelated landing from a defect caused by its own routing — and `ROUTED_CALLEES` in
`tests/architectural/test_inline_meta_read_gate.py` matches callee **names** over the whole of `src/`, not
the call graph (it counts `doc_analysis/doc_state.py`'s *locally defined* `_require_meta`). That gate's own
header records **three prior false reds** from exactly this miscount, so attribution is the deliverable,
not a nicety.

The post-plan adversarial squad (`analysis-report.md`, binding) **refuted** one of `plan.md`'s counts
outright (`31 candidates / 30 rejected at clause 3` reproduces under no definition; measured is
`19 → 17 at clause 2 → 1 at clause 3 → 1 accepted`) and found the *reasoning* behind another to be
inverted (clause 2, not clause 3, is what holds gate false positives at zero). Two counts survived on the
number and failed on the argument. That is why this manifest's contract is **reproducibility**, not
correctness: a bare output integer is not a measurement.

The decisions from `plan.md` this manifest exists to pin:

- **`C-001` / D4=(a)** — no site's arm changes. The split is **4 degrade / 2 refuse-typed / 7 refuse-raw**,
  re-derived; the inherited "6 divergent-wrapper / 7" was wrong (6 is the count of sites carrying an
  `except ValueError`; only 4 of those 6 degrade).
- **`C-003`** — cite `load_meta_fail_closed` (`src/specify_cli/core/paths.py:638`) and
  `MissionMetaReadError` (`:506`) **by symbol**, never by line alone. This binds the whole manifest: the
  mission edits these files, so every line number in it goes stale while the document is still in force.
- **`NFR-002`** — the kept clause (no second predicate answering "is this `meta.json` readable") needs a
  **pre-list of predicate symbols**, or `SC-013` closes by narration. The struck clause (immovable floor)
  must not be restated.
- **`FR-002`** — the 4 degrade sites keep degrading; their handlers are `except ValueError` today and
  `MissionMetaReadError` is a `RuntimeError` (`core/paths.py:506`), so the manifest must record each
  degrade site's fallback **and whether that fallback is derived from the malformed file or constant**.
- **Headroom** — exactly **one** net routed call exists for the whole mission. `WP05` spends it, routing
  `src/specify_cli/git/ref_advance.py:247` per operator ruling **R-1**. Every other work package is
  **0-net**. Lanes B (`WP02→WP03→WP04`) and C (`WP05→WP06`) are parallel-safe *only* because of that
  constraint, so it is load-bearing, not bookkeeping.

**Standing measurement rules for this work package** (from `spec.md` § Standing rules, binding):

1. Every count shows its **command** and its **input count**: "N candidates in, M out, and why each of the
   N−M was dropped."
2. **Control every probe against a case whose answer you already know, and show the control.** Two traps
   are documented on this tree: a naive `grep -c 'except ValueError'` returns **9** where the answer is
   **6** (it matches a comment at `src/mission_runtime/resolution.py:491` plus two unrelated handlers), and
   a naive grep for routed calls returns **296** where the answer is **129**.
3. Never pipe a suite whose exit status you need — redirect to a file, then quote the `N passed` line.
   `-ra`, never `-rf`. Count `^ERROR tests/`, not `^ERROR `.
4. Prefer an **AST walk** over a regex when the answer must be exact, and **say which you used** for every
   number.
5. `tests/sync` and `tests/cli` must never run concurrently, and a sibling mission at `~/dev/sk-missions/3167`
   may hold the `tests/sync` window. **Neither directory is in this mission's cone.** Check before any
   broad sweep (`pgrep -af 'run_sync[_]daemon'` empty); this work package should need no sweep at all.
6. `ruff check` only. This repository is **not** `ruff format`-clean; never run `ruff format`. Write scratch
   scripts under a **per-agent unique filename** — three lenses in the post-plan pass had a shared scratch
   file overwritten mid-run by a concurrent lens.

This work package owns **no source and no test files**. It writes two Markdown contracts plus one measurement
script under `scripts/` (`owned_files`), and commits once.

## Where this WP runs, how to start it, and where its evidence lands

**This WP runs from the repository root.** `execution_mode: planning_artifact` routes the workspace there
(`src/specify_cli/workspace/context.py:752`, `:761`), which is also the only tree that has a `.venv`
(`.gitignore:31-32`).

**Start command:**

```bash
spec-kitty implement WP01
```

`spec-kitty agent action implement WP01 --agent <name>` does **not** prepare a workspace — its `--help` reads
*"Display work package prompt with implementation instructions."* `CLAUDE.md` § Execution Workspace Strategy
is explicit: *"`spec-kitty implement WP##` is the only supported way to prepare a workspace."* Use it, then
read this prompt.

**`PYTHONPATH` on anything that could run outside the root.** Every `python -c` and every `pytest`
invocation in this prompt that might be executed from somewhere other than the repository root must carry
`PYTHONPATH=<workspace>/src`, where `<workspace>` is the tree whose `src/` you intend to measure. The reason
is a split-tree hazard that produces a *silently wrong* answer rather than an error:
`.venv/lib/python3.11/site-packages/_editable_impl_spec_kitty_cli.pth` pins `specify_cli` / `runtime` imports
to the **main** tree's `src/`, while `SRC_ROOT` in the gate (`tests/architectural/test_inline_meta_read_gate.py:61`,
`SRC_ROOT = _REPO_ROOT / "src"`) and `_SRC_ROOT` in the ledger test
(`tests/specify_cli/test_meta_fail_closed_full_census_contract.py:54`) are derived from **the test file's own
location**. In any tree other than the one `.venv` was installed from, the AST census therefore reads the
*edited* `src/` while every behavioural assertion imports the *unedited* one: a structural assertion goes
green while its behavioural twin stays red, with no diagnosable cause.

**Committed evidence destination.** `spec-kitty agent tasks mark-status` exposes only
`--status`, `--mission`, `--auto-commit` and `--json`; its payload is
`WPInnerStateDelta.subtasks: Mapping[str, Status]` (`src/specify_cli/status/models.py:481`) — a bare
`{T0xx: Status}`. **The record carries no evidence field.** This WP's committed destination is its own
deliverable: `kitty-specs/meta-fail-closed-3162-01KZ7FSQ/contracts/routing-manifest.md` and
`contracts/headroom-allocation.md` (out-of-map planning writes, which is what `planning_artifact` is for),
with `scripts/verify_meta_routing_manifest_3162.py` as the committed, re-runnable form of the numbers in them.
Every number, command, input count, control and quotation named below goes **into those files**. Nothing is
left in `/tmp`.

---

### Subtask T001: The 13 routable call sites — arm, handler, ledger row, by symbol

**Purpose**: Pin, in `contracts/routing-manifest.md`, the 13 `meta.json` read sites that route onto
`load_meta_fail_closed`, each with the arm it takes **today** (`C-001`: 4 degrade / 2 refuse-typed /
7 refuse-raw), the handler that must move with it, and the `_ACCOUNTED_SITES` ledger row whose deletion is
part of the same commit. Cite by **module-qualified symbol** (`C-003`), because the routing edits shift
every line number in the table.

**Steps**:

1. Re-derive the arms with an **AST classifier**, not a grep. For each censused `(file, function)` pair,
   locate the `load_meta` call node, find every same-function `ast.Try` whose *body* contains it, keep the
   handlers catching `ValueError` (or a tuple containing it, or a bare `except`), and classify each handler
   body as REFUSE (contains `ast.Raise`) or DEGRADE (does not). No such handler ⇒ REFUSE-raw. Write the
   script to `<scratchpad>/wp01_derive_arms_<unique>.py`.
2. **Run the control first and record it.** Three sites whose arm is known by reading:
   `specify_cli.core.paths.load_meta_fail_closed` must classify REFUSE-typed;
   `specify_cli.missions.mission_metadata.load_meta_or_empty` has no `try` at all and must classify
   REFUSE-raw. Quote the control output *above* the census output in the manifest. A classifier that has
   not been controlled produces a number, not a measurement.
3. Print the input count: `INPUT rows (ledger pending-batch-a): 12` and
   `expected call sites: 13`, then `DERIVED call sites: 13 / DEGRADE: 4 / REFUSE-raw: 7 / REFUSE-typed: 2`.
   State that the two 13s (`grep -c 'pending-batch-a'` → 13, and the row-count expansion → 13) agree **by
   coincidence, not construction** — the grep total is 12 rows plus the docstring mention at
   `tests/specify_cli/test_meta_fail_closed_full_census_contract.py:185`. Derive from the row *counts*.
4. Write the table with columns: row #, module-qualified symbol, current `file:line` (marked
   `[shifts — cite the symbol]`), read shape, arm today, fallback value, handler `file:line`, ledger row
   line, owning work package. Rows 1–3 are `mission_runtime.resolution._mid8_from_primary_meta` (`:509`,
   degrade → `""`), `._resolve_coordination_branch` (`:852`, degrade → `None`),
   `._resolve_mission_id` (`:1107`, degrade → the `legacy-<slug>` sentinel); rows 4–7 are
   `runtime.next._internal_runtime.planner._resolve_workflow_for_mission` (`:188`),
   `runtime.next.runtime_bridge_io._workflow_runtime_template` (`:380`),
   `specify_cli.bulk_edit.gate._is_bulk_edit_mission` (`:57`),
   `specify_cli.bulk_edit.gate.ensure_occurrence_classification_ready` (`:80`); row 8 is
   `specify_cli.context.resolver._read_meta_json` (`:75`); row 9 is
   `specify_cli.decisions.service._resolve_mission_id` (`:134`); rows 10 and 11 are both
   `specify_cli.missions._read_path_resolver.read_primary_meta` (`:846` first read, `:862` canonicalized
   re-read); row 12 is `specify_cli.missions._resolve_planning_branch.load_mission_target_branch` (`:116`);
   row 13 is `specify_cli.upgrade.feature_meta.load_feature_meta` (`:42`).
5. **Disambiguate `_resolve_mission_id`.** The symbol occurs twice — row 3 in `mission_runtime/resolution.py`
   and row 9 in `specify_cli/decisions/service.py` — with **different arms** (degrade vs refuse-typed).
   `C-003` says cite by symbol; a bare `_resolve_mission_id` is therefore ambiguous. Every citation in both
   contracts must be module-qualified. Record this as an explicit note, because it is the one place where
   `C-003` compliance is insufficient on its own.
6. Record the **counting convention** in force: "site" = **call site** (13); "ledger row" =
   `_ACCOUNTED_SITES` row (**12**, one of them — `read_primary_meta` — with count 2). State it once at the
   head of the document and again in the table caption.
7. Pin the **row → ledger line** mapping by measurement, not inference. `plan.md` distributes the ledger
   lines across concerns (`:198`–`:200` and `:249` to `WP04`; `:201`–`:204` and `:243` to `WP02`; `:215`,
   `:222`, `:244` to `WP03`) but no artifact states which row sits on which line. Read
   `tests/specify_cli/test_meta_fail_closed_full_census_contract.py` and pin row → line → symbol in one
   column, quoting each row's text. Print the input count (12 rows read).
8. Record per degrade site whether the fallback is **derived from the malformed file or constant**: rows 1
   (`""`) and 13 (`None`) are constants; row 2 (`None`, "coordination topology undeclared") and row 3
   (falls through to `legacy-<slug>`) must be read and classified, because a *derived* fallback can produce
   a plausible-but-wrong value (`FR-002`, `NFR-003`).
9. Record that all 13 sit on the canonical reader's **raise** contract (`on_malformed="raise"` — explicit
   at 7 sites, the signature default at 6), so a malformed file genuinely produces a `ValueError` at each.
10. Note the `:509` / `:512` reconciliation: `grep -n 'on_malformed' src/mission_runtime/resolution.py`
    returns `512`, `852`, `1107`; `:512` is the keyword line of the multi-line call whose `ast.Call` node
    begins at `:509`. **Same single call site.** The manifest uses the call-node line for consistency with
    every other row, and says so.

**Files**: create `kitty-specs/meta-fail-closed-3162-01KZ7FSQ/contracts/routing-manifest.md`
(§1 the 13 sites; ~110–140 lines of this document).

**Validation**: the AST classifier's control output and census output both quoted verbatim with input
counts; `DEGRADE: 4 / REFUSE-typed: 2 / REFUSE-raw: 7`, sum 13; ledger row count 12 with the count-2 row
named; every site cited by module-qualified symbol; a reader can regenerate the table from the quoted
command alone.

---

### Subtask T002: The 6 `except ValueError` handlers, and the grep trap that says 9

**Purpose**: Pin the 6 handlers that `C-002` requires to change in the same edit as their routing, by
`file:line` **and** by enclosing symbol, with the reason the count is 6 and not 4 — and with the naive
probe that returns 9 shown as a controlled negative.

**Steps**:

1. Enumerate the 6, each with its enclosing function symbol:
   `mission_runtime.resolution._mid8_from_primary_meta` → `resolution.py:514`;
   `mission_runtime.resolution._resolve_coordination_branch` → `:853`;
   `mission_runtime.resolution._resolve_mission_id` → `:1108`;
   `specify_cli.decisions.service._resolve_mission_id` → `decisions/service.py:141`;
   `specify_cli.missions._resolve_planning_branch.load_mission_target_branch` →
   `missions/_resolve_planning_branch.py:122`;
   `specify_cli.upgrade.feature_meta.load_feature_meta` → `upgrade/feature_meta.py:43`.
   Confirm each catches **bare** `ValueError` by reading the source, and quote each `except` line.
2. **Run the trap and record it.** `grep -c 'except ValueError' <the six files>` (or over `src/`) returns
   **9**. Show the 3 spurious hits explicitly: a comment at `src/mission_runtime/resolution.py:491`, plus
   two unrelated handlers that catch `ValueError` at reads which are not `meta.json` reads. Format the row
   as "9 candidates in → 3 dropped (1 comment, 2 unrelated reads) → 6 out", naming each dropped item. A
   count without its rejections is not a measurement.
3. Use an **AST walk** for the authoritative 6 (`ast.ExceptHandler` whose `type` resolves to `ValueError`
   and whose `try` body contains the censused `load_meta` node) and say so. Record that the regex was used
   only as the trap control.
4. Record **why 6, not 4**: the 2 refuse-typed sites (rows 9 and 12) also catch `ValueError`, and routing
   them without widening the handler leaks `MissionMetaReadError` where `DecisionError` /
   `PlanningBranchResolutionFailed` is contracted. The inherited "every degrade handler" scoping left those
   two unmandated — this is the `C-002` violation the previous slicing shipped.
5. Record the type fact that makes all six load-bearing: `MissionMetaReadError` is a **`RuntimeError`**
   (`specify_cli.core.paths.MissionMetaReadError`, `core/paths.py:506`, MRO `RuntimeError → Exception`),
   **not** a `ValueError`. Cite by symbol per `C-003`.
6. Record the **`except Exception` ban across all six** (not only the 2 refuse-typed), with its reason:
   `MissionSelectorAmbiguous` is confirmed **not** a `ValueError`
   (`specify_cli.missions._read_path_resolver`, `:44`, plain `Exception`) and is raised *inside* row 1's
   `try`, so a broadened catch there silently swallows an ambiguous-handle refusal (`SC-007`).
7. Record that row 1's handler (`resolution.py:514`) is **EXTENDED, not narrowed**: its `try` also wraps
   `_canonicalize_primary_read_handle` and `_compose_primary_feature_dir`, whose traversal guard raises a
   real `ValueError`. The manifest states the target shape as
   `except (MissionMetaReadError, ValueError)` and names it as **not a narrowing**, so `WP04` cannot read
   `US2` scenario 3 as licence to drop `ValueError`.
8. Record row 8's handler separately and say why it is not one of the 6: `context/resolver.py`'s arm is
   `except FileNotFoundError`, which routing makes **unreachable** (`load_meta_fail_closed` hard-codes
   `allow_missing=True`). Its removal is `FR-013`/`SC-015`, owned by `WP03`. The same is true at rows 9 and
   12, which carry *both* a `ValueError` handler (one of the 6) and a dead `FileNotFoundError` arm.

**Files**: extend `contracts/routing-manifest.md` (§2 the handlers; ~50–65 lines).

**Validation**: 6 handler lines quoted verbatim with enclosing symbols; the `9 → 6` triage printed with all
three rejections named; `MissionMetaReadError`'s MRO quoted from source; the "extended, not narrowed" note
present for `resolution.py:514`; the `except Exception` ban stated over all six.

---

### Subtask T003: The bypass set — 5 read expressions / 6 invocation sites, convention declared

**Purpose**: Pin the reads that reach `meta.json` with **no `load_meta` at all**, in **both** counting
conventions, with the convention named at every appearance of a number — and reconcile the line citations
that two upstream documents disagree about.

**Steps**:

1. State the two conventions and both totals in the same sentence: **5 read expressions / 6 invocation
   sites**. Then state *why they differ*: `specify_cli.cli.commands.merge_driver._load_json_object` is
   **invoked from two call sites** (`:243` and `:244`), so the call-site convention counts it twice. Under
   the call-site convention the total is 6; under the read-expression convention it is 5.
2. Record that the **call-site convention is the one this mission uses elsewhere** — it is exactly why
   census rows 10/11 count `read_primary_meta` as 2 and yield **13** rather than 12. Both conventions are
   defensible; **mixing them silently inside one document is the defect this row exists to prevent.**
   Declare, in a boxed note, that every count in both contracts carries its convention inline.
3. Enumerate the 5 read expressions, each with its enclosing symbol and its parser:
   - `specify_cli.git.ref_advance._committed_meta_object` — `git show` read at `ref_advance.py:203`,
     parsed by `_parse_meta_object` (`:181-189`).
   - `specify_cli.git.ref_advance._meta_change_is_vcs_lock_only` — path built at `:242`
     (`meta_path = worktree / path`), `read_text` at `:244`, parse at **`:247`**.
   - `specify_cli.cli.commands.implement_cores._committed_meta_mapping` — `show_blob` at
     `implement_cores.py:335`, parsed by `_parse_meta_mapping` (`:259`).
   - `specify_cli.cli.commands.implement_cores._is_self_write_only_diff` — `read_bytes` at `:427`, path
     built at `:421-427`, parsed by `_parse_meta_mapping`.
   - `specify_cli.cli.commands.merge_driver._load_json_object` — `read_text` at `merge_driver.py:171`.
4. **Reconcile `plan.md` `[UNVERIFIED]` item 4 by reading the source, and pin one line per read expression
   and one per parse.** `analysis-report.md` cites `ref_advance.py:203` (the `git show` read) and `:242`
   (the path build); `research/3162-census.md` cites `:206` (the parse) and `:244` (the `read_text`). The
   working hypothesis is that these are **different expressions in the same two functions**, not a
   conflict — `_committed_meta_object`: read `:203`, parse `:206`; `_meta_change_is_vcs_lock_only`: path
   `:242`, read `:244`, parse `:247`. **Verify it on the tree and quote the four source lines.** If the
   hypothesis fails, say so and pin what the tree says.
5. Record the **three private parsers** as one row each, since they are the reason the architectural gate is
   blind: `_parse_meta_object` (`ref_advance.py:181`), `_parse_meta_mapping` (`implement_cores.py:259`),
   `_load_json_object` (`merge_driver.py:167`). Record each one's blindness reason: cross-function split
   (the first two) versus path-arrives-as-a-bare-parameter (the third).
6. Record the **triage with input counts at every step**, so the set is reproducible rather than asserted:
   files containing `"meta.json"` **172** → files with zero routed `load_meta*` calls **103** → candidate
   bypass files **30** → parse sites **41** → meta-ish **13** → 7 allowlisted inline, 3 ruled out
   (`metadata.yaml` / evidence-JSON), **3 bypass parsers**. Name the 3 ruled out.
7. Record the closure probes and their results, and then state plainly that **5/6 is the current count, not
   the closure**: constants-vector probe **0** new; call-surface probe over **20 276** calls → 70
   candidates → **no new site**.
8. Record which of the 5 the seam reaches, and on what basis, because `C-004`'s original basis was refuted:
   **2 of the 5 hold real filesystem paths whose parents are feature dirs** — `ref_advance.py:242` (and
   `:315` gates on `Path(path).name == _META_FILENAME`) and `implement_cores.py:421-427` (under a
   `name == _META_JSON_FILENAME` gate). The obstacle at the second is the **routed budget**, not structure.
   Do not restate "structurally cannot use the seam".
9. Record the **three-tier seam family** as a table, with the missing primitive marked filed-not-built:
   **L1** pure decode (`text|bytes → dict|None`, typed) — **missing, must be filed** (`SC-009`);
   **L2** path-level (`specify_cli.missions.mission_metadata._parse_meta_text`, `:331-349`, exists, takes a
   `Path` and performs the read itself so it **cannot** serve the two blob sites); **L3** dir-level
   (`load_meta_fail_closed`, exists, reachable by 2 of the 5).

**Files**: extend `contracts/routing-manifest.md` (§3 the bypass set; ~60–75 lines).

**Validation**: both totals present with their convention named inline at each appearance; the four
divergent source lines quoted; the triage chain printed with every input count; the closure probes with
their denominators; no sentence in the document adds a read-expression count to a call-site count.

---

### Subtask T004: Both live floors, and the two-sided routed band

**Purpose**: Pin routed **129** and inline **7** with their commands and input counts, and state the
routed admissible band `[127, 130]` as a **two-sided** constraint with **126 RED** — machine-readably
enough that the downward failure mode cannot be lost again.

**Steps**:

1. Measure live routed with the gate's own scanner, not a grep. Run
   `.venv/bin/python -m pytest tests/architectural/test_inline_meta_read_gate.py -ra > <scratch>/wp01_routed.txt 2>&1`
   (redirect — never pipe a suite whose status you need) and quote the `N passed` line; then invoke the
   scanner directly to print the count and the **input file count** it walked. Record both: the count
   (**129**) and the denominator.
2. **Run the trap and record it.** A naive grep for routed calls returns **296** where the answer is
   **129**. Print it as "296 candidates in → 167 dropped → 129 out" with the drop reason (the regex matches
   definitions, imports, docstrings and non-`meta.json` callees). State that the authoritative figure came
   from an **AST walk** over the scanner's file population (independently reproduced over 1 199 files in
   the post-plan pass), and say which you used.
3. Record the constants as they stand today, by symbol and line:
   `ROUTED_LOAD_META_FLOOR = 126` (`tests/architectural/test_inline_meta_read_gate.py:221`),
   `ROUTED_LOAD_META_FLOOR_MARGIN = 4` (`:220`), `INLINE_META_READ_FLOOR = 7` (`:127`),
   `FLOOR_MARGIN = 2` (`:134`); allowlist `tests/architectural/inline_meta_read_allowlist.yaml` with **7**
   entries and `inline_meta_read_baseline` **7**, shrink-only.
4. **Quote all three assertions of `test_routed_load_meta_floor` (`:1084`) verbatim from the source**, not
   paraphrased: `len >= FLOOR`; `len > FLOOR` (its own docstring says "never a tautological
   `>= len(routed)`" — explicitly anti-vacuous); `len - FLOOR <= MARGIN`. Derive the band **from the quoted
   text**: with `FLOOR = 126, MARGIN = 4` the admissible band is **`[127, 130]`**, not `[126, 130]`.
5. State **`126` is RED** in its own line, and state the constraint **in both directions**: a routing pass
   that *collapses* two calls into one reds this gate **from below**, exactly as one that adds too many
   reds it from above. Record the concrete temptation: census rows 10 and 11 are **two calls inside one
   function** (`read_primary_meta`), and this programme has already had **three floor mismatches caused by
   folds that collapsed call sites**. A document that records only a ceiling reintroduces the failure mode.
6. Record the attribution machinery: `ROUTED_CALLEES` matches callee **names**, not the call graph — it
   counts `doc_analysis/doc_state.py`'s *locally defined* `_require_meta` — and the census is **global over
   `src/`**, so any unrelated commit anywhere that adds a call named `load_meta*` moves the number. Quote
   the gate header's record of **three prior false reds** from this miscount. This is why the command and
   its input count are part of the anchor: a mid-mission deviation must be **attributable** before it is
   "fixed".
7. Record live inline **7** with its command and input count, and the four mutually-locking assertions that
   govern it: `test_inline_meta_read_floor` (`:1061`),
   `test_inline_meta_read_gate_green_against_seeded_allowlist` (`:1109`), **`test_allowlist_matches_floor`
   (`:1116`, an EQUALITY — `len(allowlist) == INLINE_META_READ_FLOOR`)**, `test_allowlist_shrink_only`
   (`:1125`), plus `test_allowlist_entries_are_still_live` (`:1166`). Name the equality as the assertion
   that forecloses the `floor→8 / allowlist→7` escape; no earlier mission artifact mentioned it.
8. **Do NOT state a re-derived `ROUTED_LOAD_META_FLOOR`.** It is deliberately open. `plan.md`
   `[UNVERIFIED]` item 1 offers **127** (the 3-below-live gap applied to live 130) and a resulting band of
   **`[128, 131]`** — both **derived from R-1's stated rule, not measured**. Write an explicit prohibition
   into the manifest: *the change that moves the floor (`WP06`) must print the measured live count and
   derive the floor from it; copying `127` or `[128,131]` from any planning artifact is forbidden.* Record
   the precedent (`117 → 126` on 2026-08-04, "to restore the established 3-below-live gap") as the *rule*,
   not as a value.
9. Record the expected trajectory as a table so each work package can check itself: pre-mission routed
   **129**; `WP02`/`WP03`/`WP04`/`WP06`/`WP07` **0-net**; `WP05` **+1** → **130**; integrated tree **130**
   against the floor `WP06` re-derives. Inline: **7** throughout, returning to 7 after `:247` is routed.

10. **Create `scripts/verify_meta_routing_manifest_3162.py`** — this WP's one owned file, and a required
    deliverable, not a placeholder. It re-runs the manifest's measurements and **prints** them, so a reader can
    regenerate every number without reconstructing a command from prose: the routed count and its input file
    count, the inline count and its input file count, the four gate constants read out of
    `tests/architectural/test_inline_meta_read_gate.py`, the derived band, the `pending-batch-a` row count with
    the `:185` legend line excluded, and the two documented traps as controls (`except ValueError` **9 → 6**,
    routed **296 → 129**). Requirements: it takes the tree root as an argument and defaults to the repository
    root so it can be pointed at a worktree; it **prints the `PYTHONPATH` and the tree it measured**; it exits
    non-zero if any count is outside the band; `ruff check` clean; every function at complexity <= 15; no
    `# noqa`. Quote its output verbatim into the manifest next to each number, and cite the script by path as
    the regeneration command.

**Files**: extend `contracts/routing-manifest.md` (§4 both floors; ~55–70 lines); create
`scripts/verify_meta_routing_manifest_3162.py`.

**Validation**: `129` and `7` each printed with their command and input count; the `296 → 129` trap shown
with its drop reason; the three assertions quoted verbatim and the band derived from the quotation; "126 is
RED" and the downward direction both stated; no re-derived `ROUTED_LOAD_META_FLOOR` value anywhere in
either contract; a `grep -n '127\|\[128' contracts/routing-manifest.md` finds no copied floor or band;
**`scripts/verify_meta_routing_manifest_3162.py` exists, runs, and its output is quoted** — `ruff check` clean,
`git status --porcelain` lists it, and the manifest names it as the regeneration command.

---

### Subtask T005: The headroom allocation table and the predicate-symbol pre-list

**Purpose**: Write `contracts/headroom-allocation.md` — the routed budget with exactly **one** allocator,
the per-work-package pre/post print obligation, and the `NFR-002`/`SC-013` predicate-symbol pre-list that
the post-change enumeration is compared against.

**Steps**:

1. Open with the invariant in one sentence: **there is exactly one net routed call for the whole mission**
   (live 129, band `[127, 130]`), and **`WP05` spends it**, routing
   `specify_cli.git.ref_advance._meta_change_is_vcs_lock_only`'s parse at `ref_advance.py:247` per operator
   ruling **R-1**. Every other work package is **0-net**.
2. Write the allocation table, one row per work package: may-spend (yes/no), net routed delta, and the
   pre/post print obligation. `WP01` n/a (no code) — records 129 and the band as the anchor; `WP02` 0-net
   (6 swaps; `load_meta` and `load_meta_fail_closed` are **both** in `ROUTED_CALLEES`, so a 1:1 swap is
   count-neutral) — prints 129/129; `WP03` 0-net (3 swaps) — 129/129; `WP04` 0-net (4 swaps) — 129/129;
   `WP05` **+1** — prints pre **129**, post **130**, both inside `[127,130]` with the floor still 126;
   `WP06` 0-net, re-derives `ROUTED_LOAD_META_FLOOR` — prints pre and post the floor move and re-asserts
   all three clauses of `test_routed_load_meta_floor` at the new floor; `WP07` 0-net (test + tracker only);
   `WP08` 0-net — re-derives on the **integrated** tree rather than summing lane numbers.
3. State the print obligation as **per work package, not mission-end**, and say why: `SC-011`'s previous
   form ("print 129 pre and 129 post") was satisfiable **by changing nothing**, because the census counts
   callee **names** and cannot see whether routing happened. Each work package prints the count **before
   and after its own edit**, from its own worktree, using this manifest's recorded command and input count.
   A delta of anything other than its allocated value **stops** that work package.
4. State the lane consequence explicitly: **the 0-net constraint is what makes lanes B and C parallel-safe.**
   Each lane measures the count in its own worktree; if only `WP05` is non-zero, every lane is individually
   green and the merged tree reads 130. A lane that quietly folds two calls into one breaks this
   **downward**, and neither lane's own gate run can see it. Cross-reference the rows 10/11 fold named in
   T004.
5. Write the **predicate-symbol pre-list** for `NFR-002`'s kept clause / `SC-013`: enumerate, as
   **symbols with their defining module**, every symbol that answers "is this `meta.json` readable" over
   the population `ROUTED_CALLEES` scans plus locally-defined predicates — at minimum
   `specify_cli.core.paths.load_meta_fail_closed`, `specify_cli.missions.mission_metadata.load_meta`,
   `mission_metadata.load_meta_strict`, `mission_metadata.load_meta_or_empty`,
   `mission_metadata._parse_meta_text`, `doc_analysis.doc_state._require_meta`,
   `git.ref_advance._parse_meta_object`, `cli.commands.implement_cores._parse_meta_mapping`,
   `cli.commands.merge_driver._load_json_object`. Derive the list by command (AST walk for
   locally-defined predicates plus the `ROUTED_CALLEES` name set), print the **input count**, and enumerate
   **the symbols, not the count** — a count alone gives the post-comparison nothing to diff against.
6. State the comparison rule for `SC-013`: the post list must be **no longer** than the pre list and must
   contain **no new local predicate**. Record the mutation probe `WP08`/`WP06` will run: add a second local
   predicate at one bypass site → the criterion goes red → revert, quoted.
7. Record that a 1:1 `load_meta` → `load_meta_fail_closed` swap is predicate-**neutral** (both are in
   `ROUTED_CALLEES`), so the 13 routings cannot violate `NFR-002` by themselves; the risk is a bypass site
   authoring a *local* answer, which is why the four non-routed bypass sites are diagnosable-only and the
   L1 primitive is **filed, not built**.
8. Record the **struck clause** of `NFR-002` by name, so no downstream work package resurrects it: the
   "routed budget is ONE call / **the floor is immovable**" claim is **struck** under R-1. The budget of one
   call stands; the *immovability* does not. Raising a growth floor toward live is the ratchet
   **tightening** — the precedented operation, not a re-freeze.

**Files**: create `kitty-specs/meta-fail-closed-3162-01KZ7FSQ/contracts/headroom-allocation.md`
(~70–90 lines).

**Validation**: exactly one row of the allocation table says "may spend"; every other row reads 0-net; the
predicate pre-list is a list of module-qualified symbols with its derivation command and input count, not a
number; the struck clause is named as struck; the lane-safety consequence is stated in the document rather
than left to `plan.md`.

---

## Definition of Done

`spec-kitty agent tasks mark-status <Txxx> --status done` marks a subtask; it **records no evidence** (its
payload is a bare `{T0xx: Status}`, `src/specify_cli/status/models.py:481`). The evidence lives in the two
committed contract files. All five subtasks (`T001`–`T005`) must carry the `mark-status` record, and each of
the following must be verifiable by re-running a command quoted **inside the delivered contracts**:

1. `contracts/routing-manifest.md` and `contracts/headroom-allocation.md` both exist under
   `kitty-specs/meta-fail-closed-3162-01KZ7FSQ/contracts/`, **and**
   `scripts/verify_meta_routing_manifest_3162.py` exists — those **three** paths and nothing else.
   `git status --porcelain` shows exactly them. **`spec.md`, `plan.md`, `wps.yaml`, and every file under
   `src/` and `tests/` are untouched.**
2. Every number in both documents is accompanied by (a) the command that produced it and (b) that command's
   **input count**, in the form "N in → M out, with each of the N−M dropped for a stated reason". A bare
   integer anywhere is a defect.
3. Both documented traps are shown as controls with their wrong answers: `except ValueError` **9 → 6** with
   all three spurious hits named (including the comment at `resolution.py:491`), and routed **296 → 129**
   with the drop reason. Each probe states whether it used an **AST walk or a regex**.
4. The 13 sites are tabulated with arm (**4 degrade / 2 refuse-typed / 7 refuse-raw**, sum 13), handler,
   fallback (derived vs constant, per degrade site), and ledger row line, every site cited by
   **module-qualified symbol** — with the `_resolve_mission_id` collision (row 3 vs row 9) called out.
5. The 6 handlers are pinned at `file:line` **and** enclosing symbol; `resolution.py:514` is recorded as
   **extended, not narrowed**; the `except Exception` ban is stated over all six with
   `MissionSelectorAmbiguous` cited as the reason.
6. The bypass set states **5 read expressions / 6 invocation sites** with **the convention named inline at
   every appearance of a count**, the `merge_driver._load_json_object` `:243`/`:244` double invocation as
   the reason they differ, one pinned line per read expression and one per parse (reconciling `:203`/`:206`
   and `:242`/`:244` against the source), and "5/6 is the current count, not the closure".
7. Live routed **129** and live inline **7** are recorded with commands and input counts; the three
   assertions of `test_routed_load_meta_floor` (`:1084`) are quoted **verbatim**; the band `[127, 130]` is
   **derived from that quotation**; **126 is RED** and the constraint is stated in **both** directions with
   the rows 10/11 fold named as the concrete downward risk.
8. **No re-derived `ROUTED_LOAD_META_FLOOR` value and no `[128,131]` band appears anywhere in either
   document**, and the manifest carries an explicit prohibition on copying them from planning artifacts,
   with the measurement obligation assigned to the work package that moves the floor. `NFR-002`'s
   immovable-floor clause is recorded as **struck**; the surviving one-call budget as standing.
9. `contracts/headroom-allocation.md` names exactly **one** allocator (`WP05`, `ref_advance.py:247`, R-1),
   marks every other work package **0-net**, requires each to print the routed count **pre and post its own
   edit**, and states that lanes B and C are parallel-safe *only* because of that constraint.
10. The `NFR-002`/`SC-013` predicate pre-list is an enumeration of **module-qualified symbols** with its
    derivation command and input count — not a count — and the comparison rule plus the mutation probe are
    stated.
11. One commit, planning artifacts plus the measurement script. No `ruff format` was run. No broad test sweep
    was taken, and if any sweep ran, `pgrep -af 'run_sync[_]daemon'` was confirmed empty first and neither
    `tests/sync` nor `tests/cli` was in the selection.
12. **`scripts/verify_meta_routing_manifest_3162.py` is created and its output is quoted in the manifest.** This
    WP's declared write surface is that one file (`owned_files`), so a WP marked done with it absent is a WP
    marked done with an empty write surface. It must: regenerate the routed and inline counts with their input
    file counts, read the four gate constants off `tests/architectural/test_inline_meta_read_gate.py`, print the
    derived band, print the `pending-batch-a` row count with the `:185` legend excluded, reproduce both traps as
    controls, print the tree and `PYTHONPATH` it measured, and exit non-zero outside the band. `ruff check`
    clean, complexity <= 15 per function, no `# noqa`. The manifest cites it by path as the regeneration
    command.

## Risks

- **The manifest becomes a narration instead of a measurement.** Every count in this mission has a plausible
  wrong answer within reach (9 for the handlers, 296 for routed, `[126,130]` for the band, 5-vs-6 for the
  bypass set). Mitigation: the control-first rule is a hard requirement of each subtask's validation, not
  advice. If a control is missing, the number is not delivered.
- **Line numbers go stale while the document is still authoritative.** The mission edits every file this
  manifest cites. Mitigation: `C-003` symbol-first citation everywhere, with line numbers marked
  `[shifts — cite the symbol]`. The `_resolve_mission_id` collision proves symbol-alone is also
  insufficient, hence module-qualification.
- **Convention mixing.** Adding a read-expression count to a call-site count is the specific error `Q1`
  exists to stop, and it is easy to commit while summarising. Mitigation: the convention is declared inline
  at every count, and the two totals (13 sites; 5/6 bypass) are explicitly marked "not addable".
- **The downward band failure is lost in transcription.** Three artifacts previously stated `[126,130]`.
  Mitigation: the band is *derived in the document from the verbatim assertion text*, and "126 is RED"
  stands as its own line.
- **Someone copies the open floor value.** `127` and `[128,131]` are printed in `plan.md`
  `[UNVERIFIED]` item 1 and read as facts. Mitigation: the prohibition is written into the manifest itself,
  and the Definition of Done includes a grep for it.
- **Mid-mission drift is misattributed to this mission.** `ROUTED_CALLEES` is name-matched and global over
  `src/`; an unrelated landing moves 129. Mitigation: the command **and its input file count** are the
  anchor, so a deviation is attributable before anything is "fixed".
- **Sweep-window collision.** A broad sweep could take the `tests/sync` window a sibling mission at
  `~/dev/sk-missions/3167` may hold. Mitigation: this work package needs no sweep; the one targeted gate
  run is `tests/architectural/test_inline_meta_read_gate.py`, which is in this mission's cone and is neither
  barred directory.
- **Frontmatter rejection at finalize.** `WPMetadata` uses `extra="forbid"`. Adding any key to this file's
  frontmatter — including `plan_concern_refs` — raises a `ValidationError` in finalize-tasks. Do not touch
  the frontmatter. Note what `create_intent` does and does not buy you: it is consumed only to **suppress**
  the zero-match ownership error (`src/specify_cli/ownership/validation.py:411-433`), and no review or accept
  gate revisits existence — so it makes an *absent* script invisible to tooling. DoD item 12 is the only check
  that the file was actually written.
- **Leaving the script uncreated.** `scripts/verify_meta_routing_manifest_3162.py` is this WP's **entire**
  declared write surface. `create_intent` suppresses the zero-match ownership error
  (`src/specify_cli/ownership/validation.py:411-433`) and no review or accept gate re-checks existence, so an
  absent script is invisible to tooling and only a reviewer catches it. DoD item 12 is that check. Mitigation:
  T004 owns it, with its own Files and Validation lines.
- **Running from a lane worktree.** Nothing provisions `.venv` into a git worktree (`.gitignore:31-32`), and
  the gate's `SRC_ROOT` (`test_inline_meta_read_gate.py:61`) is derived from the test file's own location
  while `.venv`'s editable `.pth` pins imports to the main tree's `src/`. Run from the repository root, or
  carry `PYTHONPATH=<workspace>/src` and say which tree each number came from.

## Reviewer Guidance

Review this as a **measurement artifact**. The question is not "are the numbers right" but "can I
regenerate each one from what is written here, and would I notice if it were wrong".

Check, in order:

1. **Pick two counts at random and re-run them from the document alone.** If the command or the input count
   is missing, or the command needs reconstruction, reject — that is the failure mode the post-plan squad
   found (a refuted count and an inverted argument both looked fine as prose).
2. **Verify both traps are present with their wrong answers.** `9` for `except ValueError` with the
   `resolution.py:491` comment named, and `296` for routed. A manifest that reports only the right answers
   has not demonstrated it would catch itself.
3. **Check every probe declares AST-vs-regex.** Where the answer must be exact (the 13, the 6, the routed
   count) it should be an AST walk, with the regex present only as the controlled negative.
4. **Grep the document for convention mixing.** Any sentence that adds a read-expression count to a
   call-site count, or reports a bypass total without naming its convention, is a reject.
5. **Read the band derivation, not the band.** The three assertions of `test_routed_load_meta_floor` must be
   quoted verbatim and `[127, 130]` derived from them in the text. Confirm "126 is RED" and confirm the
   downward direction is stated with the rows 10/11 fold named. A ceiling-only statement is a reject.
6. **Grep for `127` and `128,131` as floor/band values.** Their presence as a re-derived
   `ROUTED_LOAD_META_FLOOR` or band is a reject; the prohibition and the measurement obligation must be
   present instead, and `NFR-002`'s struck immovable-floor clause must not be restated as live. Both holes
   are deliberate — filling them here pre-empts a measurement `WP06` owes.
7. **Confirm the allocation table has exactly one allocator** and that the pre/post obligation is
   per-work-package. If any row other than `WP05` is non-zero, or if the obligation reads as a mission-end
   check, reject — `SC-011`'s old form was satisfiable by changing nothing.
8. **Confirm the predicate pre-list is a list of module-qualified symbols**, with its derivation command and
   input count. A count is a reject: `SC-013` compares lists.
9. **Confirm scope, and confirm the write surface is not empty.** `git status --porcelain` shows exactly three
   paths: the two contracts and `scripts/verify_meta_routing_manifest_3162.py`. Any edit to `spec.md`,
   `plan.md`, `wps.yaml`, `src/` or `tests/` is out of scope. **An absent script is a rejection**: it is this
   WP's only `owned_files` entry, `create_intent` suppresses the tooling error, and no later gate re-checks it —
   so a WP marked done without it was marked done with an empty declared write surface. Run the script yourself
   and compare its output against the manifest's numbers.
10. **Confirm the run tree.** The evidence must say which tree each count came from, and any command run
    outside the repository root must show `PYTHONPATH=<workspace>/src`. A count taken in a worktree without
    it measured one tree's `src/` through another tree's imports.
