# Tracer: design-decisions

One entry per finding: `YYYY-MM-DD · actor · <text>`.

---

2026-08-06 · python-pedro/WP07 · INVERTED RED: a regression marker can be honestly red on main AND wrong. #2804's marker carried TWO assertions under one '# --- CONTRACT (RED on base) ---' banner (:476/:482/:489); the second was unsatisfiable BY DESIGN under the row-union authority model (`#3076` FR-008), so widening only the verdict could never have made it green. Re-pinned assertion 2 to EVIDENCE SURVIVAL (handle d5b8324f9 must survive the merge, take-theirs negatively controlled) rather than deleting it — its content is the real #2804 contract. Superseding issue #3232.

2026-08-06 · python-pedro/WP05 · Diagnosability channel: an optional `diagnostics: list[str] | None` SINK at all five bypass sites, never a print. Forced by two module contracts read at source — git/ref_advance.py is git plumbing that must not take a module-level specify_cli dependency (:37-38, so the seam import is deferred in-function, mirroring core/paths.py's own deferred mission_metadata import), and implement_cores.py is explicitly "zero console/typer side effects" (:559). At site B the narrower of T030's two offered options was taken: _parse_meta_object is NOT touched at all (neither raising nor given a subject arg), because run-only tests/regression/test_issue_2795_claim_blocker.py:291-294 pins its dict|None contract and that file is not in owned_files. SC-012 assertions require the diagnosis phrase "could not be decoded", NOT just "meta.json" + path: the baseline porcelain line ' M kitty-specs/…/meta.json' already contains both, so the naive assertion passes on the unrouted baseline and proves nothing — reproduced in the red run.

2026-08-06 · python-pedro/WP06 · The anchor hop is bounded by SIX conjunctive conditions in code, not by a docstring: module-level def (isinstance(parents.get(id(fn)), ast.Module) — excludes nested defs AND methods/staticmethods), private (_-prefixed), exactly one positional parameter with no *args/**kwargs/pos-only/kw-only, the argument is a bare Name naming that parameter, the parameter is never rebound in the body, and the hop is entered ONLY after direct clause-2 resolution has already failed. That last condition is what makes the pass strictly ADDITIVE, which is load-bearing rather than tidy: allowlist keys are (file, qualname, token), so a pass that could re-derive tokens for the existing 7 would turn the widening into a red via test_allowlist_matches_floor's equality + test_allowlist_entries_are_still_live. Verified byte-identical, 7 keys pre vs post. Reported site is the CALL SITE, never the helper's json.loads line, for the same keying reason. Eight bound tests, and they are labelled in their own docstring as BOUNDS rather than red-first evidence, because an unwidened scanner reports 0 for every shape in that family — the widening's actual red is the anchor-hop pin at [].

2026-08-06 · python-pedro/WP06 · Golden-count regrowth was repaired by CONVERTING the assertion, not by re-freezing the baseline and not by annotating it `# golden-count: cardinality-is-contract`. `assert len(sites) == 1` became `assert [s.key.enclosing_qualname for s in sites] == ["caller"]`, which pins BOTH the cardinality AND the property that actually matters (the site is reported at the call site, not the helper) and carries no line number, so benign edits to the scratch module cannot move it. Attribution came first: green at parent 402c90c3c, red at 172ec9aad, and of the three convert-classified sites in the file only the anchor-hop one is new — the other two merely line-shifted under the insertions. Contrast the gate-coverage red in the same run: also red, but red at the parent too and naming WP02's/WP05's files, so it was filed (#3241) rather than repaired, and regenerating _gate_coverage_baseline.json was explicitly rejected because it would make the gate green by RECORDING that two test files never run in CI.
