# Tracer: approach

One entry per finding: `YYYY-MM-DD · actor · <text>`.

---

2026-08-06 · python-pedro/WP07 · SC-010 non-vacuity trap: 'pending' IS the #2804 defect's own signature and MUST be admitted by the widened predicate (the row-aware sibling pins it), so a companion failing only on 'fail' proves nothing. The companion is fed the defect's OWN fixture (take-theirs / scaffold-clobber, accepted handle absent) and calls the SAME shared helper the marker calls, never a paraphrase. 'fail' is kept only as an explicitly-labelled-insufficient secondary witness.

2026-08-06 · python-pedro/WP05 · Routing site A (git.ref_advance:_meta_change_is_vcs_lock_only) is FORCED, not stylistic: WP06 widens scan_inline_meta_reads to see json.loads(param) inside a private same-module parse helper fed by read_text — exactly _parse_meta_object fed from this site — so a diagnosable-only edit leaves live inline going 7→8 against a shrink-only ceiling of 7, with both escapes closed (floor→8 reds test_allowlist_matches_floor's EQUALITY at :1116; weakening test_allowlist_entries_are_still_live is barred by FR-007). Routing is WP06's unique green landing state. Verified the substitution is EXACT before relying on it: the :315 gate (Path(path).name == _META_FILENAME) makes meta_path.parent / "meta.json" == meta_path by construction, and load_meta's encoding default is _UTF8 = "utf-8" (mission_metadata.py:285/:31), not BOM-tolerant utf-8-sig.

2026-08-06 · python-pedro/WP06 · SC-006's two deltas are only separable with a 2x2 matrix (two trees x two predicates), all four cells measured with the predicate RESIDENT in the tree being measured. Result: widening delta +1 at 96494e5ec (7->8, the 8th site enumerated as ref_advance.py:247) and 0 at branch head; code delta 0 (unwidened predicate, 7 at both trees). The pair says what one number cannot — the widening reaches a real site AND the routing already emptied it — and it is also the measured refutation of SC-006's "or made diagnosable -> live returns to 7" branch: with the parse merely diagnosable the widened scanner still flags the call site. The floor move needed its own separate criterion for the same reason: at live 130 with FLOOR=126/MARGIN=4 all three clauses of test_routed_load_meta_floor pass untouched, so a green gate is NOT evidence the floor was re-derived; provenance (`git show <sha> | grep '^[-+]ROUTED_LOAD_META_FLOOR'` printing both 126 and 127) plus an explicit "not 126" is, and unlike an anti-copy grep for 127 it cannot invert — the 3-below-live rule PRODUCES 127.

2026-08-06 · python-pedro/WP06 · Where a deferral has no red, the evidence is a committed control WITH a positive twin printed in the SAME run: unreachability_control.py -> sites: 0, unreachability_control_twin.py -> sites: 1. Scanned by explicit argument and filtered to each fixture's own rel_path, because a bare len(scan_inline_meta_reads(FIXTURES_DIR)) folds in bad_adapter.py and org_packs/** and makes both numbers meaningless. The control carries BOTH the fully-inlined C3 shape and the helper-delegated shape, so its 0 is a statement about the WIDENED predicate (the hop reaches the delegated call site and still rejects at clause 2) rather than only the old one. Neither fixture may move under src/ — the twin's inlined read there would raise the live census and red the very floor the control exists to prove — and that prohibition is written into each fixture's module docstring, not just into this trace.
