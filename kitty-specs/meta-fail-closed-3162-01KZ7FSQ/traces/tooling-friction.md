# Tracer: tooling-friction

One entry per finding: `YYYY-MM-DD · actor · <text>`.

---

2026-08-06 · python-pedro/WP07 · WP07 ran in the SHARED repository root (.worktrees/ absent) concurrently with WP02 and WP05 agents committing to the same branch. Consequence: 'git diff --stat 96494e5ec -- src/' (a WP07 DoD item) can no longer print nothing — sibling WPs legitimately moved src/. The WP07-attributable invariant that IS runnable is per-commit: 'git diff --stat <c>^ <c> -- src/' empty for each of a199e4865/cea544596/06258eb46. Same cause makes the live routed count non-attributable: 129 at 4147417c1 -> 130 at 06258eb46, the +1 entirely WP05's e06dfdc6f.

2026-08-06 · python-pedro/WP05 · `spec-kitty implement WP05` (the WP's own documented start path, and per CLAUDE.md "the only supported way to prepare a workspace") REFUSES: it needs an undocumented --mission <slug>, and then fails dependencies_not_satisfied because WP01 sits in for_review, not approved/done. Worked from the repository root instead, proving the tree with a module-__file__ probe. Second friction, same shape as WP07's entry: the shared root meant HEAD moved mid-WP (5ee77834a→77e3adf25 between two consecutive commands) and sibling WP02/WP07 edits sat in the working tree, so every WP05 commit had to be path-scoped (`git add <explicit paths>`, never -A) and the routed +1 had to be attributed at FILE granularity (ref_advance.py: 0 routed sites at HEAD → exactly 1 after) rather than trusted from the tree-wide 129→130 delta.

2026-08-06 · python-pedro/WP06 · Two distinct measurement traps, both of which produce a WRONG NUMBER SILENTLY rather than an error. (1) `_rel()` in test_inline_meta_read_gate.py derives _REPO_ROOT from the GATE FILE's own location and falls back to `path.as_posix()` when relative_to raises, so pointing the main tree's scanner at a worktree's src/ makes every rel absolute, `rel in EXCLUDED_REL_PATHS` silently stops matching, and src/specify_cli/mission_metadata.py:348 (_parse_meta_text — the canonical reader itself) is counted as a violation: reported inline 8 where the resident-in-tree run reports 7. Cross-tree census comparisons are wrong in exactly the direction that looks like a real regression. Fix used: copy the predicate INTO each worktree so _REPO_ROOT resolves in-tree, and print the predicate module's own __file__ + SRC_ROOT + a hasattr(_MAX_PARSE_HELPER_HOPS) flag on every run as a control against measuring the wrong pair. Filed as part of #3241. (2) WP06's own prompt supplied a clause-attribution figure (19 candidates / 17 at clause 2 / exactly 1 at clause 3) that reproduces under NEITHER available population definition — measured 9/6/2/1 for the anchor-hop population and 150/92/51/7 for the whole json.loads population. It reached a committed code comment before being caught. Prompt-supplied integers were treated as claims to verify, not as data; the same discipline the prompt itself applies to the refuted 31/30 figure applies to its replacement.

2026-08-06 · python-pedro/WP06 · A sibling lane (WP03) ran `git reset --hard` in the shared repository root mid-WP and destroyed WP06's uncommitted gate-file edits (untracked _fixtures/ modules survived). Reconstructed from the session's own authored content and re-verified (ruff, ruff C901, mypy --strict, 53 passed) rather than assumed intact; every count in the evidence was re-taken afterwards because the tree had also moved to 402c90c3c, folding in WP03's three routed swaps (re-measurement confirmed those 0-net, routed still 130). Lesson, stated as a rule: in a shared root with live siblings, commit each coherent step immediately with per-path `git add`, because an untracked file is safer than a tracked modification. Also: `spec-kitty agent action implement WP06 --agent claude --mission …` refuses with `analysis_report_required` / `invalid_analysis_report_frontmatter: File has no frontmatter` — the third distinct refusal shape recorded for this mission's start path.
