# Remediation Note — issue-matrix.md (WP03, 2026-05-12)

**Diagnostic**: `MISSION_REVIEW_ISSUE_MATRIX_SCHEMA_DRIFT`

**Observation**: `issue-matrix.md` uses the column `Verifying tests` which is not in the canonical mandatory or named-optional vocabulary.

**Auto-normalized**: Trivial capitalization drift and alias drift were resolved by WP03:
- `Issue` → `issue`, `Title` → `title`, `WP` → `wp`, `SC` → `sc`, `Verdict` → `verdict`
- `FR(s)` → `fr` (alias resolution), `NFR(s)` → `nfr` (alias resolution)

**Structural drift (not auto-fixed)**: The column `Verifying tests` is not in the named-optional set (`title`, `scope`, `wp`, `fr`, `nfr`, `sc`, `repo`). Additionally, the mandatory `evidence_ref` column is absent — this matrix uses `Verifying tests` in its place.

**Recommended operator action**:
1. Rename `Verifying tests` → `evidence_ref` to satisfy the mandatory column requirement.
2. The `evidence_ref` cell content is already present (test paths and commit refs), so this is a column header rename only.
3. Commit the fix directly in the `main` branch. The data values do not need to change.
