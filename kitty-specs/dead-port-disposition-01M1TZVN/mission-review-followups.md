# Mission review follow-ups — dead-port-disposition-01M1TZVN

One bullet per tracker issue / hand-off. Evidence paths are repo-root-relative at HEAD `e9794ddea`; kitty-specs paths are under `kitty-specs/dead-port-disposition-01M1TZVN/`. Severity refers to the finding in `mission-review-B.md`.

- **FU-01 — Run Gate 3 (cross-repo e2e) before tagging** (operator; unresolved gate). No `spec-kitty-end-to-end-testing` checkout on this machine and no `mission-exception.md` in the mission dir. The mission touched no cross-repo surface (`git diff 1ceba9f22..HEAD --stat -- src/specify_cli/status/ src/specify_cli/coordination/` → empty), so this is a confirmation run: `SPEC_KITTY_ENABLE_SAAS_SYNC=1 <runner> scenarios/ -v` over the four floor scenarios. If it cannot be run, author `mission-exception.md` per `docs/migrations/cross-repo-e2e-gate.md`.

- **FU-02 — Tracker issue: DSL-bearing copies of the three missions still shipped and preferred** (DRIFT-1, MEDIUM). Evidence: `.kittify/overrides/missions/plan/mission.yaml:9,29`, `…/research/mission.yaml:9,29`, `…/software-dev/mission.yaml:12,32` (resolver prefers these: `src/specify_cli/runtime/resolver.py:778-806`); `src/specify_cli/missions/{plan,research,software-dev}/mission.yaml` same keys (seeded into consumers by `src/specify_cli/upgrade/migrations/m_0_6_7_ensure_missions.py:175-190`; fallback at `src/specify_cli/mission.py:73-85,509,831`); residue pinned by `tests/next/test_plan_mission_runtime.py:301-304`. Also the built-ins' remaining `mission:`/`initial:`/`guards:`/`inputs:`/`outputs:` keys and `# v1 State Machine Definition` header (WP01 design note D-10, `design-notes/WP01-dsl-retirement.md`). Decide: delete the blocks from both copy sets (and rewrite the residue test) or collapse the three copies to one source.

- **FU-03 — Tracker issue: glossary degradation rule — narrow the code or widen the docs** (DRIFT-2, MEDIUM). Evidence: `src/charter/offering/missions/glossary_hook.py:114-118` (`except Exception`) vs. the four sites `glossary_hook.py:22-25`, `src/kernel/glossary_runner.py:17-21` (and its snippet `:41-54`, which contradicts the prose above it), `src/kernel/__init__.py:41-47`, `src/kernel/README.md:18`; test `tests/doctrine/missions/test_glossary_hook.py::TestSelfBootstrapContract::test_execute_with_glossary_degrades_only_when_attachment_unimportable` pins only the `ImportError` path. WP02 reviewer note at `tasks/WP02-glossary-bootstrap-repair.md` Activity Log 19:26:14Z.

- **FU-04 — Spec refresh (docs-only, next spec touch)** (DRIFT-3, LOW). Evidence: `spec.md:115` (status "Draft … no plan.md or tasks yet"), `:122` and `:241` (PR #3888 "open"), `:123`, `:181`, `:190`, `:223`, `:242`, `:264`, `:287` (ADR "Proposed" / "shrunk variant" — WP05 executed it), `:244` and `:286` (three pins; four moved incl. `strip_v1_keys`), `:256-266` (OD1–OD9 as open; all resolved in `decisions/index.json`), `:214` (FR-003 "two live consumers" — one after WP04). Analysis-report items I2/I4/I5 at `analysis-report.md:72,75-76` said "fold at next spec touch"; `spec.md` is absent from `git log 1ceba9f22..HEAD`.

- **FU-05 — Post the FR-020 tracker note on #1868** (operator action; FR-010/SC-005). Paste-ready text at `design-notes/WP02-glossary.md` §6; `issue-matrix.json` row `#1868` says "the operator posts it". Not verifiable from this checkout — confirm it was posted, then update the row's `evidence_ref`.

- **FU-06 — CHANGELOG entries owed under `[Unreleased] - 3.2.7rc1`** (DRIFT-4, LOW). Evidence: `git diff 1ceba9f22..HEAD -- docs/changelog/CHANGELOG.md` = one WP01 bullet only. Missing: (a) WP05 seam consolidation — `src/runtime/next/event_emitter.py` deleted, `runtime_event_emitter_for_mission(mission_dir=…)` replaces `RuntimeEventEmitter.for_feature(feature_dir=…)` (`src/runtime/next/runtime_bridge.py:195-198,1555-1559,2754-2758`), `RuntimeEventEmitterRegistry` (`src/runtime/next/_internal_runtime/events.py:216-240`), and the behaviour change that strict-gated / composition-path `decision_required` advances now append `DecisionInputRequested` to `decisions.events.jsonl` (`runtime_bridge.py:1985,2202`; pinned by `tests/runtime/test_decision_flush_target.py`); (b) WP03 — `mission_runtime` root `__all__` 42→31 (`src/mission_runtime/__init__.py:78-110`), `ActionContext` alias removed (`src/mission_runtime/context.py`), `src/specify_cli/team_projection/` deleted; (c) T014b — `src/charter/offering/schemas/mission.schema.yaml` lost REQUIRED `orchestration` + three definitions (−62 lines; breaking for external consumers of the published schema). WP05 reviewer recorded the debt at `tasks/WP05-emitter-adr-execution.md` Activity Log 22:38:50Z. (A concurrent uncommitted CHANGELOG edit was observed in the working tree during this review — verify it covers all three.)

- **FU-07 — Narrow the `mission_v1` export and fix its docstring** (RISK-1, LOW). Evidence: `src/specify_cli/mission_v1/__init__.py:8-9` claims `runtime/next/decision.py` reads via `read_events` (deleted by WP04, `git diff 1ceba9f22..HEAD -- src/runtime/next/decision.py`); `:26-28` still re-exports `read_events`, which has zero production callers (`grep -rn "mission_v1" src --include='*.py'` → only `next_invocation_lifecycle.py:332` `emit_event`). An in-flight uncommitted `read_events` → `_read_events` rename by another session was observed in the working tree — land or discard it deliberately, with the `test_import_hygiene.py` ratchet re-run.

- **FU-08 — Stale citations of the deleted `mission_v1/guards.py`** (RISK-2, LOW). Evidence: `src/runtime/next/runtime_bridge_composition.py:404` ("see `src/specify_cli/mission_v1/guards.py`"); 5 files under `docs/` (`grep -rln "mission_v1\.\(guards\|schema\|runner\|compat\)\|mission_v1/\(guards\|schema\|runner\|compat\)" docs`). Reword as history ("retired in dead-port-disposition-01M1TZVN") — WP01's T003 grep used the dotted form only.

- **FU-09 — Tracker issue: `sync_emitter` vocabulary rename (single pass)** (RISK-3, LOW). Evidence: 30 occurrences — `src/runtime/next/runtime_bridge.py` (13, incl. the frozen `DecideNextContext.sync_emitter` field) and `src/runtime/next/runtime_bridge_engine.py` (17, incl. the `advance_run_state_after_composition(sync_emitter=…)` kwarg at `:226`/`:344`); parity oracle sink `tests/runtime/_bridge_oracle.py`; compat delegate keyword per `contracts/compat-surface.md`. Rationale for deferral: `design-notes/WP05-emitter.md` §4.5. Owed per the WP05 reviewer (`tasks/WP05-emitter-adr-execution.md` Activity Log 22:38:50Z); not in `issue-matrix.json`.

- **FU-10 — Upstream gap: orphaned inert-slot owner gate** (RISK-5, LOW). Evidence: `tests/architectural/_inert_slots.py:574-629` (`owner_is_complete` / `unresolved_by_completed_owners`, zero callers since `177e06269`, #3285); `issue-matrix.json` row `#3285` says "file an upstream gap issue from the retrospective" but `retrospective.yaml` has `gaps: []`. Decide restore-or-delete; until then every `delete-the-declaration` promise (like WP01's four rows) is honoured only by hand.

- **FU-11 — `_gate_coverage.py` `mission_v1` row and stale `ci_topology_census.json`** (RISK-4, LOW). Evidence: `tests/architectural/_gate_coverage.py:1456` (row kept "as instructed"; `mission_v1` now 120 LOC, below the 500-LOC worklist floor); census stale on the base in `worklist` and `mapped_dirs`, no consuming test (WP01 design note D-9, `design-notes/WP01-dsl-retirement.md`). Assign an owner to regenerate the census in an isolated change.

- **FU-12 — FR-011 record marker** (DRIFT-3, LOW). Evidence: `research/emitter-adr-inputs.md:1` header "Input to PR #3898 — recorded, not executed (C-003)"; WP05 T023 (`tasks/WP05-emitter-adr-execution.md`, subtask T023) required the record be marked "executed by WP05"; `design-notes/WP05-emitter.md` §4.11 says the marker was an operator copy-in that did not happen. One-line header edit.

- **FU-13 — Replace scaffold placeholders in the acceptance and issue matrices** (RISK-7, LOW). Evidence: `acceptance-matrix.json` — every row `notes: "TODO: replace with a real acceptance criterion"`, `description: "Verify FR-NNN is satisfied"`, `mission_number: ''`; `issue-matrix.json` — `title: "<fill at WP-implementation time>"` on `#1868`, `#3888`, `#3898`, `#3899`; `#3898` `evidence_ref` still describes the pre-WP05 follow-up mint; `#3899` "draft". Consider an upstream gap on the `accept` gate letting `TODO` notes through.

- **FU-14 — Enrich the retrospective** (RISK-8, LOW; also DRIFT-5). Evidence: `retrospective.yaml` (`helped` = five "completed without rejection cycles", `gaps: []`, `proposals: []`). Source material: `design-notes/WP01-dsl-retirement.md` (D-5…D-12, RED+GREEN folded into one commit), `design-notes/WP03-residue.md` §11–§12 (8 deviations, 3 tool frictions incl. `ACTIVE_WP_SCOPE_VIOLATION` ×23 paths), `design-notes/WP05-emitter.md` §4, §7–§8 (11 deviations, ADR "net LOC drops" prediction failed at +66 `src/` LOC, `ACTIVE_WP_CONTEXT_STALE` on every commit). Run `spec-kitty agent retrospect synthesize --mission dead-port-disposition-01M1TZVN` (dry-run) and re-author `helped`/`not_helpful`/`gaps`.
