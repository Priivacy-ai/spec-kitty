**Issue 1**: `tracker status --all` does not display the installation's multiple tracked projects/resources; it only shows aggregate counts. In [src/specify_cli/cli/commands/tracker.py](/Users/robert/ClaudeCowork/Spec-Kitty-Cowork/spec-kitty/.worktrees/062-tracker-binding-context-discovery-WP12/src/specify_cli/cli/commands/tracker.py#L331), a `bindings` list is collapsed to `len(bindings)`, and the formatter never renders project/resource labels from the installation-wide payload. That misses Scenario 9 / FR-014, whose acceptance test requires output to include multiple projects and not just a project-scoped summary with a different title. Update the installation-wide formatter to render each binding/resource entry in a human-readable way (for example: one row per project/resource with its label/context/status), and add a CLI test that passes multiple bindings and asserts those project/resource names appear in the output rather than only a count.

**Downstream note**: WP13 depends on WP12. Rebase or regenerate WP13's acceptance-test branch after this fix so its assertions match the corrected `status --all` output.
