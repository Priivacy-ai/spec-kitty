# Tracer: Tooling Friction

- `setup-plan`/`spec-commit` require `SPEC_KITTY_ENABLE_SAAS_SYNC=0` locally (SaaS auth gate); noted for all mission CLI calls this session.
- "project sync store is locked" warnings emitted by mission CLI (dashboard daemon) — non-blocking.

## Friction during implement
- (append)
