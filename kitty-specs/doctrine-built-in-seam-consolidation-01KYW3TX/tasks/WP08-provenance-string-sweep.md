---
work_package_id: WP08
title: Provenance-string sweep — relocate stale src/doctrine/<kind>/built-in strings in shipped artefact YAMLs
dependencies: []
requirement_refs:
- FR-012
planning_base_branch: feat/relocate-builtin-doctrine-packs
merge_target_branch: feat/relocate-builtin-doctrine-packs
branch_strategy: Planning artifacts for this mission were generated on feat/relocate-builtin-doctrine-packs. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into feat/relocate-builtin-doctrine-packs unless the human explicitly redirects the landing branch.
subtasks:
- T029
- T030
- T031
history:
- Generated by /spec-kitty.tasks 2026-07-31
agent_profile: curator-carla
authoritative_surface: packs/built-in/
create_intent: []
execution_mode: code_change
model: claude-sonnet-4-6
owned_files:
- packs/built-in/procedures/adversarial-squad-deployment.procedure.yaml
- packs/built-in/styleguides/adversarial-squad-cadence.styleguide.yaml
- packs/built-in/styleguides/aggregate-design-rules.styleguide.yaml
- packs/built-in/styleguides/common-docs.styleguide.yaml
- packs/built-in/styleguides/divio-type-discipline.styleguide.yaml
- packs/built-in/styleguides/docs-accessibility.styleguide.yaml
- packs/built-in/styleguides/docs-freshness-sla.styleguide.yaml
- packs/built-in/styleguides/java-conventions.styleguide.yaml
- packs/built-in/styleguides/plain-language.styleguide.yaml
- packs/built-in/styleguides/planning-and-tracking.styleguide.yaml
- packs/built-in/styleguides/publication-authority.styleguide.yaml
- packs/built-in/styleguides/python-conventions.styleguide.yaml
- packs/built-in/styleguides/research-citation-discipline.styleguide.yaml
- packs/built-in/styleguides/test-desiderata-and-boundaries.styleguide.yaml
- packs/built-in/styleguides/testing-principles.styleguide.yaml
- packs/built-in/styleguides/tiered-standards.styleguide.yaml
- packs/built-in/toolguides/git-worktree-pr-workflow.toolguide.yaml
- packs/built-in/toolguides/terminology-guard.toolguide.yaml
role: implementer
tags: []
tracker_refs: []
---

## ⚡ Do This First: Load Agent Profile

Load your assigned profile: run `/ad-hoc-profile-load curator-carla` and adopt its identity/boundaries before reading further.

## Objective

Sweep the stale `src/doctrine/<kind>/built-in/…` provenance strings inside the 18 relocated shipped
artefact YAMLs to the current `packs/built-in/<kind>/…` paths (FR-012). This is the lowest-severity,
severable hygiene item and is **occurrence_map-governed** (DIRECTIVE_035, C-005). It is gated on a
**verification step first**: confirm these `related:`/`source_files:`/body strings are **descriptive**
(not runtime-resolved). If any turns out to be runtime-resolved, it is a real reader and escalates to
FR-008 (WP07) — do NOT cosmetically sweep it.

Read first: `../spec.md` (FR-012, US3 acceptance #3), `../occurrence_map.yaml` (the `target`,
`filesystem_paths: rename`, and the exception list), `../plan.md` IC-07, `../notes/research-synthesis.md`
(WS-F / #3120). Independent WP — no code dependency.

**Do NOT** touch any occurrence_map exception file (the arch guard tests, `content-manifest.json`, the
migration doc, the descriptive-docstring test files) — those name the old path deliberately.

## Subtasks

### T029 — Verify the fields are descriptive, not runtime-resolved (FR-012 gate; RECORD in evidence)
Before editing, prove the `related:` / `source_files:` / body-prose strings carrying
`src/doctrine/<kind>/built-in/…` are **not** consumed by a resolver at runtime. Trace how `related:` and
`source_files:` are read (DRG load / reference-pointer walk) and confirm they are treated as descriptive
metadata, not resolved to a filesystem path. **Record the verification result in the WP evidence** (the
occurrence_map NOTE and spec FR-012 both require this before sweeping). If any field IS runtime-resolved,
STOP the sweep for that field and **raise it to the orchestrator** as a newly-discovered FR-008 reader —
do NOT assume WP07 is still open (WP07 may already be landed by the time this severable WP runs); the
orchestrator decides whether to reopen WP07, spin a follow-up, or fold the fix. Do not silently sweep it.

### T030 — Sweep the descriptive strings (FR-012, occurrence_map filesystem_paths: rename)
For the fields confirmed descriptive in T029, rewrite every `src/doctrine/<kind>/built-in/<file>` →
`packs/built-in/<kind>/<file>` across the 18 owned YAMLs. This is the exact same-string rename the
occurrence_map governs (`target.term` → `target.replacement`). Preserve YAML structure, quoting, and
surrounding prose; change only the path substring. Note some strings sit in prose bodies (e.g.
`adversarial-squad-deployment.procedure.yaml`, `plain-language.styleguide.yaml`) and some in `related:`
lists (e.g. `testing-principles.styleguide.yaml`, `java-conventions.styleguide.yaml`) — sweep both.

### T031 — Verify no dead path + graph identity (SC-003/SC-004)
- Grep the 18 owned files for any surviving `src/doctrine/.*/built-in` — expect zero (post-sweep).
- Confirm the DRG graph identity is unchanged (NFR-001) — since these are descriptive strings, node/edge
  counts and projection must be byte-identical before/after; run the graph/snapshot check.
- Confirm the packaging-parity / content-manifest tests still pass (they derive from the pre-move manifest,
  an occurrence_map exception — untouched here).

## Branch Strategy
Base/merge target: `feat/relocate-builtin-doctrine-packs`. Lane worktree from `lanes.json`. No dependency
base; fully parallelisable.

## Definition of Done
- T029 verification recorded in evidence; any runtime-resolved field raised to the **orchestrator** as an
  FR-008 reader (not swept, not assumed WP07-owned).
- All descriptive `src/doctrine/<kind>/built-in/` strings in the 18 YAMLs renamed to `packs/built-in/<kind>/`
  (FR-012), per occurrence_map `filesystem_paths: rename`.
- Zero surviving dead-path strings in owned files; DRG graph identity unchanged (NFR-001/SC-004);
  occurrence_map exception files untouched (C-005/C-006).

## Risks
- **Sweeping a runtime-resolved field** — would change behaviour; the T029 gate exists precisely to catch
  this. Record the verification.
- **Touching an occurrence_map exception file** — the arch guards / manifest / migration doc keep the old
  path on purpose.

## Reviewer guidance
Confirm the T029 descriptive-vs-runtime verification is recorded. Confirm only path substrings changed
(no structural YAML edits). Confirm zero surviving old-path strings in the 18 files and graph identity is
unchanged. Confirm no exception file appears in the diff.
