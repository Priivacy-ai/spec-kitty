---
work_package_id: WP02
title: Charter-layer readers — route joins, remove dual-reads, strip None kwargs, repoint operator strings
dependencies:
- WP01
requirement_refs:
- FR-002
- FR-003
- FR-006
- FR-009
planning_base_branch: feat/relocate-builtin-doctrine-packs
merge_target_branch: feat/relocate-builtin-doctrine-packs
branch_strategy: Planning artifacts for this mission were generated on feat/relocate-builtin-doctrine-packs. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into feat/relocate-builtin-doctrine-packs unless the human explicitly redirects the landing branch.
subtasks:
- T005
- T006
- T007
- T008
history:
- Generated by /spec-kitty.tasks 2026-07-31
agent_profile: python-pedro
authoritative_surface: src/charter/
create_intent: []
execution_mode: code_change
model: claude-sonnet-4-6
owned_files:
- src/charter/catalog.py
- src/charter/compiler.py
- src/charter/kind_vocabulary.py
- src/charter/doctrine_service_builder.py
- src/charter/context_renderers/bootstrap_text.py
- src/charter/resolver.py
role: implementer
tags: []
tracker_refs: []
---

## ⚡ Do This First: Load Agent Profile

Load your assigned profile: run `/ad-hoc-profile-load python-pedro` and adopt its identity/boundaries before reading further.

## Objective

Migrate the **charter layer's** built-in readers onto the WP01 authorities and remove the vestigial
dual-read fallbacks. This is the load-bearing half of FR-002: the **variable-indirected** per-kind joins
in `catalog.py` and `compiler.py` / `kind_vocabulary.py` are the exact drift class paula flagged
(MAJOR-2) — miss them and SC-001 is false and WP04's ratchet false-greens. Also: retire the nested
dual-reads (FR-006), strip the now-dead `built_in_root=None` kwargs at the two charter construction
sites (FR-003 slice), route `bootstrap_text.py`'s root call through `built_in_root()`, and repoint the
two operator-facing error strings in `resolver.py` that name dead paths (FR-009).

Read first: `../plan.md` IC-02/IC-03/IC-04 (the charter surfaces), `../spec.md` (FR-002/003/006/009),
`../contracts/built-in-location-authority.md` (C1.6). Ground in `src/charter/catalog.py:74` (the
`built_in_root = resolve_pack_root("built-in")` variable, joined at :80/:89/:102/:111/:120/:129/:138).

**Do NOT** own `pack_manager.py` (WP05) or `context.py` (WP06). **Do NOT** "repoint" any architectural
guard test's forbidden-pattern literal (C-006). Depends on **WP01** — the `built_in_dir`/`built_in_root`
authorities must exist first.

## Subtasks

### T005 — Route the variable-indirected catalog + compiler + kind-vocabulary joins (FR-002)
- `src/charter/catalog.py`: line 74 binds `built_in_root = resolve_pack_root("built-in")`, then joins it
  per-kind at :80/:89/:102/:111/:120/:129/:138 (7 kinds: paradigms, directives, tactics, styleguides,
  toolguides, procedures, agent_profiles). Replace the local variable + hand joins with `built_in_dir(<kind>)`
  calls (one per kind), removing the `built_in_root` local. This is the variable-indirected form the
  ratchet must catch — after this, `catalog.py` derives no built-in path itself.
- `src/charter/compiler.py`: the inline join sites `compiler.py:842/843/934` **and `compiler.py:873`**
  → `built_in_dir(<kind>)`. Line 873 is a live BinOp
  `resolve_pack_root("built-in") / "styleguides" / "python-implementation.styleguide.yaml"` (an
  `.exists()`-guarded reference read) — route it as
  `built_in_dir(ArtifactKind.STYLEGUIDE) / "python-implementation.styleguide.yaml"`. Missing it leaves a
  join WP04's ratchet reds; the surrounding `doctrine_root` template reference stays as-is.
- `src/charter/kind_vocabulary.py`: the inline join (`kind_vocabulary.py:170` per IC-01) → `built_in_dir(<kind>)`.
Pass canonical `ArtifactKind` members. Grep each file afterwards for any surviving `"built-in"` join.

### T006 — Remove the nested dual-read fallbacks (FR-006)
Delete the vestigial nested-`/built-in` dual-read branches (they read the emptied pre-move shape):
`catalog.py:283`, `compiler.py:1162`, `kind_vocabulary.py:179`. Exactly one location contract must
remain (the WP01 authority). Confirm no test depended on the fallback for real content (if one does, it
is a stale reader — flag it; the fixtures are WP07's, not this WP's to migrate).
Note (paula NOTE-8): `resolve_doctrine_root()` at `catalog.py:160` stays live — it feeds **template
sets only**, with built-in resolved via the seam beside it. Do NOT retire it; its upward `specify_cli`
fallback is deferred to #3101 (out of scope).

### T007 — Strip dead `built_in_root=None` kwargs + route the root call (FR-003 / FR-001b)
- Remove the now-invalid `built_in_root=None` keyword from the two charter construction sites:
  `compiler.py:799` and `doctrine_service_builder.py:81,87`. This is a no-op **before** WP04 drops the
  param and correct after; it is behaviour-preserving (every prod caller already passed `None`).
- `context_renderers/bootstrap_text.py:271`: the bare `resolve_pack_root("built-in")` root call →
  `built_in_root()` (FR-001b / C1.6).

### T008 — Repoint operator-facing error strings (FR-009)
`src/charter/resolver.py:187,250`: shipped operator-facing error text names dead
`src/doctrine/<kind>/built-in/` paths. Repoint to the current `packs/built-in/<kind>/` locations so an
operator who opens the named path finds it exists (US2 acceptance #3). These are user-visible strings —
governed by `occurrence_map.yaml` `user_facing_strings: rename_if_user_visible`. Keep the message intent;
only the path changes.

## Branch Strategy
Base: WP01's landed changes on `feat/relocate-builtin-doctrine-packs` (the authorities must be present).
Merge target: `feat/relocate-builtin-doctrine-packs`. Lane worktree from `lanes.json`.

## Definition of Done
- `catalog.py`, `compiler.py`, `kind_vocabulary.py` derive no built-in path — all joins (incl. the
  variable-indirected catalog form) go through `built_in_dir(kind)` (FR-002).
- Nested dual-read fallbacks removed at `catalog.py:283`, `compiler.py:1162`, `kind_vocabulary.py:179`
  (FR-006); `resolve_doctrine_root()` left intact.
- `built_in_root=None` stripped from `compiler.py:799`, `doctrine_service_builder.py:81,87`;
  `bootstrap_text.py:271` routes through `built_in_root()` (FR-003/FR-001b).
- `resolver.py:187,250` operator strings name `packs/built-in/<kind>/` (FR-009).
- Charter context + compile behaviour unchanged (spot-check a `charter context`/compile run); `ruff` +
  `mypy` clean (NFR-004).

## Risks
- **Missed variable-indirected join** = SC-001 false + WP04 ratchet false-green. Grep every owned file
  for `resolve_pack_root("built-in")` and bare `/ "built-in"` after editing; only zero is acceptable.
- **Over-reach into `resolve_doctrine_root`** (NOTE-8) — leave it live; it is not a built-in split-brain.
- **Touching a forbidden-pattern guard literal** (C-006) — those are in `tests/architectural/`, not owned here.

## Reviewer guidance
Confirm zero residual built-in joins in the six owned files. Confirm the dual-reads are gone (not merely
short-circuited). Confirm the two `built_in_root=None` sites are stripped and behaviour-preserving.
Confirm the resolver strings point at live `packs/built-in/<kind>/` paths and match `occurrence_map.yaml`.
