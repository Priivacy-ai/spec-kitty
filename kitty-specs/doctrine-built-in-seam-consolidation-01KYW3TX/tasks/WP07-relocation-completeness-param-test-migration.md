---
work_package_id: WP07
title: Relocation-completeness — migrate built_in_root= test callers, fix glossary-gate + false-green fixtures, own the collision RED
dependencies:
- WP04
requirement_refs:
- FR-003
- FR-007
- FR-008
planning_base_branch: feat/relocate-builtin-doctrine-packs
merge_target_branch: feat/relocate-builtin-doctrine-packs
branch_strategy: Planning artifacts for this mission were generated on feat/relocate-builtin-doctrine-packs. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into feat/relocate-builtin-doctrine-packs unless the human explicitly redirects the landing branch.
subtasks:
- T024
- T025
- T026
- T027
- T028
history:
- Generated by /spec-kitty.tasks 2026-07-31
agent_profile: python-pedro
authoritative_surface: tests/doctrine/test_service.py
create_intent: []
execution_mode: code_change
model: claude-sonnet-4-6
owned_files:
- tests/architectural/test_charter_references_resolve.py
- tests/charter/test_activate_resolves_no_answers_edit.py
- tests/charter/test_answers_inert_and_org_union.py
- tests/charter/test_model_task_routing_resolves.py
- tests/charter/test_profile_channel_delivery.py
- tests/doctrine/drg/test_profile_suggests_delivery.py
- tests/doctrine/glossary_packs/test_repository.py
- tests/doctrine/test_debugger_debbie_artifacts.py
- tests/doctrine/test_pack_relocation_guard.py
- tests/doctrine/test_paula_patterns_artifacts.py
- tests/doctrine/test_service_org_layer.py
- tests/doctrine/test_service.py
- tests/doctrine/test_spdd_reasons_artifacts.py
- tests/doctrine/test_profile_inheritance.py
- tests/glossary/test_gate_terms.py
- tests/integration/test_org_pack_artifact_lifecycle.py
- tests/merge/test_profile_charter_e2e.py
- tests/specify_cli/test_provenance_integration.py
role: implementer
tags: []
tracker_refs: []
---

## ⚡ Do This First: Load Agent Profile

Load your assigned profile: run `/ad-hoc-profile-load python-pedro` and adopt its identity/boundaries before reading further.

## Objective

Finish the relocation on the **test** side: migrate every `DoctrineService(built_in_root=…)` caller off
the parameter WP04 dropped (FR-003), resolve the 7 relocation-owned CI failures at the correct root
(FR-007 — the glossary gate + the org-pack collision test), repoint the stale false-green fixtures
(FR-008 — profile-inheritance and any `parents[N]/src/doctrine` reader). **This WP owns the org-pack
collision RED** via removing `built_in_root=None` so the styleguide self-resolves and the collision
warning fires at the real root. After this, zero relocation readers remain on a dead
`src/doctrine/<kind>/built-in/` path (SC-003).

Read first: `../spec.md` (FR-003/007/008, US2 acceptance, Assumptions on the org-pack collision),
`../plan.md` IC-02/IC-03, `../notes/research-synthesis.md` (WS-A + the "Sizing correction" — this is the
~14-file coupled set, NOT the ~60 already-flat `Repository(built_in_dir=…)` param), `../occurrence_map.yaml`.

**Depends on WP04** (the param must be gone). **Non-overlap with WP06:** `test_context.py` stays in WP06
(its `built_in_root=` is only a comment). Two of this WP's files —
`tests/charter/test_profile_channel_delivery.py` and `tests/doctrine/drg/test_profile_suggests_delivery.py`
— also import the private `_render_profile_sections`; this WP repoints that import too (T027), since WP06
does not own these files. **WP06 depends on this WP** (the shim shrink must land after these two imports
are repointed) — so land WP07 first; the WP06 → WP07 chain is acyclic. **Occurrence-map guards (do NOT rename):** the `built_in_dir=` field in
`test_service.py`/`test_service_org_layer.py` is a DIFFERENT already-flat leaf param that stays; the
descriptive old-path docstring in `test_pack_relocation_guard.py` stays. Only the `built_in_root=` param
and genuine stale readers move.

## Subtasks

### T024 — Migrate the nested-tmp `built_in_root=` group to `SPEC_KITTY_PACKS_ROOT` / flat (FR-003, C-007)
Files that pass a synthetic nested built-in root via `built_in_root=<tmp/...>` must switch to the
supported synthetic-tier injection: set `SPEC_KITTY_PACKS_ROOT=<tmp>` (flat `packs/built-in/<plural>/`
layout) instead of the removed param. Group includes the artefact-fixture tests
(`test_debugger_debbie_artifacts.py`, `test_paula_patterns_artifacts.py`, `test_spdd_reasons_artifacts.py`,
`glossary_packs/test_repository.py`) and the `built_in_root=` sites in `test_service.py` /
`test_service_org_layer.py` (the 6 + 17 occurrences) that build a synthetic tier. **Do not touch** the
`built_in_dir=` leaf param in those two files (occurrence_map do_not_change). Preserve each test's intent
(same artefacts, same assertions), only the injection mechanism changes.

### T025 — Real-repo-stale group + the org-pack collision RED (FR-003/FR-007)
Files that passed `built_in_root=None` or `built_in_root=<real repo>` and should simply self-resolve:
remove the kwarg. Specifically **own the collision RED**: `tests/integration/test_org_pack_artifact_lifecycle.py`
currently mis-passes because a stale root loads 0 styleguides (nothing to collide). Remove the
`built_in_root=` so the built-in styleguide self-resolves from `packs/built-in/`, and assert the
`DoctrineLayerCollisionWarning` fires (C2.3). Also handle `test_charter_references_resolve.py`,
`test_activate_resolves_no_answers_edit.py`, `test_answers_inert_and_org_union.py`,
`test_model_task_routing_resolves.py`, `test_pack_relocation_guard.py` (migrate its `built_in_root=`;
keep its docstring), `merge/test_profile_charter_e2e.py`, `test_provenance_integration.py`.

### T026 — Glossary-gate fixture at the correct root (FR-007)
`tests/glossary/test_gate_terms.py`: the fixture that ERRORs by opening the emptied
`src/doctrine/<kind>/built-in/` path must load from `packs/built-in/<kind>/` (occurrence_map
`tests_fixtures: rename`). This is part of the 7 relocation-owned failures — after the repoint it passes
at the real root (US2 acceptance #1).

### T027 — False-green profile-inheritance fixture + dual-concern import repoint (FR-008)
- `tests/doctrine/test_profile_inheritance.py`: the fixture passes **vacuously** on an empty stale path
  (latent false-green). Repoint it to `packs/built-in/` so it loads real content and the assertion is
  meaningful (US2 acceptance #2; the arch-level anti-vacuity gate is WP04's).
- In `tests/charter/test_profile_channel_delivery.py` and `tests/doctrine/drg/test_profile_suggests_delivery.py`:
  remove the now-stale `from charter.context import _render_profile_sections` line (both already import
  `_render_profile_sections` from the leaf `charter.context_renderers.profile_sections` on the adjacent
  line) — this completes WP06's shim retirement for the two files WP06 could not own. Also migrate their
  `built_in_root=` per T024/T025.

### T028 — Verify completeness (SC-003)
- Grep `tests/` for any surviving `built_in_root=` — expect **zero** in owned files (and none anywhere
  except comments). Grep the owned files for any dead `src/doctrine/<kind>/built-in` reader path — expect
  zero (excluding the intentional forbidden-pattern guards in `tests/architectural/`, which are NOT owned
  here, C-006).
- Run the 7 relocation-owned tests + the full owned set green; `ruff` + `mypy` clean (NFR-004).

## Branch Strategy
Base: WP04 landed on `feat/relocate-builtin-doctrine-packs` (the param is gone). Merge target:
`feat/relocate-builtin-doctrine-packs`. Lane worktree from `lanes.json`. **WP06 depends on this WP** —
land WP07 first so the shim shrink lands on repointed imports (acyclic).

## Definition of Done
- Zero `built_in_root=` callers remain (FR-003); synthetic tiers use `SPEC_KITTY_PACKS_ROOT` (C-007).
- The 7 relocation-owned failures pass at the real root: glossary gate (T026) + org-pack collision
  asserting `DoctrineLayerCollisionWarning` (T025) (FR-007).
- profile-inheritance fixture non-vacuous; the 2 dual-concern files' `_render_profile_sections` import
  repointed (FR-008 / FR-011 completion).
- occurrence_map guards honoured (`built_in_dir=` leaf param + guard docstrings unchanged); zero dead
  reader paths in owned files (SC-003); `ruff` + `mypy` clean.

## Risks
- **Touching the `built_in_dir=` leaf param** (occurrence_map do_not_change) — that param stays; only
  `built_in_root=` moves. Read the occurrence_map exceptions before editing `test_service*.py`.
- **Collision test still green-on-empty** — verify it now loads a real styleguide AND asserts the warning
  (a green-without-warning is the stale false-pass, not a fix).
- **`test_pack_relocation_guard.py` docstring** — migrate the param but keep the descriptive old-path string.

## Reviewer guidance
Confirm zero `built_in_root=` in `tests/`. Confirm the collision test asserts `DoctrineLayerCollisionWarning`
at a real root, not a vacuous pass. Confirm the profile-inheritance fixture loads real content. Confirm
the `built_in_dir=` leaf param and guard docstrings are untouched (occurrence_map). Confirm the two
dual-concern files no longer import from `charter.context`.
