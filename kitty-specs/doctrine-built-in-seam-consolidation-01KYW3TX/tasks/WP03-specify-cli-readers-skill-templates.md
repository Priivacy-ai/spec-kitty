---
work_package_id: WP03
title: specify_cli readers + skill templates — route joins, CWD-walk retire, strip None kwargs
dependencies:
- WP01
requirement_refs:
- FR-002
- FR-003
- FR-006
- NFR-001
planning_base_branch: feat/relocate-builtin-doctrine-packs
merge_target_branch: feat/relocate-builtin-doctrine-packs
branch_strategy: Planning artifacts for this mission were generated on feat/relocate-builtin-doctrine-packs. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into feat/relocate-builtin-doctrine-packs unless the human explicitly redirects the landing branch.
subtasks:
- T009
- T010
- T011
- T012
history:
- Generated by /spec-kitty.tasks 2026-07-31
agent_profile: python-pedro
authoritative_surface: src/specify_cli/
create_intent: []
execution_mode: code_change
model: claude-sonnet-4-6
owned_files:
- src/specify_cli/cli/commands/doctrine.py
- src/specify_cli/cli/commands/_doctrine_asset.py
- src/specify_cli/doctrine/pack_validator.py
- src/specify_cli/tool_surface/bundles/claude.py
- src/specify_cli/doctrine_service_factory.py
- src/specify_cli/charter_runtime/lint/checks/org_layer.py
- src/specify_cli/cli/commands/charter/generate.py
- src/doctrine/skills/spec-kitty-charter-doctrine/SKILL.md
- src/doctrine/skills/spec-kitty-runtime-next/SKILL.md
role: implementer
tags: []
tracker_refs: []
---

## ⚡ Do This First: Load Agent Profile

Load your assigned profile: run `/ad-hoc-profile-load python-pedro` and adopt its identity/boundaries before reading further.

## Objective

Migrate the **specify_cli** layer's built-in readers onto the WP01 authorities, retire the duplicated
CWD ancestor-walk resolver (an intentional, called-out behaviour delta per NFR-001), strip the dead
`built_in_root=None` kwargs at the three specify_cli construction sites (FR-003 slice), and update the
shipped **SOURCE** skill-template examples that still show `DoctrineService(built_in_root=None, …)`.
Together with WP02 this completes FR-002 across every production reader.

Read first: `../plan.md` IC-01/IC-02/IC-04, `../spec.md` (FR-002/003/006, NFR-001 exception note),
`../contracts/built-in-location-authority.md` (C1.6). Ground in
`src/specify_cli/cli/commands/doctrine.py:204-210` (the CWD-walk reimpl) and
`src/specify_cli/doctrine/pack_validator.py:786` (variable bound, joined at :793).

**Edit SOURCE templates only** — `src/doctrine/skills/**/SKILL.md`, never the generated agent copies in
`.claude/` etc. **Do NOT** own `service.py` (WP04). Depends on **WP01**.

## Subtasks

### T009 — Retire the CWD ancestor-walk, route `doctrine.py` root call (FR-006 / FR-001b, NFR-001 delta)
`src/specify_cli/cli/commands/doctrine.py:204-210` reimplements a CWD-upward walk to find the built-in
root (the `doctrine regenerate-graph` resolver). Replace it with `built_in_root()` (the seam). This is
the **intentional behaviour delta** NFR-001 calls out: an operator standing in a checkout different from
the installed module now resolves through the packaged seam rather than the walked CWD. Record the delta
in the WP evidence (it is a deliberate unification, not a regression). Confirm `doctrine regenerate-graph`
still builds the graph identically for the normal (in-checkout) case.

### T010 — Route the pack-validator + tool-surface joins (FR-002)
- `src/specify_cli/doctrine/pack_validator.py:786` binds a `resolve_pack_root("built-in")` variable and
  joins it per-kind at :793 — the variable-indirected drift class (paula MAJOR-2). Replace with
  `built_in_dir(<kind>)`; remove the local variable.
- `src/specify_cli/tool_surface/bundles/claude.py:434`: the inline `/ "built-in" / <plural>` join →
  `built_in_dir(<kind>)`.
Grep both files afterwards for any residual `"built-in"` join.

### T011 — Strip dead `built_in_root=None` kwargs + fix the stale doc comment (FR-003)
Remove the now-invalid `built_in_root=None` keyword from the three specify_cli construction sites
(behaviour-preserving; a no-op before WP04's param drop, correct after):
- `src/specify_cli/doctrine_service_factory.py:82`
- `src/specify_cli/charter_runtime/lint/checks/org_layer.py:244,274`
- `src/specify_cli/cli/commands/charter/generate.py:57`
Also fix the stale prose at `src/specify_cli/cli/commands/_doctrine_asset.py:54`: its docstring references
`built_in_root=None` (the param WP04 drops) to explain built-in asset resolution. Reword to describe the
current behaviour (built-in assets self-resolve from packaged data via `importlib.resources` — no
parameter) so the comment does not name a removed kwarg. Comment-only; no logic change.

### T012 — Update the shipped SOURCE skill-template examples (FR-003)
Update the `DoctrineService(built_in_root=None, …)` examples in the two SOURCE skill templates so they
match the post-drop API (drop the kwarg from the example construction):
- `src/doctrine/skills/spec-kitty-charter-doctrine/SKILL.md:121,452,662`
- `src/doctrine/skills/spec-kitty-runtime-next/SKILL.md:234,276`
These are documentation examples of the service API; the generated agent copies propagate via
`spec-kitty upgrade` — do not edit them. Keep the surrounding prose intent; only the constructor example
changes.

## Branch Strategy
Base: WP01's landed authorities on `feat/relocate-builtin-doctrine-packs`. Merge target:
`feat/relocate-builtin-doctrine-packs`. Lane worktree from `lanes.json`. Independent of WP02 (disjoint
files) — the two may run in parallel once WP01 is in.

## Definition of Done
- `doctrine.py` CWD-walk retired → `built_in_root()`; NFR-001 delta recorded in evidence; normal-case
  graph build unchanged (FR-006/NFR-001).
- `pack_validator.py` + `tool_surface/bundles/claude.py` derive no built-in path — joins via
  `built_in_dir(kind)` (FR-002).
- `built_in_root=None` stripped from factory / org_layer / generate; the `_doctrine_asset.py:54`
  docstring no longer names the removed kwarg (FR-003).
- Both SOURCE `SKILL.md` examples updated (generated copies untouched) (FR-003).
- `ruff` + `mypy` clean (NFR-004).

## Risks
- **NFR-001 delta undocumented** — reviewers must see the CWD-walk semantics change was intentional;
  record it or it reads as a regression.
- **Missed variable-indirected join** in `pack_validator.py:793` (the exact false-green class) — grep to zero.
- **Editing generated skill copies** instead of SOURCE — only `src/doctrine/skills/**` is canonical.

## Reviewer guidance
Confirm the CWD-walk is gone and the delta is written down. Confirm zero residual built-in joins in
`pack_validator.py`/`claude.py`. Confirm the three `built_in_root=None` sites are stripped. Confirm only
SOURCE SKILL.md files changed (no `.claude/`/`.codex/` edits in the diff).
