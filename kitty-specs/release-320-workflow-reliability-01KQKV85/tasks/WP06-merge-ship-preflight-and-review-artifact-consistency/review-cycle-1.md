**Issue 1**: WP06 diagnostics do not satisfy the `merge-ship-preflight.yaml` contract.

The contract requires every branch/review-artifact failure diagnostic to include `diagnostic_code`, `branch_or_work_package`, `violated_invariant`, and `remediation`. The implementation blocks the unsafe cases, but the emitted target-branch payload only includes `error`, branch names, state, counts, and remediation, while the text output prints only the generic error plus remediation lines. The review-artifact gate likewise prints a generic error plus formatted findings, but no stable diagnostic code or violated invariant.

Fix by adding stable diagnostic fields/codes to both gates and pinning them in tests. For example, the target branch preflight should expose a code such as `TARGET_BRANCH_NOT_SYNCHRONIZED`, `branch_or_work_package=<target branch>`, `violated_invariant=local_target_branch_must_match_tracking_branch`, and the existing remediation. The review-artifact gate should expose a code such as `REJECTED_REVIEW_ARTIFACT_CONFLICT`, `branch_or_work_package=<WP id>`, `violated_invariant=terminal_wp_latest_review_artifact_must_not_be_rejected`, the latest artifact path/verdict, and required remediation. Update the JSON and text/console paths as applicable, and add assertions in `tests/merge/test_target_branch_preflight.py` and `tests/post_merge/test_review_artifact_consistency.py` so this contract cannot regress.

Validation already run by reviewer: `uv run pytest tests/merge tests/post_merge tests/integration/test_mission_review_contract_gate.py tests/integration/test_merge_resume.py -q` passed with `173 passed, 7 warnings`.

**Known workflow bug to avoid during rejection**: If `spec-kitty agent tasks move-task WP06 --to planned --review-feedback-file ...` fails from `in_review` with `Transition from in_review requires review_result (structured review outcome)`, use the canonical status emitter workaround with `ReviewResult(verdict="changes_requested", reviewer="codex:gpt-5.3-codex:reviewer-renata:reviewer", reference="feedback://WP06/review-cycle-1", feedback_path="<this file>")` to move only WP06 back to `planned`.
