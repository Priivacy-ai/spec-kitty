---
work_package_id: WP06
title: Completion-proof, docs, and trackers
dependencies:
- WP01
- WP02
- WP03
- WP04
- WP05
requirement_refs:
- FR-010
- FR-011
- FR-012
- FR-014
- FR-015
tracker_refs: []
planning_base_branch: mission/org-doctrine-profile-integrity-activation-closure
merge_target_branch: mission/org-doctrine-profile-integrity-activation-closure
branch_strategy: Planning artifacts for this mission were generated on mission/org-doctrine-profile-integrity-activation-closure. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into mission/org-doctrine-profile-integrity-activation-closure unless the human explicitly redirects the landing branch.
subtasks:
- T019
- T020
- T021
- T022
agent: claude
history:
- 2026-06-02 – generated by /spec-kitty.tasks (close-out)
agent_profile: curator-carla
authoritative_surface: CLAUDE.md
execution_mode: code_change
owned_files:
- CLAUDE.md
- src/doctrine/missions/mission-steps/software-dev/tasks/guidelines.md
role: curator
tags: []
---

## ⚡ Do This First: Load Agent Profile
Load `curator-carla` (curator) via `/ad-hoc-profile-load` before anything else. (Documentation/traceability curation.)

## Objective (FR-010, FR-011, FR-012, FR-014 · finding I-7, I-8, I-10, I-12)
Make the parent mission's completion-proof artifacts accurate, sync the living docs, and track deferred/pre-existing items. **Best landed last** — T019 references the proving test IDs the other WPs add.

## Subtasks
> Ownership note: the parent `acceptance-matrix.json` lives under `kitty-specs/` and therefore cannot be listed in `owned_files` (the finalizer disallows kitty-specs paths). WP06 still edits it as part of T019; only `CLAUDE.md` is the tracked owned file. No other WP touches the matrix, so there is no conflict.

### T019 — Populate the parent acceptance matrix (FR-010, I-7)
`kitty-specs/org-doctrine-profile-integrity-activation-closure-01KT1TV1/acceptance-matrix.json` ships as 36 scaffold stubs (`pass_fail: pending`, `evidence/verified_by: null`, `overall_verdict: pending`). Replace each FR's stub with a real acceptance criterion and the proving test ID(s) (e.g. parent FR-022 → `tests/specify_cli/test_charter_list.py`; parent FR-005..010 → `tests/doctrine/test_profile_diagnostics.py`; lineage → DRG `specializes_from` tests). Set `pass_fail`/`evidence`/`verified_by` and a non-`pending` `overall_verdict`. Cross-reference the parent debrief's verified-coverage list.

### T020 — Sync CLAUDE.md (FR-011, I-8; survey A5 → #1040)
Add a CLAUDE.md section (mirroring existing "Status Model Patterns" / "Mission Identity Model" sections) covering: the charter activation/cascade model (`charter.activation_engine`, `charter.cascade`, plan/commit seam, `CharterPackConfigError`), the canonical kind vocabulary (`doctrine.artifact_kinds` + `charter.kind_vocabulary`), and the `specializes_from` DRG lineage relation (profile lineage authored as DRG edges, not fields). **Cross-link the governing ADR** `architecture/3.x/adr/2026-05-16-1` from the section (ADR↔subsystem linkage, nudges #1040; DIRECTIVE_003).

### T021 — Trackers (FR-012, FR-014, I-10, I-12; survey-corrected)
File DIRECTIVE_013 tracker issue(s) for the genuinely pre-existing / deferred items NOT absorbed:
- the **four** (survey-corrected from 2) `git_repo` marker gaps: `tests/architectural/test_no_legacy_terminology.py`, `tests/specify_cli/sync/test_local_commit_wiring.py`, `tests/specify_cli/test_sync_state_gitignore_migration.py`, `tests/status/test_bootstrap.py`;
- the **upstream `coordination.status_service` + `lifecycle_events` dead-symbol RED** (pre-existing on `upstream/main` from merged #1614; no issue exists) — this is the ticket WP04 references in its allowlist-with-tracker entries;
- the deferred **FR-012** (`doctor.py` god-module split — do NOT attempt here) and **FR-013** (provenance typing, if WP04 defers it).
Record the issue references in the CLAUDE.md section / acceptance-matrix notes. (The `ceremony` term is NOT here — it is now ABSORBED in T022.) Do NOT edit `_baselines.yaml` (WP04 owns it; WP04 lands the in-baseline tracker refs).

### T022 — Absorb the `ceremony` term fix (FR-015; survey A7 → closes #1563)
Replace the legacy term `ceremony` with the glossary-canonical "status commit" at `src/doctrine/missions/mission-steps/software-dev/tasks/guidelines.md:26` (single hit). This flips `tests/architectural/test_no_legacy_terminology.py::...[ceremony]` GREEN. Verify the replacement reads naturally in context and the legacy-terminology gate passes.

## Definition of Done
- `acceptance-matrix.json`: zero `pending`/`null` for implemented FRs; `overall_verdict` set (SC-006).
- CLAUDE.md section present + ADR `2026-05-16-1` cross-linked (SC-007; A5).
- DIRECTIVE_013 trackers filed + referenced for the 4 `git_repo` gaps, the upstream `status_service` debt, and FR-012/FR-013 deferrals (SC-008; FR-014).
- `ceremony` replaced by "status commit"; `test_no_legacy_terminology[ceremony]` GREEN (SC-010; FR-015; closes #1563).

## Reviewer guidance
Verify the acceptance-matrix evidence cites REAL test IDs that exist and pass. Confirm WP06 does NOT edit `_baselines.yaml` (WP04 owns it). Confirm FR-012 (and FR-013 if deferred) went to a tracker, not a risky refactor. Confirm the `ceremony`→"status commit" replacement is the only legacy-terminology hit and reads naturally.

## Branch Strategy
Planning base + merge target: `mission/org-doctrine-profile-integrity-activation-closure`. Worktree per lane. Depends on WP01-05 (references their test IDs).
Implement: `spec-kitty agent action implement WP06 --agent <tool>:<model>:curator-carla:curator`
