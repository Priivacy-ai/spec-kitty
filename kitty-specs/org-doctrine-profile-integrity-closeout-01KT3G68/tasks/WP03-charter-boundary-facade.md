---
work_package_id: WP03
title: Charter boundary facade + allowlist to zero
dependencies: []
requirement_refs:
- FR-006
- FR-007
tracker_refs: []
planning_base_branch: mission/org-doctrine-profile-integrity-activation-closure
merge_target_branch: mission/org-doctrine-profile-integrity-activation-closure
branch_strategy: Planning artifacts for this mission were generated on mission/org-doctrine-profile-integrity-activation-closure. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into mission/org-doctrine-profile-integrity-activation-closure unless the human explicitly redirects the landing branch.
subtasks:
- T008
- T009
- T010
- T011
agent: claude
history:
- 2026-06-02 – generated by /spec-kitty.tasks (close-out)
agent_profile: python-pedro
authoritative_surface: src/charter/template_catalog.py
execution_mode: code_change
owned_files:
- src/charter/template_catalog.py
- src/specify_cli/cli/commands/charter/activate.py
- src/specify_cli/cli/commands/charter/list_cmd.py
- tests/architectural/test_runtime_charter_doctrine_boundary.py
role: implementer
tags: []
---

## ⚡ Do This First: Load Agent Profile
Load `python-pedro` (implementer) via `/ad-hoc-profile-load` before anything else. (Architectural-boundary work — respect the `kernel ← doctrine ← charter ← specify_cli` layering.)

## Objective (FR-006, FR-007 · finding I-4 — MEDIUM)
The two `_BASELINE_ALLOWLIST` entries (`activate.py`, `list_cmd.py`) added by the parent are half-justified: charter facades already exist for most of those imports. Route through facades, add the one missing facade, and drive the boundary allowlist back to **0**.

## Context (settled — `research.md` R4)
- `charter.kind_vocabulary` already re-exports `ArtifactKind`, `MissionTypeNotAnArtifactKind`, `CHARTER_KIND_TOKENS`.
- `charter.resolution` already re-exports `ResolutionTier`.
- `doctrine.template_catalog` has **no** charter facade yet — add one (mirroring `charter.profiles`/`charter.resolution`).
- **Only module-level imports are gated** (renata R-2): the lazy `mission_type_repository` (`activate.py:~117`) and `MissionTemplateRepository` (`list_cmd.py:~85`) imports are `# noqa: PLC0415` and boundary-invisible — leave them; do NOT facade them.

## Subtasks
### T008 — Create `src/charter/template_catalog.py`
Pure re-export facade exposing `discover_templates`, `TemplateRef`, `TierRoot` from `doctrine.template_catalog`; declare `__all__`. No behavior, identity preserved (mirror `charter.profiles`).

### T009 — Repoint module-level imports
- `activate.py:45`: `from doctrine.artifact_kinds import ArtifactKind, MissionTypeNotAnArtifactKind` → `from charter.kind_vocabulary import ...`.
- `list_cmd.py:13-15`: `CHARTER_KIND_TOKENS` → `charter.kind_vocabulary`; `ResolutionTier` → `charter.resolution`; `TemplateRef, TierRoot, discover_templates` → `charter.template_catalog`.
- Leave the lazy `doctrine.missions.*` imports untouched, but **annotate them** (`activate.py:~117`, `list_cmd.py:~85`) with `# boundary: lazy import intentionally not facaded (PLC0415; boundary-invisible)` so a future cleanup can't promote them to module scope and reintroduce allowlist growth (protects the #1111 allowlist→0 win — survey A4).

### T010 — Allowlist → 0
Empty `_BASELINE_ALLOWLIST` in `tests/architectural/test_runtime_charter_doctrine_boundary.py` (remove both `activate.py`/`list_cmd.py` entries + the now-false justification comment). The ratchet baseline `test_runtime_charter_doctrine_boundary.baseline_allowlist` in `_baselines.yaml` is **already 0** on upstream, so the live count dropping 2→0 makes the gate pass — do **not** edit `_baselines.yaml` (owned by WP04 for the dead-symbol counts); just verify it reads 0.

### T011 — Verify
`PWHEADLESS=1 pytest tests/architectural/test_runtime_charter_doctrine_boundary.py tests/architectural/test_ratchet_baselines.py -q` green; `grep -rn 'from doctrine' src/specify_cli/cli/commands/charter/{activate,list_cmd}.py` shows only the lazy (function-level) imports.

## Definition of Done
- New `charter.template_catalog` facade with `__all__`; both CLI files import doctrine only via `charter.*` at module level.
- `_BASELINE_ALLOWLIST` empty; `_baselines.yaml` boundary baseline = 0; boundary + ratchet gates green (SC-004).
- `ruff`/`mypy` clean on changed src files; charter activate/list CLIs still function (smoke).

## Reviewer guidance
Confirm allowlist is genuinely 0 (not just shrunk), the new facade re-exports with identity preserved, and the lazy mission-repo imports were correctly left alone (not scope-crept).

## Branch Strategy
Planning base + merge target: `mission/org-doctrine-profile-integrity-activation-closure`. Worktree per lane.
Implement: `spec-kitty agent action implement WP03 --agent <tool>:<model>:python-pedro:implementer`
