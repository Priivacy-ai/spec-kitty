---
work_package_id: WP02
title: CI marker visibility
dependencies: []
requirement_refs:
- FR-004
tracker_refs: []
planning_base_branch: mission/org-doctrine-profile-integrity-activation-closure
merge_target_branch: mission/org-doctrine-profile-integrity-activation-closure
branch_strategy: Execution worktrees are allocated per computed lane from lanes.json; planning base and merge target are mission/org-doctrine-profile-integrity-activation-closure.
subtasks:
- T005
- T006
- T007
agent: claude
history:
- 2026-06-02 – generated by /spec-kitty.tasks (close-out)
agent_profile: python-pedro
authoritative_surface: tests/specify_cli/test_operational_context_wiring.py
execution_mode: code_change
owned_files:
- tests/charter/test_kind_vocabulary.py
- tests/charter/test_operational_context.py
- tests/doctrine/test_drg_merge.py
- tests/doctrine/test_relationship_migration.py
- tests/specify_cli/test_operational_context_wiring.py
role: implementer
tags: []
---

## ⚡ Do This First: Load Agent Profile
Load `python-pedro` (implementer) via `/ad-hoc-profile-load` before anything else.

## Objective (FR-004 · finding I-2 — HIGH, merge blocker)
Five parent-mission test files declare no module-level `pytestmark`, so `tests/architectural/test_pytest_marker_convention.py` fails and these flagship contract tests are invisible to the marker-filtered CI profiles (`-m fast`/`-m contract`/`-m architectural`). Add correct markers.

## Context
The convention gate checks marker **presence** only — it does NOT validate correctness (P-3). So choose the right marker per file by the project's pyproject taxonomy (`fast`/`unit` = no I/O; `integration` = real filesystem / in-process I/O). Four files are pure (no I/O); one does heavy `tmp_path`/`write_text`/`mkdir` I/O.

## Subtasks
### T005 — Markers on the 4 pure files [P]
Add a module-level `pytestmark = [pytest.mark.<category>]` near the imports of `tests/charter/test_kind_vocabulary.py`, `tests/charter/test_operational_context.py`, `tests/doctrine/test_drg_merge.py`, `tests/doctrine/test_relationship_migration.py`. These are pure (no filesystem) → `unit` (and `contract` where they assert a documented contract — match sibling files in the same dir for consistency).

### T006 — Integration marker on the I/O test [P]
`tests/specify_cli/test_operational_context_wiring.py` does real filesystem I/O (~30 `tmp_path`/`write_text`/`mkdir` refs) → `pytestmark = [pytest.mark.integration]` (NOT `unit`/`fast`). If it also drives `git` via subprocess, add `git_repo` too (check `test_pytest_marker_correctness` requirements).

### T007 — Verify both marker gates
`PWHEADLESS=1 pytest tests/architectural/test_pytest_marker_convention.py tests/architectural/test_pytest_marker_correctness.py -q` — both must pass. Confirm each of the 5 files now collects under its intended `-m <marker>` profile.

## Definition of Done
- `grep -c '^pytestmark' <file>` == 1 for each of the 5 files.
- `test_pytest_marker_convention` + `test_pytest_marker_correctness` green (SC-002).
- Markers match the I/O profile of each file (pure → unit/contract; I/O → integration).

## Reviewer guidance
Verify the I/O-heavy wiring test is `integration`, not `unit` — the convention gate would pass either way, so correctness is the reviewer's job (P-3).

## Branch Strategy
Planning base + merge target: `mission/org-doctrine-profile-integrity-activation-closure`. Worktree per lane from `lanes.json`.
Implement: `spec-kitty agent action implement WP02 --agent <tool>:<model>:python-pedro:implementer`
