---
work_package_id: WP05
title: Cascade output hygiene
dependencies: []
requirement_refs:
- FR-008
tracker_refs: []
planning_base_branch: mission/org-doctrine-profile-integrity-activation-closure
merge_target_branch: mission/org-doctrine-profile-integrity-activation-closure
branch_strategy: Planning artifacts for this mission were generated on mission/org-doctrine-profile-integrity-activation-closure. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into mission/org-doctrine-profile-integrity-activation-closure unless the human explicitly redirects the landing branch.
subtasks:
- T016
- T017
- T018
agent: claude
history:
- 2026-06-02 – generated by /spec-kitty.tasks (close-out)
agent_profile: python-pedro
authoritative_surface: src/charter/pack_manager.py
execution_mode: code_change
owned_files:
- src/charter/pack_manager.py
- tests/specify_cli/cli/commands/charter/test_charter_activate_commands.py
- tests/specify_cli/cli/commands/charter/test_charter_deactivate_commands.py
role: implementer
tags: []
---

## ⚡ Do This First: Load Agent Profile
Load `python-pedro` (implementer) via `/ad-hoc-profile-load` before anything else.

## Objective (FR-008 · finding I-5 — MEDIUM)
`charter activate/deactivate --cascade` prints stale "cascade not yet implemented / deferred" warnings while cascade actually runs (parent FR-016/FR-020 violation; operator-trust). Remove them.

## Context (settled — `research.md` R3, `contracts/cascade-output.contract.md`)
`pack_manager` has **two** deferral-warning branches: `if cascade:` (~424-429, "not yet implemented … deferred") AND `if not cascade:` (~417-423, "re-run with --cascade … cascade execution deferred"). Passing `cascade=False` does NOT work — it just fires the *other* branch (proven by 3 reviewers). The real cascade is CLI-owned via `charter.cascade`; `pack_manager`'s `cascade` parameter does **no real work** (`commit_plan` is identical regardless). So **delete both branches**.

## Subtasks
### T016 — Delete the stale warning branches
Remove both warning blocks in `pack_manager.activate` (~417-429) and the deactivate equivalent (~493-498). The `cascade` parameter becomes inert (keep it for signature stability, or drop it if no caller relies on it — verify callers first). No behavioral cascade work is lost (real cascade is in `charter.cascade`).

### T017 — Cascade-output absence test
Add a test asserting "not yet implemented" / "deferred" is ABSENT from both `charter activate --cascade all <kind> <id>` and `charter deactivate --cascade ... ` output, alongside an assertion that cascade still performs (referenced targets activated / shared-safe deactivation).

### T018 — Update existing cascade tests (DD-4)
`test_activate_cascade_calls_with_true` (asserts `cascade=True` at the call site) and `test_activate_cascade_flag_accepted` (asserts a "Warning"/"cascade" string) pin the old behavior — update them to the new contract. Confirm no other test pins the deferral strings.

## Definition of Done
- Both stale warning branches deleted; no "not yet implemented"/"deferred" in activate OR deactivate `--cascade` output (SC-005).
- Cascade still works (targets activated; shared-safe deactivation intact).
- Existing cascade tests updated and green; `ruff`/`mypy` clean.

## Reviewer guidance
Verify both branches are removed (not just the `if cascade:` one) and that real cascade behavior is unchanged (commit_plan path). Confirm the updated tests assert the new contract, not the old.

## Branch Strategy
Planning base + merge target: `mission/org-doctrine-profile-integrity-activation-closure`. Worktree per lane.
Implement: `spec-kitty agent action implement WP05 --agent <tool>:<model>:python-pedro:implementer`
