# review-verify/v1 — canonical format for fix-verification output.
schema: review-verify/v1
complete: false
phase: spec
results:
  - id: SPEC-GOV-001
    status: resolved
    reason: "C-001 now lists exactly six files including the three tests/dossier/ files, FR-005 names the three real fixture helpers (test_manifest.py:516-524 _write_org_manifest/TestManifestRegistryOrgTier, test_rebaseline.py:494-500 _write_org_manifest/TestRebaselineOrgAwareness, test_indexer.py:714-721 _write_org_manifest method) with correct class/method names verified against the live files, and SC-003's pytest command now includes all five test files."
  - id: SPEC-VERIFY-001
    status: resolved
    reason: "FR-003 now names all five bypassing methods in TestResolveOrgExpectedArtifactsMalformedFile with line numbers (160, 170, 188, 206, 259) that match the live file exactly, and requires each inline target_dir/malformed_dir construction be corrected alongside the helper."
  - id: SPEC-VERIFY-002
    status: resolved
    reason: "FR-003 now requires a new test case for a fixture written only at the old path asserting None, and SC-004 explicitly distinguishes the resolve_org_expected_artifacts layer (None) from the ManifestRegistry/_resolve_expected_artifacts_slot layer (falls through to built-in for a registered type), which matches the live fallback code in mission_type_profiles.py:1044-1053."
  - id: SPEC-ARCH-002
    status: resolved
    reason: "New Constraint C-007 makes an explicit, named decision that the historical contract doc under kitty-specs/up-org-doctrine-consumers-01M05YAB/ is deliberately left stale per repo convention and cross-references C-001's file set."
  - id: SPEC-ARCH-003
    status: resolved
    reason: "Edge Cases now contains an explicit paragraph stating ManifestRegistry._cache is process-local, never persisted, and that no currently-shipped long-lived process holds a stale pre-fix cache entry across the fix boundary, matching the verified CLI-per-invocation/no-daemon-consumer facts."
  - id: SPEC-GOV-002
    status: resolved
    reason: "The Clarifications parenthetical now names both files #3702 touches: src/specify_cli/cli/commands/charter/activate.py and src/charter/mission_type_profiles.py."
complete: true
