# review-refute/v1 — canonical format for refuter-batch verdicts.
schema: review-refute/v1
complete: true             # flip to true ONLY at the very end, after the yaml self-check passes
phase: spec
batch: 1
verdicts:                   # one entry for EVERY finding in your batch — no omissions
  - id: SPEC-GOV-001
    verdict: confirmed
    reason: Verified line-for-line — test_manifest.py:516/528/542, test_rebaseline.py:494/585/680, and test_indexer.py:714/724 each independently duplicate a `_write_org_manifest`-style helper hardcoded to `org_root / mission_type / "expected-artifacts.yaml"` and drive real end-to-end assertions through ManifestRegistry/Indexer, none of which SC-003's two-file pytest scope or C-001's three-file set covers.
  - id: SPEC-VERIFY-001
    verdict: confirmed
    reason: Confirmed 5 of 6 methods in TestResolveOrgExpectedArtifactsMalformedFile (lines 158-269) hand-build `target_dir = org_root / "software-dev"` bypassing the `_write_org_expected_artifacts` helper FR-003 names, so FR-003's helper-only fix leaves them exercising the wrong path.
  - id: SPEC-VERIFY-002
    verdict: confirmed
    reason: Confirmed no test in test_org_expected_artifacts.py plants a fixture at the OLD path (only "no file at all" is tested, lines 46-63), and mission_type_profiles.py:1038-1042 confirms a None org_result falls through to the built-in MissionTemplateRepository, not to None, contradicting SC-004's unqualified wording.
  - id: SPEC-ARCH-002
    verdict: confirmed
    reason: Confirmed org_expected_artifacts.py:10 cites the contract doc, and org-tier-resolution-contract.md's C-4 code block (line ~86) embeds the same pre-fix wrong path, while C-001 fixes only three files, none of which is this doc.
  - id: SPEC-ARCH-003
    verdict: confirmed
    reason: Confirmed manifest.py's `_cache` (lines 171-190) is a process-global dict whose own inline comment names a "daemon, or a test session touching two projects" scenario, clear_cache's docstring (line 432) says "useful for testing" as the only reset path, and spec.md never mentions this cache.
  - id: SPEC-GOV-002
    verdict: confirmed
    reason: Confirmed via `gh issue view 3702` that the issue body names both src/specify_cli/cli/commands/charter/activate.py and src/charter/mission_type_profiles.py, while spec.md's Clarifications parenthetical names only "charter activate"; the finding itself concedes this doesn't break the no-overlap conclusion, just the citation.
