# review-verify/v1 — canonical format for fix-verification output.
schema: review-verify/v1
complete: true               # flipped after yaml self-check passed (python3 -c "import yaml,sys;yaml.safe_load(open(sys.argv[1]))" — PARSE_OK)
phase: spec
results:                    # one entry for EVERY finding in the findings file
  - id: SPEC-FRESH-001
    status: resolved
    reason: >-
      Clarifications now carries an "Addendum (fix round, 2026-08-24)" paragraph
      that explains the "two test fixtures" figure in the verbatim operator quote
      is superseded by the fix round's discovery of three additional
      fixture-duplicating test files, ties the expansion to C-001/FR-005, and
      reaffirms it does not reopen the separately-decided validator-gate
      question — the original operator quote itself is left byte-identical, so
      the note resolves the contradiction without altering the verbatim record.
  - id: SPEC-FRESH-002
    status: resolved
    reason: >-
      NFR-001's text now explicitly extends the RED-first mandate to "FR-003's
      new old-location-returns-None regression test case" with a stated distinct
      RED state (old-path fixture found pre-fix, None post-fix), and explicitly
      excludes FR-004/FR-005 as "maintenance of existing coverage... not new
      RED-first behaviour" — matching the remediation's exact suggested wording.
  - id: SPEC-FRESH-003
    status: resolved
    reason: >-
      A new note after the FR table requires each of FR-003/FR-004/FR-005's
      touched fixture-helper docstrings be corrected in step with its path
      join and names test_indexer.py's helper as docstring-free and unaffected;
      independently verified against the live checkout that all four cited
      docstrings (tests/charter/test_org_expected_artifacts.py:32,
      tests/charter/test_mission_type_profiles.py:997,
      tests/dossier/test_manifest.py:517, tests/dossier/test_rebaseline.py:495)
      still exist verbatim at those exact lines with the pre-fix wrong-path
      text, and that tests/dossier/test_indexer.py's _write_org_manifest
      (def at line 714) genuinely has no docstring.
