# review-verify/v1 — canonical format for fix-verification output.
schema: review-verify/v1
complete: true               # flip to true ONLY at the very end, after the yaml self-check passes
phase: spec
results:
  - id: SPEC-FRESH2-001
    status: resolved
    reason: >-
      The Addendum (spec.md line 18) now attributes the three-tests/dossier/-file
      discovery to "an earlier pass of this mission's review loop — not the pass
      that wrote this addendum," states that earlier pass "already folded the
      expansion into C-001's six-file set and FR-005," and explicitly disclaims
      "it does not re-decide it, and it is not itself the pass that made the
      discovery" — correctly describing timing/attribution without claiming
      simultaneity and without inventing numbered rounds, matching the diff at
      commit ea0d59acd.
  - id: SPEC-FRESH2-002
    status: resolved
    reason: >-
      NFR-001 (spec.md line 72) now adds "The same holds for FR-003's own
      non-new components: its `_write_org_expected_artifacts` helper correction
      and its five hand-corrected `TestResolveOrgExpectedArtifactsMalformedFile`
      path constructions ... are, like FR-004's and FR-005's fixture-helper
      corrections, maintenance of existing coverage ... and are likewise
      excluded from this RED-first requirement," leaving only FR-003's new
      old-location-returns-None test case as RED-first-governed, resolving the
      asymmetry.
