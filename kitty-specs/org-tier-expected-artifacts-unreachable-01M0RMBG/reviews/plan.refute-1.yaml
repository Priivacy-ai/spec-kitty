schema: review-refute/v1
complete: true
phase: plan
batch: 1
verdicts:
  - id: PLAN-GOV-001
    verdict: confirmed
    reason: Verified src/charter/mission_type_profiles.py:1030 and src/specify_cli/dossier/manifest.py:219-221 both still document the old pre-fix path (no `missions/` segment) in their own production docstrings, neither file is in C-001's six-file set, and plan.md's Seam/Project-Structure sections discuss these two callers only for signature/control-flow stability, never flagging or deferring this docstring-drift gap the way C-007 is deferred for the frozen contract doc.
  - id: PLAN-ARCH-001
    verdict: confirmed
    reason: Confirmed the frozen contract doc org-tier-resolution-contract.md:86 embeds the old wrong path verbatim and org_expected_artifacts.py's module docstring cites it as "Contract C-4"; plan.md lines 149-158 acknowledge the tension in prose but neither the plan nor FR-002 in spec.md mandates any caveat sentence inside the module docstring itself, matching the finding's claim exactly.
  - id: PLAN-VERIFY-001
    verdict: confirmed
    reason: ci-quality.yml:902-905 literally names the step "[INFO] Run mypy report (advisory)" with continue-on-error:true and no downstream fail-check, while plan.md item 5 labels mypy --strict "(ENFORCED)" — an internally inconsistent claim given the plan itself correctly labels the sibling ruff full-report step advisory.
  - id: PLAN-VERIFY-002
    verdict: confirmed
    reason: ci-quality.yml:672 and :731 confirm both commitlint and markdownlint steps carry continue-on-error:true with no equivalent to the explicit "[ENFORCED] Fail job if security checks failed" step (line 976) that bandit/pip-audit get, and quality-gate depends only on the job-level lint result, so plan.md's "ENFORCED" framing for both is factually wrong despite the steps' own "[ENFORCED]" name prefix.
  - id: PLAN-VERIFY-003
    verdict: confirmed
    reason: ci-quality.yml:848-861's Contextive check path filter explicitly lists 'src/charter/**' (twice) alongside glossary/**, src/specify_cli/**, and .kittify/traceability/**, so this mission's sole production file (src/charter/org_expected_artifacts.py) does trigger the check, directly contradicting plan.md line 228's claim that it is "conditionally SKIPPED" and will "report no changes."
  - id: PLAN-VERIFY-004
    verdict: confirmed
    reason: ci-quality.yml's charter dorny filter block (lines ~431-436) has six path patterns, not the two (src/charter/** + tests/charter/**) plan.md line 178 names; the applicability conclusion is unaffected but the enumeration is materially incomplete, not merely phrased differently.
  - id: PLAN-VERIFY-005
    verdict: confirmed
    reason: Confirmed fast-tests-core-misc's matrix shard is literally named "core-misc" (ci-quality.yml:1710) while only integration-tests-core-misc has a shard literally named "misc" with tests/dossier in its explicit paths list (ci-quality.yml:1983); plan.md line 184 labels both jobs' shard "(misc shard)", which is a real naming error, not a phrasing difference.
