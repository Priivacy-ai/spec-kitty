schema: review-verify/v1
complete: true               # flip to true ONLY at the very end, after the yaml self-check passes
phase: plan
results:                    # one entry for EVERY finding in the findings file
  - id: PLAN-GOV-001
    status: resolved
    reason: Plan now adds an explicit "Known gap outside the six-file set — weighed explicitly, deliberately deferred (mirrors C-007)" block naming both callers with correct, live-verified line citations (mission_type_profiles.py:1030, manifest.py:219) and a stated rationale for not folding them in.
  - id: PLAN-ARCH-001
    status: resolved
    reason: Plan's Contracts section now proposes a concrete caveat sentence for the module docstring's Contract C-4 citation, placed inside the already-in-scope org_expected_artifacts.py with no new file added to C-001's set.
  - id: PLAN-VERIFY-001
    status: resolved
    reason: Plan now labels mypy --strict ADVISORY; spot-checked ci-quality.yml — step is named "[INFO] Run mypy report (advisory)" with continue-on-error:true and its has_failures output feeds only the non-blocking lint-feedback job, no step checks steps.mypy.outcome to fail the lint job.
  - id: PLAN-VERIFY-002
    status: resolved
    reason: Plan now labels commitlint and markdownlint ADVISORY despite their "[ENFORCED]" step-name prefixes; spot-checked ci-quality.yml — both use continue-on-error:true and, unlike bandit/pip-audit, no downstream step reads steps.commitlint.outcome or steps.markdownlint.outcome to fail the job.
  - id: PLAN-VERIFY-003
    status: resolved
    reason: Plan now states the Contextive check DOES run for this diff (not skip); spot-checked ci-quality.yml's git diff path list, which explicitly includes src/charter/** in the change-detection filter, so the check executes and is expected to pass.
  - id: PLAN-VERIFY-004
    status: resolved
    reason: Plan now quotes all six charter dorny-filter patterns; spot-checked ci-quality.yml's charter filter block, which lists exactly the six patterns now cited verbatim.
  - id: PLAN-VERIFY-005
    status: resolved
    reason: Plan now correctly distinguishes fast-tests-core-misc's core-misc shard (catch-all paths, no tests/dossier ignore) from integration-tests-core-misc's misc shard (tests/dossier explicit); spot-checked both matrix blocks in ci-quality.yml and confirmed the naming and dossier coverage as stated.
