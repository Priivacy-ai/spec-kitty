phase: pr
step: R5a-verify
profile: reviewer-renata
complete: true
verifications:
  - id: PR-MERGED-003
    status: resolved
    evidence: >-
      Ran `.venv/bin/python -m pytest
      tests/dossier/test_rebaseline.py::TestRebaselineOrgAwareness::test_malformed_org_pack_does_not_raise
      -p no:randomly -q --log-cli-level=WARNING` against HEAD (af0f2a622):
      PASSED, with `WARNING charter.org_expected_artifacts:org_expected_artifacts.py:110
      Org-tier expected-artifacts file .../org-pack/missions/software-dev/expected-artifacts.yaml
      failed to parse (...)` on stdout -- confirms the prior "zero warnings"
      measurement is now refuted and the test genuinely exercises the
      found-and-rejected branch, not the no-org-file branch.
      Non-vacuity check: temporarily removed the try/except in
      `_read_yaml_mapping` (src/charter/org_expected_artifacts.py) so the
      YAMLError propagates instead of degrading -- re-ran the same test and it
      FAILED (`assert outcome.error is None` -> AssertionError with the
      propagated YAMLError message). Restored the file via Write immediately
      after and confirmed md5 ed7e1adf12194d5fd24cb6b0964a53d4 (matches the
      required value) and `git status --porcelain -uno` empty before
      proceeding. This proves the fixed test would catch a regression in the
      graceful-degrade behaviour it claims to pin.
      Secondary check (anchor-only revert): reverted only the anchor join
      (`org_root / mission_type` instead of `org_root / "missions" /
      mission_type`) with the try/except left intact, re-ran the same test:
      still PASSED with no warning (file not found at the reverted path, so
      the resolver silently returns None via the not-found path, not the
      malformed-file path). This is expected and not a defect in the fix --
      this test's stated contract (per its own docstring) is "graceful
      degrade on a found-but-malformed file", not "the anchor is correct";
      anchor correctness is independently pinned by
      tests/charter/test_org_expected_artifacts.py's
      test_file_at_corrected_missions_anchor_is_found and
      test_file_only_at_old_anchor_returns_none (both added in this diff,
      both construct fixtures directly rather than via a helper). Restored
      the file again and reconfirmed the md5 and clean tree.
      Rationale check: read `_write_org_manifest` in
      tests/dossier/test_rebaseline.py (line 494) -- it takes a `data: dict`
      argument and does `yaml.dump(data, fh)` via ruamel.yaml's `YAML()`
      round-trip dumper, which by construction always emits well-formed YAML.
      It cannot express the fixture's malformed content (an unbalanced flow
      sequence, "required_always: [this, is, not: valid"). The fixer's stated
      reason for constructing the fixture directly instead of routing through
      that helper is accurate. The comment's phrasing ("instead of coupling
      to it only to be stranded again the next time that helper's join
      changes") is framed as avoiding a *future* recurrence, not as a claim
      about what caused the original strand -- checked against git history:
      the pre-fix test already hardcoded `org_root / "software-dev"` directly
      (never called the helper), so the original strand was caused by that
      hardcoded literal falling out of sync with the anchor change in
      src/charter/org_expected_artifacts.py, not by helper-coupling. This is
      a minor imprecision in the comment's framing, not a defect in the fix
      or a misrepresentation of present fact -- no finding filed for it.
notes: >-
  Applied reviewer-renata directives 030 (test/typecheck gate -- verified via
  live pytest run, not inference), 041 (tests as scaffold, not friction --
  the entire anchored-verification exercise is a DIRECTIVE_041 check: does
  this test assert an observable contract for the right reason), and tactic
  reverse-speccing (reconstructed what the test proves from its assertions
  and the measured log output, then compared against its docstring's claim).
  Everything in the anchored-verification scope was independently measured;
  nothing here rests on the fixer's prose alone. Tree confirmed clean
  (`git status --porcelain -uno` empty) and
  src/charter/org_expected_artifacts.py confirmed at md5
  ed7e1adf12194d5fd24cb6b0964a53d4 after both temporary-edit experiments.
