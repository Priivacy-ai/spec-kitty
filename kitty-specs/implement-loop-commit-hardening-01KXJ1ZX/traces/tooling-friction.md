# Tooling Friction Log

> Log every place the tooling fought you so it can feed the tooling-gap backlog.

**Prompting questions**
- What tooling or command did you have to work around?
- What blocked you unexpectedly, and how long did it take to unblock?
- Was this a known issue or something discovered fresh?

---

## Entries

<!-- YYYY-MM-DD — 1-3 sentences: what happened, why it slowed you down. -->

2026-07-15 — SEED. This mission exists partly because of tooling friction found in #2533: (1) `move-task` fails "Illegal transition" from a lane-worktree cwd (works from repo root) — that IS issue #2647, a WP here; (2) auto-commit-disabled forced hand-commits of loop status between claims; (3) acceptance-matrix negative_invariants can't auto-verify and its NegativeInvariant schema is verification_method/result(confirmed_absent). Expect friction touching move-task/worktree resolution and the commit executors.

2026-07-15 — /TASKS FINALIZE FRICTION (4 items, all cost rework during finalize). (1) **WP-ID scheme is strictly `WP\d+`** — alphabetic split suffixes (`WP02a`/`WP04b`, the natural output of a squad's a/b decomposition) are silently collapsed by `map-requirements`/`finalize` (`WP02a`→`WP02`), and the error surfaces only as a cryptic `{"error":"Unknown WP IDs","unknown_wps":["WP02A"...]}` with the letters uppercased. Had to renumber 8 files WP01..WP08 mid-flight. GAP: either support suffixed WP ids, or reject them at authoring time with a clear message. (2) **Lane allocator produces a LANE CYCLE without detecting it** — a test-only WP (the FR-006 characterization gate, owning only `tests/…`) sequenced topologically between two `implement.py` WPs (WP03 before, ref-unif WP06 after) yielded `lane-a→lane-b→lane-c→lane-a` with contradictory `parallel_group`s (lane-b pgroup 1 but depends_on lane-a pgroup 3), yet `finalize --validate-only` reported **validation_passed**. Had to hand-diagnose and fold the gate into the implement.py WP. GAP: the allocator/validator should detect + refuse (or repair) lane-level cycles. (3) **Re-shaped finalize leaves orphans** — after renumbering 8→7 WPs, the prior finalize's stale WP files stayed git-tracked (needed manual `git rm`) AND an orphan `WP08 genesis→planned` bootstrap event stayed in `status.events.jsonl`, materialized into `status.json` (8 WPs) even though the live reader showed 7. `doctor mission-state --fix` does NOT prune WP-set membership (only artifact shapes). Had to drop the event line by hand + call `status.reducer.materialize(feature_dir)`. GAP: a `status reconcile`/`finalize --prune-orphans` that reconciles the event log + snapshot to the current WP-file set. (4) `finalize --validate-only` "passed" for BOTH the cyclic 8-WP and the clean 7-WP shape — validation covers ownership/deps/coverage but not schedule sanity.

2026-07-15 — LANE-GATING vs PER-WP READINESS GAP (post-tasks squad, priti finding #1). A dependency-free P1 bug (WP06 #2647, `deps=[]`) shares its lane's file (`tasks_move_task.py`) with a WP that has a cross-lane dep (WP07 `depends_on WP02`). Because file-linearization forces WP06+WP07 into ONE lane, the allocator marks the whole lane `depends_on_lanes:[lane-a]` (`parallel_group:1`) — so the P1 fix can be withheld behind ALL of Lane A (4 tech-debt WPs) even though WP06 itself depends on nothing. The plan's original "zero critical-path (WP06 can't start before Lane-A unit 2 anyway)" reasoning was FLAWED (it conflated the two Lane-C units). GAP: lane-level gating can silently override correct per-WP `dependency_readiness_for_wp` (WP06 deps=[]). Mitigation used: corrected the plan claim + documented that WP06 must be claimed first (per-WP readiness), or landed as an early standalone if the allocator only gates per-lane. Feeds the standing flat-mission/guard-friction backlog (#2017).

2026-07-15 — SEQUENCING CONSTRAINT (not friction, recorded for the implement run): **PR #2654 (#883 mission-type doctrine authority, slice 1 — doctrine→charter→core governance/gates/steps) MUST land before this mission's implement run.** #883 makes the doctrine MissionType load-bearing for type/steps/gates; starting implement before it lands risks running WPs under a gate/step path that then shifts under us. Operator owns landing #2654.

2026-07-15 — TERMINOLOGY FRICTION (overloaded terms slowed the plan double-check). "primary" carries ≥4 distinct meanings that collided in the None→PRIMARY verdict: (a) primary PARTITION (artifact kind: primary vs coord), (b) primary BRANCH (main/master), (c) primary SURFACE/checkout (canonical repo root vs lane worktree), (d) primary/target REF. The C-007 "kind=None→PRIMARY" verdict was nearly misread as "route to the primary branch (main)" — the exact WP00 refusal-to-main bug. "merge" is likewise overloaded: `spec-kitty merge` (LOCAL lane-consolidation into the mission branch, no push) vs git merge vs PR merge to origin/main (operator-only). CLAUDE.md already carries defensive warnings ("never say just 'main'", "spec-kitty merge merges to local main only") — a sign the terms need canonical disambiguation, not just prose guards. → planner-priti tasked to recommend a glossary/terminology change + code+template alignment.

2026-07-15 — REVIEW-vs-PARTITION FRICTION (WP06 cycle-1, cost a full review cycle). A WP fix touched a C-004 charter-frozen block, whose guard demands a discoverable authorization record. The operator's #2453 scope-expansion decision was recorded on the mission/target branch (mission/2533-…, 946360a99) — the CANONICAL location, since `kitty-specs/` changes are HARD-BLOCKED on lane branches ("planning artifacts must live on the mission branch"; move-task pre-flight refuses to_for_review if a lane commits kitty-specs/). But the reviewer reviewed lane-c IN ISOLATION, couldn't find the record there (or on the lane's integration base kitty/mission-…, which is NOT the target branch that holds planning artifacts), and HELD. My first fix (commit the record onto lane-c) was itself blocked by the code-only-lanes guard. GAP: (1) review prompts/tooling should point reviewers at the mission/target branch for planning-artifact authorization (`git show <target>:kitty-specs/…`), not the lane; (2) the cycle-1 implementer's trace writes to lane-c's kitty-specs/ were silently allowed at commit time (warning only) but block move-task later — the guard should warn/block at commit, not only at handoff. Resolution: cleaned lane-c to code-only (`git restore --source <lane-base> -- kitty-specs/`), left the authorization on the mission branch, re-submitted with a reviewer note naming the exact `git show` verification path.
