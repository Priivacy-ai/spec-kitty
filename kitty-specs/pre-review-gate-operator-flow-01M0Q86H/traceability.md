# Traceability: Responsive Pre-Review Gate Operator Flow

Evidence was collected on 2026-08-23 through independent WP review and rerun on the integrated delivery branch. Integrated commit `b67b7596f` contains the reviewed implementation, the acceptance-fixture repair, and the process-tree evidence. Every node below is a complete pytest node ID and collects; the cited nodes pass on the integrated branch. “N/A” is used only where that assertion category does not apply to the behavior under test.

## Acceptance scenarios

| Scenario / requirement | Exact pytest node ID | Human assertion | Structured assertion | Launch assertion | Lane/event assertion |
|---|---|---|---|---|---|
| US1.1 / FR-001 / NFR-001 / NFR-002 — continuing progress | `tests/specify_cli/cli/commands/agent/test_tasks_move_task_pre_review_gate_observability.py::test_exact_entry_wires_typed_observer_with_ordered_human_progress[explicit_override]` and `tests/specify_cli/cli/commands/agent/test_tasks_move_task_pre_review_gate_observability.py::test_exact_entry_wires_typed_observer_with_ordered_human_progress[registered_binding]` | Start, assessment, 30s and 60s heartbeats precede one terminal line; no callback follows terminal output. | N/A — human heartbeat mode is deliberately suppressed in JSON. | Assessment precedes launch and launch is within one controlled second. | Exactly one transition call occurs after the terminal verdict. |
| US1.2 / FR-002 / FR-008 — successful completion enters review once | `tests/specify_cli/cli/commands/agent/test_tasks_move_task_pre_review_gate_observability.py::test_exact_entry_json_suppresses_observer_and_emits_one_authoritative_document[registered_binding]` | N/A — JSON invocation. | One JSON document reports `transition_applied: true` and consistent gate metadata. | Registered evaluator runs through the normal gate binding. | Exactly one transition call is recorded. |
| US1.3 / FR-002 / FR-006 — timeout preserves authority | `tests/specify_cli/cli/commands/agent/test_tasks_move_task_pre_review_gate_observability.py::test_exact_entry_interruption_has_zero_owned_residue_across_checkouts[timed_out-timed_out]` | N/A — JSON invocation. | Final document reports `timed_out` and `transition_applied: false`. | The controlled terminal gate runs; no later mutation launch is possible. | Coordination event/status bytes, lane, WP bytes, and all checkout Git snapshots remain unchanged. |
| US1.3 / FR-002 / FR-006 — cancellation preserves authority | `tests/specify_cli/cli/commands/agent/test_tasks_move_task_pre_review_gate_observability.py::test_exact_entry_interruption_has_zero_owned_residue_across_checkouts[cancelled-cancelled]` | N/A — JSON invocation. | Final document reports `cancelled` and `transition_applied: false`. | The controlled terminal gate runs; no later mutation launch is possible. | Coordination event/status bytes, lane, WP bytes, and all checkout Git snapshots remain unchanged. |
| US1.4 / FR-009 / NFR-007 — oversized refusal | `tests/specify_cli/cli/commands/agent/test_tasks_move_task_pre_review_gate_observability.py::test_exact_entry_refuses_oversized_scope_before_launch[False]` and `tests/specify_cli/cli/commands/agent/test_tasks_move_task_pre_review_gate_observability.py::test_exact_entry_refuses_oversized_scope_before_launch[True]` | Human mode says validation did not start, names `tests/architectural`, the prior lane, bounded scope, and `--skip-pre-review-gate`. | JSON reports `scope_oversized`, `not_started`, `transition_applied: false`, and both recovery choices in one document. | Lowest-level launch spy is not called; classifier-to-return is prompt. | No transition call occurs. |
| US1.5 / FR-010 / SC-009 — unknown timeout diagnostic | `tests/specify_cli/cli/commands/agent/test_tasks_move_task_pre_review_gate_observability.py::test_exact_entry_reports_unknown_timeout_candidate_without_transition[False]` and `tests/specify_cli/cli/commands/agent/test_tasks_move_task_pre_review_gate_observability.py::test_exact_entry_reports_unknown_timeout_candidate_without_transition[True]` | Human mode names unknown classification, identity, target, 300s budget, 300.250s monotonic elapsed, unchanged lane, and reviewed metadata update. | JSON contains the same values, `classification_candidate: true`, and `transition_applied: false` in one document. | Unknown scope is allowed to launch under the incumbent timeout. | No transition call occurs and deterministic policy is not mutated. |
| US2.1 / FR-003 / NFR-004 — explicit skip | `tests/specify_cli/cli/commands/agent/test_tasks_move_task_pre_review_gate_observability.py::test_exact_entry_skip_disable_collision_precedence[True-True-env0---skip-pre-review-gate flag]` | Human mode reports `SKIPPED` and the flag reason. | JSON mode records the flag reason and `transition_applied: true` in one document. | Workspace/gate resolution is not called. | Exactly one supported transition call occurs. |
| US2.2 / FR-004 — `SPEC_KITTY_SYNC_DISABLE` | `tests/specify_cli/cli/commands/agent/test_tasks_move_task_pre_review_gate_observability.py::test_exact_entry_skip_disable_collision_precedence[True-False-env1-SPEC_KITTY_SYNC_DISABLE is set]` | Human mode reports the effective variable. | JSON mode records the same reason and one applied transition. | Gate and implicit daemon launch spies are not called. | Exactly one supported transition call occurs. |
| US2.2 / FR-004 — `SPEC_KITTY_SYNC_MINIMAL_IMPORT` | `tests/specify_cli/cli/commands/agent/test_tasks_move_task_pre_review_gate_observability.py::test_exact_entry_skip_disable_collision_precedence[True-False-env2-SPEC_KITTY_SYNC_MINIMAL_IMPORT is set]` | Human mode reports the effective variable. | JSON mode records the same reason and one applied transition. | Gate and implicit daemon launch spies are not called. | Exactly one supported transition call occurs. |
| US2.3 / FR-008 — no controls runs gate | `tests/specify_cli/cli/commands/agent/test_tasks_move_task_pre_review_gate_observability.py::test_exact_entry_wires_typed_observer_with_ordered_human_progress[registered_binding]` | Human start/progress/final sequence is visible. | Companion node `tests/specify_cli/cli/commands/agent/test_tasks_move_task_pre_review_gate_observability.py::test_exact_entry_json_suppresses_observer_and_emits_one_authoritative_document[registered_binding]` proves singular JSON. | Registered binding launches the real evaluator seam. | Exactly one successful transition call occurs. |
| US3.1 / FR-005 — default new regression warns | `tests/review/test_pre_review_gate_integration.py::test_new_failure_surfaced_by_the_real_gate_red_first` and `tests/review/test_pre_review_gate_integration.py::test_console_warning_new_failures_unaffected_by_block_enabled` | The public warning renderer preserves the genuine `NEW_FAILURES` warning under both default and configured-block modes. | The real public move entry records `outcome: new_failures`, the exact failing node ID, `block_enabled: false`, and `blocked: false`. | The repaired canonical fixture resolves the real ACTIVE `software-dev/review` binding, derives `tests/git`, and launches the genuine failing candidate test. | Warn-by-default records exactly one transition; the hook-disabled control proves the same breakage otherwise reaches review without gate metadata. |
| US3.2 / FR-005 — explicit blocking policy blocks | `tests/review/test_pre_review_gate_integration.py::test_block_mode_blocks_without_force` and `tests/review/test_pre_review_gate_integration.py::test_override_scope_new_failure_blocks_when_opted_in` | N/A — these exact public-entry nodes invoke JSON mode; human NEW_FAILURES wording is covered by `tests/review/test_pre_review_gate_integration.py::test_console_warning_new_failures_unaffected_by_block_enabled`. | Both auto-derived and explicit-override public routes emit a JSON error containing the genuine failing node ID. | Both routes launch the real failing candidate target through the shared registry/engine policy path. | Both routes exit 1 before any transition is committed. |
| US3.3 / FR-007 — timeout/cancel override severity | `tests/review/test_verdict_aggregation.py::test_single_terminal_is_hard_stop_regardless_of_block_or_force[False-False-timed_out]` and `tests/review/test_verdict_aggregation.py::test_single_terminal_is_hard_stop_regardless_of_block_or_force[False-False-cancelled]` | Terminal outcome wins regardless of severity configuration. | Aggregate result has `should_exit: true` and `transition_applied: false`; singular public JSON is proven by `tests/specify_cli/cli/commands/agent/test_tasks_move_task_pre_review_gate_observability.py::test_json_interruption_is_singular_and_precedes_every_mutation[timed_out-timed_out]` and `tests/specify_cli/cli/commands/agent/test_tasks_move_task_pre_review_gate_observability.py::test_json_interruption_is_singular_and_precedes_every_mutation[cancelled-cancelled]`. | N/A — aggregation is post-run policy. | No transition is permitted. |

## Precedence combinations

| Combination | Exact pytest node ID | Human assertion | Structured assertion | Launch assertion | Lane/event assertion |
|---|---|---|---|---|---|
| Explicit skip + both disable variables + block policy | `tests/specify_cli/cli/commands/agent/test_tasks_move_task_pre_review_gate_observability.py::test_exact_entry_skip_disable_collision_precedence[True-True-env3---skip-pre-review-gate flag]` | Flag is the reported reason. | One JSON document reports the flag reason and applied transition. | Gate/workspace resolution and implicit daemon startup do not run. | One transition is applied through lifecycle tooling. |
| Both disable variables + no skip | `tests/specify_cli/cli/commands/agent/test_tasks_move_task_pre_review_gate_observability.py::test_exact_entry_skip_disable_collision_precedence[True-False-env4-SPEC_KITTY_SYNC_DISABLE is set]` | First canonical variable is reported. | One JSON document reports `SPEC_KITTY_SYNC_DISABLE`. | Gate/workspace resolution and implicit daemon startup do not run. | One transition is applied through lifecycle tooling. |
| Disable variable + explicit daemon management | `tests/specify_cli/cli/commands/agent/test_tasks_move_task_pre_review_gate_observability.py::test_explicit_daemon_management_remains_available_under_each_disable[SPEC_KITTY_SYNC_DISABLE]` and `tests/specify_cli/cli/commands/agent/test_tasks_move_task_pre_review_gate_observability.py::test_explicit_daemon_management_remains_available_under_each_disable[SPEC_KITTY_SYNC_MINIMAL_IMPORT]` | N/A — daemon API test. | N/A — daemon API test. | Implicit startup is suppressed; explicit startup invokes the daemon exactly once. | N/A — no WP transition is requested. |
| Unknown scope runs; oversized exact/superset refuses; descendant remains unknown | `tests/review/test_pre_review_gate_engine.py::test_unknown_scope_runs_and_emits_typed_scope_then_heartbeat`, `tests/review/test_pre_review_gate_engine.py::test_oversized_override_refuses_before_process_launch`, and `tests/review/test_gate_budget.py::test_unclassified_targets_are_unknown[targets1]` | Typed assessment/heartbeat distinguishes run from refusal. | Verdicts carry unknown versus oversized classification. | Unknown launches; exact/superset refuses; descendant is not promoted by ancestry. | Oversized verdict is terminal; unknown follows normal result policy. |
| Unknown timeout versus bounded timeout | `tests/review/test_pre_review_gate_engine.py::test_unknown_timeout_is_classification_candidate_with_separate_elapsed` and `tests/review/test_pre_review_gate_engine.py::test_bounded_timeout_is_not_a_classification_candidate` | Only unknown timeout guidance proposes reviewed metadata consideration. | Candidate flag is true only for unknown; configured and observed time remain separate. | Both run under configured timeout. | Both preserve the prior lane; neither mutates policy. |
| Default warning versus opt-in block versus force | `tests/review/test_pre_review_gate_integration.py::test_new_failure_surfaced_by_the_real_gate_red_first`, `tests/review/test_pre_review_gate_integration.py::test_block_mode_blocks_without_force`, and `tests/review/test_pre_review_gate_integration.py::test_force_bypasses_block_and_is_recorded` | Default mode warns; configured block refuses; force admits while retaining the regression evidence. | Public transition metadata records `new_failures`; forced admission additionally records `block_enabled: true`, `blocked: false`, and `force_bypassed: true`. | All three auto-derived scenarios activate the real binding and run the same genuine failing candidate target. | Transition counts are one, zero, and one respectively. |

## Named interruption and mutation races

| Race | Exact pytest node ID | Human assertion | Structured assertion | Launch assertion | Lane/event assertion |
|---|---|---|---|---|---|
| Completion observed before deadline | `tests/review/test_pre_review_gate_process_tree.py::test_completion_observed_before_deadline_wins_race` | N/A — process observer unit. | Completed state is returned once. | Wait returns before the deadline observation. | Completed verdict remains eligible for later policy. |
| Deadline observed first | `tests/review/test_pre_review_gate_process_tree.py::test_deadline_observed_first_yields_one_timeout_outcome` | N/A — process observer unit. | One timed-out state is returned. | Termination is invoked exactly once for PID 5252. | Timeout is terminal and cannot transition. |
| Catchable timeout after process-tree creation | `tests/review/test_pre_review_gate_process_tree.py::test_real_timeout_reaps_command_owned_process_tree` | N/A — real-process integration. | Typed head state is `TIMED_OUT`. | Real child and grandchild start, then the owned process group is stopped. | Public timeout nodes prove no transition/event mutation. |
| Catchable cancellation after process-tree creation | `tests/review/test_pre_review_gate_process_tree.py::test_real_cancellation_reaps_command_owned_process_tree` | N/A — real-process integration. | Typed head state is `CANCELLED`. | Real child and grandchild start and are reaped; cancellation requires no unbounded wait. | Public cancellation nodes prove no transition/event mutation. |
| Abrupt CLI-parent death | `tests/specify_cli/cli/commands/agent/test_tasks_move_task_pre_review_gate_parent_death.py::test_sigkill_of_cli_parent_preserves_lane_and_event_authority` | N/A — parent is uncatchably killed. | N/A — no final document can be promised after SIGKILL. | Test synchronizes on candidate-head launch before killing only the CLI parent PID. | An independent process proves event count/IDs, `in_progress` lane, and WP bytes unchanged; orphan cleanup is explicitly not asserted. |
| Windows tree termination escalation | `tests/review/test_pre_review_gate_process_tree.py::test_windows_taskkill_contract_uses_tree_then_force_escalation` | N/A — Windows process adapter unit. | N/A — process adapter unit. | Exact calls are `taskkill /PID 4242 /T`, then `/T /F`; generic terminate/kill are not used. | Terminal public nodes preserve lane; native Windows execution remains a PR CI requirement. |
| Gate byproduct versus terminal block | `tests/specify_cli/cli/commands/agent/test_tasks_move_task_pre_review_gate_observability.py::test_gate_created_path_is_reverted_on_terminal_block[True]` | N/A — JSON invocation. | Final document omits internal checkout-path leakage. | Gate creates an enrolled byproduct before returning terminal. | Byproduct is removed and no transition is committed. |
| Gate completion versus auto-commit | `tests/specify_cli/cli/commands/agent/test_tasks_move_task_pre_review_gate_observability.py::test_successful_auto_commit_occurs_only_after_gate` | N/A — ordering test. | N/A — internal ordering test. | Recorded order begins `gate`, then `commit`, then validation. | Transition occurs only after the successful gate/commit sequence. |

## Reconciled fixture-evidence defect

Integrated commit `b67b7596f` writes canonical mission metadata for the throwaway public-integration mission and asserts that the real resolver returns `GateCoverage.ACTIVE` for `software-dev`, action `review`, edge `in_progress->for_review`, with handler `spec-kitty-pre-review`. The complete module passes (`22 passed, 1 platform skip`), so the previously dead auto-derived warning, block, force, baseline-relative, bounded-scope, and handler-owned no-coverage paths are live evidence rather than binding-level `NO_COVERAGE` fallbacks. This was a fixture/evidence repair only: no CI topology or runtime policy learning changed. Tracker issues #3694 and #3695 remain open pending tracker disposition; their locally reproduced acceptance-evidence defect is fixed in the integrated branch.
