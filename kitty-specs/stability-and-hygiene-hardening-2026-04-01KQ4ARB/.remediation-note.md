# Remediation Note — issue-matrix.md (WP03, 2026-05-12)

**Diagnostic**: `MISSION_REVIEW_ISSUE_MATRIX_SCHEMA_DRIFT`

**Observation**: `issue-matrix.md` has `repo` as the first column, placing it before the mandatory columns `issue`, `verdict`, `evidence_ref`. The schema requires mandatory columns to appear first in order.

**Auto-normalized**: Trivial alias drift was resolved by WP03:
- `theme` → `scope` (alias resolution)
- `wp_id` → `wp` (alias resolution)

**Structural drift (not auto-fixed)**: The `repo` named-optional column appears before the mandatory `issue` column, violating the mandatory-columns-first ordering rule. The validator expects the first three columns to be `issue`, `verdict`, `evidence_ref` in that order.

**Recommended operator action**:
1. Reorder the columns so mandatory columns come first: `issue`, `verdict`, `evidence_ref`, followed by optional columns in any order.
2. Suggested canonical column order: `| issue | scope | verdict | wp | evidence_ref | repo |`
3. Commit the fix directly in the `main` branch. Row data values do not need to change, only column order.
