# Mission A follow-ups (fsm-write-path-integrity-01M1TZV6) — one bullet per tracker issue / hand-off

Reviewer: claude-fable-5-1 (mission-review subagent), 2026-09-06. Source: `mission-review-A.md` (same directory). Repo paths are relative to the main checkout.

- **[process / charter gate] Per-lane review never runs the full architectural suite; three gate regressions shipped through seven reviews and an `accept`.** Evidence: `scratchpad/arch-fail-head.txt` vs `arch-fail-base.txt` (12 vs 9 failures; rows 10–12 mission-caused), `kitty-specs/fsm-write-path-integrity-01M1TZV6/plan.md:195-198` ("never the full `tests/architectural/`"), repair commit `1ceba9f22`. Ask: make one `tests/architectural/` run on the merged lane a precondition of `spec-kitty accept` (implement-review skill + charter standing order). → tracker issue.
- **[Mission C / caller-migration] Lockless rollback truncate on the non-transactional coord fallback arm (SC-001 class, sibling site).** Evidence: `src/specify_cli/coordination/status_transition.py::_restore_coord_status_artifacts` (`:364-390`, no `feature_status_lock`) called from `_emit_on_coord_then_commit` (`:118-162`) after the flat shell released L1; ledgered in `tests/architectural/test_status_events_writes_gate.py` `ALLOWED_OUT_OF_STORE_WRITE_SITES` ("WP06 may retire"). Options: locked size-verified truncate, or retire the arm when the 8 direct transactional callers migrate (Q4 rider). → tracker issue + risk-register row next to R9.
- **[Mission C] Post-commit tail fan-out can double-announce a concurrent writer's transition row.** Evidence: `status_transition.py::_fan_out_committed_coord_tail` (`:75-112`) parses every byte past `pre_emit_event_size`; WP06 review-cycle-1 minor. Fix shape: deferred-callable seam returning the arm's own rows. → tracker issue (LOW).
- **[caller-migration mission] Migrate the 8 direct `emit_status_transition_transactional` callers + 2 batch callers onto `MissionStatus`; retire the `aggregate.py:636-644` lazy `coordination` import; surface `DependencyReadiness.unsatisfied` ids in the guard refusal.** Evidence: `design-notes/WP06-convergence.md` §1 (Q4 rider), `research.md` §1.1 caller census, `contracts/dependency-guard.md` §2 vs `src/specify_cli/status/wp_state.py:31` (message carries no ids). → existing #1667/C-004 ADR amendment + tracker issue.
- **[caller-migration mission] `implement` pre-flight re-gates an `in_progress` resume before the lifecycle no-op is reached (US4-6 only pinned at the lifecycle layer).** Evidence: `src/specify_cli/cli/commands/implement.py:1869`; `design-notes/WP04-guard.md` A4; `tests/status/test_work_package_lifecycle.py::test_start_implementation_resume_is_not_regated_when_dependency_regresses`. Ask: skip the pre-flight gate on resume, then add the CLI-level pin T025.2 asked for. → tracker issue.
- **[Mission A hardening, small] Resolve dependency readiness only on the two guarded entry edges and log the missing-WP-file fail-open.** Evidence: `src/specify_cli/status/emit.py::_resolve_dependency_readiness` called unconditionally at `emit.py:535,874` and `status_transition.py:606,796`; `_declared_dependencies` returns `()` silently at `emit.py:156-158`. Reviewer shape (a) from `tasks/WP04-dependency-readiness-guard/review-feedback-1.md` Finding 2. → tracker issue (LOW).
- **[Mission A hardening, small] Add NFR-001 spawn-recorder pins for writer families 4–7.** Evidence: pins exist only for families 8/9 (`tests/specify_cli/decisions/test_emit_locking.py::test_no_git_subprocess_while_holding_the_mission_lock`, `tests/specify_cli/migration/test_rebuild_state_locking.py::…`); families 4–7 verified by inspection only (`design-notes/WP01-lock-rules.md` §3). → tracker issue (LOW).
- **[#3893] Finite DEFAULT L1 timeout with holder-naming error (decision Q2 / R9) and event-log compaction / snapshot-anchored read with the O(n²) evidence (Non-Goal 3 / Q7).** Evidence: `decisions/DM-01M1V8HZ5DC4QX1YDG1PRYNX9T.md`, `DM-01M1V8J4KEQPRGGR4MQABJA8DN.md`; `src/specify_cli/status/emit.py::_reduce_write_surface` still reads the full log per emit; `issue-matrix.json` row #3893 (`deferred-with-followup`). → operator posts on #3893.
- **[#3895] Census-gate rule note (first instance).** Evidence: text drafted in `design-notes/WP03-gates.md` §1; `issue-matrix.json` row #3895. → operator posts.
- **[#2173] StatusReader de-serialization note; StatusWriter seam shaped-not-built.** Evidence: `spec.md` Non-Goal 1; `issue-matrix.json` row #2173. → operator posts.
- **[#1868] FR-020 glossary enforcement-honesty note (Mission B WP02 drafts).** Evidence: `issue-matrix.json` row #1868. → Mission B hand-off.
- **[#3893 milestone] Reconcile #3893 (4.0.0) vs this mission (3.2.7) and mint the mission's tracker issue under #3893.** Evidence: `spec.md:276` ("Nothing is posted on the tracker yet"). → operator.
- **[operator confirmation] Plain batch door lost the #946 fail-closed skip (workspace-context default now applied on `claimed→in_progress`).** Evidence: `design-notes/WP02-pipeline.md` §6 D-2, `WP06-convergence.md` §5; `tests/status/test_emit.py::TestBatchShellLock::test_batch_claimed_to_in_progress_defaults_workspace_context_like_every_other_door`. → confirm or reinstate as a pipeline policy knob.
- **[docs, next touch] Fold spec staleness (Q1–Q10 markers, `Status: Draft`, C-008 four-caller rationale), reconcile contract deviations (`dependency-guard.md` §2 `<ids>`; `emit-pipeline.md` §1 annotation type and the `_unsafe` allowlist membership), and fix two stale comments.** Evidence: `spec.md:5,159,188-201`; `analysis-report.md` I4/I5/A1; `src/specify_cli/retrospective/lifecycle_events.py:102` (`.git/spec-kitty-locks` → `.kittify/spec-kitty-locks` per `status/locking.py:167-171`); `src/specify_cli/orchestrator_api/commands.py:3575` ("seven" → nine families). → docs PR.
- **[risk register] Transient-git-probe degrade inside a linked worktree now yields a worktree-local lock (`.kittify/spec-kitty-locks`) instead of a loud failure.** Evidence: `src/specify_cli/status/locking.py:157-171`; WP01 review-cycle-1 minor. → risk-register row (LOW).
- **[optional hardening] `_write_snapshot` (and the `reducer.materialize` precedent) do not fsync the directory after `os.replace`.** Evidence: `src/runtime/next/_internal_runtime/engine.py:137-149`; `src/specify_cli/status/reducer.py:278`. → tracker issue (LOW) or accept.
- **[tooling / environment, not mission code] Shared `.venv` editable `.pth` points at the removed lane-e worktree, so `.venv/bin/spec-kitty` fails with `ModuleNotFoundError: specify_cli`.** Evidence: `.venv/lib/python3.11/site-packages/_editable_impl_spec_kitty_cli.pth` (mtime 14:40, six lines of `.worktrees/fsm-write-path-integrity-01M1TZV6-lane-e/src`); `git worktree list` shows no lane worktrees. Fix: `uv sync --frozen --all-extras` from the main checkout. Also: `issue-matrix.json` still carries nine `title: "<fill at WP-implementation time>"` placeholders (cosmetic).
