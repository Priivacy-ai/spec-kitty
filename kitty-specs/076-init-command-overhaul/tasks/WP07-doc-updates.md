---
work_package_id: WP07
title: Documentation Updates
dependencies:
- WP06
requirement_refs:
- FR-027
planning_base_branch: main
merge_target_branch: main
branch_strategy: Planning artifacts for this feature were generated on main. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into main unless the human explicitly redirects the landing branch.
subtasks:
- T042
- T043
- T044
- T045
- T046
- T047
agent: "opencode:opencode:reviewer:reviewer"
shell_pid: "15581"
history:
- date: '2026-04-08'
  status: planned
  note: Generated by /spec-kitty.tasks
authoritative_surface: docs/
execution_mode: planning_artifact
owned_files:
- docs/how-to/non-interactive-init.md
- docs/reference/cli-commands.md
- docs/reference/configuration.md
- docs/how-to/manage-agents.md
- docs/2x/model-discipline-routing.md
tags: []
---

# WP07 — Documentation Updates

## Objective

Remove all references to the deleted CLI flags and the removed preference system from user-facing documentation. Five files across `docs/` need updating. These are text-only changes with no code impact.

## Branch Strategy

- **Planning base:** `main`
- **Merge target:** `main`
- **Implementation command:** `spec-kitty implement WP07`
- Lane C, position 2. Sequential after WP06.

## Context

The following strings must not appear in `docs/` after this WP:
- `--preferred-implementer`
- `--preferred-reviewer`
- `--script` (as a flag — keep any prose that says "script" in other contexts)
- `--debug` (as an init flag)
- `--skip-tls`
- `--github-token`
- `--template-root`
- `--force` (as an init flag)
- `--here` (as an init flag)
- `--mission` (as an init flag)
- `--ignore-agent-tools`
- `preferred_implementer` (as a config key)
- `preferred_reviewer` (as a config key)
- `selection.preferred_implementer` / `selection.preferred_reviewer`

---

## Subtask T042 — Update docs/how-to/non-interactive-init.md

**Purpose:** This file has the most concentration of removed flag examples (~lines 13–14, 82–83, 113–114, 359–360 and surrounding prose).

**Steps:**
1. Read the file in full.
2. Find all command examples that include `--preferred-implementer`, `--preferred-reviewer`, or `--script`. Remove those flags from the examples.
3. Find prose sections that describe what these flags do. Remove or rewrite those sections.
4. Update any "available flags" table or list to reflect only: `--ai`, `--non-interactive`, `--no-git`.
5. Ensure all remaining command examples are syntactically valid after the removals.
6. Read through the full file once more to check for dangling references (e.g., "as shown in the `--preferred-implementer` example above" without the example).

**Validation:** `grep -n "preferred_implementer\|preferred_reviewer\|--script\|--force\|--here\|--debug\|--skip-tls\|--github-token\|--template-root\|--mission\|--ignore-agent-tools" docs/how-to/non-interactive-init.md` → 0 results.

---

## Subtask T043 — Update docs/reference/cli-commands.md

**Purpose:** The init command reference table lists all flags. Remove the 11 deleted flags.

**Steps:**
1. Find the `spec-kitty init` section. It has a table of options.
2. Remove table rows for all 11 removed flags:
   - `--here`
   - `--script` / `--script-type`
   - `--preferred-implementer`
   - `--preferred-reviewer`
   - `--force`
   - `--template-root`
   - `--debug`
   - `--skip-tls`
   - `--github-token`
   - `--mission`
   - `--ignore-agent-tools`
3. Verify the remaining flags (`--ai`, `--non-interactive`, `--no-git`) have accurate descriptions.
4. Update any "Examples" section that used removed flags.

**Validation:** `grep -n "preferred-implementer\|preferred-reviewer\|--script\b\|--force\|--here\b\|--debug\b\|--skip-tls\|--github-token\|--template-root\|--mission\b\|--ignore-agent-tools" docs/reference/cli-commands.md` → 0 results.

---

## Subtask T044 — Update docs/reference/configuration.md

**Purpose:** This file documents the `config.yaml` schema. The `agents.selection` block must be removed.

**Steps:**
1. Find the `agents.selection` section (around lines 300–336). It documents:
   ```yaml
   agents:
     selection:
       preferred_implementer: opencode
       preferred_reviewer: claude
   ```
2. Remove the entire `selection:` subsection from the YAML examples.
3. Remove the table rows for `selection.preferred_implementer` and `selection.preferred_reviewer`.
4. Remove any prose description of what these fields do.
5. If the `selection` heading/subsection becomes empty after removal, remove the heading too.
6. Update the example `config.yaml` snippet at the end of the section to not include `selection`.

**Validation:** `grep -n "preferred_implementer\|preferred_reviewer\|selection\." docs/reference/configuration.md` → 0 results.

---

## Subtask T045 — Update docs/how-to/manage-agents.md

**Purpose:** Lines ~200–201 contain commented-out YAML with `preferred_implementer` and `preferred_reviewer`. Remove these.

**Steps:**
1. Read the file. Find the commented-out YAML block (approximately):
   ```yaml
   #     preferred_implementer: claude
   #     preferred_reviewer: codex
   ```
2. Remove these comment lines.
3. Check surrounding context — if there is prose explaining what `preferred_implementer` does, remove or update that prose too.
4. If a YAML block that previously showed the `selection` section now looks incomplete, add a note that `selection` has been removed.

**Validation:** `grep -n "preferred_implementer\|preferred_reviewer" docs/how-to/manage-agents.md` → 0 results.

---

## Subtask T046 — Update docs/2x/model-discipline-routing.md

**Purpose:** Line ~19 references the static `preferred_implementer`/`preferred_reviewer` config as the current routing mechanism.

**Steps:**
1. Read the file. Find the line that says something like:
   > "Tool selection config is currently static (`preferred_implementer`, `preferred_reviewer`) with first-available fallback"
2. Remove or rewrite this sentence. If the surrounding paragraph is about agent routing, update it to reflect that routing is now determined by `agents.available` list order (first-available fallback) without a static preference.
3. Read through the full file to check for any other preference references.

**Validation:** `grep -n "preferred_implementer\|preferred_reviewer" docs/2x/model-discipline-routing.md` → 0 results.

---

## Subtask T047 — Final Verification Grep

**Purpose:** Confirm no preference references survive in docs/.

**Run all of these. Each must return 0 results:**

```bash
# Preference config keys
grep -r "preferred_implementer\|preferred_reviewer" docs/

# Removed init flags
grep -r "\-\-preferred-implementer\|\-\-preferred-reviewer" docs/
grep -r "\-\-script\b\|\-\-script-type" docs/
grep -r "\-\-debug\b.*init\|\-\-skip-tls\|\-\-github-token\|\-\-template-root" docs/
grep -r "\-\-force\b.*init\|\-\-here\b.*init\|\-\-mission\b.*init\|\-\-ignore-agent-tools" docs/
```

Note: be careful with `--script` and `--debug` — these may appear in other contexts unrelated to init (e.g., other commands, code blocks). The search should be specific enough to catch init-related usages without false positives. Use context (`-B2 -A2`) to verify any hits.

If any grep returns results, go back to the appropriate subtask and fix them.

---

## Definition of Done

- [x] `grep -r "preferred_implementer\|preferred_reviewer" docs/` → 0 results
- [x] `grep -r "\-\-preferred-implementer\|\-\-preferred-reviewer" docs/` → 0 results
- [x] All 5 doc files render valid markdown (no broken table rows, no dangling section headings)
- [x] The `config.yaml` example in configuration.md has no `selection` block

## Risks

- **False-positive grep hits:** `--script` and `--debug` appear in other command docs. The verification step (T047) uses context-aware greps. Do not over-remove — check surrounding context before deleting any line.
- **Downstream doc links:** If any doc file links to a removed section within another doc file (e.g., "see the --preferred-implementer section"), the link will be broken. Check for cross-file references to removed sections.

## Activity Log

- 2026-04-08T05:52:07Z – unknown – Moved to in_progress
- 2026-04-08T05:56:45Z – unknown – Ready: 5 doc files updated, all preference references removed
- 2026-04-08T05:57:05Z – opencode:opencode:reviewer:reviewer – shell_pid=15581 – Started review via action command
