---
work_package_id: WP03
title: Template Module Cleanup and Test Suite Overhaul
dependencies:
- WP01
- WP02
requirement_refs:
- FR-003
- FR-007
- FR-008
- FR-009
- FR-010
- FR-011
- FR-012
- FR-013
- FR-014
- FR-015
- FR-016
planning_base_branch: main
merge_target_branch: main
branch_strategy: Planning artifacts for this feature were generated on main. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into main unless the human explicitly redirects the landing branch.
base_branch: kitty/mission-076-init-command-overhaul
base_commit: 00e15448146d25e00995886babf77deac6a68025
created_at: '2026-04-08T06:00:09.517051+00:00'
subtasks:
- T019
- T020
- T021
- T022
- T023
agent: "codex:gpt-4o:python-reviewer:reviewer"
shell_pid: "44790"
history:
- date: '2026-04-08'
  status: planned
  note: Generated by /spec-kitty.tasks
authoritative_surface: src/specify_cli/template/
execution_mode: code_change
owned_files:
- src/specify_cli/template/github_client.py
- src/specify_cli/template/manager.py
- tests/specify_cli/cli/commands/test_init_doctrine.py
- tests/agent/test_init_command.py
- tests/upgrade/migrations/test_m_2_0_1_tool_config_key_rename.py
- tests/specify_cli/cli/commands/test_init_integration.py
tags: []
---

# WP03 — Template Module Cleanup and Test Suite Overhaul

## Objective

Delete `github_client.py`, remove dead code from `template/manager.py`, delete the doctrine test file, update affected test files, and write the comprehensive new integration tests for the redesigned init command. This WP completes Lane A and must leave the full test suite green.

## Branch Strategy

- **Planning base:** `main`
- **Merge target:** `main`
- **Implementation command:** `spec-kitty implement WP03`
- Final WP in lane-a worktree. WP02 must be complete.

---

## Subtask T019 — Delete github_client.py and Clean manager.py

**Purpose:** Remove the remote-download infrastructure and dead script-copy logic.

### Part A: Delete github_client.py
```bash
rm src/specify_cli/template/github_client.py
```
Confirm no remaining imports of `github_client` anywhere:
```bash
grep -r "github_client\|download_and_extract_template\|build_http_client" src/ tests/
```
All hits should already have been cleaned from init.py in WP02 (T014). If any remain, remove them now.

### Part B: Clean template/manager.py

1. **Remove `get_local_repo_root()`** — find the function definition (starts with `def get_local_repo_root(` around line 52 in manager.py). Delete it entirely.

2. **Remove bash/powershell script-copy branches** from `copy_specify_base_from_local()` and `copy_specify_base_from_package()`:
   - Find the sections (lines ~131–150 in copy_specify_base_from_local) that copy `scripts/bash/` or `scripts/powershell/` variants.
   - Delete these branches. The functions may still copy other things (missions, memory) — leave those intact.

3. After edits, run `ruff check src/specify_cli/template/manager.py --select F401` to find newly unused imports. Remove them.

4. Run `mypy --strict src/specify_cli/template/manager.py`.

**Validation:**
- `src/specify_cli/template/github_client.py` does not exist
- `grep -r "get_local_repo_root\|bash.*scripts\|powershell.*scripts" src/specify_cli/template/manager.py` → 0 results

---

## Subtask T020 — Delete test_init_doctrine.py; Update test_init_command.py

**Purpose:** Remove tests for deleted code; strip removed-flag tests from the command test file.

### Part A: Delete doctrine test file
```bash
rm tests/specify_cli/cli/commands/test_init_doctrine.py
```
Verify it's gone. Confirm no other test file imports from it.

### Part B: Update test_init_command.py

Open `tests/agent/test_init_command.py`. Find and remove all test cases that exercise the removed flags:
- Any test passing `--preferred-implementer`, `--preferred-reviewer`
- Any test passing `--script` or `--script-type`
- Any test passing `--debug`, `--skip-tls`, `--github-token`
- Any test passing `--template-root`
- Any test passing `--force` or `--here`
- Any test passing `--mission` or `--ignore-agent-tools`

Leave tests for `--ai`, `--non-interactive`, `--no-git`, and any positional `project_name` tests.

Run `pytest tests/agent/test_init_command.py -v` and confirm remaining tests pass.

---

## Subtask T021 — Update test_m_2_0_1_tool_config_key_rename.py

**Purpose:** The fixture YAML in this test file contains `preferred_implementer` / `preferred_reviewer` keys. These are no longer valid after WP01's changes.

**Steps:**
1. Open `tests/upgrade/migrations/test_m_2_0_1_tool_config_key_rename.py`.
2. Find fixture strings or dict literals containing `preferred_implementer` or `preferred_reviewer`.
3. Remove those keys from the fixture data. The migration test is about the `agents → tools` key rename, not about selection keys.
4. Run: `pytest tests/upgrade/migrations/test_m_2_0_1_tool_config_key_rename.py -v`

---

## Subtask T022 — Write New Init Integration Tests

**Purpose:** The deleted doctrine tests and removed flag tests must be replaced with tests that cover the redesigned init's actual behavior.

Create `tests/specify_cli/cli/commands/test_init_integration.py` with the following test cases. Use `tmp_path` fixtures and mock `ensure_runtime()` at the appropriate boundary to avoid touching `~/.kittify/` in CI:

| Test | FR | Description |
|------|----|-------------|
| `test_init_no_args_uses_current_dir` | FR-001 | Running `init` with no args initializes in cwd |
| `test_init_project_name_creates_subdir` | FR-002 | `init myproject` creates `./myproject/` |
| `test_ensure_runtime_failure_exits_1` | FR-003 | When `ensure_runtime()` raises, init exits 1 with error message |
| `test_ensure_runtime_success_bootstraps` | FR-003 | When `ensure_runtime()` succeeds, global runtime exists |
| `test_ai_flag_selects_agents` | FR-008 | `--ai claude,codex` sets available agents without prompting |
| `test_non_interactive_no_prompts` | FR-009 | `--non-interactive --ai claude` exits 0, no stdin |
| `test_no_git_skips_git_init` | FR-010 | `--no-git` leaves no `.git/` in project dir |
| `test_gitignore_written` | FR-011 | `.gitignore` contains agent dir entries after init |
| `test_claudeignore_written` | FR-012 | `.claudeignore` exists after init |
| `test_metadata_written` | FR-013 | `.kittify/metadata.yaml` exists with `spec_kitty_version` field |
| `test_config_has_no_selection_block` | FR-014 | `.kittify/config.yaml` has no `selection` key |
| `test_no_charter_dir_created` | FR-015 | `.kittify/charter/` does not exist after init |
| `test_no_dashboard_started` | FR-015 | `ensure_dashboard_running` is not called |
| `test_reinit_is_idempotent` | FR-016 | Running init twice on same dir produces no file changes on second run |

Each test should be ~15–25 lines. Mock `ensure_runtime()`, `install_all_global_skills()`, and any interactive prompts using `unittest.mock.patch`.

---

## Subtask T023 — Full Suite Validation

**Purpose:** Lane A must be completely clean before merge.

**Steps:**
1. Run `mypy --strict src/specify_cli/cli/commands/init.py src/specify_cli/template/manager.py src/specify_cli/core/agent_config.py`
2. Run `pytest tests/agent/ tests/specify_cli/cli/commands/ tests/upgrade/migrations/test_m_2_0_1_tool_config_key_rename.py -v`
3. Run `ruff check src/specify_cli/cli/commands/init.py src/specify_cli/template/manager.py src/specify_cli/core/agent_config.py`
4. Verify: `src/specify_cli/template/github_client.py` does not exist
5. Verify: `tests/specify_cli/cli/commands/test_init_doctrine.py` does not exist
6. Verify: `spec-kitty init --help` output lists ≤4 option flags

Fix any issues found before marking this WP done.

---

## Definition of Done

- [ ] `src/specify_cli/template/github_client.py` does not exist
- [ ] `tests/specify_cli/cli/commands/test_init_doctrine.py` does not exist
- [ ] `tests/specify_cli/cli/commands/test_init_integration.py` exists with 14 test cases, all green
- [ ] `pytest` full suite exits 0
- [ ] `mypy --strict` clean on all Lane A files
- [ ] `spec-kitty init --help` shows ≤4 option flags

## Risks

- **Manager.py collateral:** `copy_specify_base_from_local()` and `copy_specify_base_from_package()` may have callers outside init.py that still pass `script_type`. Check: `grep -r "copy_specify_base_from" src/` and update any callers that no longer need the script_type parameter.
- **Test mocking depth:** `ensure_runtime()` touches the filesystem. Mock at the right boundary — patch `specify_cli.cli.commands.init.ensure_runtime` not the underlying implementation. Same for `install_all_global_skills`.

## Activity Log

- 2026-04-08T06:24:54Z – unknown – shell_pid=16554 – Ready: github_client.py deleted, manager.py cleaned (bash/powershell script-copy removed, get_local_repo_root moved to template/__init__.py), 13 new integration tests (FR-001 to FR-016), doctrine tests deleted, test suite 8689 passed 0 failed, mypy --strict clean on manager.py
- 2026-04-08T06:25:38Z – claude:sonnet:python-reviewer:reviewer – shell_pid=39715 – Started review via action command
- 2026-04-08T06:27:09Z – claude:sonnet:python-reviewer:reviewer – shell_pid=39715 – Moved to planned
- 2026-04-08T06:34:44Z – claude:sonnet:python-reviewer:reviewer – shell_pid=40911 – Cycle 2: init.py reverted to WP02 state, get_local_repo_root deleted from __init__.py and restored to manager.py, 14th integration test added (test_ensure_runtime_success_bootstraps), all 14 tests pass
- 2026-04-08T06:35:36Z – codex:gpt-4o:python-reviewer:reviewer – shell_pid=44790 – Started review via action command
- 2026-04-08T06:37:50Z – codex:gpt-4o:python-reviewer:reviewer – shell_pid=44790 – Cycle 2 approved: init.py clean (no stubs, only import cleanup from WP02 scope), github_client.py deleted, test_init_doctrine.py deleted, get_local_repo_root retained in manager.py due to legitimate callers in runtime/agent_skills.py, skills/verifier.py, and 3 upgrade migrations, re-exported from __init__.py as simple import, 14/14 integration tests pass, mypy clean on template/manager.py and template/__init__.py
