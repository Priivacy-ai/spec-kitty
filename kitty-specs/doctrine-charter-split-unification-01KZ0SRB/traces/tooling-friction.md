# Tracer: tooling-friction

One entry per finding: `YYYY-MM-DD · actor · <text>`.

---

2026-08-02 · orchestrator · spec-kitty implement <WP> repeatedly failed with 'workspace allocation failed: cannot auto-merge the recorded planning commit <sha> into lane <lane>: the merge conflicts' for EVERY Wave-0 lane (WP01/02/03/04/05/07/10/12/14) on first materialization. Root cause: lanes.json's fixed planning_commit_sha (last commit touching kitty-specs/<mission>/tasks/) predates later coord-branch bootstrap commits, so lane branches (cut from the coord tip) hit add/add conflicts against the feat-branch's own placeholder (unbootstrapped, 0-WP) copies of lanes.json/status.json/status.events.jsonl. Resolved uniformly by taking --ours (the coord-authoritative, bootstrapped copy) for all three files in each lane worktree, then re-running implement. Separately: also hit 'WP not finalized' initially because no local coord worktree existed yet ('spec-kitty doctor coordination --json' gave the exact git worktree add recovery command) -- do not re-run finalize-tasks to fix this, it just duplicates already-bootstrapped coord state and makes the conflict worse.

2026-08-02 · orchestrator · WP01 implementer flagged a research.md D1 inaccuracy: charter/bundle.py __all__ exports only CHARTER_YAML, not CHARTER_MD (CHARTER_MD is defined at :35 but absent from __all__ at :469). Import still works fine (context.py:30 imports it directly), so nothing is broken, but D1's 'FR-001 near-empty, both constants already exported' claim is imprecise. Left as a one-line follow-up outside WP01 owned_files (bundle.py). WP11 (the FR-016 sink WP) should double-check/fix this when it touches bundle.py's allowlist story.

2026-08-02 · orchestrator · Upstream gap: 'spec-kitty agent tasks mark-status T001 T002' always resolves to WP01 regardless of which WP you intend, because every WP in this mission's tasks/ dir restarts subtask numbering at T001/T002 (non-unique across WPs), and owning_wp_from_authored_roster() (tasks_mark_status.py:520) does first-match-wins over tasks_dir.glob('*.md') sorted alphabetically -- WP01 always sorts first and always contains a T001/T002, so it wins every time, for every WP in this mission. This means the 'unchecked subtasks' gate on move-task --to for_review/approved can never be satisfied via mark-status for any WP whose subtask IDs collide with an earlier-sorting WP's IDs. Worked around by using move-task --force with an explicit --note citing this exact root cause, after independent reviewer verification confirmed the WP's actual DoD items were genuinely done. Filed as a gap: mark-status/owning_wp_from_authored_roster needs a WP-scoping argument (e.g. 'WP04:T001') or ambiguity-detection instead of silent first-match.

2026-08-02 · orchestrator · WP10 reviewer found a stale comment at src/charter/activation/synthesizer/graph_residue.py:19-21 asserting 'charter.activation.synthesizer already has a deferred import specify_cli' -- no longer true after WP10 deleted that edge. Outside WP10's owned_files; needs a follow-up sweep (not folded into this mission, flagging for post-merge or a future doc-hygiene pass).

2026-08-02 · orchestrator · WP07's census artifact (notes/meta-load-census.md) had to be committed inside kitty-specs/<mission>/ on the LANE branch (lane-g), tripping a 'Protected path: implementation branches must not modify kitty-specs/' warning (non-blocking). On a coord-topology mission, kitty-specs/<mission>/ content is coord-owned, but lane worktrees can't write to the coord branch directly, and the implementer correctly did not attempt to reach into the coord worktree (out of bounds per dispatch instructions). This creates a real handoff risk: WP08/WP09 (dependencies: [WP07]) need this census file in THEIR OWN freshly-cut lanes, which branch from the coord tip, not from WP07's lane. Plan: verify whether 'spec-kitty implement WP08' auto-merges the approved WP07 lane commit (dependency-aware lane creation per research.md D5) when WP08 is started; if not, manually merge WP07's lane branch tip into WP08/WP09's lanes before dispatching their implementers, same pattern as the planning_commit_sha conflict resolution.

2026-08-02 · orchestrator · WP07 census (notes/meta-load-census.md) has a minor prose typo in section 3: claims DEF B (task_utils/support.py:599, meta_path signature) is reached by '7 rows tree-wide plus 2 aliased references' - reviewer's independent count confirms the ACTUAL number is 5 rows (1 def + 4 tests), no phantom aliased references exist as grep rows. The row-level classifications/tags are all correct; only the summary sentence is off. WP08/WP09 implementers should be told to trust the per-row tags, not the '7' figure in the prose summary.

2026-08-02 · orchestrator · WP09's rework discovered a REAL gap in WP07's caller census: tasks_parsing_validation.py:762 uses 'import load_meta as _load_meta_lggrd' (an aliased import) which is invisible to a raw text grep for 'load_meta(' -- yet WP07's census asserted its grep-count-reconciliation proved zero undercount. The undercount guard was validating grep-against-grep (structurally circular), not catching aliased-import blind spots. WP09 fixed this by building a proper AST-based independent discovery scanner (resolving ImportFrom aliases, local defs, and attribute calls) instead of trusting the census file as ground truth. Upstream learning for future missions: a 'grep-count matches census-row-count' anti-omission guard is necessary but not sufficient -- it doesn't catch call sites reached through import aliasing. Prefer AST-based call-site discovery for any future census/inventory work of this kind.

2026-08-02 · orchestrator · WP11's implementer (during its final pre-existing-failure attribution check) found a real, simple defect in already-approved WP07: tests/specify_cli/core/test_load_meta_fail_closed_authority.py declared no pytestmark, unlike every sibling file in tests/specify_cli/core/ (which all use pytestmark = [pytest.mark.unit, pytest.mark.fast]). This meant ~10 test nodes never ran under any CI job's marker-based selection, cascading into 4 downstream architectural gate failures (collection-completeness, orphan-surface, same-tier, marker-convention). Fixed directly by the orchestrator in WP07's lane (commit ce7b1efc3) since WP07 hadn't been merged to target yet and the fix was a 2-line, unambiguous, mechanical correction. Confirms the value of running the broader architectural test suite at the mission's sink WP, not just the WP's own targeted tests -- catches cross-WP integration gaps that per-WP targeted testing misses.

2026-08-02 · orchestrator · Process learning: WP11's reviewer was dispatched right as the implementer's FIRST completion notification arrived, but the same implementer agent then continued working (per the harness's notification semantics - an agent can keep going after a first 'completed' notification if it decides there's more to do) and pushed a 3rd commit (3badb67a8) WHILE the reviewer was already running against the same lane-k worktree. The reviewer noticed the worktree mutate mid-review (HEAD advanced, files transiently deleted) and self-corrected by restoring from HEAD before finishing. No harm done this time, but future missions should either wait a beat after a completion notification before dispatching the reviewer (to let a 'still working' agent settle), or have the reviewer defensively re-check git log/status at both start and end of its own review.
