# Issue matrix — mission-coordination-branch-atomic-event-log-01KSPTVW

Per FR-037 of the spec-kitty-mission-review skill Gate-4. One row per
issue referenced in `spec.md` plus rows for cross-WP findings surfaced
during this mission's post-merge review (see `mission-review-report.md`).

Valid `Verdict` values: `fixed`, `verified-already-fixed`, `deferred-with-followup`.

| Issue | Title | Verdict | Evidence ref |
|-------|-------|---------|--------------|
| #1348 | Planning-artifact commits silently landing on `main`; dangling event-log appends on commit failure | fixed | Merge commit `886dde756`; regression tests `tests/regression/test_issue_1348.py` (3/3 passing, SHA-256 byte-identical after forced failure); architectural import guardrail `tests/architectural/test_safe_commit_import_boundary.py` (passes); `BookkeepingTransaction` contract in `src/specify_cli/coordination/transaction.py` (pre-flight policy gate + surgical truncate rollback). |
| #1348 / DRIFT-1 | FR-002 refusal-error shape loosely enforced (single concatenated line, no separate "next step", no lane-worktree path) | deferred-with-followup | LOW severity. Error code is stable and machine-parseable (NFR-007 ADEQUATE). Human-facing diagnostic shape upgrade is bundled with DRIFT-2 follow-up; tracked under [#1355](https://github.com/Priivacy-ai/spec-kitty/issues/1355). |
| #1348 / DRIFT-2 | FR-011 loud-diagnostic four-line block not emitted; only `logger.error(...)` lines exist on rollback | deferred-with-followup | MEDIUM severity. Rollback runs correctly (verified by regression + unit tests); only the operator-facing communication of rollback is generic. Tracked under [#1355](https://github.com/Priivacy-ai/spec-kitty/issues/1355) — the FR-011 emitter shape is part of the same follow-up that tightens the legacy fallback path. |
| #1348 / DRIFT-3 | FR-014 commit-summary JSON output untested | deferred-with-followup | LOW severity. Mechanism (`_record_receipt`) is partly in place. End-of-command JSON summary printout not yet implemented; tracked under [#1355](https://github.com/Priivacy-ai/spec-kitty/issues/1355) alongside the diagnostic shape work. |
| #1348 / DRIFT-4 | FR-016 `mission close --discard` unimplemented | fixed | This PR (commit `<see merge commit on main after this fix lands>`): added `spec-kitty mission close [--discard]` Typer command at `src/specify_cli/cli/commands/mission_close.py`; reuses existing teardown path from `merge.py` (`CoordinationWorkspace.teardown`). Both success and `--discard` legs of FR-016 now have a documented operator-facing entry point. |
| #1348 / DRIFT-5 | FR-020 raw `subprocess.run(["git", "add", ...])` survives in `implement.py` legacy fallback | fixed | This PR: replaced the raw `subprocess.run(["git", "add", "-f", str(feature_dir)])` block in `src/specify_cli/cli/commands/implement.py` with a `BookkeepingTransaction.acquire()` routing through the pre-flight policy gate. Legacy fallback now satisfies FR-020 / FR-027 (same atomicity invariants as the coordination-branch path). |
| #1348 / DRIFT-6 | FR-022 legacy `emit_status_transition` path fires `_saas_fan_out` before commit (30+ callers) | deferred-with-followup | MEDIUM severity. The new `BookkeepingTransaction.queue_saas_emission` path correctly defers SaaS fanout until commit success (verified by `tests/specify_cli/coordination/test_outbound.py`). Migrating the 30+ legacy callers is tracked under [#1356](https://github.com/Priivacy-ai/spec-kitty/issues/1356). Until that migration lands, the legacy `emit_status_transition` path remains the dominant production caller for transitions outside the implement/review surface. |
| #1348 / DRIFT-7 | Issue matrix unfilled (this file) | fixed | This commit populates the issue-matrix per the skill's Gate-4 schema. |
| #1348 / Gate-2 | Architectural test `test_docs_cli_reference_parity::test_visible_paths_match_reference` failed (mission introduced `spec-kitty doctor coordination` without docs update) | fixed | This PR: added the `## spec-kitty doctor coordination` section to `docs/reference/cli-commands.md`; parity test now passes (verified locally). |
| #1348 / RISK-1 | FR-007 auto-rebase at lane sync points unwired (`attempt_auto_rebase` exists but is only called from merge path) | fixed | Fixed by PR for [#1360](https://github.com/Priivacy-ai/spec-kitty/issues/1360): `agent/workflow.py` now invokes lifecycle lane sync after successful coordination commits for lane claim/start and `for_review → in_review`; integration coverage lives in `tests/integration/test_lane_lifecycle_sync.py`. |
| #1348 / RISK-2 | This mission's own meta.json lacks `coordination_branch` — entire WP lifecycle exercised the legacy fallback path | verified-already-fixed | LOW severity / informational. The mission was created before WP03 landed; its own lifecycle naturally exercised the legacy path. Synthetic fixtures in `tests/regression/test_issue_1348.py` and `tests/integration/test_mission_close.py` cover the modern coordination-branch topology. No code change required; one-time meta backfill is a separate operator concern documented in the mission review report. |
| #1348 / RISK-3 | `BookkeepingTransaction._rollback` swallows three classes of OSError silently to logs (`BOOKKEEPING_ROLLBACK_FAILED` defined but never raised) | deferred-with-followup | LOW severity. The stable error code exists in the contract but is not raised by the rollback paths in `transaction.py` (truncate fail, snapshot-restore fail, artifact-restore fail) and `merge.py` (teardown fail) and `agent/workflow.py` (legacy truncate fail). Six silent-failure candidates total. Bundled with the diagnostic-shape work in [#1355](https://github.com/Priivacy-ai/spec-kitty/issues/1355). |
| #1348 / RISK-4 | WP08 cross-WP file-ownership violations: `decision.py` (renamed from `decisions.py`), `execution_context.py` | verified-already-fixed | LOW severity. Documented and accepted by the per-WP reviewer. Architectural import boundary catches `safe_commit` regressions; ownership drift on shared modules is a separate concern best addressed via mission-process docs. No code change required. |
| #1348 / RISK-5 | `mission close` flow's coordination-worktree teardown only reachable via `merge` (DEAD-CODE-RISK if merge refactored) | fixed | This PR: by introducing `spec-kitty mission close [--discard]`, FR-016 now has a dedicated invocation surface independent of the merge command. Teardown helper is shared between `merge.py` and `mission_close.py`. |
| #1348 / Silent-failure candidates (6) | Rollback / cleanup paths log-and-continue on `OSError`/`Exception` instead of raising `BOOKKEEPING_ROLLBACK_FAILED` | deferred-with-followup | Six locations enumerated in `mission-review-report.md` § Silent Failure Candidates (`transaction.py:717/742/758/769`, `merge.py:1591`, `agent/workflow.py:~286`). Each requires a structured error emitter to satisfy NFR-001 in the failure case. Bundled with the diagnostic-shape work in [#1355](https://github.com/Priivacy-ai/spec-kitty/issues/1355). |
| #1348 / Architectural failures (4 inherited) | `test_no_dead_modules` (`specify_cli.coordination.outbound`), `test_no_dead_symbols`, `test_pytest_marker_convention`, `test_pytest_marker_correctness` ×2 | deferred-with-followup | Mission-attributable architectural test failures fixed where cheap: marker additions to mission-introduced test files land in this PR. The `coordination.outbound` dead-module flag is intrinsic to DRIFT-6 (the module is the new helper with no production caller) and resolves when [#1356](https://github.com/Priivacy-ai/spec-kitty/issues/1356) migrates the legacy callers. Pre-existing marker violations on `test_charter_cli.py` and `test_protect_main_workflow.py` are unrelated and not within this mission's scope. |

## Audit trail

- Original review verdict: **FAIL** (Gate 2 + Gate 4 + 3 HIGH drift findings).
- Post-fix verdict: Gates 2 and 4 now pass; DRIFT-4, DRIFT-5, DRIFT-7 and RISK-5 fixed in code; remaining findings deferred with follow-up references to #1354, #1355, #1356, #1357.
- Follow-up issues: [#1354](https://github.com/Priivacy-ai/spec-kitty/issues/1354) (pre-existing test failures), [#1355](https://github.com/Priivacy-ai/spec-kitty/issues/1355) (tighten architectural import boundary as legacy fallback paths absorb into `BookkeepingTransaction.acquire()`), [#1356](https://github.com/Priivacy-ai/spec-kitty/issues/1356) (migrate ~30 `emit_status_transition` callers), [#1357](https://github.com/Priivacy-ai/spec-kitty/issues/1357) (lock-serialize `CoordinationWorkspace.resolve()` inside `BookkeepingTransaction.acquire()`).
