{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "System Operations Tool",
  "description": "Server health checks, project validation, and configuration queries",
  "type": "object",
  "properties": {
    "operation": {
      "type": "string",
      "enum": ["health_check", "validate_project", "list_missions", "server_config"],
      "description": "Which system operation to perform"
    },
    "project_path": {
      "type": "string",
      "description": "Project path (required only for validate_project operation)"
    }
  },
  "required": ["operation"],
  "additionalProperties": false
}
