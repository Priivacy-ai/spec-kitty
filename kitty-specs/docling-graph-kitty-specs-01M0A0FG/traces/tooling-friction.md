# Tracer: tooling-friction

One entry per finding: `YYYY-MM-DD · actor · <text>`.

---

2026-08-18 · codex · spec-kitty specify/next advancement paused on a self-visible sync SQLite busy wait despite SPEC_KITTY_SYNC_DISABLE=1; command eventually completed after bounded warnings. Query mode remained responsive. Preserve as workflow evidence; no state was bypassed.
