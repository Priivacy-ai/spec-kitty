schema_version: 1
namespace_components: [repository_uuid, mission_id, artifact_identity, local_id]
fixtures:
  - id: two_repositories_same_local_id
    inputs:
      - [repo-uuid-alpha, mission-01, spec, FR-001]
      - [repo-uuid-beta, mission-01, spec, FR-001]
    expected_unique: 2
    may_merge: false
  - id: two_missions_same_local_id
    inputs:
      - [repo-uuid-alpha, mission-01, spec, FR-001]
      - [repo-uuid-alpha, mission-02, spec, FR-001]
    expected_unique: 2
    may_merge: false
  - id: case_and_punctuation_variants
    inputs:
      - [repo-uuid-alpha, mission-01, spec, FR-001]
      - [repo-uuid-alpha, mission-01, spec, fr_001]
      - [repo-uuid-alpha, mission-01, spec, FR.001]
    expected_unique: 3
    may_merge: false
  - id: diacritic_normalization_variants
    inputs:
      - [repo-uuid-alpha, mission-01, spec, café]
      - [repo-uuid-alpha, mission-01, spec, café]
    expected_unique: 2
    may_merge: false
merge_orders: [ascending, descending, interleaved]
missing_repository_uuid:
  namespace: provisional_repository_remote_or_path_hash
  may_cross_repository_merge: false
