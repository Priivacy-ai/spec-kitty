# Remediation Note — issue-matrix.md (WP03, 2026-05-12)

**Diagnostic**: `MISSION_REVIEW_ISSUE_MATRIX_MULTI_TABLE`

**Observation**: `issue-matrix.md` contains 3 Markdown tables at the top level:
1. "Product findings" table (`| ID | Title | Surface | Where surfaced in code | Verdict | Evidence ref |`)
2. "Workflow papercuts" table (`| ID | Title | Surface | Verdict | Evidence ref |`)
3. "Pre-existing failures observed" table (`| ID | Title | Surface | Verdict | Evidence ref |`)

The validator requires exactly one Markdown table. Additionally, the columns `ID`, `Title`, `Surface`, and `Where surfaced in code` are not in the canonical named-optional vocabulary.

**Structural drift (not auto-fixed)**: Multi-table layout and unknown columns (`Surface`, `Where surfaced in code`) require operator intervention.

**Recommended operator action**:
1. Consolidate the three tables into a single table with an optional `scope` column to distinguish "product finding", "workflow papercut", and "pre-existing failure" categories.
2. Remove or rename unknown columns: `Surface` has no canonical equivalent; `Where surfaced in code` maps closest to `evidence_ref` (but that column already exists). Consider folding `Surface` information into the `evidence_ref` cell.
3. The `ID` column has no canonical equivalent; issue identifiers can be moved to the `issue` column (e.g., F1, W1, P1 as issue values when no GitHub issue number exists).
4. `Evidence ref` → `evidence_ref` (alias normalization, safe to auto-apply when restructuring).
5. Commit the fix directly in the `main` branch.
