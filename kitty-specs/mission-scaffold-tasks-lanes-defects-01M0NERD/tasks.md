# Work Packages: mission-scaffold-tasks-lanes-defects-01M0NERD

_Generated by finalize-tasks from wps.yaml. Do not edit directly._

---

## Work Package WP01: FR-001 - meta.json commit raises on hard git failure (mission_creation.py)

**Dependencies**: None
**Requirement Refs**: FR-001, FR-005, NFR-001, NFR-003, C-001, C-005
**Owned Files**: src/specify_cli/core/mission_creation.py, tests/core/test_mission_create_checkout_restore.py, tests/specify_cli/core/test_feature_creation.py, tests/core/test_mission_creation_topology.py
**Subtasks**: T001, T002, T003, T004, T005, T006, T007
**Prompt**: `tasks/WP01-meta-json-commit-fails-loudly.md`

---

## Work Package WP02: FR-002/FR-003/FR-004 - ownership bootstrap, lane computation, and authoritative_surface validation fail loudly (mission_finalize.py)

**Dependencies**: None
**Requirement Refs**: FR-002, FR-003, FR-004, FR-005, NFR-001, NFR-002, NFR-004, C-001, C-002, C-004, C-005
**Owned Files**: src/specify_cli/cli/commands/agent/mission_finalize.py, tests/specify_cli/cli/commands/agent/test_mission_finalize_phases.py, tests/tasks/test_finalize_tasks_json_output_unit.py
**Subtasks**: T008, T009, T010, T011, T012, T013, T014, T015, T016, T017
**Prompt**: `tasks/WP02-ownership-lanes-fail-loudly.md`

---

## PR Size / Diff Estimate (tasks-authoring re-count — plan.md §12)

Plan.md §12 conditions its "single PR, fits in one sitting" judgment on the tasks-authoring
step re-counting the assembled diff and saying so explicitly here if the count disagrees. This
is that re-count, built primarily from each WP's own subtask "Files" line-count ranges where a
subtask states one
(`tasks/WP01-meta-json-commit-fails-loudly.md`, `tasks/WP02-ownership-lanes-fail-loudly.md`);
the few subtasks below whose Files line carries no numeric range of its own are called out
explicitly as an estimator's judgment call rather than folded silently into the total:

- **WP01** (`src/specify_cli/core/mission_creation.py`): exactly 4 production lines across 2
  call sites in one function — T005's Files line states this precisely ("2 lines
  removed/restructured at each of 2 call sites"), so this is a stated figure, not an estimate.
  Test lines: T002 states ~40-60 and T003 states ~20-30 (~60-90 stated), plus a judgment-call
  estimate of roughly ~45-65 additional test lines for T004 and T006, **neither of which states
  a numeric range of its own** (T004 follows T002's technique at a second call site, so its new
  assertions/fixture wiring are judged smaller than T002 since most fixture setup is shared;
  T006 extends whichever file T002 used with one additional JSON-payload assertion, judged as a
  small addition). Total: ~105-155 test lines, of which ~60-90 is stated and ~45-65 is judgment,
  not derivation. 1 production file; up to 3 test files
  (`test_mission_create_checkout_restore.py`, `test_feature_creation.py`,
  `test_mission_creation_topology.py`).
- **WP02** (`src/specify_cli/cli/commands/agent/mission_finalize.py`): production changes
  across 4 functions (`_apply_ownership_inference`, `_run_bootstrap_loop`,
  `_validate_ownership_manifests`, `_compute_and_write_lanes`) plus 1 new `_BootstrapState`
  dataclass field — T015 self-labels "a moderate, scoped diff", T016 (the raise + two inline
  comments + short-circuit removal + possible conditional helper extraction) is labeled
  "small-to-moderate, potentially expanding to moderate" — estimated combined at roughly
  40-70 production lines (no per-function numeric range is stated in the WP text itself, only
  size adjectives). Test lines: T009 ~65-95 (revised — step 4's `_run_bootstrap_loop` fixture
  plus its `state.inmemory_frontmatter`/`state.ownership_contradictions` assertions are
  heavier than steps 1-2's direct-construction calls) + T010 ~60-80 + T011 ~30-40 + T012
  ~50-60 + T013 ~40-50 + T014 ~50-60 = ~295-385 test lines. 1 production file; 2 test files
  (`test_mission_finalize_phases.py`, `test_finalize_tasks_json_output_unit.py`).
- **Combined**: roughly 444-614 diff lines across 2 production files and up to 5 test files,
  of which roughly 400-540 lines (the large majority) are tests — consistent with plan.md
  §11's red-first/ATDD strategy that the bulk of the diff is tests that read independently of
  the production-code diff.

**Agreement with plan.md §12**: this re-count still agrees with the "one PR, fits in one
sitting" judgment — no split is taken. WP02 is markedly larger than WP01 in both production
and test surface (as plan.md §12 anticipated when it flagged the split option), but both WPs'
production changes remain small relative to their enclosing functions/file, the diff is
dominated by test code that reads independently of the production changes, and the two WPs
touch disjoint functions in disjoint files (`mission_creation.py` vs `mission_finalize.py`)
with no dependency between them. Nothing in this re-count surfaces information that would
change plan.md §12's conclusion.

---
