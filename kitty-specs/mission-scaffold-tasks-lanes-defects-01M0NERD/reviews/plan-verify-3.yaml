schema: review-verify/v1
complete: true
phase: plan
results:
  - id: PLAN-FRESH2-001
    status: resolved
    reason: >
      §7 now correctly states #3666 touches both the _run_bootstrap_loop signature
      (merge_target_branch param) and one body line (merge_target_branch=merge_target_branch,
      inserted into the _apply_bootstrap_fields(...) call), and the §7(a) touch-scope guidance
      now names that specific call-argument line as #3666's territory; verified directly
      against `gh pr diff 3666` (hunk @@ -1361,6 +1428,7 @@ in mission_finalize.py) which
      shows exactly this signature-plus-body-line change.
  - id: PLAN-FRESH2-002
    status: resolved
    reason: >
      §2 now reads "All six changed call sites in §1..." matching §1's "6 changed call sites"
      and §12's "6 changed call sites across 2 files"; a full-file grep for "five"/"four" found
      no other stale count — the remaining "five touched functions" occurrences (§4, §10) and
      "All four" (§10, ruff-measured functions) correctly use the functions-vocabulary
      distinction, consistent with §10's five-named-functions list.
  - id: PLAN-FRESH2-003
    status: resolved
    reason: >
      _apply_ownership_inference's new return type is now pinned everywhere to one concrete
      shape, tuple[bool, list[str], str | None], consistently stated in §1's two rows and both
      §11 FR-002 rows (direct-seam descriptor row and planning-artifact row); verified the
      function's current live signature returns tuple[bool, list[str]] and the cited test
      (tests/specify_cli/cli/commands/agent/test_mission_finalize_phases.py:496) confirms the
      2-tuple unpack the plan's "CURRENT" baseline claims.
  - id: PLAN-FRESH2-004
    status: resolved
    reason: >
      Both guard citations in §1 and §10 now describe the two-line if/return shape (1484-1485
      and 1834-1835) rather than a false single-line "verified exact" quote; verified directly
      against src/specify_cli/cli/commands/agent/mission_finalize.py lines 1484-1485 and
      1834-1835, which are indeed split across two lines each, matching the corrected text.
  - id: PLAN-FRESH2-005
    status: resolved
    reason: >
      §9 now cites `gh issue view 3284` / `gh issue view 3283` directly (verified 2026-08-22)
      instead of attributing the figures to AGENTS.md/the ledger, and explicitly notes neither
      document names these issues; verified live via `gh issue view 3284` and `gh issue view
      3283`, both matching the plan's stated failure counts and descriptions.
  - id: PLAN-FRESH2-006
    status: resolved
    reason: >
      §10's _run_bootstrap_loop row now distinguishes "3 levels deep in two places" (for→try/
      except→if, and for→if→if/else+if) from "2 levels deep in a third" (the dependency-
      preservation branch at mission_finalize.py:1344); verified against the live source that
      the dependency-preservation branch is a for→if/else (2 levels) while the other two cited
      examples are genuinely 3 levels deep.
