# review-refute/v1 — canonical format for refuter-batch verdicts.
schema: review-refute/v1
complete: true
phase: spec
batch: 1
verdicts:
  - id: SPEC-GOV-001
    verdict: confirmed
    reason: >-
      D1's blockquote reads "PRs without prior agreement will be closed" but
      CONTRIBUTING.md:377 actually reads "Pull requests with large changes that did not
      have a prior conversation and agreement will be closed"; C-001 (spec.md:351) quotes
      that real sentence verbatim, confirming the D1 blockquote's quotation marks
      misrepresent a paraphrase as a verbatim quote.
  - id: SPEC-GOV-002
    verdict: confirmed
    reason: >-
      mission_finalize.py:1834 reads "if not (wp_manifests and wp_dependencies): return
      None, None" — a compound guard — while all four User Story 3 acceptance scenarios
      (spec.md:188-215) only construct/discuss the wp_manifests-empty case, never a
      wp_manifests-non-empty + wp_dependencies-empty fixture.
  - id: SPEC-ARCH-001
    verdict: confirmed
    reason: >-
      Every cited line number and call order checks out exactly against
      mission_finalize.py — _flush_frontmatter_writes (disk-mutating write_frontmatter at
      line 1450) is called at line 2752, before _validate_ownership_manifests at 2766 and
      before _run_commit_pipeline at 2789, and inside _run_commit_pipeline
      _emit_local_canonical_events (which persists TasksCompleted, confirmed by its own
      docstring) runs at 2332 before _compute_and_write_lanes at 2342; the outer
      except-handlers at 2810-2829 only call _revert_unpersisted_target_branch_override,
      confirming no revert path for either side effect — this directly violates NFR-004's
      literal text for the FR-003 reject path (NFR-004 itself only names FR-002/FR-003,
      not FR-004, which is a minor overstatement in the finding's title, but the core
      write-before-validate defect it documents is real and precisely evidenced.
  - id: SPEC-VERIFY-001
    verdict: confirmed
    reason: >-
      spec.md never mentions AGENTS.md's "Test-run baseline-red gotcha" (pre-existing P0
      reds #2736/#2772/#1834) or charter.md Standing Order 9 in the context of the new
      FR-001..FR-004 test-writing acceptance scenarios, despite NFR-002 explicitly
      discussing the ~17,000-test suite's runtime budget elsewhere in the same document.
