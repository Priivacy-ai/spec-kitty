# review-verify/v1 — canonical format for fix-verification output.
schema: review-verify/v1
complete: true
phase: spec
results:
  - id: SPEC-GOV-001
    status: resolved
    reason: Both spec.md's D1 blockquote and tracer-design-decisions.md now quote "Pull requests with large changes that did not have a prior conversation and agreement will be closed." which matches CONTRIBUTING.md:377 verbatim (confirmed by reading the live file).
  - id: SPEC-GOV-002
    status: resolved
    reason: User Story 3's Independent Test and new Acceptance Scenario 5 explicitly cover wp_manifests-non-empty/wp_dependencies-empty (the second half of the compound guard at mission_finalize.py:1834, confirmed live), and Scenario 6 covers the reject regardless of which half trips.
  - id: SPEC-ARCH-001
    status: resolved
    reason: NFR-004 is now explicitly scoped to the FR-002 reject path only, honestly states FR-003/FR-004 rejects may leave WP frontmatter writes (and, for FR-003, the TasksCompleted event) already persisted from earlier pipeline phases, cites live line numbers that all check out (2752, 2766, 2789, 2332, 2342, 1834), and explicitly declines to adopt a pipeline-reorder as an actual requirement (framed as a hypothetical needing separate operator sign-off, and SK-61's ledger entry confirms no reorder is otherwise implied elsewhere in the spec).
  - id: SPEC-VERIFY-001
    status: resolved
    reason: New Constraint C-005 requires the implementing WP to confirm a red is red-on-branch AND green on the merge-base/upstream main before attributing it to this mission, matching the remediation exactly and cross-referenced from NFR-002.
