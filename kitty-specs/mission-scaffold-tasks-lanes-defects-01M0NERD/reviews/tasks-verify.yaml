# review-verify/v1 — canonical format for fix-verification output.
schema: review-verify/v1
complete: true               # flipped after the yaml self-check passed
phase: tasks
results:                    # one entry for EVERY finding in the findings file
  - id: TASKS-DECOMP-001
    status: resolved
    reason: tasks.md now carries a concrete "PR Size / Diff Estimate" section with per-WP production/test
      line-count reconciliation (WP01 ~4-6 prod + ~105-155 test lines; WP02 ~40-70 prod + ~280-360 test
      lines; combined ~425-590 lines) and an explicit agreement statement against plan.md §12's one-PR
      judgment, and WP02's T016 Files line now carries "a small-to-moderate diff on its own; potentially
      expanding to a moderate diff if step 4's mandatory complexity re-check forces the helper extraction,"
      consistent in style with T005's "a small diff" and T015's "a moderate, scoped diff."
  - id: TASKS-SEQ-001
    status: resolved
    reason: WP01's T007 step 5 and the Definition of Done now both name the mission-loader-coverage CI job
      concretely (the core_misc path filter, the specific file it matches, the expected-trivial-hold
      rationale, and the instruction to record the confirmation), closing the plan.md §8 traceability gap.
  - id: TASKS-VERIFY-001
    status: resolved
    reason: WP02's T009 gained a new step 4 driving _run_bootstrap_loop (or finalize_tasks --validate-only)
      with the planning_artifact + explicit-owned_files-[] fixture and asserting the WP is not dropped from
      the run, explicitly closing the seam-only vs full-pipeline gap against spec.md US2 AS3's literal claim.
  - id: TASKS-VERIFY-002
    status: resolved
    reason: WP02's T013 step 2 now mandates wrapping the _compute_and_write_lanes/finalize-tasks call in
      pytest.raises(...) and asserting on the exception FIRST as the as-written default, with the
      lanes.json-absence check demoted to a secondary defense-in-depth assertion, and step 3 confirms both
      halves are RED pre-fix — the corrective instruction is no longer stranded as a post-hoc validation
      discovery.
