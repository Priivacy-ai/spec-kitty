# review-refute/v1 — canonical format for refuter-batch verdicts.
schema: review-refute/v1
complete: true             # flip to true ONLY at the very end, after the yaml self-check passes
phase: tasks
batch: 1
verdicts:                   # one entry for EVERY finding in your batch — no omissions
  - id: TASKS-DECOMP-001
    verdict: confirmed
    reason: "plan.md §12 literally instructs the tasks-authoring step to re-count the assembled diff and say so explicitly in tasks.md if it disagrees; tasks.md (25 lines) has no size content, WP01/WP02 only restate the one-PR conclusion verbatim, WP01 T005 says 'a small diff', WP02 T015 says 'a moderate, scoped diff', but WP02 T016's Files line carries no size adjective at all."
  - id: TASKS-SEQ-001
    verdict: confirmed
    reason: "plan.md §8 explicitly says the mission-loader-coverage floor should be 'state[d] explicitly in the WP validation notes,' and a grep of WP01's full file (T007 steps and Definition of Done) confirms zero occurrences of mission-loader-coverage/mission_loader/core_misc anywhere."
  - id: TASKS-VERIFY-001
    verdict: confirmed
    reason: "spec.md AS3 literally says the WP 'is accepted exactly as it is today' (a full-pipeline claim), but T009 step 2 only calls _apply_ownership_inference directly at the seam and asserts descriptor is None; grep confirms 'planning_artifact' appears only once in WP02's file (T009), and T010 (the only _run_bootstrap_loop-driving test) covers only the code_change offending case, not the planning_artifact escape hatch."
  - id: TASKS-VERIFY-002
    verdict: confirmed
    reason: "live code at mission_finalize.py:1820-1834 confirms the pre-fix guard 'if not (wp_manifests and wp_dependencies): return None, None' returns before write_lanes_json is even imported/called, so lanes.json absence is not revert-sensitive as literally described in T013 step 2, and the corrective instruction to assert the raised exception instead does sit in step 3 (the Confirm-RED validation step) rather than being folded into step 2 where the test is authored."
