schema: review-verify/v1
complete: true
phase: analyze
results:
  - id: ANALYZE-FRESH2-001
    status: resolved
    reason: Draft now adds ANALYZE-005 (verdict summary updated to "Five") whose content matches ANALYZE-GOV-001's original scope/severity-3 confirmation and correctly attributes commit 27862b16b's fix, verified directly against analyze.confirmed.yaml and git show.
  - id: ANALYZE-FRESH2-002
    status: resolved
    reason: ANALYZE-004 and the new ANALYZE-005 now correctly separate T009 (non-raising descriptor shape) from T010/T011 (raise-asserting), verified against the live WP02 T009 text which explicitly asserts the seam call does NOT raise.
