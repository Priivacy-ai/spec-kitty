---
work_package_id: WP02
title: 'Investigate #1141 — canary scenario 4 rollback contract drift (14-day window)'
dependencies:
- WP01
requirement_refs:
- FR-005
- FR-006
- FR-007
- FR-010
planning_base_branch: main
merge_target_branch: main
branch_strategy: Planning artifacts for this mission were generated on main. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into main unless the human explicitly redirects the landing branch.
subtasks:
- T009
- T010
- T011
- T012
- T013
- T014
- T015
agent: "claude:opus-4-7:reviewer-renata:reviewer"
shell_pid: "70145"
history:
- timestamp: '2026-05-19T12:46:34Z'
  actor: claude
  event: wp_created
  note: Generated by /spec-kitty.tasks
agent_profile: researcher-robbie
authoritative_surface: kitty-specs/investigate-canary-followups-1142-1141-01KS02TV/research/
execution_mode: planning_artifact
mission_id: 01KS02TVCYPQXQ9DX1Z39SXZ6K
mission_slug: investigate-canary-followups-1142-1141-01KS02TV
owned_files:
- kitty-specs/investigate-canary-followups-1142-1141-01KS02TV/research/issue-1141-snapshot.json
- kitty-specs/investigate-canary-followups-1142-1141-01KS02TV/research/comment-1141.md
- kitty-specs/investigate-canary-followups-1142-1141-01KS02TV/research/outcome-1141.md
- kitty-specs/investigate-canary-followups-1142-1141-01KS02TV/research/h4-evidence-1141.md
- kitty-specs/investigate-canary-followups-1142-1141-01KS02TV/research/h3-evidence-1141.md
- kitty-specs/investigate-canary-followups-1142-1141-01KS02TV/research/h2-evidence-1141.md
- kitty-specs/investigate-canary-followups-1142-1141-01KS02TV/research/h1-evidence-1141.md
role: researcher
tags: []
---

# WP02 — Investigate #1141 (canary scenario 4 rollback contract drift, 14-day window)

## ⚡ Do This First: Load Agent Profile

**Before reading anything else in this prompt**, load your assigned agent profile via the Skill tool:

```
/ad-hoc-profile-load researcher-robbie
```

This injects identity, governance scope, boundaries, and initialization declaration appropriate for the **researcher** role — this WP is an investigation, not implementation. Only after the profile is loaded should you continue with the Objective below.

## Objective

Investigate GitHub issue [#1141](https://github.com/Priivacy-ai/spec-kitty/issues/1141) — "Canary scenario 4 tests lifecycle rollback emission contract not covered by mission spec" — within the **14-day operator-committed window** that closes **2026-06-02 (UTC end-of-day)**.

Exercise the four ranked hypotheses **cheapest-first**: **H4 (fixture state error) → H3 (sequencing race) → H2 (canary drift) → H1 (CLI regression)**. Stop at the first hypothesis that explains the failure. Post a substantive comment on #1141 satisfying the NFR-003 reproducibility contract, **including** a Recommendation heading with one of A / B / C per the issue body. Update the parent mission's `mission-exception.md` `## Follow-up` row on the focused-PR branch.

**This WP produces no spec-kitty source code.** If a hypothesis surfaces a real code defect, open a separate 1-WP follow-up mission (C-003) and link it from this WP's outcome record.

## Context

- **Depends on WP01**: WP01 establishes the pre-flight repo state and posts/edits to `mission-exception.md` first. WP02 starts after WP01's T008 lands on the focused-PR branch so the `## Follow-up` edits do not collide.
- **Scenario source**: `Priivacy-ai/spec-kitty-end-to-end-testing/tests/identity_boundary/test_scenario_4_review_rejection_contract.py`
- **Key spec-kitty references**:
  - `src/specify_cli/status/lifecycle_events.py` — `WPStatusChanged` payload shape
  - `src/specify_cli/status/store.py` — event-log writer
  - `src/specify_cli/cli/commands/agent/tasks.py` — `move-task` and backward-transition emission
- **Recommendations vocabulary** (from issue body):
  - **A** — open new mission to extend spec coverage / fix a real defect
  - **B** — patch the canary to match current spec-kitty emission (canary drift)
  - **C** — small fix already in place; close with verification

**Read before starting**:
1. `spec.md` (this mission) — full FR/NFR/Constraints set
2. `plan.md` (this mission) — Technical Context + cross-branch mechanics
3. `research.md` (this mission) — especially R2 (hypothesis-snapshot rationale) and R4 (cross-branch mechanics)
4. `contracts/issue-comment-shape.md` — comment shape contract (binding; note Recommendation heading is mandatory for #1141)
5. `contracts/follow-up-update-shape.md` — `## Follow-up` row shape (binding)
6. `quickstart.md` — Steps 4, 5, 6, 7 are this WP's runbook
7. WP01's `research/outcome-1142.md` — confirms pre-flight + branch decision for the cross-branch edit

## Branch Strategy

- **Planning base branch**: `main`
- **Final merge target**: `main`
- **Execution worktree**: allocated by `lanes.json` at finalize-tasks; WP02 lands in the same lane as WP01 because of the WP01 dependency. Enter the worktree path returned by `spec-kitty implement WP02`.
- **Cross-branch edit**: T015 edits `mission-exception.md` on `kitty/pr/unblock-sync-identity-boundary-canary-01KRZJ07-to-main` (or `main` per WP01's T008 decision). Same mechanics as WP01-T008.

## Subtasks

### T009 — Snapshot issue #1141 body

**Purpose**: Pin the canonical hypothesis numbering before any third party edits the body (R2 in `research.md`). Same rationale as WP01-T003.

**Steps**:
1. ```bash
   gh issue view 1141 --repo Priivacy-ai/spec-kitty --json title,body,labels,state,createdAt,updatedAt \
     > kitty-specs/investigate-canary-followups-1142-1141-01KS02TV/research/issue-1141-snapshot.json
   ```
2. Verify the snapshot contains four hypotheses labeled H1/H2/H3/H4 in the body. If labeling differs from `spec.md` assumptions, halt and report drift in `research/outcome-1141.md` — do not proceed silently.

**Validation**:
- [ ] `research/issue-1141-snapshot.json` exists and is valid JSON.
- [ ] The `body` contains "H1", "H2", "H3", "H4" — quote them in the outcome record.

**Files written**: `research/issue-1141-snapshot.json`.

---

### T010 — Execute #1141 H4 (fixture state) — cheapest-first

**Purpose**: H4 is "fixture state error" — the cheapest test. Confirm whether the scenario-4 fixture actually advances the WP to `in_review` or stops at `for_review`.

**Steps**:

1. Read `Priivacy-ai/spec-kitty-end-to-end-testing/tests/identity_boundary/test_scenario_4_review_rejection_contract.py` end-to-end. Locate the fixture function (typically `setup_scenario_4` or similar at the top of the file).

2. Identify the assertion (or absence thereof) that the WP reaches the `in_review` lane before the rollback assertion at line 543. Look for:
   - `wait_for_lane("in_review")` or equivalent polling
   - `assert wp_lane == "in_review"` precondition

3. If the fixture **stops at `for_review`** (no transition to `in_review`), the rollback assertion at line 543 has no `in_review`-shaped event to assert against — that is H4 confirmed.

4. Write the audit to `research/h4-evidence-1141.md`:
   - File path + line numbers cited
   - Quoted assertion or quoted "missing assertion"
   - Verdict: CONFIRMED / RULED_OUT

**Interpretation**:
- **H4 CONFIRMED** → skip T011, T012, T013. Proceed to T014 with Conclusion `CONFIRMED — H4` and Recommendation **B** (patch canary fixture).
- **H4 RULED OUT** → proceed to T011.

**Validation**:
- [ ] `research/h4-evidence-1141.md` exists with file/line citations and verdict.
- [ ] H4 verdict recorded in `research/outcome-1141.md`.

**Files written**: `research/h4-evidence-1141.md`.

---

### T011 — Execute #1141 H3 (sequencing race) — CONDITIONAL

**Purpose**: H3 is "sequencing race" — the peek-the-queue assertion at canary line 543 may be racing the `move-task` write that produces the rollback row.

**Skip this subtask entirely if T010 confirmed H4.** Mark it complete in the outcome record with `n/a — H4 confirmed`.

**Steps** (only if H4 RULED OUT):
1. Open scenario-4 at line 543 and surrounding ~50 lines. Identify the peek operation (`gh ...`, `spec-kitty agent tasks status`, or direct read of `status.events.jsonl`).
2. Trace the sequencing: does the test wait for the `move-task` write to be flushed before the peek? Check for:
   - Explicit polling loop with timeout (`wait_for_event` or similar)
   - File-modification-time check on `status.events.jsonl`
   - Subprocess `wait()` on the `move-task` CLI call
3. If no synchronization exists between `move-task` write and the peek, H3 is plausible. Re-run scenario 4 with the canary venv (if accessible from WP01's `/tmp/canary-clean`) and capture timing:
   ```bash
   pytest tests/identity_boundary/test_scenario_4_review_rejection_contract.py -v --capture=no 2>&1 | tee /Users/robert/spec-kitty-dev/1122-1123-1124-43/spec-kitty/kitty-specs/investigate-canary-followups-1142-1141-01KS02TV/research/h3-rerun-1141.log
   ```
4. Write the audit to `research/h3-evidence-1141.md` with citations and verdict.

**Interpretation**:
- **H3 CONFIRMED** → skip T012, T013. Proceed to T014 with Conclusion `RULED_OUT — H4; CONFIRMED — H3` and Recommendation **B** (patch canary to add sync).
- **H3 RULED OUT** → proceed to T012.

**Validation**:
- [ ] `research/h3-evidence-1141.md` exists with citations and verdict.
- [ ] H3 verdict recorded in `research/outcome-1141.md`.

**Files written**: `research/h3-evidence-1141.md`, optionally `research/h3-rerun-1141.log`.

---

### T012 — Execute #1141 H2 (payload shape comparison) — CONDITIONAL

**Purpose**: H2 is "canary drift" — the expected `WPStatusChanged` payload shape in scenario 4 may have drifted from what current spec-kitty emits.

**Skip this subtask entirely if T010 or T011 confirmed their hypothesis.** Mark it `n/a — H<n> confirmed` in the outcome record.

**Steps** (only if H3 RULED OUT):
1. Open `tests/identity_boundary/test_scenario_4_review_rejection_contract.py` and extract the expected payload schema for the rollback `WPStatusChanged` event (look for `assert event["payload"] == {...}` or `expected_payload = {...}` near line 543).
2. Open `src/specify_cli/status/lifecycle_events.py` and find the actual emission of `WPStatusChanged` for a backward transition. Cross-reference with `src/specify_cli/status/store.py` where the row is appended.
3. Diff the expected payload (from canary) vs the emitted payload (from spec-kitty). Capture both shapes side-by-side in `research/h2-evidence-1141.md`.

**Interpretation**:
- **H2 CONFIRMED** (drift exists) → recommend either A (extend mission spec to cover the contract — if spec-kitty is right) or B (patch canary — if canary is right). Pick the recommendation based on which side carries authority: if `lifecycle_events.py` matches the parent mission's WP01 contract, B; if the canary asserts a stronger contract that spec-kitty SHOULD honor, A.
- **H2 RULED OUT** (payloads match) → proceed to T013.

**Validation**:
- [ ] `research/h2-evidence-1141.md` shows expected vs emitted payload side-by-side with file/line citations.
- [ ] H2 verdict + (if confirmed) recommendation rationale recorded in `research/outcome-1141.md`.

**Files written**: `research/h2-evidence-1141.md`.

---

### T013 — Execute #1141 H1 (CLI regression) — CONDITIONAL

**Purpose**: H1 is "CLI regression" — last resort. Recent backward-transition emission changes in spec-kitty may have broken the contract.

**Skip this subtask entirely if any previous hypothesis confirmed.** Mark it `n/a — H<n> confirmed`.

**Steps** (only if H2 RULED OUT):
1. ```bash
   git log --oneline --since='2026-03-01' -- src/specify_cli/status/store.py src/specify_cli/cli/commands/agent/tasks.py \
     > kitty-specs/investigate-canary-followups-1142-1141-01KS02TV/research/h1-evidence-1141.md
   ```
2. Inspect each listed commit for changes to backward-transition row emission (lane → lane events going "backwards", e.g., `in_review → for_review`).
3. For any suspect commit, check out that commit's parent and re-run scenario 4 in the canary venv:
   ```bash
   git checkout <suspect-parent-sha> -- src/specify_cli/status/store.py
   pip install . -q --no-deps --force-reinstall  # acceptable for this CLI-regression bisection only
   # Re-run pytest scenario 4
   git checkout HEAD -- src/specify_cli/status/store.py  # restore
   ```
4. Record findings in `research/h1-evidence-1141.md`.

**Interpretation**:
- **H1 CONFIRMED** → Recommendation **A** (new mission to fix the regression in spec-kitty).
- **H1 RULED OUT** (and H4/H3/H2 also ruled out) → Recommendation requires escalation: write the outcome with `Conclusion: INCONCLUSIVE_IN_WINDOW`, comment on the issue inside the window, and leave the issue open with a "next operator: walk hypothesis space again with new data" note.

**Validation**:
- [ ] `research/h1-evidence-1141.md` exists with `git log` output and per-commit analysis.
- [ ] H1 verdict + (if confirmed) recommendation rationale recorded in `research/outcome-1141.md`.

**Files written**: `research/h1-evidence-1141.md`.

---

### T014 — Compose + post #1141 substantive comment with A/B/C recommendation (FR-006)

**Purpose**: Discharge FR-006 — substantive comment with hypothesis tested, evidence, conclusion, **and** recommendation letter A/B/C.

**Steps**:

1. Draft `research/comment-1141.md` using the **exact heading order** from `contracts/issue-comment-shape.md`, plus the mandatory `### Recommendation` heading:
   ```
   ### Hypothesis tested
   ### Commands run
   ### Evidence
   ### Conclusion
   ### Recommendation
   ```

2. Fill each section:
   - **Hypothesis tested**: `H4 (fixture state)` or `H4 ruled out → H3 (sequencing race)` etc.
   - **Commands run**: copy-pasteable shell sequence (or "see research/h<n>-evidence-1141.md" for non-shell investigations like reading the test file)
   - **Evidence**: file/line citations from the corresponding `h<n>-evidence-1141.md`, ≥ ~20 lines around the assertion or the diff in payload shapes
   - **Conclusion**: `CONFIRMED — H<n>`, `RULED_OUT — H<n>; advancing to H<n-1>`, or `INCONCLUSIVE_IN_WINDOW` (if all four ruled out)
   - **Recommendation**: one of `A — new mission`, `B — patch canary`, `C — small fix already in place` (with one-paragraph rationale)

3. Post the comment:
   ```bash
   gh issue comment 1141 --repo Priivacy-ai/spec-kitty \
     --body-file kitty-specs/investigate-canary-followups-1142-1141-01KS02TV/research/comment-1141.md
   ```

4. Capture the comment URL in `research/outcome-1141.md` under `comment_url:`.

5. Note: #1141 is **not closed** by this WP unless Recommendation == C **and** the small fix has already landed. In every other case, leave it open with the recommendation as the next action.

**Validation**:
- [ ] `research/comment-1141.md` contains all five required headings.
- [ ] Recommendation is exactly one of A / B / C with a rationale paragraph.
- [ ] Comment posted; URL captured in outcome record.
- [ ] A second operator reading the comment alone can reproduce the test in ≤ 15 minutes (NFR-003).

**Files written**: `research/comment-1141.md`.

---

### T015 — Update `mission-exception.md` `## Follow-up` row for #1141 (FR-007, C-001)

**Purpose**: Mirror the outcome to the parent mission's exception record. Same mechanics as WP01-T008 but for the #1141 row.

**Steps**:

1. Re-check PR #1143 state (it may have merged since WP01-T008):
   ```bash
   gh pr view 1143 --repo Priivacy-ai/spec-kitty --json state -q .state
   ```
   Use the same branch-decision rules as WP01-T008.

2. **Default path** (focused-PR branch still open):
   ```bash
   git fetch origin
   git checkout kitty/pr/unblock-sync-identity-boundary-canary-01KRZJ07-to-main
   git pull --ff-only
   ```
   Edit `kitty-specs/unblock-sync-identity-boundary-canary-01KRZJ07/mission-exception.md` `## Follow-up`, replacing the deferred row for **#1141** with the resolved row shape from `contracts/follow-up-update-shape.md`.
   ```bash
   git add kitty-specs/unblock-sync-identity-boundary-canary-01KRZJ07/mission-exception.md
   git commit -m "Record outcome of #1141 follow-up commitment

   Investigation: investigate-canary-followups-1142-1141-01KS02TV
   Result: <one-line summary>"
   git push origin kitty/pr/unblock-sync-identity-boundary-canary-01KRZJ07-to-main
   ```

3. **Fallback path** (PR #1143 has merged):
   ```bash
   git checkout main && git pull --ff-only
   git checkout -b chore/record-canary-followup-outcome-1141
   ```
   Edit the same path on `main`. Commit + push + `gh pr create`.

4. Record commit hash + push target in `research/outcome-1141.md`.

**Validation**:
- [ ] `mission-exception.md` `## Follow-up` row for #1141 matches `contracts/follow-up-update-shape.md`.
- [ ] Commit pushed to the correct branch; URL/hash recorded.
- [ ] WP01's row for #1142 is preserved (do not regress that edit).
- [ ] No other section of `mission-exception.md` was modified (`git diff` shows only the `## Follow-up` block).

---

## Definition of Done

This WP is done when **all** of:

- [ ] T009–T015 validation boxes are checked or marked `n/a` for skipped conditional steps.
- [ ] `research/outcome-1141.md` is fully populated (Investigation Outcome record per `data-model.md`).
- [ ] Issue #1141 has a substantive comment posted **before 2026-06-02 EOD UTC** (NFR-002) with a Recommendation A/B/C.
- [ ] `mission-exception.md` `## Follow-up` row for #1141 reflects the outcome (FR-007).
- [ ] No spec-kitty source code modified (C-003).
- [ ] No file outside `owned_files` modified.

## Risks & Mitigations

| Risk | Mitigation |
|---|---|
| WP01's `mission-exception.md` edit not yet on remote when WP02-T015 starts | T015 step 1 `git pull --ff-only` after checkout; if not pulled, halt and resume after WP01 push lands |
| Hypothesis mis-ordering tempts skipping H4 | C-004 + the cheapest-first rule documented in spec.md and quickstart.md; T010 is mandatory first hypothesis |
| Recommendation A/B/C ambiguous | T012 + T013 require explicit rationale paragraphs citing which side carries authority |
| All four hypotheses ruled out within window | T013 step 4 allows `INCONCLUSIVE_IN_WINDOW`; this still discharges NFR-002 if the comment is substantive |
| `gh` token scope insufficient | `unset GITHUB_TOKEN && gh auth status` |

## Reviewer guidance

A reviewer should verify:

1. The posted #1141 comment carries the five required headings (Hypothesis / Commands / Evidence / Conclusion / **Recommendation**).
2. The hypotheses were exercised cheapest-first per C-004 (H4 → H3 → H2 → H1). Verify the `h<n>-evidence-1141.md` files exist in the correct order and that earlier-skipped hypotheses are not back-filled retroactively.
3. The Recommendation letter (A/B/C) matches the rationale in `research/outcome-1141.md`.
4. The `mission-exception.md` `## Follow-up` row for #1141 matches `contracts/follow-up-update-shape.md` exactly.
5. WP01's row for #1142 is unchanged.
6. No spec-kitty source code modified.

## Implementation command

```bash
spec-kitty agent action implement WP02 --agent claude --mission investigate-canary-followups-1142-1141-01KS02TV
```

## Activity Log

- 2026-05-19T13:19:34Z – claude:opus-4-7:researcher-robbie:implementer – shell_pid=67792 – Started implementation via action command
- 2026-05-19T13:29:02Z – claude:opus-4-7:researcher-robbie:implementer – shell_pid=67792 – H4/H3 ruled out; H2 partial; H1 likely (silent fan-out failure); rec A; comment posted; mission-exception.md updated.
- 2026-05-19T13:29:10Z – claude:opus-4-7:reviewer-renata:reviewer – shell_pid=70145 – Started review via action command
- 2026-05-19T13:31:38Z – claude:opus-4-7:reviewer-renata:reviewer – shell_pid=70145 – Review passed: cheapest-first sweep H4→H3→H2→H1; H4/H3 ruled out; H2 partial (canary force-contract matches current spec-kitty); H1 likely (silent fan-out failure on backward emission to OfflineQueue); comment NFR-003-reproducible with documented trusted-runner requirement; mission-exception.md update matches contract and preserves WP01 #1142 row; no src/ modifications (C-003); recommendation A appropriately scoped as 1-WP follow-up.
- 2026-05-19T13:54:15Z – claude:opus-4-7:reviewer-renata:reviewer – shell_pid=70145 – Moved to done
