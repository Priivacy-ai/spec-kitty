schema: review-refute/v1
complete: true
phase: spec
batch: 1
verdicts:
  - id: SPEC-GOV-001
    verdict: confirmed
    reason: >
      Direct grep of the checkout shows the real count of `.action_sequence` attribute
      accesses is 10, not 8: mission_type_profiles.py:272 and :955 are docstring/prose
      mentions (not real accesses, leaving only :985/:994 real, ×2 not ×3), while
      mission_type.py (1469, 1479) and charter/activate.py (183, 205) each have two real
      accesses, not the one implied by the spec's unmultiplied listing — and the spec's own
      stated per-file multipliers (×3, ×1, ×1, ×2, ×1, ×1) sum to 9, not 8, confirming the
      miscount is real and self-inconsistent.
  - id: SPEC-GOV-002
    verdict: confirmed
    reason: >
      Direct read of mission_type_repository.py confirms the hardcoded `pack_context=None`
      call is at line 245 (line 244 is `MissionStepRepository.default()`), and
      `_inject_projected_fields` spans 209-253 (`return payload` is line 253), so both cited
      numbers in the Summary are off by one against this checkout.
  - id: SPEC-GOV-003
    verdict: confirmed
    reason: >
      Grep of spec.md confirms C-002 and C-003 are pure prose with no SC/NFR row, and C-007
      (the only concrete blast-radius bound) is never cross-referenced by ID from any
      Success Criterion or from C-002/C-003 themselves, so there is genuinely no checkable
      acceptance criterion tying the scope claim to an automated diff/file check.
  - id: SPEC-ARCH-001
    verdict: confirmed
    reason: >
      Direct read of mission_type_repository.py:165 confirms `_load()` calls
      `_inject_projected_fields(raw, mission_type_id=yaml_file.stem)` with no pack_context
      keyword at all today, so FR-005's literal demand to "keep passing pack_context=None
      explicitly" requires an edit to this fifth call site, which C-007 explicitly bounds to
      only four named functions (excluding `_load()`) — a genuine contradiction.
  - id: SPEC-VERIFY-001
    verdict: confirmed
    reason: >
      Direct read of SC-004 and NFR-001 confirms neither names a phase or actor for the
      stash/revert verification step, and the mission's own workflow has two plausible
      review points (WP-implementer review, pre-merge squad), so the obligation can
      genuinely go unperformed without either phase noticing.
  - id: SPEC-VERIFY-002
    verdict: confirmed
    reason: >
      `gh issue view 3284` shows the actual "Failure groups" list (config_stem_parity,
      drg/test_reachability, runtime timing, console ANSI, CI route collection, mission
      template resolution, compact charter, commit helper, venv portability, sync teardown,
      #2782) contains no test under tests/doctrine/missions/ or
      tests/runtime/test_runtime_seam.py, confirming zero overlap with SC-005's scoped run
      and no stated triage mechanism.
