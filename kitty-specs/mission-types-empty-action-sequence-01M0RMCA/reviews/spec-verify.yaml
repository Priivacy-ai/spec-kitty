schema: review-verify/v1
complete: true
phase: spec
results:
  - id: SPEC-GOV-001
    status: resolved
    reason: Recounted live .action_sequence attribute accesses (grep against this checkout confirms exactly 2/2/1/2/1/2 = 10 real sites, excluding the two docstring mentions at mission_type_profiles.py:272/955) and spec.md now states "ten real call sites" with the correct per-file/line breakdown, matching the live code exactly.
  - id: SPEC-GOV-002
    status: resolved
    reason: mission_type_repository.py confirmed at line 165/209/245/253 in this checkout — the def is at 209, the pack_context=None call at 245, and the closing return payload at 253; spec.md's Summary now cites "lines 209-253" and "line 245", matching exactly.
  - id: SPEC-GOV-003
    status: resolved
    reason: New SC-006 makes the C-007 blast-radius bound a checkable git-diff-name-only assertion, and C-002/C-003 both now explicitly cross-reference "Checkable via SC-006", closing the prose-only gap.
  - id: SPEC-ARCH-001
    status: resolved
    reason: FR-005/C-001 and the matching Edge Cases bullet were reworded to state _load()'s zero-argument call site requires no source change (the new pack_context default of None already satisfies it) and explicitly say "_load() is therefore not a fifth touched function under C-007" — the four-vs-five-function contradiction with C-007 is gone and no stray "explicitly passing pack_context=None" language remains elsewhere in the spec.
  - id: SPEC-VERIFY-001
    status: resolved
    reason: SC-004 now names a concrete owning actor and moment ("the WP-implementer authoring NFR-001's test, before marking the WP done") and a concrete artifact obligation (record both stash/unstash runs in tracer-approach.md or tracer-design-decisions.md for the pre-merge squad), closing the unassigned-owner gap.
  - id: SPEC-VERIFY-002
    status: resolved
    reason: SC-005 now specifies a concrete triage mechanism (rerun the identical pytest invocation against the mission's unmodified base commit; same-test-id red there is pre-existing, green-there/red-post-fix is mission-introduced and must be fixed before merge), replacing the untriaged "possibly baseline" phrasing.
