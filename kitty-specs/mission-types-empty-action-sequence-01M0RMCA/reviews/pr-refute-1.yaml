# review-refute/v1 — canonical format for refuter-batch verdicts.
schema: review-refute/v1
complete: true
phase: pr
batch: 1
verdicts: []
refute_note: >
  R3 is VACUOUS BY CONSTRUCTION on this run, and that is recorded rather than silently
  skipped. R2 (pr.merged.yaml) carried zero findings forward, so there was no claim for a
  refuter to attack. No refuter subagent was dispatched, because dispatching one against an
  empty finding set would produce a session with nothing to adjudicate and an artifact that
  falsely implied contested findings existed.
  This is NOT the same as a squad that did not run: pr.boundary/contract/tests.findings.yaml
  each carry complete: true from an independent lens session with its own profile loaded from
  this checkout, and each recorded substantive verification in its session report —
  including a per-test revert falsification of all four new tests.
