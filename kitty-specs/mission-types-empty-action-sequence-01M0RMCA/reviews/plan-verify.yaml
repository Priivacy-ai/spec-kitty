# review-verify/v1 — canonical format for fix-verification output.
schema: review-verify/v1
complete: true
phase: plan
results:
  - id: PLAN-ARCH-001
    status: resolved
    reason: >-
      plan.md's Blast radius section (lines 336-383) now states "there are three production src/
      callers, not one" and names all three by file:line (_resolve_action_slot at
      charter/mission_type_profiles.py:976; resolve_layered_roster at
      specify_cli/cli/commands/charter/mission_type.py:87, reused by show_mission_type at
      mission_type.py:1454; _resolve_layered_roster at
      specify_cli/cli/commands/_mission_type_audit.py:170, wired into doctor.py's mission-type
      audit) alongside their CLI surfaces (charter mission-type list, mission-type show, and the
      audit CLI) and explains why NFR-002/SC-003's existing golden-parity test on
      resolve_layered_mission_types already covers the two additional entry points (they call it
      directly with no independent projection). Independently re-grepping
      `resolve_layered_mission_types(` in src/ (excluding the definition, docstring/comment
      mentions in pack_manager.py, activate.py, and _mission_type_audit.py's own docstrings, and
      the charter/missions.py re-export which only imports the name) confirms exactly these three
      real call sites — matching the fix's claimed count exactly, not just restating a number.
  - id: PLAN-VERIFY-002
    status: resolved
    reason: >-
      plan.md's IC-01 Affected Surfaces section (lines 195-209) now adds a distinct, named test
      case (test_project_tier_steps_only_projection_resolves or equivalent) separate from NFR-001's
      org-or-project case, explicitly pointing pack_context.repo_root at a synthetic project root
      with its own .kittify/missions/mission_types/<id>.yaml plus a mission-steps/<id>/ tree,
      exercising resolve_layered_mission_types' project layer specifically rather than its org
      layer, and explicitly ties it to "spec.md's Acceptance Scenario 5 (line 62)". Re-reading
      spec.md's Acceptance Scenario 5 (line 62 as cited) confirms it is exactly this scenario -
      a project-layer .kittify/missions/mission_types/<id>.yaml override with its own steps-only
      mission-steps/<id>/ projection - so the added case matches the cited scenario precisely.
