# review-refute/v1 — canonical format for refuter-batch verdicts.
schema: review-refute/v1
complete: true
phase: plan
batch: 1
verdicts:
  - id: PLAN-ARCH-001
    verdict: confirmed
    reason: >
      plan.md lines 336-338 literally state "resolve_layered_mission_types has exactly one
      production caller today (_resolve_action_slot...)" twice, but a direct grep of src/ confirms
      three distinct src/-level call sites that invoke resolve_layered_mission_types (directly or
      via its charter.missions re-export) with a real PackContext.from_config(repo_root):
      mission_type_profiles.py:976 (_resolve_action_slot, the one the plan names),
      charter/mission_type.py:87 (resolve_layered_roster, wired into charter_mission_type_list
      i.e. "spec-kitty charter mission-type list", and reused by mission_type.py:1454
      show_mission_type / "mission-type show"), and _mission_type_audit.py:170
      (_resolve_layered_roster, wired into doctor.py's "spec-kitty doctor mission-type" command
      via run_mission_type_audit at doctor.py:107-108/581). All three are live @app.command-backed
      CLI surfaces, not tests or dead code.
  - id: PLAN-VERIFY-002
    verdict: confirmed
    reason: >
      spec.md Acceptance Scenario 5 (line 62) does mandate a project-layer
      .kittify/missions/mission_types/<id>.yaml steps-only-projection case, and NFR-001 (line 91)
      is worded disjunctively ("org-tier (or project-tier)"); plan.md's IC-01 affected-surfaces
      bullet for tests/doctrine/missions/test_mission_type_repository.py (lines 195-200) only
      commits to "new test class/cases for NFR-001 (red-first, steps-only-projection
      reproduction)" without naming a project-layer case, and a full-text grep of plan.md for
      "project-layer"/"project-tier" outside prose returns only the title (line 1) and one
      architecture-prose mention (line 28) — no test-strategy section (IC-01, Test baseline, or
      the Red-first/ATDD section) names a concrete project-layer test tied to Scenario 5.
