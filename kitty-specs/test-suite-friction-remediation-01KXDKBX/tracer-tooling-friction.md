# Tracer — Tooling Friction

Mission: `test-suite-friction-remediation-01KXDKBX` · #2620.
Seeded at planning with **known friction to watch** (from in-lineage retrospectives/memory). Append every real friction hit during implement; assess at close and file the durable ones to #2017 / the relevant epic.

## Watch-list carried in from prior missions
- **F1 — Gate-coverage TWO baselines.** New WP test files trip BOTH gc3b orphan (`_gate_coverage_baseline.json`, `--update-baseline`) AND gc2b selection (`baselines/*.txt`, `--freeze-baselines`); residual must negate every `next_shard`/`arch_shard` marker. Every new-guard-file IC (IC-10/11/12, and the IC-14 guard) hits this. **#2616 (IC-13) is the fix for the gc2b half over-firing.**
- **F2 — New `tests/architectural/*.py` files need a `_arch_shard_map.py` row** or the completeness guard fails. IC-01 stays Lane-0-local by EXTENDING existing files to dodge this; new-guard ICs must register.
- **F3 — `tests/conftest.py` co-tenants the quarantine-deselection block AND the shard-marker hook** — cross-IC collision risk. IC-10 is its sole owner; the IC-08 hygiene WP must NOT edit it.
- **F4 — Arch marker/gate runs are vacuous under `.worktrees/`.** Verify arch gates from the PRIMARY checkout, not inside a lane worktree.
- **F5 — In a clone/lane, bare `python`/`pytest` imports the PRIMARY src.** Always `.venv/bin/…` or `uv run` in this clone.
- **F6 — `kitty-specs/` edits (incl. these tracer files) committed inside a lane worktree can BLOCK `move-task`.** Write tracer/spec edits from the mission-root checkout (this clone, primary partition), not a lane worktree.
- **F7 — CI-only shards.** Terminology + some integration/git gates run only in CI's `integration-tests-core-misc`. Run `tests/architectural/` locally before pushing; run `test_no_legacy_terminology.py` if touching prose/doctrine.
- **F8 — Status daemon stale auto-commit.** A background daemon may commit staged files with the PREVIOUS mission's message — commit promptly, reword any stray `chore(<other>):` commit.
- **F9 — `move-task` can hang on sync-daemon fan-out** — background it + `SPEC_KITTY_SYNC_MINIMAL_IMPORT=1` if it stalls.
- **F10 — No `git stash` in lane worktrees** (shared stash stack steals a sibling lane's WIP); trust committed HEAD.

## Ratchet/parity friction observations (feeds the operator's harm>good hypothesis — see tracer-design-decisions catalog)
- _Append here every time a ratchet re-anchor or a parity-test false-red costs time during implement — this is the direct evidence for the close-out keep/consolidate/retire verdict._
- **2026-07-13 (WP15, gc2b — the F1 half being fixed):** the gc2b exact-selection ratchet is the archetypal "shape-pinning ratchet that over-fires on churn" the operator's hypothesis predicts: it demanded exact selection equality, so ANY routine test-file add/remove (which THIS mission does 4–5× via new guard files) forced a `--freeze-baselines` refreeze with zero real coverage change — pure friction. Re-scope-to-orphan removes that friction while keeping the one load-bearing signal (a node-id that fell to zero gates). This is direct evidence that the *shape*-pinning half of gate-coverage was net-negative; the *orphan-detection* half is genuinely load-bearing and kept. Once WP15 lands, WP11/WP16/WP17 no longer eat a gc2b refreeze per new guard file (only the gc3b orphan-baseline half of F1 remains — that stays, it IS the load-bearing ratchet).

## Appended entries (during implement)
- _2026-07-13 (seed): no implement entries yet._
- **2026-07-13 (WP17, F1/F2 confirmed):** landing the two FR-012/FR-013 guard
  files (`test_suite_jobs_gate_blocking.py`, `test_ui_e2e_coverage_discovered.py`)
  required the full F1+F2 refreeze ritual even though neither guard changes
  what any CI job *selects*: `tests/_arch_shard_map.py` needed a 2-line
  registration append (F2), gc3b's `--update-baseline` shifted only
  `total_tests` (30946→30960, `orphan_files` stayed `[]`), and gc2b's
  `--freeze-baselines` produced a byte-identical `baselines/*.txt` (its 3
  `BASELINE_TARGETS` — `integration-tests-next`/`slow-tests`/
  `fast-tests-core-misc` — are structurally unrelated to `tests/architectural/`,
  so `git status` showed zero diff there). First-hand confirmation that gc3b's
  ceremony is pure overhead for an architectural-marked new file (never an
  orphan by construction) — direct evidence for #2616/IC-13's "scope gc2b/gc3b
  to genuine orphans, stop firing on routine test-file add" fix.
- **2026-07-13 (WP16, confirms F2/F11, low friction):** confirms F2 — the seam swap in `tests/_arch_shard_map.py`/`tests/_next_shard_map.py` required no NEW arch guard file (only the `register()` rewrite of the existing rows), so the completeness guard needed no shard-map registration of its own; the 4 new regression tests landed inside the already-registered `test_arch_shard_marker_completeness.py`. Confirms F11 (WP11's finding): those 4 new tests carry only the `architectural` marker, so — same as WP11 — the gc2b node-id baselines (`fast-tests-core-misc`/`slow-tests`/`integration-tests-next`) are untouched regardless of WP15's merge state into this lane's history; only the gc3b `total_tests` count in `_gate_coverage_baseline.json` needs `--update-baseline`.
- 2026-07-13: WP07 — no friction; single-file swap in `tests/status/test_models.py` (owned_files scope), `uv run --extra test pytest` + `--extra lint ruff/mypy` clean on first pass. Confirms F5/F6 (bare `uv run python -m pytest` fails without `--extra test`; tracer edits done from primary checkout, not the lane worktree).
- 2026-07-13: WP08 — low friction. Confirms F5 (`uv run python -m pytest` needed `--extra test` to resolve `pytest`). Audit-before-touch (paula's framing) paid off: a naive sweep of the 3 named siblings would have "converted" several `assert_called_once()`-style delegation tests (`tests/sync/tracker/test_service.py::TestDelegation`), but on inspection every one already asserts call-arg *content* or a returned/observable payload, not a bare source string — so they don't match the strict source-as-text definition (`read_text()` of the module under test) and were correctly left untouched. Concurrent edit collision on `tracer-design-decisions.md` (WP07 appended between my read and write) — reread + reapplied, no data loss; confirms the append-only tracer pattern is safe under multi-lane concurrency as long as edits are re-read before write.
- **2026-07-13 (WP06, low friction, confirms F5):** `uv run python -m pytest` needed `uv sync --extra test --extra lint` first (bare `.venv` in this lane worktree had only the 54 base-dependency packages, no `pytest`/`ruff`/`mypy`). Design friction (not tooling): the laundering detector needed 3 layers of AST matching (module-level named-seed detection, unpack-target-position matching, sink-call-arg-name matching) to stay precise — a coarser "any comprehension iterating a seed with an int in it" heuristic would have false-positived on `test_single_mission_surface_resolver.py`'s `_RAW_JOIN_SEEDED_KEYS` dict-comprehension (which iterates a `ContentDescriptor`-row seed, no bare int, single-Name unpack target) had the detector not required an actual tuple/list unpacking target at the seed's exact int position. Verified no other `composite_key`/`composite_key_from_file` call site in `tests/architectural/` matches the laundering shape before writing the fix (grep of every call site + manual read), confirming `test_trio_seam_only.py` really was the sole remaining launderer as the WP prompt asserted.
- **2026-07-13 (WP01, F-new — allowlist ratchet vs. new-liveness-detector conflict):** wiring the new dynamic-access detector (`find_module_factory_functions`/`bind_call_accessor_aliases`/`record_call_chain_attr_edges`, all in `_symbol_key.py`) directly into the SHARED `_imports_by_target()` that the production offender/**stale**/dangling ratchet consumes immediately flips `test_no_public_symbol_in_all_is_unimported` RED: the 4 `runtime.next.runtime_bridge` façade rows in `_CATEGORY_C_RUNTIME_BRIDGE_DEGOD_COMPAT_SURFACE` become genuinely reachable, and `_compute_stale` (ratchet direction 1, "gained a caller → prune the row") correctly flags them stale — but WP01's own DoD explicitly forbids touching allowlist rows (that's WP05's surface, NFR-001 ordering) while ALSO requiring the gate green on the real tree. Both constraints cannot hold simultaneously if the detector is folded into the shared pipeline. **Resolution:** added a separate, non-wired overlay helper (`_record_dynamic_call_accessor_edges`, `test_no_dead_symbols.py`) that proves the detector recognises the 4 symbols as live — driven through the same `_symbol_has_caller` the production gate uses — WITHOUT perturbing `_imports_by_target()`/the shared stale ratchet. Net effect: the capability is delivered, generalised, and fixture+live-tree tested (T001-T004 all satisfied), the suite stays green with zero allowlist-row edits, and **WP05 must fold `_record_dynamic_call_accessor_edges`'s logic into `_imports_by_target()` proper in the SAME commit that removes the 4 rows** (its own task text already assumes "the WP01 gate now recognises them as live" — that statement becomes true only once WP05 does this fold). Flagging this explicitly so WP05's implementer does not skip the wiring step and accidentally reintroduce a false-offender red after deleting the rows.
- **2026-07-13 (WP11, F5/F6/F2/F1-adjacent confirmed + one new):** confirms F5 (bare `uv run python -m tests.architectural.test_golden_count_ban --freeze-baseline` failed with `ModuleNotFoundError: No module named 'pytest'` until `--extra test` was added — the module imports `pytest` at top level even for its CLI entrypoint, since the guard tests live in the same file) and F2 (registered the new guard file into `tests/_arch_shard_map.py`'s current `SHARD_GROUPS`-dict idiom; `test_arch_shard_marker_completeness.py` 3 passed confirming the completeness guard). Confirms F1 (gc3b half): `_gate_coverage_baseline.json`'s `total_tests` count bumped 30920→30940 on `--update-baseline` (9 new test functions in this WP's file); the gc2b half (`baselines/*.txt`) came back **byte-identical after `--freeze-baselines`** — a genuine no-op, exactly as the WP prompt predicted for "if WP15 is already merged" even though WP15 (the gc2b re-scope, #2616) has NOT yet merged into `feat/test-suite-friction-remediation` from this lane's vantage point (it only landed in its own `lane-o` branch, approved but unmerged) — the three specific gate-selection baselines (`fast-tests-core-misc`, `slow-tests`, `integration-tests-next`) simply don't select `architectural`-marked tests at all, so a new `architectural`-marked file never perturbs them regardless of WP15's merge state. New friction item for the catalog (**F11**): a WP whose new test file carries ONLY the `architectural` marker (not `unit`/`contract`/`fast`) never touches the gc2b node-id baselines — the "if WP15 already merged" framing in several WP prompts is a red herring for architectural-marker-only additions; the real gc2b trigger is a new `fast-tests-core-misc`/`slow-tests`/`integration-tests-next`-selected test, which this WP's file is not.
- **2026-07-13 (WP11, F12 — NEW, `move-task` commits kitty-specs status to whichever branch is checked out, contradicting its own lane-branch guard):** running `spec-kitty agent tasks move-task WP11 --to <lane>` from inside the lane worktree (`lane-k`) repeatedly committed `status.events.jsonl`/`status.json` changes onto the **lane branch itself** (3 separate `chore(spec-kitty): status transition WP11` commits across the claim/in_progress/for_review calls), even though the tool prints `Note: Using planning repo's kitty-specs/ on feat/test-suite-friction-remediation (worktree copy ignored)` (implying reads AND writes go to the planning branch) and separately enforces `kitty-specs/ changes are not allowed on lane branches` as a HARD gate blocking the *next* `move-task` call once it detects this drift. Net effect: every `move-task` invocation from a lane worktree silently mis-commits authoritative status data to the wrong branch, and the mis-commit is only discovered on the FOLLOWING call (a lagging, not a preventive, check) — the fix requires manually diffing the lane branch's kitty-specs against the planning branch, porting the delta onto the planning branch by hand, and cleaning the lane branch back to match. Confirmed reproducible 3× in this single WP (claimed, in_progress, for_review calls each did it). This is a structural gap in the lanes-topology tooling (not just this WP's mistake — `move-task` should either refuse to write kitty-specs on a lane branch at all, or auto-redirect the commit to the planning branch), filed for follow-up alongside the existing `project_flat_mission_implement_loop_friction`/`project_guard_friction_trace` lineage.
- **2026-07-13 (WP12, F5/F6 confirmed, low friction):** confirms F5 (bare `python -m pytest` in this lane worktree resolves the PRIMARY checkout's `.venv`, not this lane's — `uv run --extra test` used throughout) and F6 (this tracer append itself is written from the mission-root primary checkout, not `lane-l`). One tooling wrinkle not yet catalogued: a handful of owned-dir sites already carried a full member-exact identity assertion on the line immediately below the flagged `len(...)==N` (e.g. `tests/charter/test_bundle_manifest_model.py:28`, `tests/charter/test_pack_context.py:191`, `tests/doctrine/test_profile_model.py:151`) — the inventory's AST scan correctly flags the bare count line but has no way to see that the very next assertion already makes it redundant; those sites were resolved by deleting the now-fully-redundant count line rather than adding a second, duplicate set-check. Running the owned-dir suites + `test_golden_count_ban.py` together (T058) after `--freeze-baseline` cleanly dropped `tests/charter`/`tests/doctrine`/`tests/doctrine_synthesizer`/`tests/glossary` out of the baseline entirely (0 remaining convert sites) with zero collateral drift in any sibling directory's ceiling.
- **2026-07-13 (WP02, corroborates F12 — a THIRD facet of the same split-brain):** `spec-kitty agent tasks move-task WP02 --to for_review` run from inside the lane worktree (`lane-b`) failed outright with `Error: Illegal transition: planned -> for_review`, even though `spec-kitty agent tasks status` (also run from the lane worktree) correctly reported WP02 as `in_progress`, and directly materializing the event log (`specify_cli.status.reducer.materialize`) against the primary checkout's `kitty-specs/` also confirmed `in_progress`. Root cause traced to `aggregate.py`'s `_resolve_current_lane` → `read_current_wp_state_transactional`: this transactional status-READ path does NOT apply the same "worktree copy ignored, route to planning repo" resolution the tool prints and applies to the WP/tasks.md content read — it reads the LANE WORKTREE's own local `status.events.jsonl` (still on the last-rebased snapshot, only carrying the `genesis -> planned` bootstrap event, missing the later `claimed`/`in_progress` transitions that only ever landed on the primary `feat/test-suite-friction-remediation` branch), computing a stale `from_lane=planned` and correctly-per-its-own-data rejecting `planned -> for_review` as illegal. **Workaround:** re-run the identical `move-task` command from the PRIMARY checkout (cwd = mission root, not the lane worktree) — succeeded immediately (`Moved WP02 from in_progress to for_review`), confirming the read-side inconsistency (not a real illegal transition) was the cause. Net effect with F12: `move-task` from a lane worktree is unsafe in BOTH directions on this lanes-topology mission — reads can see a stale local copy (this entry, spurious rejection) and writes can land on the wrong branch (F12, silent corruption) — both point at the same underlying gap: the transactional status contract resolution needs to unconditionally route through the planning/primary partition for lanes-topology missions, matching the WP-content resolver's behaviour, not just print a note claiming it does. Filed alongside F12 for the same follow-up.
- **2026-07-13 (WP14, F13 — NEW, rich-console-output text-splitting breaks ~60 unrelated `--json`/colorized-output assertions repo-wide, discovered while validating the batch-3 owned-dir suite, NOT caused by this WP's diff):** the owned-dir foreground suite run (23 dirs + `test_golden_count_ban.py`, `-n auto --dist loadfile`) surfaced 81 failing tests, none touching a line this WP's diff changed. Root-caused two independent, pre-existing, environment-level causes, confirmed by reproducing each on files this WP never opened: **(a) JSON-decode failures** (`json.loads(result.output)` → `JSONDecodeError: Expecting value: line 1 column 1`) across every `--json` CLI-command test that asserts on `result.output` after a Typer `CliRunner.invoke(...)` call — e.g. `tests/cli/test_charter_mission_type_commands.py`, `tests/cli/test_doctrine_commands.py`, `tests/cli/test_agent_retrospect_missing_record.py`, `tests/cli/test_agent_retrospect_synthesize.py`, `tests/doctor/test_shim_registry.py`, `tests/dashboard/test_duplicate_prefix_rendering.py::test_dashboard_json_cli_renders_three_distinct_rows` — none of which this WP edited; isolating `tests/cli/commands/test_retrospect.py::TestCreateCommand::test_create_mission_not_completed_json` (a file THIS WP did touch, one converted assertion) confirmed the JSONDecodeError fires at `json.loads(result.output)` — the line BEFORE this WP's edited assertion is ever reached, so the conversion itself is unreachable-affected. **(b) Rich-console ANSI-styling splits a literal substring the test expects contiguous** — e.g. `assert "delivered 1" in result.output` fails because the real rendered string is `...delivered \x1b[0m\x1b[1;32m1\x1b[0m...` (the label and the styled number are two separate `Text`/`Span` segments joined by ANSI escape codes, so the plain substring never appears verbatim) — hits `tests/cli/commands/test_sync_commands.py` (10 tests, including this WP's own `test_sync_now_posts_exactly_once_and_drains_body`, whose OWN edited line — `assert len(posts) == 1` — passes; the failure is a DIFFERENT, unrelated assertion earlier in the same test body), `tests/cli/commands/test_sync_routes.py`, `tests/cli/commands/test_sync_status_singleton_diagnostics.py`, `tests/auth/integration/test_headless_login_e2e.py::test_full_device_flow_happy_path` (`assert "ABCD-1234" in result.stdout` — the device code's hyphen-joined halves are styled in different colors), `tests/missions/test_mission_switching_integration.py`. Confirmed NOT a terminal-width wrapping artifact (reran two of these with `COLUMNS=300`, identical failure). Two remaining failures are unrelated environment gaps, not (a) or (b): `tests/cross_cutting/test_mypy_strict_mission_step_contracts.py` (`No module named mypy` — `mypy` isn't installed under `--extra test`, only `--extra lint`) confirms F5's "extras" pattern extends to mypy itself for that one self-checking test. **Net effect for future WPs/reviewers:** none of these 81 are golden-count regressions or caused by this batch; they are a pre-existing, cross-cutting rich/Console rendering-format break (likely a `rich` version bump changing how `Text.assemble`/styled segments serialize to plain `.stdout`) affecting dozens of files this mission's WPs never touch, reported per the charter's pre-existing-failure-reporting rule rather than "fixed" here (console-output rendering is a completely different concern from `len(x)==N` assertion strength, well outside FR-014's scope) — worth its own follow-up issue given the surface area (~15+ distinct test files across `tests/cli`, `tests/auth`, `tests/doctor`, `tests/dashboard`, `tests/missions`).
- **2026-07-13 (WP02, corroborates F12 — a THIRD facet of the same split-brain):** `spec-kitty agent tasks move-task WP02 --to for_review` run from inside the lane worktree (`lane-b`) failed outright with `Error: Illegal transition: planned -> for_review`, even though `spec-kitty agent tasks status` (also run from the lane worktree) correctly reported WP02 as `in_progress`, and directly materializing the event log (`specify_cli.status.reducer.materialize`) against the primary checkout's `kitty-specs/` also confirmed `in_progress`. Root cause traced to `aggregate.py`'s `_resolve_current_lane` → `read_current_wp_state_transactional`: this transactional status-READ path does NOT apply the same "worktree copy ignored, route to planning repo" resolution the tool prints and applies to the WP/tasks.md content read — it reads the LANE WORKTREE's own local `status.events.jsonl` (still on the last-rebased snapshot, only carrying the `genesis -> planned` bootstrap event, missing the later `claimed`/`in_progress` transitions that only ever landed on the primary `feat/test-suite-friction-remediation` branch), computing a stale `from_lane=planned` and correctly-per-its-own-data rejecting `planned -> for_review` as illegal. **Workaround:** re-run the identical `move-task` command from the PRIMARY checkout (cwd = mission root, not the lane worktree) — succeeded immediately (`Moved WP02 from in_progress to for_review`), confirming the read-side inconsistency (not a real illegal transition) was the cause. Net effect with F12: `move-task` from a lane worktree is unsafe in BOTH directions on this lanes-topology mission — reads can see a stale local copy (this entry, spurious rejection) and writes can land on the wrong branch (F12, silent corruption) — both point at the same underlying gap: the transactional status contract resolution needs to unconditionally route through the planning/primary partition for lanes-topology missions, matching the WP-content resolver's behaviour, not just print a note claiming it does. Filed alongside F12 for the same follow-up.
- **2026-07-13 (WP03):** Both the WP03 task table's per-file occurrence counts (~241) and the DoD's own single-line strict survivor grep (`setattr\([^,]*runtime_bridge\b|"runtime\.next\.runtime_bridge\.`) undercount/overcount differently than the true forwarding-delegate patch surface: (1) the loose per-file counts in the task table include plain `from runtime.next.runtime_bridge import X` direct-call imports (not patch sites at all) — 4 of the 12 listed files (`test_occurrence_gate_next_loop.py`, `test_finalized_task_routing.py`, `test_next_claimable_payload.py`, `test_decision_unit.py`) had ZERO actual monkeypatch/patch sites once filtered; (2) the strict single-line survivor grep misses multi-line `monkeypatch.setattr(\n    obj,\n    "name",\n    ...)` calls entirely (grep is not multi-line aware), so it under-detects real forwarding sites using an aliased object (`rb`, `bridge`) — a DOTALL-aware Python scan was needed to find the true site count (23 genuine repoints across 5 files, not ~241). Lane worktree edited `kitty-specs/` tracer files directly per the WP prompt's instruction (F6 in this same file warns against that for `move-task`-blocking risk) — flagging for the orchestrator to reconcile if a dirty-classifier block appears at hand-off.
- **2026-07-13 (WP04):** (1) **The DoD's "zero forwarding-delegate patch survivors" is NOT a literal grep-zero.** After repointing the 25 genuine generic-consumer sites, ~180 `setattr(rb,…)`/`patch("runtime.next.runtime_bridge.…")` sites REMAIN and are all genuine non-forwarding (KEEP-`__all__`, façade-resident, composite `_check_cli_guards`, plain re-exports, external re-exports, and — the bulk — the #2531 seam-family's own live-lookup/wiring regression suites that patch `rb` *on purpose*). A naive grep-to-zero would have forced repointing tests whose entire assertion is "the `rb` patch is observed via the `_rb` live lookup," silently converting a load-bearing wiring guard into a vacuous forward-check — the single most likely false-green on this WP. The real gate is per-name AST classification (delegate? same-name? KEEP/composite/plain-reexport? consumer-vs-wiring-test?), not a survivor count. (2) **`_composition_dispatch_inputs` is a PLAIN re-export, not a native delegate** — `rb._composition_dispatch_inputs` and `composition._composition_dispatch_inputs` are two *separate* module bindings of the same object; patching the seam attr does NOT rebind the façade attr, so a bare `_dn_composition_dispatch` call (resolving rb's global) would NOT observe a seam-level patch → a repoint here is a false-green (or a hard red). Native-thin-delegate (`def …: return _seam.x(...)`, repointable) vs plain `from … import x` re-export (NOT repointable) is only visible by reading the src, not the grep. (3) **`_advance_run_state_after_composition` is a DIFF-NAME delegate** (façade name ≠ seam name `advance_run_state_after_composition`) — the repoint target is `runtime_bridge_engine.advance_run_state_after_composition`, discoverable only by reading the delegate body. (4) **Harness auto-backgrounds slow foreground pytest** — a plain `uv run pytest tests/specify_cli` (10995 tests, 10m30s) is auto-detached even without `run_in_background`; drove completion via a Monitor until-loop on the output file. Full-suite chunks on this seam are 3–10 min each (`tests/runtime` 604 / 9m51s; `tests/specify_cli` 10995 / 10m30s), so scoped-file runs are strongly preferable during the edit loop.
- **2026-07-13 (WP18):** (1) **The delegate-deletability signal is the frozen guard's COVERAGE SET, not a grep or the "Thin compat delegate" docstring.** The one reliable discriminator for "safe to delete now" was: *is the name absent from `test_bridge_compat_surface.py`'s `REACH ∪ UNREACHABLE ∪ GUARD_B_ONLY` set?* Exactly the 8 self-alias re-exports are absent — and they are the only ones deletable without relaxing a frozen guard-A round-trip sentinel. All 34 "Thin compat delegate" native `def`s are IN REACH and are patched by the #2531 wiring suites *on purpose*; the WP18-prompt framing of "~35 repointed forwarders, just delete them" undercounts this coupling — each REACH deletion is a guard-A REACH-map edit + a bespoke wiring-sentinel retirement, not a mechanical delete. (2) **`test_bridge_compat_surface.py::_grep_derived_inventory` is a self-deriving closed loop:** it re-derives the compat inventory from `tests/**` every run and asserts `REACH∪UNREACH∪GUARD_B == inventory` (both directions). This means a REACH deletion that removes the name from REACH but leaves ANY test binding (even a wiring-suite `setattr(rb,…)`) → instant "missing from guard" RED; and leaving it in REACH after the delegate is gone → guard-A `getattr(rb,name)` AttributeError RED. The two must move together — high blast-radius, and the reason the 34 are deferred. (3) **WP03/WP04 left 7 un-repointed CONSUMER façade bindings** (`_BufferingRuntimeEmitter`, `_advance_run_state_after_composition`, `_build_run_ref`, `_resolve_retrospective_policy_for_runtime`, `_run_retrospective_learning_capture`, `_runtime_template_key`, `_start_ephemeral_query_run`) in non-wiring consumer tests — WP18 cannot delete these delegates (grep≠0 on the façade path) and the consumer files are not WP18-owned. Residual for a WP03/WP04 follow-up or the deferred-34 mission. (4) **`tests/runtime` full suite is ~10 min (604 tests); even the 9-file seam-family + dead-symbols subset is multi-minute** (drives real engines) — scoped iteration + one final full run is the only workable loop; harness auto-backgrounds these, drive completion via a Monitor until-loop.
- **2026-07-13 (WP05):** (1) **`uv sync` alone did not install `pytest`/`ruff`/`mypy` in this lane worktree's `.venv`** — they live under `[project.optional-dependencies]` `test`/`lint` extras, not the default dependency set or the `dev` dependency-group; `uv sync --extra test --extra lint --group dev` was required before any gate would run (bare `uv sync` silently reported "Audited 54 packages" with no pytest present — an easy false-"nothing to install" trap for a fresh lane worktree). (2) **The WP05 task-file premise ("its delegate was deleted in WP02") was stale by the time this WP landed** — a mid-mission re-sequencing (`plan(re-seq): Lane-0 WP02 classify-only + new WP18 delete pole`) moved ALL actual deletions to WP18 and left WP02 at 0 deletions; WP18's own 8 deletions turned out to be private non-`__all__` self-alias re-exports that never touched the allowlist. Net: the "grandfathered burndown" DoD checkbox assumed removable material that the corpus's actual history no longer provides — worth flagging at close so the checkbox isn't read as a missed obligation. (3) **F6 acknowledged and knowingly overridden**: per the parent orchestrator's explicit instruction, all edits (incl. these tracer files) were made from the lane-d worktree, not the mission-root checkout, consistent with WP03/WP04/WP18's same choice — flagging again for the orchestrator's hand-off reconciliation pass.
