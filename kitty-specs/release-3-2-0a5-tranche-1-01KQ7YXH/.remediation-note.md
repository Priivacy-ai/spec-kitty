# Remediation Note — issue-matrix.md (WP03, 2026-05-12)

**Diagnostic**: `MISSION_REVIEW_ISSUE_MATRIX_MULTI_TABLE`

**Observation**: `issue-matrix.md` contains two Markdown tables — the canonical issue-closure table and a summary "Aggregate" table. The validator requires exactly one table at the top level.

**Auto-normalized**: header capitalization in the main table was case-folded to lowercase by WP03 (`Issue` → `issue`, `FR` → `fr`, `WP` → `wp`, `Verdict` → `verdict`). The `evidence_ref` column was already lowercase.

**Structural drift (not auto-fixed)**: The summary aggregate table (`| Verdict | Count |`) is a second top-level Markdown table. The validator fires `MISSION_REVIEW_ISSUE_MATRIX_MULTI_TABLE` on any file with more than one table.

**Recommended operator action**:
1. Convert the `## Aggregate` table to a prose summary or a code block so it is no longer parsed as a Markdown table by the validator.
2. Example: replace the table with a plain sentence such as "Verdicts: 7 fixed, 2 verified-already-fixed, 0 deferred-with-followup."
3. Commit the fix directly in the `main` branch (this is an existing-mission artifact, not gated by a worktree).
