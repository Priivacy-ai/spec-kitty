---
work_package_id: WP04
title: setup-plan preflight integration
dependencies:
- WP01
requirement_refs:
- FR-002
- FR-008
- FR-009
- NFR-001
- NFR-002
planning_base_branch: kitty/pr/mvp-sync-boundary-cli-01KRVCQS
merge_target_branch: kitty/pr/mvp-sync-boundary-cli-01KRVCQS
branch_strategy: Planning artifacts for this mission were generated on kitty/pr/mvp-sync-boundary-cli-01KRVCQS. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into kitty/pr/mvp-sync-boundary-cli-01KRVCQS unless the human explicitly redirects the landing branch.
subtasks:
- T017
- T018
- T019
- T020
agent: "codex:gpt-5:reviewer-rita:reviewer"
shell_pid: "4591"
history:
- at: '2026-05-18T08:00:00Z'
  actor: planner
  note: Initial generation
agent_profile: implementer-ivan
authoritative_surface: src/specify_cli/cli/commands/agent/mission.py
execution_mode: code_change
mission_id: 01KRX11MCY70M5NFBBHT4DQHJ2
mission_slug: mvp-cli-sync-boundary-completion-01KRX11M
owned_files:
- src/specify_cli/cli/commands/agent/mission.py
- src/specify_cli/cli/commands/doctor.py
- tests/runtime/test_setup_plan_sync_evidence.py
role: implementer
tags: []
---

## ⚡ Do This First: Load Agent Profile

Before reading anything else in this prompt, run the `ad-hoc-profile-load` skill to adopt the assigned profile (`implementer-ivan`, role: `implementer`). The profile sets the identity, governance scope, and boundaries for the work in this WP.

## Objective

Wire `run_preflight` (from WP01) into `setup_plan` and any other SaaS-producing mission lifecycle paths so they refuse loudly on daemon-owner mismatch before enqueueing events or body uploads. Confirm `doctor orphan-daemons` (top-level CLI in `doctor.py`) shares the same `list_orphan_records()` detection as the in-`sync.py` orphan section.

## Context

- Existing `setup_plan()` lives at `src/specify_cli/cli/commands/agent/mission.py:881`.
- Existing hosted-SaaS-sync auth preflight lives at `:926`; blocking unauthenticated diagnostic at `:942`. These already implement FR-008's auth-absent refusal — preserve them.
- WPCreated SaaS emission area is at `:2354`. New preflight must run before this and any other SaaS-visible enqueue / body-upload write.
- `src/specify_cli/cli/commands/doctor.py:1172` is the top-level `doctor orphan-daemons`. WP03 (T015) handed cross-file consistency to this WP.

## Branch strategy

- Planning/base branch: `kitty/pr/mvp-sync-boundary-cli-01KRVCQS`.
- Final merge target: `kitty/pr/mvp-sync-boundary-cli-01KRVCQS`.
- Execution worktree: allocated per computed lane from `lanes.json`. Depends on WP01; runs in parallel with WP03 (different files).

## Subtasks

### T017 — Wire `run_preflight` into `setup_plan`

**Purpose**: Refuse `setup-plan` on daemon-owner mismatch before any enqueue.

**Steps**:

1. Open `mission.py:881` (`setup_plan`) and identify the order: existing hosted-auth preflight at `:926`, blocking unauth diagnostic at `:942`, then mission logic.
2. Immediately after the hosted-auth preflight succeeds (and before any other side-effect-producing code), call `run_preflight(repo_root=..., require_auth=True)`.
3. If `result.ok` is `False`, call `result.render(console)` and exit `2`. Do not write to scoped queue, body upload queue, or legacy queue.
4. If `result.ok` is `True`, continue with the existing setup-plan logic.
5. Trace forward to `:2354` (WPCreated SaaS emission area). Confirm that *every* code path reaching that emission has passed through the preflight gate. If there are multiple entry conditions, factor the preflight call into a helper called once at the top, not scattered.

**Files**:
- `src/specify_cli/cli/commands/agent/mission.py` (edited)

**Validation**:
- New test in `test_setup_plan_sync_evidence.py` (T019) asserts refusal on each documented split-brain shape.

### T018 — Document inventory of SaaS-producing mission lifecycle paths

**Purpose**: Prove FR-002 / FR-009 by enumerating all paths that could enqueue SaaS-visible work, and verifying each is gated by the preflight.

**Steps**:

1. Grep `src/specify_cli/cli/commands/agent/` for calls that enqueue events or body uploads (look for `OfflineQueue`, `BodyUploadQueue`, `enqueue`, `body_upload`, `WPCreated`, `setup-plan`, mission-lifecycle commands).
2. Produce a markdown inventory at `kitty-specs/mvp-cli-sync-boundary-completion-01KRX11M/evidence/saas-producing-paths.md` listing each entry point, the SaaS-visible side effect, and whether it is gated by `run_preflight`. (This file is in WP05's owned tree, but generated by this WP — copy it into WP05's `evidence/` directory on completion, or have WP05 import it; coordinate during implementation.)
3. For each entry point not yet gated, file a follow-up subtask. If the inventory shows additional paths beyond `sync now` (WP03) and `setup-plan` (T017), wire them in this WP if owned (i.e., inside `mission.py`); otherwise open a decision and capture as a known limitation.
4. Update `src/specify_cli/cli/commands/doctor.py:1172` (`doctor orphan-daemons`) if and only if it does not already call `list_orphan_records()`. The change is to consume the same detection function as `sync doctor` / `run_preflight`. If it already does, no edit needed; record that in the inventory file.

**Files**:
- `src/specify_cli/cli/commands/agent/mission.py` (edited if additional paths surface; otherwise unchanged)
- `src/specify_cli/cli/commands/doctor.py` (edited only if inconsistency exists)
- `kitty-specs/mvp-cli-sync-boundary-completion-01KRX11M/evidence/saas-producing-paths.md` (written by this WP; ownership transferred to WP05 on completion)

**Validation**:
- Inventory file exists with one row per entry point and a gate-status column.

### T019 — Extend `test_setup_plan_sync_evidence.py` with preflight integration

**Purpose**: Lock the new gating behavior.

**Steps**:

1. Open `tests/runtime/test_setup_plan_sync_evidence.py`. Existing anchors: `:116` (authenticated writes scoped queue), `:296` (unauthenticated refuses), `:369` (no `_legacy_queue_db_path()` in setup-plan path).
2. Add the following test cases. Isolate the operator's home directory in a cross-platform way per C-008: prefer `monkeypatch.setattr(pathlib.Path, "home", lambda: tmp_path)` over bare `monkeypatch.setenv("HOME", ...)` so fixtures work on Windows `USERPROFILE` and POSIX `HOME` identically.
   - `test_setup_plan_refuses_on_daemon_owner_mismatch`: write a daemon owner record with a mismatched canonical field (e.g., `package_version`); set `SPEC_KITTY_ENABLE_SAAS_SYNC=1`; authenticate; run `setup-plan` (programmatically via the typer test runner). Assert exit code 2 and that no row is written to scoped queue, no body upload row written.
   - `test_setup_plan_refuses_on_orphan_owner_record`: stage an orphan owner-record fixture (no `os.kill`); assert refusal before any enqueue.
   - `test_setup_plan_preflight_runs_after_auth_preflight`: with no auth and a mismatched daemon record, the auth-absent refusal fires first (preserves FR-008).
   - `test_setup_plan_authenticated_coherent_succeeds`: positive case — coherent host, auth present, `setup-plan` writes scoped queue rows successfully and emits `WPCreated`.

**Files**:
- `tests/runtime/test_setup_plan_sync_evidence.py` (extended; +~150 lines)

**Validation**:
- `uv run pytest tests/runtime/test_setup_plan_sync_evidence.py -q` exits 0.

### T020 — Regression: no legacy-queue writes from `setup-plan` when authenticated

**Purpose**: FR-009 — `setup-plan` and other SaaS-producing mission paths never silently write body uploads into the legacy unscoped queue when authenticated.

**Steps**:

1. Add a test `test_setup_plan_never_writes_legacy_queue` that:
   - Authenticates as a known scope.
   - Stages an empty legacy queue and an empty scoped queue.
   - Runs `setup-plan` end-to-end.
   - After completion, asserts the legacy queue still has zero rows (no `sync_events` rows, no `body_upload_queue` rows for the current scope or any scope).
   - Asserts the scoped queue has the expected number of rows.
2. If the existing test at `:369` already covers this, extend it to also assert body-upload counts are zero in legacy.

**Files**:
- `tests/runtime/test_setup_plan_sync_evidence.py` (extended; +~40 lines)

**Validation**:
- New test passes.

## Definition of Done

- [ ] All four subtasks complete.
- [ ] `uv run pytest tests/runtime/test_setup_plan_sync_evidence.py -q` exits 0.
- [ ] `uv run mypy --strict src/specify_cli/sync/` exits 0 (note: scope is `src/specify_cli/sync/`; if mission.py imports from `sync/`, any type changes must propagate cleanly).
- [ ] Inventory file `evidence/saas-producing-paths.md` exists and lists every SaaS-producing entry point with gate status.
- [ ] No edits outside `src/specify_cli/cli/commands/agent/mission.py`, `src/specify_cli/cli/commands/doctor.py`, `tests/runtime/test_setup_plan_sync_evidence.py`, and the one inventory file in `evidence/`.

## Risks

- **Missing a SaaS-producing path**: T018 explicitly enumerates and gates each. If an entry point lives outside this WP's owned files, escalate via a decision rather than reaching into another WP's surface.
- **Auth-preflight regression**: FR-008's existing refusal at `:942` must still fire first. The new preflight call must come *after* the auth preflight, not before or in place of.
- **Body-upload routing**: T020 is the regression guardrail. Confirm by hand once with a small mission setup that no row appears in `~/.spec-kitty/queue.db` after `setup-plan`.

## Reviewer guidance

- Verify call order in `setup_plan`: input parse → hosted-auth preflight → boundary preflight → mission logic. No side effects between the auth preflight and the boundary preflight.
- Verify `doctor orphan-daemons` consumes `list_orphan_records()` (or the canonical detection helper used by `sync doctor`).
- Spot-check inventory file: every entry point listed has a "yes" or an explicit follow-up.

## Implementation command

```bash
spec-kitty agent action implement WP04 --agent <name> --mission mvp-cli-sync-boundary-completion-01KRX11M
```

## Activity Log

- 2026-05-18T10:24:32Z – claude:opus-4.7:implementer-ivan:implementer – shell_pid=95721 – Started implementation via action command
- 2026-05-18T10:37:08Z – claude:opus-4.7:implementer-ivan:implementer – shell_pid=95721 – setup-plan now gated by run_preflight before any enqueue; SaaS paths inventoried; doctor consistency verified
- 2026-05-18T10:37:45Z – codex:gpt-5:reviewer-rita:reviewer – shell_pid=4591 – Started review via action command
- 2026-05-18T12:03:58Z – codex:gpt-5:reviewer-rita:reviewer – shell_pid=4591 – Review passed: preflight wired correctly into setup_plan after FR-011 auth gate and before all side effects; all tests green; mypy delta zero; finalize_tasks skip rationale defensible.
