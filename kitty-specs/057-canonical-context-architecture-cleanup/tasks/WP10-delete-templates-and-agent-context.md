---
work_package_id: WP10
title: Delete Command Templates and Agent Context
lane: "doing"
dependencies: [WP09]
requirement_refs:
- NFR-006
planning_base_branch: main
merge_target_branch: main
branch_strategy: Planning artifacts for this feature were generated on main. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into main unless the human explicitly redirects the landing branch.
base_branch: 057-canonical-context-architecture-cleanup-WP09
base_commit: 194bf77749ef77a7b73290131ff36dccd1f3b7fa
created_at: '2026-03-27T18:18:17.598805+00:00'
subtasks:
- T050
- T051
- T052
- T053
phase: Phase D - Surface and Migration
assignee: ''
agent: ''
shell_pid: "10711"
review_status: ''
reviewed_by: ''
review_feedback: ''
history:
- timestamp: '2026-03-27T17:23:39Z'
  lane: planned
  agent: system
  shell_pid: ''
  action: Prompt generated via /spec-kitty.tasks
---

# Work Package Prompt: WP10 – Delete Command Templates and Agent Context

## Branch Strategy

- **Planning/base branch at prompt creation**: `main`
- **Final merge target for completed work**: `main`

---

## Objectives & Success Criteria

- All `command-templates/*.md` files deleted from all mission directories.
- `core/agent_context.py` deleted entirely.
- Migration code that copies/patches markdown templates is deleted or simplified.
- Shim generation (from WP09) is the only mechanism for creating agent command files.

## Context & Constraints

- **Spec**: NFR-006 (backward deletion)
- **Plan**: Move 5 — Deletion targets
- **Depends on**: WP09 (shim system must exist before old templates are deleted)
- **Key constraint**: Some migration logic for pre-3.0 → 3.0 upgrade may still need to read old templates as migration input. Keep those read paths only in the one-shot migration module.

## Subtasks & Detailed Guidance

### Subtask T050 – Delete all command-templates

- **Purpose**: Remove the source of agent-template drift.
- **Steps**:
  1. Delete `src/specify_cli/missions/software-dev/command-templates/` directory and all contents
  2. Delete `src/specify_cli/missions/research/command-templates/` directory and all contents
  3. Delete `src/specify_cli/missions/documentation/command-templates/` directory and all contents
  4. Delete `src/specify_cli/missions/plan/command-templates/` directory (if exists) and all contents
  5. Search for any other `command-templates/` directories: `find src/ -name "command-templates" -type d`
  6. Count deleted files for the report (~14 files per mission x 4 missions = ~56 files)
- **Files**: Delete ~56 markdown files across 4 mission directories
- **Parallel?**: Yes — independent of T051

### Subtask T051 – Delete core/agent_context.py

- **Purpose**: Remove tech-stack parsing from plan.md — no longer needed with thin shims.
- **Steps**:
  1. Delete `src/specify_cli/core/agent_context.py` (~300 lines)
  2. Search for all imports: `from specify_cli.core.agent_context import`, `from specify_cli.core import agent_context`
  3. Search for function calls: `update_agent_context(`
  4. Remove all callers — the plan command no longer needs to update agent context files
  5. Remove from `core/__init__.py` exports if present
- **Files**: Delete 1 file, update callers
- **Parallel?**: Yes — independent of T050

### Subtask T052 – Delete template-copying migration code

- **Purpose**: Remove migration logic that copies full markdown templates into agent directories.
- **Steps**:
  1. Search migrations directory: `grep -rn "command.templates\|command_templates" src/specify_cli/upgrade/migrations/`
  2. For each migration that copies templates:
     - If the migration is for version < 3.0.0: keep the `should_apply()` logic (it won't match post-migration), but remove the copying logic or mark as no-op
     - If the migration is for version >= 3.0.0: it should use shim generation instead
  3. Search for migrations that reference `agent_context.py`: remove those calls
  4. The one-shot migration (WP13) handles the full conversion
- **Files**: Multiple migration files in `src/specify_cli/upgrade/migrations/` (modify)
- **Notes**: Be careful not to break the migration chain for pre-3.0 → 3.0 upgrades. The one-shot migration handles template replacement.

### Subtask T053 – Tests verifying deletions

- **Purpose**: Confirm deleted modules are gone and shim-only surface is working.
- **Steps**:
  1. Delete tests for `agent_context.py`
  2. Delete tests that verify command template content or copying
  3. Add test: `command-templates/` directories do not exist in any mission
  4. Add test: `agent_context` module cannot be imported
  5. Add test: agent command files are generated by shim generator, not copied from templates
  6. Run full test suite to catch any broken imports
- **Files**: Update/delete various test files
- **Parallel?**: Yes

## Risks & Mitigations

- **Breaking migration chain**: Pre-3.0 migrations may reference command-templates. Keep them as no-ops that skip based on version, don't delete the migration files themselves.
- **Missing imports**: Run full test suite after deletion. Any ImportError is a missed reference.

## Review Guidance

- `find src/ -name "command-templates" -type d` returns nothing
- `grep -r "agent_context" src/` returns zero results (except migration/shim code)
- Full test suite passes with no ImportErrors
- No agent command files are generated from templates anymore

## Activity Log

- 2026-03-27T17:23:39Z – system – lane=planned – Prompt created.
