---
work_package_id: WP06
title: 'Durable record: egress-consent boundary ADR and the "engagement" glossary entry'
dependencies:
- WP03
- WP04
requirement_refs:
- FR-022
- FR-023
planning_base_branch: pr/egress-refusal-consolidation-3110
merge_target_branch: pr/egress-refusal-consolidation-3110
branch_strategy: Planning artifacts for this mission were generated on pr/egress-refusal-consolidation-3110. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into pr/egress-refusal-consolidation-3110 unless the human explicitly redirects the landing branch.
subtasks:
- T035
- T036
phase: Phase 3 - Durable record
task_type: implement
execution_mode: code_change
agent_profile: curator-carla
role: implementer
agent: ""
model: ""
assignee: ""
shell_pid: ""
authoritative_surface: docs/adr/3.x/
owned_files:
- docs/adr/3.x/*-egress-consent-boundary.md
- docs/context/identity.md
- docs/context/planning-and-tracking.md
create_intent:
- docs/adr/3.x/2026-07-31-1-egress-consent-boundary.md
tags: []
tracker_refs: []
history:
- Generated by /spec-kitty.tasks 2026-07-31
---

# Work Package Prompt: WP06 – Durable record: ADR + glossary

## ⚡ Do This First: Load Agent Profile

Run `/ad-hoc-profile-load curator-carla` and adopt its identity, boundaries and governance scope
before parsing the rest of this prompt.

## Objective

Put the egress-consent boundary into the **charter authority path**. This is the only item in the
bundle acting on **recurrence** rather than on an instance, and the cheapest (paula P-4).

**Read first**: `../spec.md` → **SC-019**, **SC-020** (and both honesty notes). `../plan.md` →
**IC-06**.

**Discharges**: **SC-019**, **SC-020**. **Requirements**: FR-022, FR-023.
**Depends on**: **WP03** (the ADR names the wrapper's home) and **WP04** (the ADR states the
provenance invariant WP04 implements). *The plan's IC-06 text names IC-03 only; its sequencing
diagram joins IC-04's chain as well, and the ADR cannot state the invariant honestly before the fix
that expresses it exists. Depending on both costs nothing — this WP is last but one.*

## Both criteria are grep-gates. Read the honesty note before you trust either green

**SC-019 and SC-020 are grep-gates and the spec says so in their own text: presence is mechanical,
content is not enforceable by them.** A file containing only the three search terms passes SC-019.
That is **accepted rather than papered over** — the alternative pins prose wording, and the ADR's
value is in being read, not in being matched.

**⇒ The content is a PR-review item, and the reviewer must read the ADR rather than trust the
green.** Do not over-read either criterion in the evidence.

## Subtasks

### T035 — FR-022: the one-page ADR

- **File**: `docs/adr/3.x/2026-07-31-1-egress-consent-boundary.md` (adjust the date/sequence prefix to
  the convention in `docs/adr/3.x/README.md`; the `owned_files` glob
  `docs/adr/3.x/*-egress-consent-boundary.md` accommodates a different date).
- **Frontmatter is not optional, and it has a specific shape — get it from the neighbours, not the
  template.** `scripts/docs/freshen_adr_inventory.py:162-168` raises `FreshenError(f"{adr_path}
  frontmatter is missing a title and/or date")` when either field is absent, and this WP's own next
  bullet requires running that script. The shared `docs/architecture/adr-template.md` carries
  `title`, `description`, `doc_status`, `updated` — **no `date:`** (it puts `**Date:**` in the body,
  which the script does not read). Measured: **all 95 live `docs/adr/3.x/*.md` carry both `title:`
  and `date:`** in frontmatter — that is the real convention, not the shared template. **Write MADR
  frontmatter with at least `title` and `date`**, following an existing 3.x ADR's shape (`title`,
  `description`, `status`, `date`, `related`). **The ADR must NOT carry `doc_status` or `updated`** —
  `adr/**` is excluded from `frontmatter_required_fields` at
  `common-docs.styleguide.yaml:150-159`, whose own comment states that backfilling `doc_status`/
  `updated` onto an ADR **violates** the directive it is exempting; MADR `status`/`date` are the
  correct fields for an ADR, not the docs styleguide's `doc_status`/`updated` pair. Check the result
  read-only before writing anything else: `python scripts/docs/freshen_adr_inventory.py --check
  docs/adr/3.x/<the-adr>.md`.
- **The README you are quoting for the filename convention also mandates a step this WP does not
  own — read both lines.** `docs/adr/3.x/README.md:9-10`:

  > *"After adding an ADR file, run `python scripts/docs/freshen_adr_inventory.py
  > docs/adr/3.x/<your-adr>.md` to update the page-inventory lockfile and add the row to the index
  > table below."*

  **Run the freshen script** — it is what regenerates `docs/development/3-2-page-inventory.yaml`,
  a **1:1 lockfile over every `docs/**/*.md`** (measured clean at baseline:
  `inventory_lockfile: exit=0 generated=685 committed=685 drift=False`; `docs_structural_lint:
  685 pages, 0 violations`). **Adding the ADR produces an `added` drift row**, and
  `scripts/docs/check_docs_freshness.py:742-758` makes **every** drift row `severity="error"`, which
  reds `check_docs_freshness` — run by `.github/workflows/docs-freshness.yml:56-59`.
- **BUT: this WP does NOT own the lockfile or the ADR index README, and that is deliberate.**
  `docs/development/3-2-page-inventory.yaml` and `docs/adr/3.x/README.md` are in **WP07's**
  `owned_files` and **lane-planning's** `write_scope` — **not this lane's.** **Two lanes cannot own
  one lockfile**: WP06 and WP07 each add a page, and if both regenerate it they collide on every
  line the other added.

  > **⇒ WP06 DELIBERATELY LANDS LOCKFILE-DIRTY. This is a designed state, not an oversight.**
  > Lane branches merge locally and the mission opens a single PR only after the final package
  > reconciles, so no CI ever sees this dirty state on this lane's own diff. Note that
  > `.github/workflows/docs-freshness.yml` is the **one** workflow with no base-branch filter, so it
  > **would** red a per-lane PR if one were opened — do not open one, and do not reach for a skip
  > label.
  > Expect `check_docs_freshness` to report **one `INVENTORY-LOCKFILE-DRIFT` `added` row naming your
  > ADR**, and expect the ADR index table in `docs/adr/3.x/README.md` to be missing its row.
  > **Do not "fix" it by editing either file — they are outside this lane's write scope and the
  > attempt is a hard stop at implement time. Do not weaken, skip or allowlist the freshness check
  > either.**
  >
  > **What you owe instead**: run the freshen script **read-only if you can, or run it and revert**,
  > and **state in the PR body**: the ADR's final filename, that the lockfile drift and the missing
  > index row are **known and assigned to WP07**, and the exact command WP07 must run
  > (`python scripts/docs/freshen_adr_inventory.py docs/adr/3.x/<your-adr>.md`). **Hand the filename
  > to WP07 explicitly** — WP07 cannot regenerate a row for a file whose name it has to guess.
  > WP07 depends on WP06 and lands last; **it is the reconciler.**
- **It must name three things, and all three are load-bearing:**
  1. **The boundary** — where project-egress consent is decided, and that both transports now reach
     it through one presentation (`src/specify_cli/egress.py`, WP03).
  2. **The provenance invariant** — *consent is keyed on something derived from **the record being
     sent**, never from ambient context.* This is framing 3, and it **supersedes** the orchestrator's
     earlier *"a path-typed seam is the defect"* framing. It is the idea `#3111` is an instance of,
     and the reason a uuid-typed seam (Q3/FU-5) is optional rather than indicated: **a type cannot
     express provenance.**
  3. **That the attribution guard is SYNTACTIC** — it can prove a root **was passed**, never that it
     was the **owning** root. The guard's own docstring concedes this at
     `test_client_consent_gate_3030.py:312-315`. Say it here so a future reader does not read a green
     guard as an ownership proof (R5).
- **SC-019's verification is the same grep that measured zero, with its controlled diagnostic already
  established** (a known-present term returns 12, a nonsense term returns 0):

  ```
  grep -rn "resolve_egress_consent\|ConsentedBatch\|project_egress_refusal" docs/adr/3.x/ docs/context/
  ```

  It must return a non-zero count **with the file named**. **Print the input count** alongside
  (rule 5); a grep over zero files returns zero and looks like a measurement.
- Worth recording in the ADR, because it is the durable lesson: **`#3111` is consent *laundering*, not
  unconsented egress** — every gate answered truthfully, about the wrong project.

### T036 — FR-023: define "engagement" in the terminology authority

- **Measured**: `grep -ril engagement docs/context/` returns **0 of 22 entries** (probe non-vacuous —
  the same grep for `mission_slug` returns 1, in `historical-terms.md`).
- **FR-023 is UNCONDITIONAL.** Round 1 offered an escape — *"or resolve Q2 toward vocabulary the
  glossary already knows"* — and **the operator's Q2 decision keeps "engagement" verbatim** in the
  tracker fragment (`"mission and engagement identifiers"`). The escape is **closed**. Do not ship an
  operator-facing term the glossary does not know (DIR-032).
- **Where.** `docs/context/*.md` are **bounded-context glossaries**; check `docs/context/index.md`'s
  taxonomy first and put the term in the context that actually owns it. `identity.md` (which owns
  Mission Owner / Mission Participant and the identity vocabulary) and `planning-and-tracking.md` are
  the two candidates this WP owns. **Record which you chose and why** in the PR body. `historical-terms.md`
  is the legacy-mapping surface and is **not** the right home for a live term.
- The entry must **define the term as used in the operator-facing refusal string** — i.e. that a
  mission slug is a **client engagement name**, which is the confidentiality argument the whole
  spec rests on. SC-020's grep only proves presence; the definition is what the criterion is *for*.
- **Before pushing prose, run the terminology guard** (charter standing order 6 — it runs only in
  CI's `integration-tests-core-misc` job, so a regression passes local doctrine runs and fails at CI):

  ```
  pytest tests/architectural/test_no_legacy_terminology.py
  ```

  It enforces the Terminology Canon (`Mission` not `feature`, `status commit` not `ceremony`). ≈0.1 s.
- **Same lockfile caveat as T035.** `docs/development/3-2-page-inventory.yaml` carries a row per
  `docs/**/*.md`, including the two `docs/context/` pages this subtask edits. `PageInventoryEntry`
  (`scripts/docs/_inventory.py:94-109`) has exactly `path`, `tag`, `divio_type`,
  `owning_workstream`, `current_target`, `notes` — **`updated` is not a row field, so bumping it alone
  produces no drift.** **If you change any frontmatter field the inventory row actually derives from
  (`tag`, `divio_type`, `owning_workstream`, `current_target`) you produce a `changed` drift row**,
  which is `severity="error"`. **That is expected and it belongs to WP07** — name the pages you
  touched and which of those fields (if any) changed in the PR body, and do not edit the lockfile from
  this lane.

## Verification

```
grep -rn "resolve_egress_consent\|ConsentedBatch\|project_egress_refusal" docs/adr/3.x/ docs/context/ > /tmp/wp06-sc019.txt
grep -ril engagement docs/context/                                                                    > /tmp/wp06-sc020.txt
pytest tests/architectural/test_no_legacy_terminology.py                                              > /tmp/wp06-term.txt 2>&1
```

Rule 1: write to a file, read the file. Rule 5: **print the input count** — number of files searched
— alongside each grep result. Rule 10: run the controlled diagnostic (known-present term → non-zero,
nonsense term → zero) **before** trusting either grep.

## Bundle A bite

**None.** This WP touches no test surface whose greens depend on `#3115` or `#3113`, and its two
criteria are greps. It is the one package in this mission whose evidence is unconditional — say so,
so the successor does not re-take it.

## Risks

- **R10 — the glossary entry is skipped.** *Restated: Q2 is decided and it **kept** "engagement", so
  this is no longer a risk about which word gets picked — the word is fixed and it is one the
  glossary does not know.* Now **unconditional**. **Low → Medium.**
- **Over-reading the green.** Both criteria are greps. SC-019 passes on a file containing only the
  three search terms; SC-020 passes on any file containing the substring. **The reviewer must read
  the ADR and the glossary entry.**

## Review guidance

- **Read the ADR.** Does it actually name all three things — the boundary, the **provenance
  invariant**, and that the guard is **syntactic**? Or does it merely contain the three grep terms?
- Does the glossary entry **define** "engagement", or merely mention it?
- Is the entry in a bounded context that owns the term, per `docs/context/index.md`?
- Did the terminology guard run before the prose was pushed?
- **Is the PR body carrying the lockfile handoff** — the ADR's **final filename**, the statement that
  the `INVENTORY-LOCKFILE-DRIFT` row and the missing `docs/adr/3.x/README.md` index row are **known
  and assigned to WP07**, and the exact `freshen_adr_inventory.py` command? **Did anyone edit
  `docs/development/3-2-page-inventory.yaml` or `docs/adr/3.x/README.md` from this lane?** They must
  not have — those belong to WP07.

## Activity Log

- 2026-07-31T00:00:00Z – system – Prompt created.
