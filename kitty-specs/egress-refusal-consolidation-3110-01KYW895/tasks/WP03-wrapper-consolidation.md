---
work_package_id: WP03
title: Wrapper consolidation into src/specify_cli/egress.py
dependencies:
- WP01
requirement_refs:
- FR-008
- FR-009
- FR-010
- FR-011
- FR-012
- FR-013
- FR-024
- FR-025
- FR-026
- FR-027
- NFR-004
- C-001
- C-003
- C-004
- C-005
planning_base_branch: pr/egress-refusal-consolidation-3110
merge_target_branch: pr/egress-refusal-consolidation-3110
branch_strategy: Planning artifacts for this mission were generated on pr/egress-refusal-consolidation-3110. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into pr/egress-refusal-consolidation-3110 unless the human explicitly redirects the landing branch.
subtasks:
- T016
- T017
- T018
- T019
- T020
- T021
- T022
- T023
- T024
phase: Phase 2 - Consolidation
task_type: implement
execution_mode: code_change
agent_profile: python-pedro
role: implementer
agent: ""
model: ""
assignee: ""
shell_pid: ""
authoritative_surface: src/specify_cli/egress.py
owned_files:
- src/specify_cli/egress.py
- src/specify_cli/saas_client/egress_consent.py
- src/specify_cli/saas_client/client.py
- src/specify_cli/saas_client/__init__.py
- src/specify_cli/tracker/egress_consent.py
- src/specify_cli/tracker/saas_client.py
- tests/architectural/test_integration_boundary.py
- tests/specify_cli/test_egress_consolidation_3110.py
- tests/specify_cli/saas_client/test_client_consent_gate_3030.py
- tests/sync/tracker/test_saas_client_consent_gate_3030.py
create_intent:
- src/specify_cli/egress.py
- tests/specify_cli/test_egress_consolidation_3110.py
tags: []
tracker_refs: []
history:
- Generated by /spec-kitty.tasks 2026-07-31
---

# Work Package Prompt: WP03 – Wrapper consolidation into `src/specify_cli/egress.py`

## ⚡ Do This First: Load Agent Profile

Run `/ad-hoc-profile-load python-pedro` and adopt its identity, boundaries and governance scope
before parsing the rest of this prompt.

## Objective

One editable presentation of the project-egress refusal policy, in a **plain module owned by neither
transport**, with both `*/egress_consent.py` **deleted**.

**Read first**: `../spec.md` → **SC-004** (all three clauses and the note on which discriminates),
**SC-010**, **SC-015**, **SC-016**, **SC-021**, **SC-022**, **SC-025**, plus the *Edge Cases* entries
on renaming-during-consolidation and partial consolidation. `../plan.md` → **IC-03**, §Q1 and
**§The package→module adjudication**, **§The shim decision (PB-5)**, **§Rot-mode 5 in detail**,
§Mutation suite (MUT-1, MUT-2).

**Discharges**: **SC-004** (report **per clause**), **SC-010**, **SC-015**, **SC-016**, **SC-021**,
**SC-022**, **SC-025**. **Requirements**: FR-008…FR-013, FR-024, FR-026, FR-027, NFR-004; C-001,
C-003, C-004, C-005.
**Depends on**: **WP01** — the guards must be non-vacuous per class **before** anything moves (US3).
Otherwise the only protection against F-A3's halving mechanisms is a global `assert scanned` that
stays positive from the surviving class.

> *FR-025 appears in this WP's `requirement_refs` frontmatter solely so the finalize coverage gate
> sees the retired ID mapped. **It is retired and carries no instruction** — its content is SC-004
> clause 3, which is where this prompt cites it. Do not implement "FR-025".*

## Two decisions that are already made — do not re-litigate, and do not "tidy" either away

### 1. The wrapper is a **plain module**, `src/specify_cli/egress.py` — NOT a package

`specify_cli.egress.project_egress_refusal` must be **the definition site itself**, not a re-export.

> **SC-015's own text still names `src/specify_cli/egress/` — a directory (`../spec.md:1450`). That
> path is superseded; read it as `egress.py`.** T021 sends you to SC-015, and SC-015 sits in the
> **operative** Success Criteria block, so a cold reader following it lands on the **package** form —
> which is the measured rot-mode-5 shape this section exists to forbid (fourth name, MUT-1 inert,
> SC-004 clause 3 green), and which would additionally leave WP02's two `egress.py` dorny globs
> matching nothing. **SC-025 already carries the corrected form**, and the spec now carries a
> **POST-ACCEPTANCE CORRECTION directly under SC-015** saying so. Do not "reconcile" the two by
> creating a directory.

**Measured (PR-1, 8 cases, control first).** Under an `egress/` **package**, `egress/refusal.py`
defines the symbol and `egress/__init__.py` re-exports it **by value**, creating a **fourth** name.
A mutation patching the definition site — the natural target for MUT-1 — was measured **inert at both
decision points while SC-004 clause 3 stayed green**: rot-mode 5 reappearing inside the fix for
rot-mode 5, reporting a false finding about the gate rather than about the code.

**Adding an `egress/__init__.py` that re-exports from `egress/refusal.py` re-creates that.** If the
module ever genuinely needs splitting, the split must land **with a third `is` comparison and a
restatement of the three-names list** — that is a reversal of this decision, not a refactor.

Verified in this clone for the module form: it is C-001-clean; it imports neither transport and does
not reach `specify_cli.sync`; a module has **no `__init__.py` to execute**, so A-3's mechanism cannot
fire at all; `test_no_dead_symbols.py` walks *every* `*.py` under `src/`, so FR-011 stays
self-enforcing; and `test_layer_rules.py:202-208` counts **top-level `src/` directories** only, so
`test_no_unregistered_src_packages` is untouched in two independent ways.

### 2. Both `egress_consent.py` modules are **DELETED** (PB-5), not kept as shims

The accepted spec implements **exactly two** `is` comparisons in SC-004 clause 3. Keeping shims would
impose a third assertion **the spec does not carry**, and an implementer working from the three
operative blocks would never write it — the shim hazard would be *known and unguarded*. Deletion is
the choice that leaves the accepted spec sufficient on its own. Secondarily: a surviving
`egress_consent.py` in each transport is precisely the file a future author edits when adding a
branch — the second editable presentation FR-008 exists to make impossible.

**C-004 does not force a shim to survive.** It requires the literal text `project_egress_refusal` to
remain in `src/specify_cli/tracker/saas_client.py`; the rebound import line at `:34` —
`from specify_cli.egress import project_egress_refusal` — **already satisfies it on its own**
(C-004 is a substring test). FR-027/SC-021 keep the *call* at `:329` live behaviourally, which is the
half C-004 does not protect.

**Falsifier F3, re-runnable in one command before you start:**

```
grep -rn "egress_consent" --include=*.py src tests
grep -rn "saas_client/egress_consent\|tracker/egress_consent" tests/architectural/
```

Measured at plan time: the complete importer set is **four sites, all touched by this WP**
(`saas_client/client.py:23`, `tracker/saas_client.py:34`,
`tests/specify_cli/saas_client/test_client_consent_gate_3030.py:488`,
`tests/sync/tracker/test_saas_client_consent_gate_3030.py:513`), and the allowlist grep returns
**zero** (`_baselines.yaml` keys `test_egress_consent_boundary`, the *boundary guard's* baseline, not
any `*/egress_consent.py` source path). **If F3 fires** — a fifth importer outside that set, or an
allowlist naming either path — **keep both shims as pure re-exports and add the third `is` comparison**
(`egress.project_egress_refusal is tracker.egress_consent.project_egress_refusal`, and its SaaS
twin), and say so in the PR body. That is a **reversal** of the decision, not a variation.

## Subtasks

### T016 — Create `src/specify_cli/egress.py`

- Contents: the **shared sentence template**, the **four verdict branches**,
  `UNDETERMINED_PROJECT_REFUSAL`, the `None` guard, the import-failure degradation, **and — named
  explicitly because it was missing from this list and nothing else would have caught it — the
  module's statement that it resolves through
  `specify_cli.invocation.adapters.resolve_egress_consent` and NEVER re-derives the
  checkout→project→consent chain locally.**
- **`resolve_egress_consent` and "never re-derive the chain locally" are FR-012's content, and FR-012
  is High-impact, `[ratchet]`, and deliberately has NO success criterion** (`../spec.md:1631`). Its
  **only written rationale in the repository today** is the prose at
  `src/specify_cli/tracker/egress_consent.py:18-45` — the C-003 single-chain argument — **in a file
  T017 deletes**, with `saas_client/egress_consent.py:20` deferring to it in writing. **Cite FR-012
  by name in the module's docstring** so a future editor who reaches for a local re-derivation trips
  over something. See T019 (b) for the relocation obligation this pairs with.
- **Q2's binding shape**: **each transport passes its own identifier-set fragment as an ARGUMENT.**
  **Both current `DENIED` strings survive verbatim** — `saas_client` "mission and **decision**
  identifiers", `tracker` "mission and **engagement** identifiers". **This consolidation changes no
  operator-visible text.**
- A "consolidation" that leaves **two templates** and merely parameterises them does **not** satisfy
  FR-008 (SC-015).
- **The module must import from neither `saas_client/` nor `tracker/` at module scope** — that is
  falsifier **F2**'s antecedent, and F2 not firing is why this module is legal at all. It must also
  not import `specify_cli.sync` at module level; FR-013's lazy in-function
  `import specify_cli.sync` (today at `saas_client/egress_consent.py:107-113`) must **stay lazy and
  inside the function**, degrading to a refusal naming the failure.
- **`UNDETERMINED_PROJECT_REFUSAL` stays out of `__all__`** (FR-011 — self-enforcing via the
  dead-symbol ratchet).
- **FR-009 is `[ratchet]`, and its bar is scoped** (ruling PB-3): it covers **the identifiers of the
  project whose consent was refused**. It does **not** cover `team_slug` (the *destination*) or
  `invited_user_ids` (recipient ints). **Corollary, and this is why the scoping is written down:
  nobody may "fix" the refusal string by appending the destination team's name** — that would *add*
  an identifier to an operator-facing message rather than remove one.

### T017 — Delete both `egress_consent.py` modules and rebind the two deciding modules

- Delete `src/specify_cli/saas_client/egress_consent.py` and `src/specify_cli/tracker/egress_consent.py`.
- Rebind `src/specify_cli/saas_client/client.py:23` and `src/specify_cli/tracker/saas_client.py:34`
  to `from specify_cli.egress import project_egress_refusal`.
- The decision points stay where they are: `client.py:157` and `tracker/saas_client.py:329`. **A
  mutation that patches only `specify_cli.egress.…` proves nothing** — see T024.

### T018 — PB-5 carry-through: **five** pointers, and one of them is on the src side

> **Locate these by CONTENT, not by the line numbers below.** WP01 hoisted both guards'
> predicates to module level, which moved every anchor in these two files once already —
> the numbers here were re-measured at `c7e9b0340`+1 and are correct as of that commit, but
> any further edit to either file moves them again. Use:
> ```
> grep -n 'egress_consent' tests/specify_cli/saas_client/test_client_consent_gate_3030.py \
>                          tests/sync/tracker/test_saas_client_consent_gate_3030.py
> ```
> Six hits total: two in-function imports, four prose pointers. If the count is not six,
> stop and reconcile before repointing anything — a missed pointer dangles silently.

1. `tests/specify_cli/saas_client/test_client_consent_gate_3030.py:488` — in-function import, repoint.
   Content anchor: `from specify_cli.saas_client.egress_consent import project_egress_refusal`.
2. `tests/sync/tracker/test_saas_client_consent_gate_3030.py:513` — same.
   Content anchor: `from specify_cli.tracker.egress_consent import project_egress_refusal`.
3. Four **prose** pointers (failure messages and docstrings telling a future reader where the
   precondition is written down): `test_client_consent_gate_3030.py:374` (*"enumerated per site in
   ``saas_client/egress_consent.py``"*) and `:409` (*"saas_client/egress_consent.py for the
   precondition"*); `test_saas_client_consent_gate_3030.py:407` (*"``tracker/egress_consent.py``; a
   prose statement alone…"*) and `:449` (*"tracker/egress_consent.py for the precondition"*).
   **Hand-correct them.** A dangling pointer
   on a consent guard is exactly the campsite debt standing order 2 folds.
4. **`src/specify_cli/saas_client/__init__.py:17`** — the package docstring's *"…(#3030 FR-030); see
   ``egress_consent.py``"*. **It dangles the moment the file is deleted.** The F3 grep above is
   scoped to *importers*, so the src side reads as clear and is not (measured: exactly one hit).

### T019 — Relocate FR-026's enumeration into `egress.py` — **rewriting**, not copying

- The per-site enumeration at `saas_client/egress_consent.py:52-76` relocates into the new module,
  **rewritten for the post-FR-001 state**: item 4 currently argues the `decision widen` entry is
  benign because `decision_id` *"is a ULID rather than a slug"* — **F-B2 falsified that**; there is no
  validation anywhere on the path and the string is interpolated raw into the URL (SC-022).
- **A verbatim move ships pointers to deleted files.** Measured: the cross-references to
  `tracker/egress_consent.py` sit at `:20`, `:48`, `:83` and `:148`, and the enumeration being
  relocated (`:52-76`) sits **between** `:48` and `:83`. **That file is deleted too.** Rewrite the
  surrounding cross-references to point at the consolidated module.
- **SC-022's anchor moves with its content. Say so in the PR body**, or a reviewer reads SC-022 as
  unsatisfiable. This is the one place where the deletion decision costs something the spec did not
  anticipate; it is cheap, but it must not be silent.
- **SC-022 is ungated prose with no verification step of its own** — see the ungated-obligations
  table in `../tasks.md`. Nothing reds if the rewrite is skipped or if it is copied verbatim with the
  falsified item 4 intact. **The reviewer must read the relocated enumeration**; do not report
  "T019 done" as a measurement.

#### T019 (b) — the TRACKER file's rationale: a second relocation, and it is the one nobody enumerated

**T019 above relocates only the SaaS file's enumeration. That leaves 219 lines in
`src/specify_cli/tracker/egress_consent.py` with no destination — and T017 deletes the file.** It is
not a duplicate of the SaaS file; the SaaS file **defers to it in writing** at
`saas_client/egress_consent.py:20` (*"Same reason as `tracker/egress_consent.py`, and the reasoning is
recorded in full there"*) and again at `:48` (*"See `tracker/egress_consent.py` for the full statement
of why this is written down rather than assumed"*). **Four blocks are unique to it:**

| Block | Lines | Why it is load-bearing |
|---|---|---|
| Why the wrapper asks `resolve_egress_consent` instead of resolving consent itself — the C-003 single-chain argument | `:18-45` | **The only written rationale of FR-012**, which is High-impact, `[ratchet]` and **has no SC**. See T016. |
| *"What a refusal is **not** allowed to be"* | `:47-61` | Names the 2026-07-27 incident mechanism: machine-global arming is never a grant; an unresolvable project is a **refusal**. This is the mission's own defect class, stated once. |
| The full attribution-precondition statement | `:63-81` | *"Every construction site must pass the root of the project that owns the record the request will carry"* — with the paragraph explaining **why it is written down rather than left implicit**. The SaaS file quotes the sentence but **not** the reasoning. |
| The **three-site tracker enumeration** | `:83-…` | Covers `bind_mission_origin`'s **non-interactive** reach (mission creation, no operator action), `SaaSTrackerService`'s benign locality argument, and `search_origin_candidates`' *"whoever wires a caller owes this precondition"*. **FR-026/T019's enumeration is the SaaS one; this one is enumerated nowhere else.** |

**Obligation.** Relocate the precondition statement **and** the three-site enumeration into
`src/specify_cli/egress.py` — or name **WP06's ADR** (`docs/adr/3.x/*-egress-consent-boundary.md`) as
their home, **in which case say so in BOTH places**: a forward pointer in `egress.py` and a statement
in the ADR that it is the canonical home of the tracker precondition text. **A destination named in
only one of the two is the dangling-pointer class this carry-through exists to prevent.** Whichever
home you choose, the FR-012 rationale (`:18-45`) goes into `egress.py` per T016 — it is about the
wrapper, not about the ADR.

> **This is not bookkeeping — there is a live dangling pointer riding on it.**
> `tests/sync/tracker/test_saas_client_consent_gate_3030.py:449` reads *"See
> `tracker/egress_consent.py` for the precondition and what falsifies it."* **T018 repoints that
> prose at `specify_cli/egress.py`.** If T019 (b) is skipped, T018 has repointed a reader at a module
> that **does not contain the text** — a dangling pointer created *by* the carry-through that exists
> to prevent them, on a consent guard, in the one file nobody enumerated. **T018 pointer 3 and
> T019 (b) must land together, and the reviewer must open `egress.py` and check the text is there.**

### T020 — SC-004 clause 3: the two `is` comparisons (**the only clause that reds**)

- **File**: new `tests/specify_cli/test_egress_consolidation_3110.py`.

```python
from specify_cli import egress
from specify_cli.saas_client import client as saas_mod
from specify_cli.tracker import saas_client as tracker_mod

assert saas_mod.project_egress_refusal    is egress.project_egress_refusal
assert tracker_mod.project_egress_refusal is egress.project_egress_refusal
```

- **Anti-vacuity, binding**: the comparison must be between **two independently imported names**,
  never two imports of one path — comparing an object to itself proves nothing (R-7).
- **RED-FIRST, AND THE BASELINE RED IS NOT THE DISCRIMINATING ONE. State this in the evidence.**
  Written at `bb2020fea`, this test's baseline failure is `ModuleNotFoundError: No module named
  'specify_cli.egress'` at **collection** — an import error, i.e. **exactly the red-shape this
  mission rejects everywhere else** (rot-mode 2; *"a red whose text is an import or signature error
  is not a kill"*). Write it and observe it, but **do not offer it as the discriminating red**: it
  fires for "the file does not exist yet", which is true of *any* unwritten module and says nothing
  about whether a stale binding would be detected.
  **The discriminating red for clause 3 comes from MUT-2** (T024): the delegating-wrapper mutation
  rebinds `specify_cli.tracker.saas_client.project_egress_refusal` on a **fully consolidated** tree,
  every string observation stays green, and **only the `is` assertion reds**. **Quote MUT-2's
  assertion text as clause 3's proof, and say in the evidence that the baseline red was an import
  error and therefore not a kill.**
- **Why identity and not text.** A surviving re-export renders the **identical correct string**, so
  three correct text observations are what a correct consolidation *and* a stale re-export both
  produce — **text cannot separate them**. Under Q2 the two `DENIED` strings are moreover *supposed*
  to differ, so clause 1 no longer even corroborates clause 3. SC-015 does not close it either: **a
  re-export is not "a second definition."**
- **What identity buys, restated because the older statement was measured false** (PR-1). It does
  **not** make any patch effective: after `specify_cli.egress.project_egress_refusal = mutant`, the
  decision point at `client.py:157` still returns the real string, because `client.py:23` holds its
  own by-value binding. **Rebinding a name is not patching a function.** What identity buys is: it
  **DETECTS a stale binding**, and it converts *"did the consolidation happen?"* from a source scan
  into a runtime assertion. **Identity is a detector, never an actuator** — reading it as the
  actuator is what lets a mutation be applied to one name and reported as a kill.
- **Report SC-004 per clause.** Clauses 1 and 2 are `[ratchet]` and hold of the **unconsolidated**
  state; *"SC-004 passes"* is compatible with the consolidation not having happened (R13). Clause 2
  has one live edge: if the "fully named / no foreign kind" check fails against the Key-Entities sets
  **on the existing text**, that is a real finding — the fix is a wording change and FR-009 returns
  to `[build]`.

### T021 — SC-015: the "a second definition appears" test — **BOTH halves**

**SC-015 is tagged `[standing: the mechanism] + [one-off: the divergence demonstration]`. Both halves
are obligations of this subtask.** An earlier draft mandated only the standing half; that was wrong,
and it mattered more here than anywhere else in the mission: **FR-008 — *exactly one editable
presentation*, the mission's headline requirement — maps to SC-015 ALONE**, and **no mutation in the
suite targets it** (MUT-1 targets SC-016's content pins, MUT-2 targets SC-004 clause 3).

**Read `../spec.md:1450` with its POST-ACCEPTANCE CORRECTION**: SC-015's own text still names
`src/specify_cli/egress/` — a directory. Read it as **`egress.py`**.

**(a) `[standing]` — the mechanism, and its FORM is pinned**

- Same new test module. Name the mechanism enforcing *exactly one editable presentation* and exercise
  it: a test that **fails if a second definition of the template or the four verdict branches
  appears**.
- **MANDATED MECHANISM: an AST (preferred) or text scan over `src/specify_cli/**/*.py` asserting that
  the template and each of the four verdict branches has EXACTLY ONE definition site — reported as a
  count, with the file:line of the site.** Print the **input file count** alongside (rule 5); a scan
  over zero files finds one of nothing and looks like a measurement.
- **NOT a file-absence check. This is the specific laziness being blocked.** The cheapest thing that
  makes the standing half green is:

  ```python
  assert not (SRC / "saas_client" / "egress_consent.py").exists()   # FORBIDDEN as the mechanism
  ```

  It passes forever after T017, and it **can never red on a second definition appearing in a NEW
  file** — which is the only way FR-008 is ever going to be violated in practice. A reviewer seeing
  a `.exists()` assertion offered as SC-015's mechanism must reject it.
- Q2 does not weaken this: per-caller identifier fragments are **arguments**, not second
  presentations.

**(b) `[one-off]` — the divergence demonstration, and it is what proves (a) is not vacuous**

- In a **throwaway `git worktree`** pinned to this WP's head commit, with `PYTHONPATH=$WT/src`:
  **introduce a synthetic second definition** — the simplest honest one is a **new file**, e.g.
  `src/specify_cli/_egress_second_definition_probe.py`, carrying a copy of the template and one
  verdict branch. **A new file, not an edit to `egress.py`** — the new-file case is precisely the one
  a file-absence check cannot see, so it is the case the demonstration must cover.
- Run the standing test and **quote the red's assertion text verbatim** in the mission evidence,
  including the count it reports (*"expected 1 definition site, found 2"* or equivalent) and the
  file:line of the probe.
- **Never committed, never made in the lane worktree** — same discipline as WP01/T008 and WP05/T034.
- A red whose text is an import or collection error **is not a kill** (rot-mode 2). If the probe file
  breaks collection instead of failing the assertion, fix the probe, not the test.

### T022 — SC-016 `DENIED` pins in **both** trees, plus NFR-004's five per-branch pins

- **SC-016**: a content assertion on the merged `DENIED` wording in **each** package's test tree,
  **added alongside** the four existing `could not be determined` assertions — never replacing them
  (`delete-the-assertion-not-the-test`). Deleting the `DENIED` branch must red.
- **SC-010 is a `[ratchet]`**: the four pre-existing assertions pass **unmodified**. They target
  `UNDETERMINED`, a different branch, and they would stay green if the `DENIED` branch were deleted
  entirely — which is why SC-016 exists.
- **NFR-004's five per-branch pins**, all of which already hold at `bb2020fea` (so the build work is
  *pinning* them, and the pins do **not** discriminate a correct implementation from an incorrect
  one): the `DENIED` branch contains `sync opt-in`; the import-failure branch contains the exception
  text; the `NO_RESOLVER` branch names the resolver; and `UNDETERMINED` and `UNANSWERABLE` remain
  **distinguishable from each other** despite both containing `could not be determined` (correction
  C-1). **"Non-empty and distinguishable" is not the bar** — five strings `"denied 1".."denied 5"`
  satisfy that weaker form, which is a defect-masking assertion under DIR-041.

### T023 — C-005 classification + the SC-025 assertion

- Add `"specify_cli.egress"` to `INTEGRATION_PREFIXES`
  (`tests/architectural/test_integration_boundary.py:75-81`). **Measured green today: 0 CORE
  violations over 93 CORE files.** The gate's matcher at `:151-152` is
  `if mod == prefix or mod.startswith(prefix + ".")` — the **`mod == prefix` arm is the module case,
  and it is the first arm**, so no second edit is needed.
- **SC-025**: a standing assertion that **reds if the classification line is removed**. It must
  compare the module's dotted name **as a value against the gate's own prefix list**.
  **`"specify_cli.egress" in ["specify_cli.egress"]` is the vacuous form the criterion forbids.**
- **Why this is gated at all, and why nothing else notices**: `_gate_coverage._src_dir_of_glob`
  returns `None` for any `src/specify_cli/<file>.py` glob and the unclaimed-src-dir worklist iterates
  direct child **directories** — so a module is **structurally outside** that detector *at any size*.
  A package that grew past `T_LOC = 500` would eventually surface there; a module never can. **That
  is the loss of a latent detector, not a strength** — SC-025 is what closes the gap that loss opens.
- The spec's SC-025 and C-005 antecedents were **widened at plan review** to cover a new **module OR
  package**; without that widening the criterion would be vacuous here and the classification would
  silently return to ungated (R2a).

### T024 — MUT-1 and MUT-2 (mandatory evidence)

Both are `pytest` plugins on `PYTHONPATH`. **Rule 11: no source edits during a verification run.**
Write them outside the repository tree; never commit them. **Rule 15: each must fail loudly when its
target is absent.**

**After consolidation the symbol is reachable by exactly THREE names, and it is three by
construction:**

```
1. specify_cli.egress.project_egress_refusal                 <- the ONE definition site
2. specify_cli.saas_client.client.project_egress_refusal     <- by-value rebind (client.py:23)
3. specify_cli.tracker.saas_client.project_egress_refusal    <- by-value rebind (saas_client.py:34)
```

| ID | Mutation | Must red | Must stay green (control) |
|---|---|---|---|
| **MUT-1** | Delete the `DENIED` branch from the consolidated wrapper. **BINDING: the plugin must rebind ALL THREE names above.** Patching only the shared module is **inert at both decision points** and reports "survived", which reads as *"SC-016's content pins do not detect deletion of the `DENIED` branch"* — **a false finding about the gate**, and the exact measured failure PR-1 reproduced. **Report the per-site split `egress=<n>, saas_client=<n>, tracker=<n>`; a zero or uneven split is a finding about the mutation, not about the code.** An aggregate count cannot distinguish "both mutated" from "one mutated, the other inert." | SC-016's content pins in **both** packages' test trees | The four pre-existing `could not be determined` assertions (they target `UNDETERMINED`, a different branch) |
| **MUT-2** | **Plugin form, mandatory**: a `pytest_configure` that rebinds **the deciding module's attribute** — `specify_cli.tracker.saas_client.project_egress_refusal` — to a **delegating wrapper returning the identical string** the real function produces. That reproduces the stale-binding state *behaviourally*. **It must not create, edit or restore any file** — in particular it must not write a `tracker/egress_consent.py` back into the tree. | **SC-004 clause 3's `is` assertion** for the tracker binding | **Every string observation stays green** — clauses 1 and 2 and SC-016 all pass, because the delegating wrapper renders the identical correct string. **That is the point of the mutation**: it demonstrates in the evidence that *text cannot separate the two states and identity can.* |

- **Recover the refusal string from the module under test; never hard-code today's wording**
  (rot-mode 3 — the merged string is precisely what the tests vary).
- **Quote the assertion text of at least one red** (rot-mode 2). A red whose text is an import or
  signature error is not a kill.
- **The primary decision points are `client.py:157` and `tracker/saas_client.py:329` — not the shared
  module** (rot-mode 1). Patch the decision points' names, not only the definition.

## Verification

Isolated single-file runs from the clone root (`#3115` is open):

```
pytest tests/specify_cli/test_egress_consolidation_3110.py          > /tmp/wp03-clause3.txt 2>&1
pytest tests/specify_cli/saas_client/test_client_consent_gate_3030.py > /tmp/wp03-saas.txt 2>&1
pytest tests/sync/tracker/test_saas_client_consent_gate_3030.py     > /tmp/wp03-tracker.txt 2>&1
pytest tests/architectural/test_integration_boundary.py             > /tmp/wp03-integ.txt 2>&1
```

Rule 1: file, not pipe; **quote the `N passed` line**. Rule 13: **never `ruff format`** — `ruff check`
only. Rule 14: **explicit-path staging; never `git add -A`** (the parent mission's commit
`2e6aa1d78f` swallowed eight files that way).

**SC-021 is a `[ratchet]` — no new test is required.** The two existing tests in
`tests/sync/tracker/test_saas_client_consent_gate_3030.py` —
`test_unconsented_project_transmits_no_engagement_name` (`:258-289`) and
`test_project_local_refusal_is_honoured` (`:311-324`) — already drive the tracker into a
non-consenting project **through the call at `saas_client.py:329`**. **The requirement is that they
keep passing**; a change that leaves only the import at `:34` reds them. Quote both as passing.

**Re-run WP01's live scan after the move** and quote `SaasClient scanned=4 unattributed=0` /
`SaaSTrackerClient scanned=3 unattributed=0` with the input file count — **placement cannot shrink
coverage** (both guards `rglob` the entire `src/specify_cli` tree), so a moved count is a finding.

## Bundle A bite

- **`#3113`** — if the consolidation **moves a sink** or reaches the wrapper through an alias, the
  *boundary* guard (`test_egress_consent_boundary.py`) may not see it: its `_transmits_a_body`
  derives `kwargs` solely from `node.keywords`. **Any green from that guard on a moved sink must be
  reported as conditional on `#3113`.** Do not scope a fix for it here.
- **`#3115`** — only the isolated runs above are trustworthy greens.

## Risks

- **R1 — partial consolidation.** A surviving re-export leaves a deciding module bound to the old
  object and **renders the identical correct string**. C-004 does not catch it (substring, satisfied
  by `:34` alone); SC-015 does not (a re-export is not a second definition). **SC-004 clause 3 is the
  only thing that reds**, and MUT-2 must demonstrate in the evidence that *every string observation
  stays green* on the mutated state. Structurally reduced by the PB-5 deletion — the collapse route
  has no file to live in.
- **R2a — the C-005 line is forgotten.** Not fail-safe: forgetting it makes the boundary gate
  *permissive*. SC-025 is the gate.
- **R7 — mutation rot.** Rule 15 plus the per-site split.
- **R13 — a green SC-004 is over-read.** Report per clause, always.

## Review guidance

- Is `src/specify_cli/egress.py` a **file**? If a directory appeared, reject — and check whether a
  third `is` comparison landed with it.
- Are there **two** `is` comparisons between **independently imported** names, or two imports of one
  path?
- Did MUT-1's plugin patch **all three** names, and is the **per-site split** in the evidence?
- Are the four pre-existing `could not be determined` assertions **untouched**?
- Did all **five** PB-5 pointers get repointed — including the **src-side** one at
  `saas_client/__init__.py:17`?
- Does the relocated FR-026 enumeration still name a deleted file?
- **T019 (b):** open `src/specify_cli/egress.py` (or the ADR, if that is the declared home) and
  confirm the **tracker** file's precondition statement and **three-site enumeration** are actually
  there. `test_saas_client_consent_gate_3030.py:449` now points at `egress.py`; if the text is not
  there, T018 has created a dangling pointer rather than repaired one.
- **T021:** is SC-015's standing mechanism an **AST/text scan counting definition sites**, or a
  `.exists()` file-absence check? Was the **`[one-off]` red quoted**, taken by adding a **new file**?
- **T020:** is the discriminating red taken from **MUT-2**, and does the evidence say the baseline red
  was a `ModuleNotFoundError` and therefore not a kill?
- **`transmitted_text` must remain an importable module-level symbol** in
  `tests/specify_cli/saas_client/test_client_consent_gate_3030.py` (defined at `:73`). **WP04/T028
  imports it across lanes** — `lane-c` depends only on `lane-b`, so nothing serialises WP04 behind
  this WP and nothing else would catch its loss. Do not inline it, rename it, or move it inside a
  test function while editing this file. *(No dependency edge is added for this: the symbol already
  exists at the base commit, and adding one would serialise the P1 package behind the consolidation.)*

## Activity Log

- 2026-07-31T00:00:00Z – system – Prompt created.

## Carried residuals (round-2 review, both agreed by implementer and reviewer)

Neither was a condition of approval. Recorded here because a residual nobody
writes down is indistinguishable from one nobody found.

**R-1 — SC-015's scan scope is a spec/contract divergence, not a deviation.**
`test_egress_consolidation_3110.py` scans `src/specify_cli/**/*.py`. SC-015's
POST-ACCEPTANCE CORRECTION (`spec.md:1477`) mandates a scan over **`src/`**; this
WP's T021(a) narrows it to `src/specify_cli/**`. The implementer followed its
binding instruction — the narrower of the two. **Measured as currently empty:**
the six other top-level `src/` packages (`charter`, `doctrine`, `glossary`,
`kernel`, `mission_runtime`, `runtime`) carry no egress policy today, so a second
definition could only hide there in principle. Reconcile when SC-015's text is
next touched; do not silently widen or narrow either surface in the meantime.

**R-2 — three dangling pointers left in another mission's inventory.**
`docs/development/process-global-inventory-3115.md:141`, `:164` and `:398` cite
`src/specify_cli/tracker/egress_consent.py:147-190` and `:158-159`. T017 deletes
that file. The document is a **Bundle A** artifact (mission
`verification-trust-3115-01KYVYWM`, PR #3144) — a point-in-time inventory outside
this WP's `owned_files`, and outside falsifier F3's `--include=*.py` scope by
design, which is why F3 reported clear. Needs a follow-up against Bundle A's
record rather than an edit here.

**Standing limit, not a residual.** Every claim that this module's gate *runs*
rests on `_gate_coverage`'s parse of the workflow YAML plus a reading of the job's
`if:` at `ci-quality.yml:1596-1598`. They agree, and the model evaluates markers
with pytest's own `Expression`, but **no CI run was observed**. WP07 watching the
`specify-cli-rest` shard actually execute closes the last inch.
