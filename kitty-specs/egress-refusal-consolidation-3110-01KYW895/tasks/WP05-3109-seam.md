---
work_package_id: WP05
title: 'The #3109 seam: keep register_saas_client_factory and pin its export'
dependencies: []
requirement_refs:
- FR-018
- FR-019
planning_base_branch: pr/egress-refusal-consolidation-3110
merge_target_branch: pr/egress-refusal-consolidation-3110
branch_strategy: Planning artifacts for this mission were generated on pr/egress-refusal-consolidation-3110. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into pr/egress-refusal-consolidation-3110 unless the human explicitly redirects the landing branch.
base_branch: kitty/mission-egress-refusal-consolidation-3110-01KYW895
base_commit: e5aba4109b085866139cd543fcc0223d9110848a
created_at: '2026-08-03T11:07:03.885162+00:00'
subtasks:
- T033
- T034
phase: Phase 1 - Independent
assignee: ''
agent: ''
shell_pid: ''
history:
- Generated by /spec-kitty.tasks 2026-07-31
agent_profile: implementer-ivan
authoritative_surface: src/specify_cli/invocation/
create_intent: []
execution_mode: code_change
model: ''
owned_files:
- src/specify_cli/invocation/adapters.py
- src/specify_cli/invocation/__init__.py
- tests/invocation/test_adapters.py
role: implementer
tags: []
task_type: implement
tracker_refs: []
---

# Work Package Prompt: WP05 – The `#3109` seam: keep and pin

## ⚡ Do This First: Load Agent Profile

Run `/ad-hoc-profile-load implementer-ivan` and adopt its identity, boundaries and governance scope
before parsing the rest of this prompt.

## Objective

Decision **D-1** resolved `#3109`'s residual question: **keep** `register_saas_client_factory`, pin
its **export**, and correct its stale docstring — so the empty seam reads as a decision rather than
an oversight.

**Read first**: `../spec.md` → **SC-008**, **SC-023**. `../plan.md` → **IC-05**, and
§Verification Strategy **rule 7** (a plain revert may not be a valid before-state).

**Discharges**: **SC-008**, **SC-023**. **Requirements**: FR-018, FR-019.
**Depends on**: nothing. **Blocks**: nothing. Fully independent — may land at any time, in parallel
with everything else. Two cheap edits and one pin.

## Subtasks

### T033 — FR-018: hand-edit the docstring, and do not go near its twin

**File**: `src/specify_cli/invocation/adapters.py`, `register_saas_client_factory` (`:130-145`).

Three changes, exactly:

1. **Delete the false sentence at `:135`** — *"Called once at sync package startup."* Nothing calls
   it.
2. **State that nothing registers a factory today.**
3. **Point to `propagator._get_saas_client` (`propagator.py:70-83`) as the canonical record** of the
   `request_text` hazard — **do not restate it.** A second independently-editable copy of one
   rationale is the exact defect FR-008 exists to remove (A-9).

> **TRAP — R8, and it is a one-line grep away from happening.** The **identical sentence** appears at
> `adapters.py:113`, on the sibling registrar `register_egress_consent_resolver` — **where it is
> TRUE**, because sync *does* register that resolver. **A grep-and-replace across `adapters.py` would
> break a correct docstring.** Edit by hand, never by grep. **SC-023 asserts `:113` is unchanged.**

**SC-023's full shape**: `register_saas_client_factory`'s docstring **contains `request_text`** and
**does not contain "Called once at sync package startup"**; **and** `adapters.py:113` is
**unchanged**.

### T034 — FR-019: pin the **export half only**, and demonstrate the red

**File**: `src/specify_cli/invocation/__init__.py` — the re-export at `:21` and the `__all__` entry at
`:111`. **File** for the pin: `tests/invocation/test_adapters.py` (it already imports the symbol at
`:29`).

**SC-008 is `[standing]` pin + `[one-off]` removal demonstration.**

> **RULE 7 — a plain revert may NOT be a valid before-state, and this is the worked example.**
> **Deleting the `def` is a collection-time `ImportError` in two test files**
> (`tests/invocation/test_adapters.py:29`,
> `tests/specify_cli/invocation/test_propagator_consent_gate_3030.py:53`) **and is pinned by a
> node-id baseline** (`fast-tests-core-misc-nodeids.txt:1841`). **It is not a valid before-state** —
> an implementer working red-first would find the red already present and land a pin with **no
> discriminating power**.
>
> **Only the export half is unpinned.** The before-state is **removing `invocation/__init__.py:21`
> and `:111`, and nothing else** (D-4).

- **The export half is genuinely unpinned today, independently verified in spec round 2**:
  `invocation/adapters.py` has **zero** `__all__` declarations; `grep "from specify_cli.invocation
  import" src/` returns **zero** hits; and `test_all_declarations_required.py:1-20` gates only
  `src/charter/` and `src/kernel/`.
- **The `[one-off]` demonstration**: actually remove `:21` and `:111` in a **throwaway `git worktree`**
  pinned to this WP's head commit, with `PYTHONPATH=$WT/src`, run the pin, and **quote the red's
  assertion text**. Never commit the removal; never make it in the lane worktree.

## Verification

```
pytest tests/invocation/test_adapters.py > /tmp/wp05-adapters.txt 2>&1
```

Rule 1: write to a file, read the file, **quote the `N passed` line**; the exit code is noise.
Rule 5: state the input count — this is a two-file WP, so say which two.
Rule 13: **never `ruff format`.** Rule 14: **explicit-path staging; never `git add -A`.**

**Do not chase this pre-existing red** (it is on exactly this surface and its text reads like the
defect under repair): running `tests/specify_cli/invocation/test_propagator_consent_gate_3030.py`
**before** `tests/specify_cli/saas_client/test_client_consent_gate_3030.py` in one process fails with
*"no hosted-sync consent resolver is registered"* — a fixture-teardown ordering artefact,
deterministic in alphabetical order, **not a gate defect**.

## Bundle A bite

- **`#3115`** — the isolated single-file run above is the trustworthy green; a full-suite green on
  this surface is not attributable. Hand the file to WP07's SC-009 list.
- `#3113` does not bite this WP at all.

## Risks

- **R8 — the grep-and-replace breaks a correct docstring** at `adapters.py:113`. **Medium.**
  Mitigation: hand-edit, and SC-023's assertion.
- Landing a pin with no discriminating power by using the wrong before-state. Mitigation: rule 7
  above; the demonstration must remove **only** the export.

## Review guidance

- Is `adapters.py:113` **byte-identical** to `bb2020fea`?
- Does the new docstring **point at** `propagator._get_saas_client` rather than restating the
  `request_text` hazard?
- Was the `[one-off]` red taken by removing **only** `__init__.py:21` / `:111` — not by deleting the
  `def`?

## Activity Log

- 2026-07-31T00:00:00Z – system – Prompt created.
