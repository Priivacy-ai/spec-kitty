---
work_package_id: WP07
title: Cross-cutting verification evidence (3.11 run, isolation runs, friction tracer)
dependencies:
- WP01
- WP02
- WP03
- WP04
- WP05
- WP06
requirement_refs:
- NFR-004
- NFR-005
- NFR-006
planning_base_branch: pr/egress-refusal-consolidation-3110
merge_target_branch: pr/egress-refusal-consolidation-3110
branch_strategy: Planning artifacts for this mission were generated on pr/egress-refusal-consolidation-3110. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into pr/egress-refusal-consolidation-3110 unless the human explicitly redirects the landing branch.
subtasks:
- T037
- T038
- T039
phase: Phase 4 - Evidence
task_type: implement
execution_mode: planning_artifact
agent_profile: implementer-ivan
role: implementer
agent: ""
model: ""
assignee: ""
shell_pid: ""
authoritative_surface: docs/plans/engineering-notes/
owned_files:
- docs/plans/engineering-notes/01KYW895-verification-evidence.md
- docs/development/3-2-page-inventory.yaml
- docs/adr/3.x/README.md
- docs/development/3-2-docs-retrieval-index.yaml
create_intent:
- docs/plans/engineering-notes/01KYW895-verification-evidence.md
tags: []
tracker_refs: []
history:
- Generated by /spec-kitty.tasks 2026-07-31
---

# Work Package Prompt: WP07 – Cross-cutting verification evidence

## ⚡ Do This First: Load Agent Profile

Run `/ad-hoc-profile-load implementer-ivan` and adopt its identity, boundaries and governance scope
before parsing the rest of this prompt.

## Objective

Produce the evidence NFR-005 and NFR-006 require **in the stated form**, and record which greens are
conditional on Bundle A so the successor can tell which are trustworthy.

**This WP writes no source and no tests.** It runs what the other packages built, quotes it, and
records the friction. **If a run reds, hand it back to the owning WP — do not fix it here.**

> **"Hand it back" is a MECHANISM, not a sentiment. Use this route.** Reopen the owning work package
> with `spec-kitty agent tasks move-task <WPID> --to blocked`, giving the **evidence ref** (the
> output file path and the quoted assertion text) as the reason — never by editing frontmatter, and
> never by silently omitting the failing file from T038's list. If the owning WP is already `done`
> or `approved`, or if the failure spans two lanes, **escalate to the operator** with the same
> evidence rather than reopening unilaterally. **A red that this WP absorbs — fixed here, worked
> around, or dropped from the file list — is exactly the "unstated set" R-22 names**, and it lands in
> the one artifact whose whole purpose is to be trusted by a successor.

**Read first**: `../spec.md` → **SC-009**, **SC-014**, NFR-004's five pins. `../plan.md` → **IC-07**,
§Verification Strategy rules 1, 2, 5, 12, and the R13 per-clause reporting rule.

**Discharges**: **SC-009**, **SC-014** (`[one-off]` half — the 3.11 run; the `[standing]` test itself
is WP04/T030). **Requirements**: NFR-004 (per-clause reporting), NFR-005, NFR-006.
**Depends on**: all.

**Where the evidence lives (read before T039).** This WP's owned deliverable is
**`docs/plans/engineering-notes/01KYW895-verification-evidence.md`** — a real, git-tracked,
PR-reviewable file, not a kitty-specs/ dossier edit.

> **The path moved, and the reason is a second blocking gate — do not move it back to
> `docs/01KYW895-closeout/`.** `src/doctrine/styleguides/built-in/common-docs.styleguide.yaml`
> routes point-in-time content to **`plans/engineering-notes/`** (`:114-119`) and allowlists only
> `adr/**`, `plans/research/**`, `plans/investigations/**`, `plans/**` for point-in-time placement
> (`:137-141`). A new **top-level** `docs/01KYW895-closeout/` is in **neither**, so it fails
> `point_in_time_placement`. The same styleguide requires **`doc_status` and `updated`** frontmatter
> on every in-scope page (`:144-146`), and the only exclusions are `**/README.md` and `adr/**`
> (`:150-159`) — a closeout page under `docs/` is **not** excluded. The repo's own precedent is
> `docs/plans/engineering-notes/883-*.md` (and the `01KSMG8Y-closeout/` directory alongside it).
> `docs_structural_lint` measured **685 pages, 0 violations** at baseline; landing this file at the
> old path would be the first.

**Do not create
`kitty-specs/egress-refusal-consolidation-3110-01KYW895/tracer-tooling-friction.md` from this WP's
lane.** `finalize-tasks` rejects *any* WP `owned_files` entry under `kitty-specs/` unconditionally —
the check does not exempt `execution_mode: planning_artifact` — and the underlying reason is the
same one that produced that rejection: **a lane branch cannot commit dossier edits.** If the mission
dossier still wants a canonical `tracer-tooling-friction.md` per charter standing order 3, that file
is created or updated by the **orchestrator on the mission branch**, after this WP merges, sourced
from this WP's `docs/plans/engineering-notes/01KYW895-verification-evidence.md` and the other WPs' PR
bodies — that
step is explicitly **outside this WP's scope**. A successor picking this file up should not re-add a
`kitty-specs/` `owned_files` entry to "fix" this; it was tried, and it is what `finalize-tasks`
rejects.

**This WP also owns two files it writes nothing original into**: `docs/development/3-2-page-inventory.yaml`
and `docs/adr/3.x/README.md`. They are here because they are a **1:1 docs lockfile and its ADR index**
— shared, regenerated surfaces that **two lanes cannot both own**. WP06 adds an ADR and edits two
`docs/context/` pages and **deliberately lands lockfile-dirty**; this WP adds the evidence page.
**This WP is the reconciler, and it is last for exactly that reason.** See T039. Lane branches merge
locally and the mission opens a single PR after this, the final package, reconciles — so no CI ever
saw WP06's dirty state along the way. Note that `.github/workflows/docs-freshness.yml` is the **one**
workflow with no base-branch filter, so it **would** red a per-lane PR — no lane opened one, and none
reached for a skip label.

## Subtasks

### T037 — NFR-006 / SC-014 `[one-off]`: the Python 3.11 run

- **Rule 12**: only **3.14** is installed locally; **CI runs 3.11/3.12**. `uv venv --python 3.11` is
  available and **mandatory** here. A plan-phase judgement about version-divergent branches does not
  satisfy NFR-006 — *"asking the implementer to identify divergent branches in the plan is a
  judgement made on the wrong interpreter and is not verification."*
- **What to run**: this mission's touched test files **plus both attribution guards**, under the 3.11
  venv. **Quote the resulting `N passed` line verbatim.**
- **THE RUN MUST INCLUDE WP04's UNREADABLE-LEDGER TEST (T030), AND YOU MUST NAME IT IN THE
  EVIDENCE.** *"A green 3.11 run over files that never touch the branch is a true statement about
  nothing."*
  - The test's shape must be the one that can reach the branch: **chmod the containing `decisions/`
    directory to `0o000`, file left readable.** The `file=0o000` shape returns an **identical** result
    on 3.11 and 3.14 — and the honest reading of an identical result is *"no divergence, NFR-006
    discharged"*, a **false negative in this mission's only portability gate**.
  - What it asserts is **`resolve_decision_ownership`'s OUTCOME** — *not established*, unreadable flag
    set, **no exception escaping** — **on both interpreters**. Not `Path.exists()`'s return value:
    that is a `pathlib` characterization test, true whether or not `ownership.py` carries the
    `except OSError`, therefore green on **precisely the regression this exists to catch**.
  - A `file=0o000` **companion** is permitted only if labelled as `read_text` → `PermissionError` at
    `store.py:66` and explicitly **not** offered as NFR-006 evidence.
  - **Skip honestly**: if the process can read regardless of mode bits (root, or a filesystem
    ignoring them), the test must **skip with a stated reason**. **A silently-passing `0o000` test is
    the vacuous case.** If it skips, say so — a skip is not a discharge of NFR-006, and the evidence
    must state that plainly rather than quoting the `N passed` line as if it were.
- **Rule 1**: write full output to a file and read the file. **`pytest … | tail` reports `tail`'s
  exit status**, and `tail` buffers until exit so the output file reads empty meanwhile. **Quote the
  `N passed` line as the evidence; the exit code is noise. An empty output file is no measurement.**
- **Rule 2**: a killed run is neither a pass nor a fail. Re-run **narrowed**; do not explain it.
  `exit 143` on a `timeout N … | tail` pipeline is triply ambiguous — check elapsed-time-vs-timeout
  before attributing anything. **`pytest.ini` still has no `--timeout` (FU-6, re-measured OPEN), so a
  hang consumes a run rather than failing it.**
- **Environment**: `pytest` from the clone root is safe (`pytest.ini` sets `pythonpath = src`). **Any
  other python invocation must set `PYTHONPATH=/home/jeroennouws/dev/sk-missions/3110/src`** — a bare
  `python3 -c "import specify_cli"` resolves to a **different checkout**, concurrently edited.

### T038 — NFR-005 / SC-009: isolated single-file runs

- Every test **added or changed** by this mission passes both inside a full run **and** as an
  **isolated single-file invocation from this clone's root**.
- **THE EVIDENCE MUST STATE THE COUNT OF FILES AND LIST THEM.** *"All isolated runs passed"* over an
  unstated set is **vacuous** — an empty set satisfies the bare claim (R-22). This is rule 5 applied
  to the mission's own evidence.
- Expected file list (confirm against the actual diff — do not transcribe this one blind):

  | File | From |
  |---|---|
  | `tests/specify_cli/saas_client/test_client_consent_gate_3030.py` | WP01, WP03 |
  | `tests/sync/tracker/test_saas_client_consent_gate_3030.py` | WP01, WP03 |
  | `tests/architectural/test_gate_coverage.py` | WP02 |
  | `tests/architectural/test_integration_boundary.py` | WP03 |
  | `tests/specify_cli/test_egress_consolidation_3110.py` | WP03 |
  | `tests/specify_cli/decisions/test_ownership_3111.py` | WP04 |
  | `tests/specify_cli/saas_client/test_decision_widen_ownership_3111.py` | WP04 |
  | **`tests/specify_cli/cli/commands/test_decision_widen_subcommand.py`** | **WP04 (T027 (b)) — pre-existing, CHANGED, not new** |
  | `tests/invocation/test_adapters.py` | WP05 |

- **The `test_decision_widen_subcommand.py` row is the one most likely to be dropped, so it is
  labelled.** It is a **pre-existing green module** that WP04 must change: its
  `DECISION_ID = "01KWIDETEST00000000001"` fixture (`:45`) is **22 chars and contains an `I`**, so it
  fails the FR-005 ULID check, and its nine `patch(… SaasClient.from_env …)` live-path tests run
  against a `tmp_path` that owns no ledger, so they hit the new ownership refusal. **Measured
  baseline: `28 passed in 56.40s`, 28 items, 1 file.** *"Every test **added or changed**"* includes
  changed ones — a file that only appears in the diff as fixture corrections is exactly the file an
  isolated-run list forgets. **Quote its `N passed` line like any other.**

- **Why isolation is the compensation and not belt-and-braces**: `#3115` (shard-parallel isolation) is
  **OPEN**, so full-suite reds on this surface are **not attributable**. The isolated runs are the
  only trustworthy greens this mission can produce.
- **Attribute before you fix.** A broad run will show red that is **not this mission's**. The
  do-not-chase list is in `../tasks.md`; in particular
  `tests/specify_cli/invocation/test_propagator_consent_gate_3030.py` run **before**
  `tests/specify_cli/saas_client/test_client_consent_gate_3030.py` in one process fails with *"no
  hosted-sync consent resolver is registered"* — a fixture-teardown ordering artefact, deterministic
  in alphabetical order. **On a consent mission that failure text reads exactly like the defect under
  repair. Do not chase it, and do not green-wash it.**

### T039 — The friction tracer, and the conditionality ledger

- **Create `docs/plans/engineering-notes/01KYW895-verification-evidence.md`** — this WP's owned
  deliverable (see "Where the evidence lives" above). It carries the friction tracer and the
  conditionality ledger that charter standing order 3 asks for; it is **not** the `kitty-specs/`
  dossier file, and it is the only home for this content this WP is permitted to write.
- **The page MUST carry two frontmatter fields, or it reds `frontmatter_contract`**
  (`common-docs.styleguide.yaml:144-146`; exclusions at `:150-159` are `**/README.md` and `adr/**`
  **only**, neither of which covers this page):

  | Field | Value |
  |---|---|
  | `doc_status` | **`closeout`** (or `point_in_time`) — both are recognised `point_in_time_markers` at `:129-133`, which is what lets a non-dated basename pass `point_in_time_placement` |
  | `updated` | the date this evidence was taken |

- **RECONCILE THE DOCS LOCKFILE — this WP is the only lane that may, and WP06 is depending on it.**
  `docs/development/3-2-page-inventory.yaml` is a **1:1 lockfile over every `docs/**/*.md`**
  (baseline measured clean: `inventory_lockfile: exit=0 generated=685 committed=685 drift=False`;
  `docs_structural_lint: 685 pages, 0 violations`), and **every** drift row is
  `severity="error"` (`scripts/docs/check_docs_freshness.py:742-758`), run by
  `.github/workflows/docs-freshness.yml:56-59`. **Two pages are added by this mission and both rows
  are yours**:
  1. **WP06's ADR — the index table.** Run
     `python scripts/docs/freshen_adr_inventory.py docs/adr/3.x/<the-adr>.md` per
     `docs/adr/3.x/README.md:9-10`, which adds the row to the ADR index table in that README (and
     also touches the lockfile row for that one file). **Take the ADR's exact filename from WP06's PR
     body** — WP06 is required to hand it over; if it is not there, ask rather than guess. **If the
     filename is genuinely unavailable, run `freshen_adr_inventory.py --all` instead** — it
     auto-detects every ADR missing from its era README table and regenerates every row and the whole
     lockfile without needing a filename. **Do not guess a filename.**
  2. **Both rows, in one pass.** Run
     `python scripts/docs/inventory_lockfile.py --write docs/development/3-2-page-inventory.yaml` —
     this is what the failing check's own `suggested_action` at `check_docs_freshness.py:756-757`
     tells you to run, and it covers **both** new pages' lockfile rows (the ADR's and this evidence
     page's) in a single write. `freshen_adr_inventory.py` (item 1) is needed only for the ADR
     **index table**; `freshen_adr_inventory.py` accepts only ADR paths under `docs/adr/<era>/`
     (explicit out-of-tree rejection at `:116-121`) and will refuse this evidence page's path.
  Then confirm clean and **quote it**: `generated=<n> committed=<n> drift=False`, with `<n>` equal to
  `685 + 2` unless another page landed, in which case say which. **`drift=False` is the evidence;
  the exit code is noise** (rule 1). **Do not weaken, skip, or allowlist the freshness check to get
  there** — a lockfile made green by exempting the rows is the vacuous case.
- **Consolidate the friction entries every other WP recorded** in its own Activity Log and PR body.
  Record measurement failures, not just outcomes: killed runs, empty output files, probes that needed
  a control, `PYTHONPATH` mistakes, mutation plugins whose target was absent.
- **Record the conditionality of every green**, so the Bundle A successor can tell what to re-take:

  | Evidence | Conditional on |
  |---|---|
  | Any full-suite green on this surface | **`#3115`** — not attributable. Only the T038 isolated runs count. |
  | Any green from `test_egress_consent_boundary.py` on a **moved sink** (WP03) | **`#3113`** — `_transmits_a_body` derives `kwargs` solely from `node.keywords`. |
  | SC-006's `[one-off]` CI observation (WP02/T015) | **Necessarily post-merge** — `ci-quality.yml` is itself a `core_misc` glob member, so a `core_misc`-green run on the mission PR is a **tautology, not evidence**. |
  | A killed or timed-out run | **FU-6** — no global `pytest` timeout; a hang consumes a run. |

  The attribution guards are **not** bounded by `#3113` (R-12/D-10) — say so, so the successor does
  not re-take work that was never conditional.
- **Rule 14, corrected**: an earlier draft of this WP instructed creating the friction tracer under
  `kitty-specs/`, reasoning that `execution_mode: planning_artifact` routes the WP to the
  repository-root checkout and so avoids the "no lane-branch dossier edits" rule. That reasoning was
  wrong: `finalize-tasks`'s `owned_files`-under-`kitty-specs/` rejection is unconditional and does not
  key off `execution_mode` — the earlier `owned_files` entry naming the `kitty-specs/` path was
  rejected outright, mission-wide, before any lane distinction mattered. **Write the evidence to
  `docs/plans/engineering-notes/01KYW895-verification-evidence.md` only.**

## The reporting discipline this WP owns (NFR-004, R13)

**Report per clause, never as one verdict.** Three criteria in this mission are compatible with the
work not having been done:

- **SC-004** — clauses 1 and 2 are `[ratchet]` and hold of the **unconsolidated** state. **Only
  clause 3 discriminates.** *"SC-004 passes"* is not evidence the consolidation happened.
- **SC-010** — the four `could not be determined` assertions were already passing at `bb2020fea`.
- **SC-021** — the two tracker behavioural tests were already passing; the requirement is that they
  **keep** passing.
- **NFR-004's five per-branch pins** — `DENIED` contains `sync opt-in`; the import-failure branch
  contains the exception text; `NO_RESOLVER` names the resolver; `UNDETERMINED` and `UNANSWERABLE`
  remain **distinguishable** despite both containing `could not be determined` (correction C-1).
  **All five already hold at `bb2020fea`.** NFR-004 is a `[ratchet]`: the build work is pinning them,
  **and the pins do not discriminate a correct implementation from an incorrect one.**

Where a criterion is a **grep-gate** (SC-019, SC-020), report the presence result **and** state that
content is a PR-review item. Do not let a green stand in for a reading.

## Verification of this WP itself

There is nothing to test here — the deliverable **is** the evidence. It is complete when:

1. The 3.11 `N passed` line is quoted **verbatim**, and **T030's test is named as included** (or its
   skip reason is stated).
2. The isolated-run list is stated **with its count**, and every file in it passed — **including
   `tests/specify_cli/cli/commands/test_decision_widen_subcommand.py`**, the changed pre-existing
   module.
3. `docs/plans/engineering-notes/01KYW895-verification-evidence.md` exists **with `doc_status` and
   `updated` frontmatter**, carries the other WPs' friction entries, and carries the conditionality
   ledger.
4. Every `[ratchet]` criterion in the mission evidence is reported **per clause**.
5. **The docs lockfile is reconciled**: both new pages have rows, the ADR has its index row in
   `docs/adr/3.x/README.md`, and `drift=False` is quoted with its `generated=`/`committed=` counts.

## Bundle A bite

**This WP is where Bundle A's cost is written down.** It does not fix `#3113` or `#3115` — this
mission halts at design for both. Its job is to leave a record precise enough that whoever lands
Bundle A knows exactly which greens to re-take and which were never conditional.

## Risks

- **R9 — `#3115` is open**: a full-suite red on this surface is not attributable. NFR-005's isolated
  per-file runs are the compensation, **and an unstated file set satisfies the bare claim**. State the
  set. **Medium.**
- **R11 — a hang consumes a run.** Narrow the scope rather than raising the timeout; a killed run is
  re-run, not explained. **Low.**
- **R13 — a green `[ratchet]` is over-read.** The whole per-clause discipline above. **Medium.**
- **R3's residue** — a 3.11 green over the wrong test shape is a true statement about nothing. Name
  the test. **High.**

## Review guidance

- Is the 3.11 `N passed` line quoted **verbatim**, and is **T030's test named** in it?
- Is the isolated-run set **stated with a count**, or is it "all isolated runs passed"?
- Does any evidence quote an **exit code** instead of an `N passed` line, or a `| tail` pipeline?
- Are any pre-existing reds folded into this mission's scope? They must not be. Conversely: is any
  **red this mission caused** absorbed here instead of handed back via `move-task … --to blocked`?
- Does the evidence page live at `docs/plans/engineering-notes/`, and does it carry **`doc_status`**
  and **`updated`**?
- Is the lockfile reconciled with **`drift=False` and its counts quoted**, and does
  `docs/adr/3.x/README.md` carry the ADR's index row — or was the freshness check skipped?
- Does the conditionality ledger distinguish what `#3113` **does** bound (the boundary guard) from
  what it **does not** (the attribution guards)?

## Activity Log

- 2026-07-31T00:00:00Z – system – Prompt created.
