# Post-plan COVERAGE/CI lens — sync-deactivate-by-default-01M16M1P

## Task 1 — Emission-site completeness (spot-checked 4)
Spot-checked: init, merge, next, doctor.
- **init.py:145-152** calls `get_emitter().emit_build_registered()` DIRECTLY (emitter method, not events.py). All 30 emitter `emit_*` methods funnel through `EventEmitter._emit` (emitter.py:2174).
- **next / status commands** fan out via `emit_status_transition` → `fire_saas_fanout`/`fire_lifecycle_saas_fanout` (status/adapters.py) → registered handlers `_saas_fanout_handler`/`_lifecycle_saas_fanout_handler` (sync/__init__.py:213/220), registered ONLY by `register_default_handlers` (gated on the seam). Covered.
- **doctor** daemon path (`run_orphan_daemons`/`run_restart_daemon`) → daemon spawn, covered by daemon seam.
- **merge** `_emit_merge_diff_summary` → emitter `emit_diff_summary_recorded` → `_emit`.

### LOAD-BEARING GAP — seam line is wrong for the direct-emitter path
Plan Decision #1 / mermaid gate local-capture at **emitter.py:2648**, which is inside `_route_event` (def emitter.py:2633). But **`_emit` writes to the journal/outbox BEFORE `_route_event`**: `_emit` calls `_capture_to_journal` (emitter.py:2280) which calls `_queue_event_locally` (emitter.py:2166→2651) DIRECTLY, and the missing-`project_uuid` branch (~emitter.py:2308) also calls `_queue_event_locally` directly — both **bypass `_route_event`**. Gating only `_route_event` (2648) still persists events to disk on the inactive path via the direct-emitter path (init.py:151, merge diff-summary, any `get_emitter()` caller).
**Fix:** gate at the top of `_emit` (single funnel for all emit_* methods) OR at `_queue_event_locally` (emitter.py:2651, single funnel for ALL local writes) — NOT `_route_event`. The events.py daemon-publish path (`_publish_event_via_sync_daemon`, events.py:159/182) is already env-gated and will be swapped, so it is fine; the emitter local-capture funnel is the miss.

## Task 2 — CI opt-in job (FR-010)
- Existing job: **`fast-tests-sync` (.github/workflows/ci-quality.yml:1108)**; test step at **:1135-1146** runs `pytest tests/sync/ -m "fast and not windows_ci"`. Runs on push (`if: sync=='true' || github.event_name=='push'`, :1112-1114).
- **Concrete hook:** add a job-level or step-level `env: { SPEC_KITTY_ENABLE_SAAS_SYNC: "1" }` to `fast-tests-sync` (at the run step ~:1135) so `tests/sync/` is exercised in the ARMED state. To also exercise default-off, the *rest* of the suite (now default-off after conftest invert) covers the skip path. Any NEW opt-in job MUST carry `github.event_name == 'push'` in its `if` or the completeness gate will flag its nodes as uncollectable.
- **Collection-completeness keys on markers, not skip state — CONFIRMED.** `tests/architectural/test_ci_collection_completeness.py` (issue #2957) works on the `--collect-only` universe (`gc.collect_universe()`, marker set per node). `pytest.mark.skipif` is a RUNTIME skip; skipif-gated files stay COLLECTED and must remain routed by marker/path. Occurrence_map `keep_lane_markers: true` satisfies this — the `fast`/`integration` markers keep `tests/sync/` routed to `fast-tests-sync`. The marker-routing guard (`test_issue_2957_named_files_are_collected_on_a_push_to_main`, :409) is satisfied as long as markers are retained and the job runs on push.

## Task 3 — Test-cluster completeness (367 coupled files; 126 outside plan list)
~60 out-of-list files self-set `SPEC_KITTY_ENABLE_SAAS_SYNC` (safe). Most remaining exercise sync PRIMITIVES BELOW the arming seam (EventJournal, render fns, classify) and won't red: `tests/event_journal/*` (construct `EventJournal` directly), `tests/characterization/test_sync_doctor_render.py`/`test_sync_status_render.py` (pure render of synthetic facts).

### GENUINE UNCOVERED RED (not in plan list, does NOT self-enable, goes end-to-end)
- **tests/contract/test_event_envelope.py** — constructs `EventEmitter(...)` (:44) and asserts `emit_mission_created(...)` returns a non-None event (`:75 assert event is not None`, and ~15 more). If the seam gates `_emit` to return None (the clean placement from Task 1), this **REDS**. Coupled: the correct seam placement (Task 1) and this test are in direct tension — the plan must either add `tests/contract/` to the skipif list OR the no-op must return the envelope but not persist. Likely-same-shape siblings: `tests/contract/test_machine_facing_canonical_fields.py`, `test_handoff_fixtures.py`, `test_identity_contract_matrix.py`.
### Lower-confidence, verify:
- **tests/specify_cli/status/test_saas_resolved_binding_fanout.py** — fanout seam; plan lists `tests/status/` fanout but NOT this `specify_cli/status/` path (asserts actor round-trip, may survive).
- `tests/specify_cli/events/test_decision_log{,_coord}.py`, `tests/specify_cli/coordination/test_outbound.py`, `tests/specify_cli/cli/commands/test_daemon_doctor.py` / `test_doctor_restart_daemon.py` / `test_doctor_cli_surface_golden.py` (daemon-inactive golden shift).

## Task 4 — Arch-guard update scope (FR-014)
- **test_saas_sync_gate_selection_invariance.py — FIGHTS default-off (STRONGEST).** `test_flag_is_set_at_collection_time` (:45-52) asserts `os.environ.get("SPEC_KITTY_ENABLE_SAAS_SYNC") == "1"` collection-wide, set by root `tests/conftest.py:pytest_configure`. FR-010 INVERTS that conftest to default-OFF → this assertion becomes FALSE → RED. This is exactly the "invariance test asserting gate selection identical regardless of env" the change violates. MUST be rewritten: new contract = flag UNSET by default; invariance re-expressed as "skip/run decision stable given env, default = skip." `test_no_test_module_sets_the_flag_at_import_time` (:119) is compatible with skipif (reads, no write) but its premise inverts.
- **test_sync_writer_census.py — LIKELY FORCES UPDATE.** `test_source_discovered_grant_census_cannot_grow` (:804) fails on any NEW grant-producing / `DECISION_RETURN` path. `sync_active()` is a new arming decision-return in core/env.py → census growth → RED unless registered as an owned site.
- **test_sync_env_census.py — MAY FORCE UPDATE (low-mod).** `test_env_reference_set_is_frozen_exactly` (:110-119) reds on any growth/shrink of `SPEC_KITTY_*` literals on the sync surface (husk + sync package). Seam swap moves is_saas_sync_enabled()/first_set_sync_disable_env() *calls* (not literals), so likely neutral; but #2801's new `SPEC_KITTY_PRE_REVIEW_GATE_DISABLE` literal must NOT be added to the sync surface or must be folded into `_EXPECTED_ENV_REFS`. Companion doc `docs/plans/code-quality/sync-env-census.md` updates with it.
- **test_egress_consent_boundary.py — NEUTRAL / additive only, does NOT fight.** Keys on egress SINKS + file allowlist. `sync_active()` is arming, upstream of consent (C-007), adds no sink. egress.py unchanged. At most add an assertion that arming is upstream; no fight.
- **test_sync_no_early_bind.py — NEUTRAL.** AST guard on early-binding `cli.commands.sync` seam callees; `sync_active` lives in core/env.py, unrelated. No update forced.
- **test_sync_two_authority.py — NEUTRAL.** Three-port non-unification (read/write/admission); `sync_active` is arming, not a 4th port, routed through core/env.py not sync_authority. No update forced.
- **test_sync_cli_safe.py (golden) — FORCES UPDATE (additive).** Autouse fixture pins `SPEC_KITTY_ENABLE_SAAS_SYNC=1` (:187), so the ENABLED-shape goldens for the 22 subcommands stay green as-is. FR-014 / plan risk table want an added ARM asserting the DEFAULT-OFF no-op/inactive contract — that arm does not exist yet and must be added.

## Task 5 — Docs completeness (FR-017)
Plan's FR-017 list is mostly complete. **MISSED (behavioral, #2801-tied):**
- **docs/api/skills/spk-run-implement-review.md:44-45** — documents `SPEC_KITTY_SYNC_DISABLE` / `SPEC_KITTY_SYNC_MINIMAL_IMPORT` as the way to disable the pre-review regression gate. #2801/FR-009 CLEAN-CUTS the gate onto its own `SPEC_KITTY_PRE_REVIEW_GATE_DISABLE` and DROPS the sync-disable coupling → this doc becomes WRONG. Not in FR-017 list.
- **docs/plans/code-quality/sync-env-census.md** — companion to `test_sync_env_census`; must move in lockstep if the frozen env set changes.
- ADR glob note: FR-017 uses `docs/adr/3.x/*sync*`; **docs/adr/3.x/2026-08-04-1-egress-consent-boundary.md** (C-007-relevant) has no "sync" in the filename → not matched. Likely no change needed (egress unchanged) but confirm arming≠consent wording.
Not needing change (checked): project-sync-consent.md, pr-landing.md ("by default" = fold-by-default), contributing.md, manage-agents.md.
