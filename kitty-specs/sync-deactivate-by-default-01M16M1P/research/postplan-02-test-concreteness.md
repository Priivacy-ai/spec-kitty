# Post-plan lens: TEST-CONCRETENESS (anti-laziness)

## SC-001 / NFR-001 — seam-not-reached spies
Existing spy patterns EXIST and are directly reusable:
- `tests/sync/test_emitter_observability.py:135,157,224,252` — `monkeypatch.setattr(emitter, "_queue_event_locally", _boom)`. Reuse: patch `emitter._queue_event_locally` with an `assert_not_called`-style boom/Mock and assert it is never invoked on the default path.
- `tests/sync/test_dossier_pipeline.py:203-204,284-285,370-371,468` — `mock_emit.assert_not_called()` / `mock_prepare.assert_not_called()`. Direct precedent for enqueue-not-reached.
- Daemon spawn: `tests/sync/test_orphan_sweep.py` uses REAL subprocess spawn (harness.spawn_daemon) — NOT a spy. For SC-001 spy, patch the implicit-spawn entrypoint in `sync/daemon.py:1131/1154` (the site the seam replaces) with a Mock + assert_not_called. Plan must name it: patch `daemon.<implicit-spawn-fn>` (whatever `daemon.py:1154` calls) — currently unnamed in plan; FLAG.
Exact seams to assert-not-called: `emitter._queue_event_locally`, `emitter._route_event` (:2648), `events.emit_*`/`publish` (:109/182), `sync/__init__.register_default_handlers` (:455/458), daemon implicit-spawn (:1131/1154), `dossier_pipeline.trigger_feature_dossier_sync_if_enabled` (:471).

## NFR-004 — --collect-only node-id diff vs baseline
Existing pattern: `tests/architectural/test_ci_collection_completeness.py` already does ONE whole-tree `--collect-only` (~50s, module-scoped, shared with 5 sibling arch guards) and parses node-IDs deterministically via `_gate_coverage`. Reuse this collector. Deterministic parse is proven.
CHICKEN-AND-EGG (real, must call out in tasks): baseline = "pre-mission non-skipped node-IDs with ENABLE_SAAS_SYNC=1". Currently conftest FORCES the flag ON (`:223` setdefault + `:427` autouse), so the current HEAD collection IS the opt-in baseline. **The baseline census MUST be captured on the tip BEFORE WP05 skipif rollout and BEFORE WP04 conftest flip** — otherwise once skipif lands the pre-state is unrecoverable. Resolution: WP01 (or a WP04 pre-step) checks in the frozen node-ID list captured with `ENABLE_SAAS_SYNC=1 pytest --collect-only`; NFR-004 test re-collects with flag ON and diffs against the frozen list (skipif is `skipif(not ENABLE_SAAS_SYNC)` so with flag ON it must NOT fire → diff=0).

## FR-013 — file-count census
Existing patterns: `tests/architectural/test_sync_env_census.py` (recomputes `SPEC_KITTY_*` ref SET from live tree, pins to frozen `_EXPECTED_ENV_REFS` frozenset — reds in BOTH directions) and `test_sync_writer_census.py` (AST call-graph census). Reuse test_sync_env_census SHAPE exactly.
Key on: a FROZEN SORTED SET of relative file paths that carry the skipif marker (not a bare count — a count alone lets a rename mask a deletion). Concrete assertion: collect all `tests/sync/**`,`tests/specify_cli/sync/**` + the FR-011 clusters that contain the module-level `skipif(not ENABLE_SAAS_SYNC)`; assert `live_set == FROZEN_SET`. Deletion shrinks live→red; un-skipping (removing the marker) drops the file from live→red; new sync file without marker also reds. This satisfies "both deletion AND un-skipping fail".

## SC-005 — anti-swallow (LEGACY + ENABLE_SAAS_SYNC=1)
LEGACY fixture EXISTS: `tests/sync/test_body_integration.py:46-65` builds a ProjectSyncStore, checks `authority.peek_state().mode is LayoutMode.LEGACY`, and can leave it LEGACY (skip the begin_cutover/publish_project_only). `_require_project_destination` is at `body_queue.py:104` (raises RuntimeError if `permit.destination is not PROJECT_STORE`). Recipe: set ENABLE_SAAS_SYNC=1, delenv disable vars (sync_active True), build a LEGACY-layout store so the permit.destination != PROJECT_STORE, call `trigger_feature_dossier_sync_if_enabled` → assert the RuntimeError/error SURFACES (the gated early-return must be `if not sync_active(): return`, above the real call — with flag ON it falls through). NOTE: `trigger_feature_dossier_sync_if_enabled` docstring says "Never raises. Logs failures" — so SC-005 must assert the error surfaces via LOG/return-value/errors list, NOT a raised exception. FLAG: contract says "surfaces as an error"; concretely that means a non-None error in `DossierSyncResult.errors`, not a raise. Tasks must specify the assertion channel.

## FR-010 — conftest de-masking + blast radius
`tests/conftest.py:223` `os.environ.setdefault("SPEC_KITTY_ENABLE_SAAS_SYNC","1")` (collection-time, #3213) and `:427-430` autouse `_enable_saas_sync_feature_flag` (per-test setenv "1"). Both must invert to default-OFF; add `sync_enabled`(setenv 1)/`sync_disabled`(delenv) fixtures.
BLAST RADIUS (measured): **98 test files** read the flag/predicate but DO NOT self-set it → they rely solely on the autouse and BREAK when it flips. Breakdown: sync 30, cli/commands 11, specify_cli/cli/commands/agent 10, specify_cli/cli/commands 5, specify_cli/sync 4, auth 4, architectural 3, readiness 2, saas 2, integration 2, e2e 2, docs 2, + ~15 singletons. The 30+4=34 sync files get skipif (WP05) so they're fine. The ~60 NON-sync files (auth/readiness/saas/cli/agent/architectural/docs) are the danger: they need the flag ON but get NO skipif — they must be moved onto the `sync_enabled` fixture or they silently change behavior/red. 20 files use the `sync_module.` late-bind co-gate (C-006) — these must keep working via the fixture, not the removed autouse.
#3213 collision: `:223` setdefault was added precisely so import-time skipif gates evaluate consistently at collection. Inverting it means the NEW skipif gates (WP05) evaluate default-OFF at collection — that's the intent — but the CI opt-in job (WP04) must set ENABLE_SAAS_SYNC=1 in the ENV before pytest starts (not via fixture) so collection-time skipif sees it. Fixture alone is too late for import-time skipif (this is the exact #3213 lesson). FLAG: plan says "add sync_enabled/sync_disabled fixtures" — fixtures cannot satisfy collection-time skipif; the opt-in path MUST be an env-var/CI-job, fixtures only cover call-time non-skipif consumers.
