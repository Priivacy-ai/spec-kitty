---
work_package_id: WP02
title: '#3033 red-first repro: failing safe-commit on an E2 mission'
dependencies: []
requirement_refs:
- FR-003
planning_base_branch: feat/post-merge-write-authoring-finish
merge_target_branch: feat/post-merge-write-authoring-finish
branch_strategy: Planning artifacts for this mission were generated on feat/post-merge-write-authoring-finish. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into feat/post-merge-write-authoring-finish unless the human explicitly redirects the landing branch.
subtasks:
- T005
- T006
- T007
history:
- Generated by /spec-kitty.tasks 2026-07-30
agent_profile: python-pedro
authoritative_surface: tests/regression/
create_intent:
- tests/regression/test_issue_3033_post_consolidation_write.py
execution_mode: code_change
model: claude-sonnet-4-6
owned_files:
- tests/regression/test_issue_3033_post_consolidation_write.py
role: implementer
tags: []
tracker_refs: []
---

## ⚡ Do This First: Load Agent Profile

Load your assigned profile: run `/ad-hoc-profile-load python-pedro` (or `spk-doctrine-profile-load`) and adopt its identity/boundaries before reading further.

## Objective

Author the **red-first** regression that evidences the #3033 P0 (post-consolidation "E2" writes fail against a deleted Target Ref) through the **pre-existing** `safe-commit` entry point (NFR-002, C-006). This test is authored to FAIL on today's code and turn green after WP03/WP04 — it is independently mergeable ahead of the fix. Do NOT change any product code here.

Read first: `../spec.md` (US1, FR-003, C-006, SC-001/002), `../research.md` (D1/D2), `../contracts/consolidated-write-surface.md`. Ground the entry point in `src/specify_cli/cli/commands/safe_commit_cmd.py` (`_resolve_commit_target` → `resolve_placement_only`) and the git-guard `src/specify_cli/git/commit_helpers.py` (`SafeCommitHeadMismatch`).

## Subtasks

### T005 — E2 fixture builder
Build a fixture that constructs a genuine **E2** state in an isolated git repo through real entry points: a mission consolidated (E1) then published-to-trunk — `baseline_merge_commit` present in `meta.json`, the mission's committed content on the Primary Branch checkout, and the **Target Ref (feature branch) deleted**. Reuse existing test helpers where possible ([[feedback_realistic_test_data]] — production-shaped ids/paths).

### T006 — Red-first `safe-commit` assertion (PRIMARY kind)
From the repo-root checkout on the Primary Branch, run `safe-commit` on a classified **PRIMARY** artifact (e.g. `retrospective.yaml` / a `tasks/WP*.md`). Assert the CURRENT (pre-fix) behaviour: it refuses at the HEAD-vs-ref guard because the resolved `CommitTarget.ref` is the deleted Target Ref. Mark with a clear `#3033` docstring. This is the SC-001 pin (flips green after WP04).

### T007 — Red-first sibling arm: coord-kind via write_seam
Assert that `write_artifact(kind=ISSUE_MATRIX | TRACER_FILE | ACCEPTANCE_MATRIX)` on the same E2 mission returns `status="refused"` today (naming #3033). This is the SC-002 pin.

## Branch Strategy
Base/merge target: `feat/post-merge-write-authoring-finish`. Implement in the lane worktree from `lanes.json`.

## Definition of Done
- New `tests/regression/test_issue_3033_post_consolidation_write.py` with the E2 fixture + the two red arms, each with a `#3033` docstring citing the entry point.
- The red arms **fail on the current tree** (demonstrating the bug) — run them and confirm red; do NOT green-wash. `ruff`/`mypy` clean.
- No product code touched.

## Reviewer guidance
Confirm the fixture builds a real E2 state (deleted Target Ref, content present) via real entry points, not a mock. Confirm the assertions fail today for the right reason (HEAD-guard / refused), not an unrelated error. This is a genuine red-first pin per [[feedback_bugfix_red_first_reproduction]].
