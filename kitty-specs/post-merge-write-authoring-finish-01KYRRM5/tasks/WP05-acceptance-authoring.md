---
work_package_id: WP05
title: 'Acceptance authoring (#2318): negative invariants + diagnose + fresh verdict + prompt'
dependencies:
- WP01
- WP04
requirement_refs:
- FR-007
- FR-008
- FR-009
- FR-010
- FR-011
planning_base_branch: feat/post-merge-write-authoring-finish
merge_target_branch: feat/post-merge-write-authoring-finish
branch_strategy: Planning artifacts for this mission were generated on feat/post-merge-write-authoring-finish. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into feat/post-merge-write-authoring-finish unless the human explicitly redirects the landing branch.
subtasks:
- T019
- T020
- T021
- T022
- T023
- T024
- T025
history:
- Generated by /spec-kitty.tasks 2026-07-30
agent_profile: python-pedro
authoritative_surface: src/specify_cli/acceptance/
create_intent:
- tests/specify_cli/acceptance/test_negative_invariant_authoring.py
execution_mode: code_change
model: claude-sonnet-4-6
owned_files:
- src/specify_cli/cli/commands/agent/acceptance_verdict.py
- src/specify_cli/acceptance/matrix.py
- src/specify_cli/acceptance/gates_core.py
- src/specify_cli/cli/commands/accept.py
- src/doctrine/missions/mission-steps/software-dev/accept/prompt.md
- tests/specify_cli/acceptance/test_negative_invariant_authoring.py
role: implementer
tags: []
tracker_refs: []
---

## ⚡ Do This First: Load Agent Profile

Load your assigned profile: run `/ad-hoc-profile-load python-pedro` and adopt its identity/boundaries before reading further.

## Objective

Finish the #2318 authoring gaps so an agent drives a full accept pass with **zero hand-edited JSON**. Additive only — extend the canonical `agent mission acceptance-verdict` command; the `NegativeInvariant` dataclass already carries every field, so **no acceptance-matrix schema migration** (C-008).

Read first: `../research.md` (D6), `../contracts/authoring-commands.md`, `../spec.md` (US2, FR-007..011, C-008, SC-006/007). Ground in `src/specify_cli/acceptance/matrix.py` (`NegativeInvariant:107-146`, `enforce_negative_invariants:513`, `overall_verdict` property `:192`, persist path), `gates_core.py` (verdict persist `~:520`), `cli/commands/agent/acceptance_verdict.py`, `cli/commands/accept.py` (`--diagnose:593`). Verify the exact accept mission-step prompt path under `src/doctrine/missions/mission-steps/` before editing (SOURCE only — never the 12 generated agent copies; propagation via `spec-kitty upgrade`).

## Subtasks

### T019 — Register a negative invariant (FR-007)
Extend `acceptance-verdict` with a negative-invariant register path that persists a well-shaped `NegativeInvariant` via `write_target(ACCEPTANCE_MATRIX)`. No hand-edited JSON. Flags per the dataclass fields.

### T020 — Execute the invariant (FR-008)
Run the registered invariant through the existing `enforce_negative_invariants` engine and record its `result` (pass/fail). Reuse the engine — do not reimplement.

### T021 — Harden `accept --diagnose` (FR-009)
Wrap `NegativeInvariant.from_dict` (and criterion load) per-item at the `AcceptanceMatrix.from_dict` load path so a malformed shape is **reported** (which item, why) and the command exits non-zero — instead of the current unhandled crash at load (mgifford). Keep `--diagnose` the surface (do NOT add a parallel `--explain`).

### T022 — Persist a fresh `overall_verdict` (FR-010)
Ensure canonical acceptance persists the recomputed `overall_verdict` on the **all-pass / no-negative-invariant** branch (samuelgoff: `pass`, not stale `pending`) — extend the #3076 persist fix (currently only the negative-invariant branch persists).

### T023 — Rewire accept mission-step prompt (FR-011)
Edit the accept mission-step prompt SOURCE to DRIVE `acceptance-verdict` (not merely mention it). SOURCE only.

### T024 — Write-seam thunk call-site (folds #3073 for this file)
Migrate `acceptance/matrix.py`'s write path to pass a thunk to `write_artifact` (WP04's API) instead of pre-staging. Depends on WP04.

### T025 — Tests (SC-006/007)
`tests/specify_cli/acceptance/test_negative_invariant_authoring.py`: SC-006 (full accept pass incl. a negative invariant, zero hand-edited JSON; a malformed invariant reported with 0 unhandled exceptions); SC-007 (all-pass persists `pass`). Red-first where behaviour changes.

## Branch Strategy
Base/merge target: `feat/post-merge-write-authoring-finish`. Lane worktree from `lanes.json`. Runs in parallel with WP06 after WP04.

## Definition of Done
- Negative-invariant register+execute via `acceptance-verdict`; `--diagnose` reports malformed (no crash); fresh verdict persisted; accept prompt drives the CLI (SOURCE only); matrix.py write path uses the thunk.
- `ruff` + `uv run mypy --strict` clean; targeted `pytest tests/specify_cli/acceptance/…` green; SC-006/007 covered. No acceptance-matrix schema migration.

## Reviewer guidance
Confirm additive (no schema change; extends `acceptance-verdict`, not a new command). Confirm the prompt edit is SOURCE (`src/doctrine/missions/mission-steps/`), never an agent copy. Confirm `--diagnose` reports-not-raises and the all-pass verdict persists `pass`.

## Squad Hardening (BINDING — supersedes conflicting wording above)

- **T021 layering (renata M2).** The crash is `AcceptanceMatrix.from_dict` → `NegativeInvariant.from_dict` at load (matrix.py:245/68-78), and `read_acceptance_matrix`/`from_dict` are SHARED by `gates_core.py:468` + `post_consolidation.py:243`. **Do NOT put the exit-code / item-dropping inside `from_dict`** (would change gate behavior — a partial matrix silently passing is worse than a crash). Instead: `from_dict` (or a new `try_from_dict`) raises a **typed `AcceptanceMatrixParseError(item_index, field, reason)`**; the **`accept --diagnose` CLI layer** (accept.py:~693) catches it, reports which item + why, sets the exit code. Test BOTH a malformed `negative_invariants` entry AND a malformed `criteria` entry, and prove `gates_core`/`post_consolidation` behavior is unchanged for well-formed input.
- **Red-first FIRST (priti M1).** Author the failing-first reds BEFORE the fixes: the **mgifford `--diagnose` crash** red (before T021) and the **samuelgoff stale `overall_verdict: pending`** red (before T022), each citing its named fixture. Mirror WP02's red-first structure.
- **T024 per-writer residue + accept.py note (paula M2 / paula MINOR-1).** Migrate `acceptance/matrix.py:319`'s write path to the thunk AND add a "refused write via matrix.py → 0 untracked files" regression to T025. **Note explicitly:** `accept.py:359 _commit_coord_residuals` stays on the `files=` sweep (it commits pre-existing dirt, not a fresh stage) — intentional, not a residue hole.
- **T023 structural assertion (priti M2).** Do not self-certify "drives the CLI". Add a concrete test asserting the accept prompt SOURCE (`src/doctrine/missions/mission-steps/software-dev/accept/prompt.md`) contains an **executable `acceptance-verdict` invocation** in its steps section (structural check).
- **Campsite (renata m1/m2).** If T022 touches `overall_verdict` (matrix.py:191-215, a dense S3776 hotspot), extract `_criterion_verdict`/`_invariant_verdict` pure helpers + hoist the bare `"pass"/"fail"/"pending"` verdict literals to constants. If T022 only edits the persist call-site, leave the property but flag it as a known S3776 hotspot in the PR (do not silently expand it).
