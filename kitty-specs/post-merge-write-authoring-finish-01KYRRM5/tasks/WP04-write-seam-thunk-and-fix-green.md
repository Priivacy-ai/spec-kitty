---
work_package_id: WP04
title: Write-seam no-residue thunk + off-checkout refuse + fix-green
dependencies:
- WP03
requirement_refs:
- FR-003
- FR-004
- FR-005
- FR-006
planning_base_branch: feat/post-merge-write-authoring-finish
merge_target_branch: feat/post-merge-write-authoring-finish
branch_strategy: Planning artifacts for this mission were generated on feat/post-merge-write-authoring-finish. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into feat/post-merge-write-authoring-finish unless the human explicitly redirects the landing branch.
subtasks:
- T014
- T015
- T016
- T017
- T018
history:
- Generated by /spec-kitty.tasks 2026-07-30
agent_profile: python-pedro
authoritative_surface: src/specify_cli/coordination/
create_intent:
- tests/specify_cli/coordination/test_write_seam_thunk.py
execution_mode: code_change
model: claude-sonnet-4-6
owned_files:
- src/specify_cli/coordination/write_seam.py
- src/specify_cli/retrospective/tracer_writer.py
- src/specify_cli/cli/commands/safe_commit_cmd.py
- tests/specify_cli/coordination/test_write_seam_thunk.py
role: implementer
tags: []
tracker_refs: []
---

## ⚡ Do This First: Load Agent Profile

Load your assigned profile: run `/ad-hoc-profile-load python-pedro` and adopt its identity/boundaries before reading further.

## Objective

Complete the #3033 fix at the seam: a **single-locus** no-residue staging thunk (#3073, FR-005), off-checkout **refuse-with-recovery** (FR-006), and flip the WP02 red regressions **green** (SC-001/002/003/004/009). The routing itself comes from WP03's phase-aware `resolve_placement_only`; this WP consumes it and closes the residue + refusal behaviour.

Read first: `../research.md` (D1), `../contracts/consolidated-write-surface.md` (staging thunk, acceptance scenarios), `../spec.md` (FR-005/006, C-004/C-009, SC-001..004/009). Ground in `src/specify_cli/coordination/write_seam.py` (`write_artifact:128`, `_probe_write_target:108`), the three composed writers (this WP owns only `tracer_writer.py`; the other two call-sites are done in WP05/WP06), `safe_commit_cmd.py`.

**Do NOT** change `post_merge/retrospective_terminus.py` — it stays fail-open (C-009).

## Subtasks

### T014 — Staging-thunk API in `write_artifact`
Change `write_artifact` to accept a `stage: Callable[[], tuple[Path, ...]]` invoked **only after** `_probe_write_target` returns OK (immediately before `commit_for_mission`). A refused write never calls `stage()` → zero residue. Keep the existing `files=` path working for callers not yet migrated (deprecate internally). This is the single locus of the probe-before-stage invariant (no per-writer reordering).

### T015 — `tracer_writer` passes a thunk
Migrate `retrospective/tracer_writer.py` to pass its `mkdir`+`write_text` as the thunk instead of pre-staging (it currently stages then calls `write_artifact`).

### T016 — Off-checkout refuse-with-recovery (FR-006)
When WP03's content-presence predicate fails in a consolidated/published phase, return a structured **refused** `WriteSeamResult` whose recovery message names a checkout that carries the mission's consolidated content (derived from the durable signal — never a fabricated ref, C-004). Surface it through `safe_commit_cmd.py`; do NOT force a checkout.

### T017 — Flip #3033 green (SC-001/002)
With WP03 wired, the WP02 red arms turn green: `safe-commit` on a PRIMARY artifact in E2 commits to the CONSOLIDATED surface (exit 0, clean tree — SC-001); the coord-kind `write_artifact` returns `committed` (SC-002). Make any minimal seam/`safe_commit_cmd` adjustment needed to consume the CONSOLIDATED target.

### T018 — Regressions
`tests/specify_cli/coordination/test_write_seam_thunk.py`: SC-003 (refused write → 0 untracked files), SC-004 (off-checkout → refuse-with-recovery naming a ref, non-zero, no checkout performed), SC-009 (`review --mode post-merge` on an E2 mission exits 0 end-to-end). Confirm the WP02 pins flip green.

## Branch Strategy
Base/merge target: `feat/post-merge-write-authoring-finish`. Lane worktree from `lanes.json`.

## Definition of Done
- Single-locus thunk (probe-before-stage); tracer migrated; off-checkout refuse-with-recovery; WP02 pins green (SC-001/002); SC-003/004/009 green.
- `ruff` + `uv run mypy --strict` clean; targeted `pytest` green; `write_acceptance_matrix`'s no-commit/fixture callers unaffected (thunk wraps, not breaks).
- `retrospective_terminus.py` untouched (C-009). Boyscouting C-012: canonical `consolidate` in any new wording.

## Reviewer guidance
Confirm the thunk is the SINGLE locus (probe-before-stage lives in `write_artifact`, not per-writer). Confirm the FR-006 recovery names a real checkoutable ref. Confirm no torn-merge risk was introduced (terminus untouched). Verify SC-001/002/003/004/009 green.

## Squad Hardening (BINDING — supersedes conflicting wording above)

- **T014 mutual-exclusion (renata m4).** `write_artifact` accepts **exactly one** of `files=` / `stage=`; add an explicit guard that raises on neither/both. **T018 must test the neither/both misuse**, not only SC-003/004/009.
- **Caller census (priti B1 / paula M2).** As part of T014/T018, enumerate all `write_artifact` callers (~13) and classify materialize-before-probe vs commit-existing-dirty. Record that ONLY `tracer_writer` (T015), `acceptance/matrix.py` (WP05 T024), and `issue_matrix.py` (WP06 T029) pre-stage; `accept.py:359 _commit_coord_residuals` passes **pre-existing dirty** files (a commit-existing-dirty sweep, refusal-safe) and is intentionally NOT migrated. Make the "only 3 materialize" claim explicit, not assumed.
- **T016 FR-006 recovery = branch-derived (operator HiC).** The recovery message names the **branch-derived default** resolved via `git symbolic-ref refs/remotes/origin/HEAD` (e.g. "check out `main`") — branch-*derived*, never hard-coded `main` (research D1) and never a bare SHA. This satisfies SC-004's "branch-named". Specify this exact derivation in the implementation.
- **Resolver-only, downstream frozen.** `coordination/commit_router.py` + `git/commit_helpers.py` stay byte-unchanged (see WP03 hardening). Consuming the CONSOLIDATED `CommitTarget` needs no change there.
- **T018 probe/commit agreement (priti m2).** The E2 safe-commit end-to-end test must explicitly assert probe and commit resolve the **identical phase** (name the NFR-001 invariant it already exercises).
- **Per-writer residue is gated in the owning WPs** (paula M2): tracer here (T018); `matrix.py` in WP05 T025; `issue_matrix.py` in WP06 T030.
