---
work_package_id: WP03
title: Phase-aware CONSOLIDATED resolution (resolver core)
dependencies:
- WP01
- WP02
requirement_refs:
- FR-001
- FR-002
- FR-003
planning_base_branch: feat/post-merge-write-authoring-finish
merge_target_branch: feat/post-merge-write-authoring-finish
branch_strategy: Planning artifacts for this mission were generated on feat/post-merge-write-authoring-finish. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into feat/post-merge-write-authoring-finish unless the human explicitly redirects the landing branch.
subtasks:
- T008
- T009
- T010
- T011
- T012
- T013
history:
- Generated by /spec-kitty.tasks 2026-07-30
agent_profile: python-pedro
authoritative_surface: src/mission_runtime/
create_intent:
- src/mission_runtime/lifecycle_phase.py
- tests/mission_runtime/test_lifecycle_phase.py
- tests/mission_runtime/test_consolidated_resolution.py
execution_mode: code_change
model: claude-sonnet-4-6
owned_files:
- src/mission_runtime/resolution.py
- src/mission_runtime/lifecycle_phase.py
- tests/mission_runtime/test_lifecycle_phase.py
- tests/mission_runtime/test_consolidated_resolution.py
role: implementer
tags: []
tracker_refs: []
---

## ⚡ Do This First: Load Agent Profile

Load your assigned profile: run `/ad-hoc-profile-load python-pedro` and adopt its identity/boundaries before reading further.

## Objective

The resolver core of the #3033 fix: derive lifecycle **phase internally** in `resolve_placement_only` and wire `SurfaceLocations.consolidated` so `TopologySurface.CONSOLIDATED` resolves the integrated tree by phase — Target Ref tree in E1, repo-root checkout (content-presence gated, squash-robust) in E2. **Single resolver, phase is an input the resolver computes — NOT a parameter threaded by callers, NOT a second resolver** (C-001/C-006, NFR-001).

Read first: `../research.md` (D1/D2/D3 — decisive), `../contracts/consolidated-write-surface.md`, `../data-model.md`, `../spec.md` (FR-001/002, C-001/002/003/005, NFR-001/004). Ground in `src/mission_runtime/resolution.py` (`resolve_placement_only:1274`, `SurfaceLocations:1505`, `translate_surface:1525`, `resolve_artifact_surface:1812`), `src/mission_runtime/artifacts.py` (`TopologySurface.CONSOLIDATED`), `src/specify_cli/merge/baseline.py` (`baseline_merge_commit`).

## Subtasks

### T008 — `lifecycle_phase.py` reader
New module `src/mission_runtime/lifecycle_phase.py`: `resolve_lifecycle_phase(mission_slug, repo_root) -> LifecyclePhase` returning PRE_CONSOLIDATION / CONSOLIDATED(E1) / PUBLISHED(E2) per research.md D2. Signal: `baseline_merge_commit` presence (phase) + Target-Ref branch existence + **C-003 disambiguation** (Target-Ref-absent alone is ambiguous → conjoin with terminal-completion: all WPs `done` in the status event log and/or `mission_number` assigned). `get_feature_target_branch` stays unrouted (do NOT add an existence check there).

### T009 — Internal phase derivation in `resolve_placement_only`
Derive phase inside the resolver; **no** phase parameter added to `PlacementSeam.write_target` or `commit_for_mission`. Ensure the probe path and the materializer both re-derive the identical phase from the same durable signal (NFR-001, no split-brain). Keep it the single resolver (no parallel path; no per-kind branching — C-005/C-006).

### T010 — Populate `SurfaceLocations.consolidated`
Set `consolidated` per phase: E1 → Target Ref tree; E2 → repo-root checkout. (Was always `None`.) `resolve_artifact_surface` supplies it so `translate_surface(CONSOLIDATED, …)` no longer hits the "no location" `ValueError` in valid states.

### T011 — Content-presence predicate (squash-robust, D1)
Implement the E2 location-validity check: the mission's committed content is present at HEAD via `git cat-file -e HEAD:kitty-specs/<mission_slug>/meta.json` (or an equivalent committed marker). **Not** commit-ancestry (a squash publish makes the E1 commit a non-ancestor). On false in a consolidated/published phase → signal the FR-006 refusal (implemented in WP04).

### T012 — Route CONSOLIDATED through the seam
Ensure `resolve_placement_only` returns the CONSOLIDATED-resolved target for post-consolidation writes of the relevant kinds; keep PRE_CONSOLIDATION behaviour byte-identical to #3076 (regression floor).

### T013 — Unit tests
`tests/mission_runtime/test_lifecycle_phase.py` (all three phases + C-003 never-created vs deleted) and `test_consolidated_resolution.py` (E1 target-tree, E2 content-present, E2 content-absent, squash case). **SC-005**: assert `STATUS_STATE`/`DECISION_LOG` PRE_CONSOLIDATION resolution is unchanged (shared-resolver non-regression). Realistic fixtures.

## Branch Strategy
Base/merge target: `feat/post-merge-write-authoring-finish`. Lane worktree from `lanes.json`.

## Definition of Done
- Phase derived internally; `SurfaceLocations.consolidated` wired; content-presence predicate squash-robust; PRE_CONSOLIDATION unchanged.
- `ruff` + `uv run mypy --strict` clean; targeted `pytest tests/mission_runtime/…` green; the write-side/read-side arch gates that touch resolution.py stay green (targeted node-ids; do NOT run the full arch suite locally — [[feedback_no_full_arch_suite_locally]]).
- No second resolver; no threaded phase param; `get_feature_target_branch` unrouted. Boyscouting C-012: any lane-consolidation-sense "merge" wording you add uses canonical `consolidate`.

## Reviewer guidance
Verify probe/commit phase agreement (no split-brain), C-006 single-resolver preserved (no per-kind branch), the squash-robust predicate (D1) is content-presence not ancestry, and SC-005 non-regression. Confirm no new bypass allow-list entry beyond the CONSOLIDATED wiring (NFR-001).

## Squad Hardening (BINDING — supersedes conflicting wording above)

Post-tasks squad (paula/renata/priti) + operator HiC decisions. These are binding corrections:

- **E2 scope is the 3 coord write kinds + PRIMARY only (operator HiC).** The E2 CONSOLIDATED routing (T012) applies ONLY to `ISSUE_MATRIX` / `TRACER_FILE` / `ACCEPTANCE_MATRIX` (coord) and the PRIMARY safe-commit kinds. **`STATUS_STATE` and `DECISION_LOG` must NOT re-route in E2** — they resolve exactly as today (unchanged = non-regressive, C-005). The resolver is kind-aware (it already keys on `MissionArtifactKind`), so this is resolver-internal kind selection, NOT call-site kind-conditioning (C-006 preserved). **T013 must add an explicit E2 pin asserting `STATUS_STATE` + `DECISION_LOG` resolution is UNCHANGED in E2 (not re-routed to CONSOLIDATED).**
- **T011 content-presence — fix the predicate (renata B1). Extract a named helper** `content_present_at_primary_tip(mission_slug, repo_root)`:
  - Derive the marker path from the **resolved canonical dir name** (`primary_dir.name`, i.e. `<slug>-<mid8>`), NOT the bare `mission_slug` (bare-vs-suffixed drift → false-negative → wrongly refuses a valid E2 write).
  - Probe with `git -C <get_main_repo_root(...)>` against the **resolved Primary-Branch tip ref**, NOT literal `HEAD` (HEAD is checkout-dependent; a lane/coord worktree HEAD ≠ Primary Branch).
  - Distinguish `git cat-file -e` **exit 1 (path absent → FR-006 refuse)** from **exit 128 (bad rev / not a repo → raise LOUDLY)**, mirroring `write_seam.py:47-57` `_UNROUTABLE_EXCEPTIONS` "unrelated bug must still raise" discipline. Do NOT collapse both into "absent".
- **Single phase source (renata M1).** `resolve_lifecycle_phase` (T008) is the ONLY phase derivation. BOTH `resolve_placement_only` (probe, `:1263`) and `resolve_artifact_surface` (materializer/consolidated wiring, `:1740`) call it — no second derivation. **T013 asserts byte-identical phase from both entry points** (patch the durable signal once, check both agree). This is the NFR-001 no-split-brain guarantee.
- **CommitTarget.ref invariant (paula MINOR-2).** The E2 CONSOLIDATED `CommitTarget.ref` MUST equal the resolved Primary target-branch NAME (so `commit_router.use_coord` is False and `_materialise_coord_worktree` is skipped) — **never a SHA or `HEAD`**. Add a reviewer-guidance assertion.
- **Resolver-only, downstream frozen (paula MAJOR-1, resolved by priti).** The fix lives ONLY in the resolver. `coordination/commit_router.py` and `git/commit_helpers.py` stay **byte-unchanged** (priti verified `commit_for_mission` re-derives phase through the single `resolve_placement_only`). If an implementer finds a change is needed there, STOP and escalate — the fix escaped the single resolver (a C-006 violation), it is not an in-lane edit.
- **T013 squash test (priti m3).** Name the squash scenario a test that asserts an **ancestry-based** check (`git merge-base --is-ancestor`) would false-negative on the fixture — actively pinning out the rejected approach (research D1).
