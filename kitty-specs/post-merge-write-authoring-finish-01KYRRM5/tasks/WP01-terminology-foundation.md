---
work_package_id: WP01
title: 'Terminology foundation (tidy-first): ADR + glossary + drift guard'
dependencies: []
requirement_refs:
- FR-014
- FR-015
- FR-016
planning_base_branch: feat/post-merge-write-authoring-finish
merge_target_branch: feat/post-merge-write-authoring-finish
branch_strategy: Planning artifacts for this mission were generated on feat/post-merge-write-authoring-finish. During /spec-kitty.implement this WP may branch from a dependency-specific base, but completed changes must merge back into feat/post-merge-write-authoring-finish unless the human explicitly redirects the landing branch.
subtasks:
- T001
- T002
- T003
- T004
history:
- Generated by /spec-kitty.tasks 2026-07-30
agent_profile: curator-carla
authoritative_surface: docs/adr/3.x/
create_intent:
- docs/adr/3.x/2026-07-30-1-consolidated-write-surface-and-consolidate-terminology.md
execution_mode: code_change
model: claude-sonnet-4-6
owned_files:
- docs/adr/3.x/2026-07-30-1-consolidated-write-surface-and-consolidate-terminology.md
- docs/context/orchestration.md
- tests/architectural/test_no_legacy_terminology.py
role: implementer
tags: []
tracker_refs: []
---

## ⚡ Do This First: Load Agent Profile

Before reading anything else, load your assigned profile: run `/ad-hoc-profile-load curator-carla` (or the `spk-doctrine-profile-load` skill) and adopt its identity, boundaries, and governance scope. Then return here.

## Objective

Land the **tidy-first foundation** of #3080 ("do not make it worse") so the `merge`/`consolidate` overload cannot grow while the full rename (#3080) is pending. This WP is sequenced FIRST (C-010). It does NOT rename the `spec-kitty merge` command or any code symbol — that is #3080.

Read first: `../spec.md` (Terminology section, FR-014/015/016, C-007/C-012), `../research.md` (D4, D5), `../plan.md` (IC-01), `../contracts/authoring-commands.md` (Terminology foundation), and `CLAUDE.md` Terminology Canon + ADR `docs/adr/3.x/2026-07-23-2-post-consolidation-deferral-and-external-enforcement.md` (names the 3-sense overload).

## Subtasks

### T001 — Author ADR `2026-07-30-1-consolidated-write-surface-and-consolidate-terminology.md`
Follow `docs/architecture/README.md` / the repo's ADR template. Two decisions in one record (research.md D4):
1. **Design** — wire `TopologySurface.CONSOLIDATED` as the phase-resolved integrated-tree surface: E1 (lane consolidation) = Target Ref tree; E2 (publish-to-trunk) = repository-root checkout gated by a **content-presence** predicate (squash-robust; NOT commit ancestry). Phase derived internally in `resolve_placement_only` (no threaded param). Extends `2026-07-23-2`; re-affirms `2026-06-24-1` C-006 (single write resolver).
2. **Terminology** — `consolidate`/`consolidation` is canonical for the **lane-consolidation** sense of `merge`; git-merge/branch-integration keep "merge"/"integrate"; push/PR to origin keeps "publish". Records the "foundation now / full rename in #3080" split.
- Register the ADR in the ADR inventory if the repo requires (run `freshen_adr_inventory` if that is the convention; check `docs/adr/`).

### T002 — Glossary canonicalization (`docs/context/orchestration.md`)
- Under the `#lane-consolidation` / merge-sense entries, mark **`consolidate`/`consolidation` canonical** for the lane-consolidation sense; mark that sense of "merge" a **legacy alias**; add "do NOT use when" guards distinguishing it from branch-integration and publish-to-origin.
- Keep the existing `primary`/`merge` sense entries (#2653) intact; this ADDS the canonical-term ruling, does not rewrite the disambiguation.

### T003 — Drift-ratchet guard (`tests/architectural/test_no_legacy_terminology.py`)
- Add a curated **forbidden-phrasing** check for the lane-consolidation sense — e.g. `"lane merge"`, `"merge the lanes"`, `"merging lanes"`, `"lane-merge"` (canonical: "consolidate"). Do NOT ban bare "merge" (floods git-merge/publish).
- Use a **baseline allow-set** grandfathering existing occurrences (shrink-only ratchet, matching the repo's arch-gate campsite pattern). Scan `src/` + `docs/` (exclude immutable ADR snapshots + `kitty-specs/`).

### T004 — Guard bite fixture + green-on-legit (`tests/architectural/test_no_legacy_terminology.py`)
- Red-first: a test proving the guard FAILS when a new forbidden lane-consolidation phrasing is introduced (SC-011 bite).
- Green: prove it stays green on legitimate git-merge / publish-to-origin "merge" uses and on the grandfathered baseline.

## Branch Strategy
Planning/base: `feat/post-merge-write-authoring-finish`. Final merge target: `feat/post-merge-write-authoring-finish`. The execution worktree for this lane is allocated from `lanes.json`; implement inside it.

## Definition of Done
- ADR authored + registered; glossary canonicalizes `consolidate` with guards; drift guard added with a proven bite fixture and green on legit/grandfathered uses (SC-010, SC-011).
- `pytest tests/architectural/test_no_legacy_terminology.py` green; the new bite fixture fails only when the forbidden phrasing is present.
- `ruff` clean on the test file. Run `pytest tests/architectural/test_no_legacy_terminology.py` (the CI-only terminology gate — see CLAUDE.md pre-push note).
- No code symbol renamed (that's #3080). Boyscouting C-012 applies to later WPs, not a rename here.

## Reviewer guidance
Verify the guard does NOT false-positive on git-merge/publish "merge" (run it against the full tree). Confirm the ADR names both decisions and extends 2026-07-23-2. Confirm the glossary keeps the #2653 sense entries.

## Squad Hardening (BINDING)

- **T001 ADR must also record the two operator HiC decisions** so they are citable: (1) E2 CONSOLIDATED routing is scoped to `ISSUE_MATRIX`/`TRACER_FILE`/`ACCEPTANCE_MATRIX` + PRIMARY only — `STATUS_STATE`/`DECISION_LOG` are NOT re-routed in E2 (non-regressive, C-005); (2) the FR-006 off-checkout recovery names the branch-derived default via `git symbolic-ref refs/remotes/origin/HEAD` (not hard-coded, not a SHA).
