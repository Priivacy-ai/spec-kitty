# Tracer: design-decisions

One entry per finding: `YYYY-MM-DD · actor · <text>`.

---

2026-07-30 · operator:stijn-dejongh · DELIBERATE FINER-GRAINED SCOPE (operator directive). This mission is intentionally scoped narrow — A+D only (#3033 post-merge write mode + #3073; #2318 negative-invariant authoring; #1738 URL refs) — with the B/C consolidation seams (#2980/#2549 guard-unify, #2960/#3029 reducer cluster) split to a later mission. Rationale: the operator is running an experiment on mission granularity — assessing whether a tightly-scoped single-surface mission produces HIGHER quality and coherence (of both the mission artifacts and the code) than a longer, broader same-surface mission. Motivation: recent broad missions accumulated many 'defer-with-followup' calls, which the operator reads as a signal that at some point the agent becomes 'exhausted' and starts pushing work out of scope in order to claim victory. This mission is the counter-test. SUCCESS SIGNAL TO WATCH during implement: few/zero mid-mission defers; each FR should land COMPLETE, not deferred. If this mission also drifts into defers, that is evidence the exhaustion effect is not merely a function of scope size.

2026-07-30 · operator:stijn-dejongh · TERMINOLOGY-OVERLOAD RESOLUTION (operator-identified, load-bearing). The post-spec squad raised two 'unfixable as specified' BLOCKERs — (paula) CONSOLIDATED is a write-side phantom, do a ref-swap; (architect) no durable branch name records the merge target. Operator identified these as artifacts of the 3-sense 'merge' footgun (Terminology Canon; ADR 2026-07-23-2), NOT real design holes. Disambiguation: E1=lane consolidation (spec-kitty merge folds lanes into the Target Ref=feature branch, which SURVIVES; deletes only lane branches; baseline_merge_commit lands here; LifecyclePhase.POST_CONSOLIDATION). E2=publish-to-trunk (PR integrates the Target Ref into the Primary Branch/main and DELETES the feature branch; integrated tree now on the repository-root checkout). #3033's 'deleted target_branch' is E2, not E1. EVIDENCE CONSOLIDATED is NOT a phantom: artifacts.py:41-52 ('declared WITH the translate_surface seam... every member maps to a real location, so none is a phantom... CONSOLIDATED is wired by the consolidation flow'); translate_surface (resolution.py:1517) has a TOTAL CONSOLIDATED arm (_SURFACE_LOCATION_FIELD[CONSOLIDATED]='consolidated'); the ValueError paula hit is the deliberate 'caller must supply the location, don't guess' guard. RESOLUTION: WIRE CONSOLIDATED (populate SurfaceLocations.consolidated) as the phase-resolved integrated-tree surface — Target Ref tree in E1, repo-root checkout on the Primary Branch in E2 (validated by baseline_merge_commit content/ancestry, squash-robust). NOT a ref-swap, NO new merge_target_branch field. The architect's ancestry check becomes the E2 resolution rule FOR CONSOLIDATED, not a route-around. Squad findings that SURVIVED disambiguation: scope-leak (STATUS_STATE/DECISION_LOG share the resolver — reconcile in C-005) + internal-phase-derivation (NFR-001). FR-006 'terminus fail-closed' DROPPED as misdirected: the terminus fires at E1 (Target Ref exists) so it never hits the E2 wall; converting it to fail-closed post-mutation = torn merge (C-009). Spec rewritten with a binding Terminology section + disambiguation pass.

2026-07-30 · operator:stijn-dejongh · TIDY-FIRST #3080 FOUNDATION FOLDED (operator directive). The full consolidate rename (#3080) is too big for this mission, but the 'do not make it worse' foundation lands here FIRST as a tidy-first enabler: FR-014 ADR canonicalizes consolidate/consolidation for the lane-consolidation (E1) sense — folded INTO the CONSOLIDATED-wiring ADR (C-002), one ADR does both surface-wiring + term-canonicalization; FR-015 glossary update (docs/context/orchestration.md, consolidate canonical + lane-consolidation-sense merge as legacy alias + do-NOT-use-when guards); FR-016 drift-ratchet guard in tests/architectural/test_no_legacy_terminology.py (curated forbidden lane-consolidation-sense phrasings; must NOT false-positive on git-merge/publish). Sequenced BEFORE Lane A. BOYSCOUTING (C-012): when this mission touches code/docs using merge in the lane-consolidation sense, disambiguate to canonical in NEW symbols/touched prose/comments; leave existing public merge-named symbols (spec-kitty merge, MergeState, baseline_merge_commit → #3080) and git-merge/publish senses unchanged. #3080 remains for the full command/code/docs swap. Spec now 16 FR / 4 NFR / 12 C / 11 SC.
