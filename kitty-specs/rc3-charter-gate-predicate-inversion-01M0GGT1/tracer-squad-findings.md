# Tracer — Adversarial Squad Findings (M3)

## POST-SPEC squad (2026-08-21) — 4 lenses, profile-loaded, read-only

### reviewer-renata (anti-laziness / red-by-design) — VERDICT: mostly reversal-proof, not fully; fold before ADR
- **[MAJOR] Uncovered red-by-design test #1:** `tests/charter/test_context_schema_version_ledger.py:104::test_non_bootstrap_action_carries_stamped_version` calls `build_charter_context_json(action="tasks", mission_type="software-dev")` asserting `mode=="compact"`. Flips RED under FR-001. NOT in AC-3 sweep, NOT named for FR-016. → **Add to AC-3 sweep + FR-016 enumeration.**
- **[MAJOR] AC-11 names zero tests** but FR-016 demands each named. Directly-named: `tests/git_ops/test_worktree.py:263::test_creates_empty_spec_when_no_template` (asserts `spec_file.exists()` + `read_text()==""`). Also classify `tests/integration/test_specify_plan_commit_boundary.py:247` + `tests/agent/test_agent_feature.py:346` (scaffold-stays-green vs stray-touch-reverses). → **Enumerate.**
- **[MAJOR] AC-10 fakeable:** requires only present→pass; an implementer could ship a fail-open gate. → **AC-10 must assert BOTH present→gate-passes AND absent→gate-blocks (fail-closed).**
- **[MAJOR] AC-2 green guard conflates typeless + undeclared** (`action="custom-action"`, no mission_type, doctrine-less tmp_path) → can't isolate node-membership. → **Add companion: `mission_type="software-dev"` + undeclared action → compact.**
- **[MAJOR] AC-9 satisfiable without proving literals removed** (byte-compat ⊥ conversion). → **Add load-bearing assertion: patching the per-type source changes the call-site output, or static check the named sites hold no literal tuples.**
- **[MINOR] FR-012 range 595-610 too broad** — includes legit template-copy path (`test_copies_spec_template_when_exists:243` stays green). → **Scope to the `else: spec_file.touch()` branch (~609-610).**
- **[MINOR] NFR-003** only testable in aggregate. → **Add a characterization/snapshot test over built-in resolution outputs, or state NFR-003 is verified solely by enumerated ACs.**
- **[MINOR] AC-12** could rot as broad `pytest.raises(Exception)`. → **Pin specific exception type + message fragment at `_substantive.py:281` / `_ARTIFACT_TYPE_TO_KIND:62-66`.**
- Concession: inferred the two red tests from fixture-equivalence docstrings, did not run the suite; plan-phase red-first run must confirm.

### paula-patterns (seams / C-001) — VERDICT: NOT plan-ready for surface C; A/B/D/E ready
- **[BLOCKER→ADJUDICATED TRUE] "there is no step.yaml" is FALSE.** 30 `step.yaml` files exist at `packs/built-in/missions/mission-steps/<type>/<step>/step.yaml`; `software-dev/specify/step.yaml` carries `template: {artifact_key: spec, template_file: spec-template.md}` = the MissionStepTemplateRef source. Agent B's "no step.yaml" was a scope error (searched src/ only). **My spec §III correction was WRONG.**
- **[BLOCKER→ADJUDICATED TRUE] The locked NAME source (MissionStepTemplateRef) covers only 2/10 tags.** template refs exist only for template-backed steps (spec/plan). The other 8 `_PRESENCE_FILE_TAGS` (source-register.csv, findings.md, report.md, gap-analysis.md, audit-report.md, release.md, research.md, tasks.md) are non-template outputs. Their filenames live in `expected-artifacts.yaml` `path_pattern` — verified: `research/expected-artifacts.yaml` has `path_pattern: "source-register.csv"`, `"findings.md"`, `"report.md"`, `"gap-analysis.md"`, `"research.md"`, `"spec.md"`, `"plan.md"`. **`path_pattern` IS the filename** — the "two vocabularies cannot join" claim is false. → **REVERSE fork (e): single filename authority = `expected-artifacts.yaml` `path_pattern` (per-type, 10/10, all 4 types).**
- **[MAJOR] Adding `artifact_file` to MissionStepTemplateRef = dual-authority whack-a-field** (filename in both artifact_file AND path_pattern). The exact anti-pattern KDD-1 kills. → **Keep MissionStepTemplateRef template-only; artifact names have ONE home (path_pattern).**
- **[MINOR] C-001 relocation verified clean** (`src/doctrine/missions/` imports neither charter nor specify_cli; ExpectedArtifactManifest is pure pydantic; test_layer_rules stays green; charter slot `Mapping[str,Any]` keepable — minor mypy-strict typing cost, note it). **"Same-file M4 coordination" overstated:** relocation is models.py (different file); M4 owns repository.py:316 `get_action_index`; M3's repository.py touch (if any) is near `get_expected_artifacts:362` — different symbol. → Correct "same-file" wording; pin M3's exact repository.py touch at plan.
- **Decomposition:** A (context gate + _KNOWN_ACTIONS fold, coupled via the vocab constant), B (probe, independent), D (:797 routing, independent), E (ADR, cross-cuts) → clean parallel WPs. C is the L-blob with forced internal ordering (relocate → projection/resolver seam → named-call-site conversion under NFR-003 → per-type presence gate → third-kind pins) and is NOT plan-sliceable until fork (e) re-opened.
- Concession: did not run suite (NFR-001 is debugger's call); verified research+software-dev path_pattern coverage but not exhaustive per-type audit of all 10 — plan must confirm coverage.

**ADJUDICATION (source-verified):** step.yaml exists (30 files); expected-artifacts.yaml path_pattern = literal filename, single per-type authority 10/10. Fork (e) REVERSED to path_pattern source. Fixes spec §III, FR-009, KDD-5, fork (e).
### doctrine-daphne (DRG wiring) — VERDICT: predicate achievable; 2 integrity gaps to close
- **[MAJOR] FR-015 carrier model wrong for retrospect.** `mission_type.graph.yaml` has NO edge to any `*/retrospect` action (not in any mission_type's action_sequence). The `action:*/retrospect` nodes exist in `action.graph.yaml` (23/50/65) with scope edges but are mission_type-orphans. AC-3 retrospect still works because delivery builds the URN directly (`action_doctrine_bundle.py:196 f"action:{resolved_type}/{action}"`), never traversing from mission_type. Only `tasks` (in software-dev's sequence) got a `requires` edge (`mission_type.graph.yaml:79`). → **FR-015: grain delivered by direct `action:<type>/<step>` URN + the action node's own `scope` edges; the `mission_type→action requires` edge is NOT load-bearing for delivery. Either wire the 3 retrospect nodes into their mission_type, or record them as sequence-orphan on-demand actions.**
- **[MAJOR] Node-membership ⊊ grain-delivery (silent gap).** `action` absent from `_SINGULAR_TO_PLURAL` (drg.py:270-281) → action nodes always survive activation filtering (default-allow — good for AC-3). BUT scope-edge targets (directives/tactics) ARE activation-gated; `filter_graph_by_activation` keeps an edge only if both endpoints survive (drg.py:537-541). So a project that deactivated a kind → `bundle.merged` declares the action node while `resolve_context` walks zero surviving scope edges → FR-001 returns `mode=bootstrap` + empty arrays. → **Refine FR-001: "node declared AND resolved grain non-empty," OR document that declared-but-starved → bootstrap+empty is legitimate and confirm no consumer branches on `mode` expecting non-empty.**
- **[MINOR] FR-008/AC-8 interview mechanism under-specified.** `validate_local_support_declarations` is pure over the 4-token frozenset, no mission-type arg, no graph. Accepting `action: tasks` needs either a static label-union (~19 tokens) or loading action.graph.yaml (new I/O); the bare label isn't a URN and the interview doesn't know the type → acceptance flattens across types. → **Pin at plan: static label-union vs loaded nodes; state validation is type-agnostic.**
- **[MINOR] FR-007 vs FR-008 conflation.** `BOOTSTRAP_ACTIONS` (fast-path set) and `_KNOWN_ACTIONS` (acceptance allowlist FR-008 must widen) are semantically distinct despite identical contents. Folding to one constant risks a later reader treating it as the closed acceptance set (reintroducing the warn-drop). → **Keep fold but name it for its fast-path role; interview consults it PLUS the declared-node source (two inputs); AC-7 asserts single def + sibling test asserts interview acceptance not bounded by it.**
- FR-005 confirmed sound (id-match intrinsic via `MissionTypeProfileRepository._key`→obj.id; `get_provenance→None` clean witness).
- Concession: MAJORs dormant for the 4 built-ins at full activation (shipped scope); hardening for the custom-type case (AC-10). Didn't execute resolve_context or read `_classify_artifact_urns` kind-delivery table — check at plan for retrospect AC-3 byte-grain.

## POST-PLAN squad (2026-08-21) — 3 lenses on plan.md + ADR

### planner-priti (WP decomposition) — VERDICT: implementable with one required split
- **[MAJOR] Split WP04.** WP04a = relocate + resolver seam + call-site conversion (green characterization, NFR-003 witness; AC-9/12). WP04b = live gate + third-kind pins + stray-touch delete (behavioral reds AC-10/AC-11), depends WP04a. Don't over-slice to WP04c. Reason: bundling "prove nothing changed" with "prove the reversal" muddies the red→green witness.
- **[MAJOR] Third `BOOTSTRAP_ACTIONS` consumer missed:** `src/specify_cli/cli/commands/charter/context.py:199` gates a display header on set-membership. After the fold, `tasks`/`retrospect` deliver `mode=bootstrap` but aren't in the set → header silently won't print. → **Add to WP02 site list; print header on `mode==bootstrap`, not set-membership.**
- **[MAJOR] Unfiled `_KNOWN_ACTIONS` ticket** (no #NNNN anywhere). → **Add "file #NNNN before WP02 implement" + explicit issue→WP map; note WP01/ADR owns NO issue (else issue-verdict flags an orphan WP).**
- **[MINOR] WP05→WP01 dep spurious** (WP05 reverses no red-by-design test; #3407 is pure route-around). → Drop or annotate sequencing-only.
- **[MINOR] Ownership: WP04 CONSUMES `get_expected_artifacts` read-only** (it has live callers: dossier/manifest.py, org_expected_artifacts.py, mission_type_profiles.py); does NOT edit repository.py. `get_action_index:295/except:316` (M4/#3412) vs `get_expected_artifacts:362/except:382` (WP04) distinct. → **State consume-only → M4 conflict is zero-edit, rebase risk evaporates.**
- Concession: didn't run red-first suite; WP04a/b boundary shifts one subtask later if the resolver seam itself needs a behavioral change for AC-9.

### reviewer-renata (ADR + fidelity) — VERDICT: ADR names all 4 tests + FR/AC coverage complete; but 2 of 5 delegated decisions unresolved
- **[MAJOR] ADR does not resolve the custom-family gate mechanism** (registration vs strict-raise) that AC-10's red-first test form depends on. → **ADR MUST decide.** *Resolution (orchestrator):* presence gate (AC-10/FR-011) is **data-driven per-type via `expected-artifacts.yaml` `path_pattern`** — a custom family gates on its own filenames by shipping the manifest (registration-by-data, no `_GUARD_TABLES` code change); the `evaluate_guards_strict` `UnregisteredMissionFamilyError` strict-raise is **retained** as the fail-closed default for guard-table *dispatch* of a genuinely unregistered family (distinct concern from presence). State this in the ADR.
- **[MINOR] ADR omits `load_validated_graph` memoization line** (spec pushed it). Mitigated by the new load-count test, but add the decision.
- **[MINOR] plan.md self-contradicts on memoization owner** (§2 "verify at WP02" vs §7 "deferred to WP01 ADR"). → Pick one (WP02 verifies; ADR records the decision).
- **[MINOR] Residual stale fork-(e) framing in spec §VIII (line 100) + Issues table (line 230)** ("short-key … SETS from expected-artifacts.yaml"). ADR + plan are correct (path_pattern). → Fix the 2 spec lines.
- **[MINOR] ADR: stray-spec.md deletion is a THIRD behavior change** (bugfix) without a safety rationale — label it distinct from the two policy reversals (unread empty file; template-copy path stays green).
- **[MINOR] FR-015 has no verifiable WP02 DoD** — add a WP02 acceptance note ("retrospect nodes recorded as on-demand sequence-orphans per ADR").
- Confirmed: all 4 red-by-design tests named + exist; **FR/AC→WP coverage complete, no orphans** (FR-001..016, AC-1..14, C-001/002/003 all owned).
- Concession: didn't run the 4 tests (confirmed function anchors only); didn't audit all 10 path_pattern tags (a deferred WP01-ADR item).

### architect-alphonso (single-load proof / seam) — VERDICT: architecture sound; single-load proof VALID (stronger than argued)
- **[MAJOR] C-001 relocation blast radius understated (4 consumers, not 1):** `dossier/indexer.py`; `dossier/__init__.py:12-15,50-52` re-export (public API); `ManifestRegistry.load_manifest` return type + `_cache` annotation (`manifest.py:276,281` — deliberately keeps doctrine imports lazy, `:31-33`); tests (`test_dossier_pipeline.py:234`). → **WP04a subtask 1 enumerates all four; preserve `manifest.py` import-time isolation via `TYPE_CHECKING`-only import; decide `dossier/__init__` re-export-from-new-home vs drop.** Direction stays legal; blast radius is the risk.
- **[MINOR] FR-001 predicate needs a None guard:** `bundle.merged` is `None` on typeless + `DRGLoadError` fallback → `f"action:…" in bundle.merged.node_urns()` NPEs. Use `bundle.merged is not None and urn in bundle.merged.node_urns()`.
- **[MINOR] DROP memoization — unnecessary AND harmful.** `load_validated_graph` is NOT memoized, but within one `build_charter_context_json` it's called exactly once (`action_doctrine_bundle.py:194`) and `.merged` is reused → the FR-001 predicate adds ZERO loads. Process-wide memoization would serve STALE graphs (test suites/daemon). → **Resolves the plan §2/§7 memoization contradiction: no memo needed; the `.merged` carrier already guarantees single-load-per-call.**
- **[MINOR] Citation drift:** `node_urns` is `src/doctrine/drg/models.py:413` (not `src/charter/models.py`); `_non_bootstrap_context_result` (not `build_non_bootstrap_context_result`), `context.py:256`.
- **Verified sound (no finding):** single-load proof (a) gates short-circuit before bundle; (b) `.merged: DRGGraph|None` + `node_urns()` exist; WP04 seam `repository.py:362 get_expected_artifacts` + `:477`; WP05 routing already `_GUARD_TABLES.get(family)` fail-closing for custom — only `:797` hardcode changes; `get_mission_type` already imported (`runtime_bridge.py:177`).
- Concession: didn't trace all 10 path_pattern tags for NFR-003; didn't confirm `get_mission_type` == guard-table family key for every built-in (WP05 risk if divergent — verify at WP05).

## POST-TASKS squad (2026-08-21) — 2 lenses on the 6 WP files

### reviewer-renata (anti-laziness) — VERDICT: NOT yet clean; fix before implement
- **[BLOCKER] FR-010 two homeless conversion sites.** "accept triple" (`src/specify_cli/acceptance/__init__.py:86-88` `SPEC_FILE`/`PLAN_FILE`/`TASKS_FILE`) + "retrospective precondition" (`src/specify_cli/cli/commands/agent_retrospect.py:247` `for required in ("spec.md","plan.md","tasks.md")`) are owned by NO WP; WP04 body assigns them but owned_files lacks them → WP04 DoD satisfiable while they stay hardcoded (fakeable). → **Add both files to WP04 owned_files; extend AC-9 to assert each site reads the resolved set (patch path_pattern → output changes).**
- **[MAJOR] Relocation test-importer blast radius undercounted.** 3 test files import from `specify_cli.dossier.manifest` directly: `test_dossier_pipeline.py:234`, `tests/dossier/test_manifest.py:458`, `tests/dossier/test_manifest_guard_parity.py:39`. TYPE_CHECKING-only import → they red at runtime. → **Keep a runtime re-export in `dossier/manifest.py` (drop the TYPE_CHECKING-only claim) OR add the 3 test files to WP04 owned_files; relocation test asserts the legacy import path still resolves.**
- **[MAJOR] WP02 AC-3 retrospect half unpinned.** Only the software-dev `tasks` half is pinned. → **Add owned red-first: `build_charter_context_json(action="retrospect", mission_type="documentation"/"research")` delivers `mode=bootstrap` + non-empty.**
- **[MINOR] WP02 AC-7 no structural guard.** → Add a regression asserting a single definition site (`interview` imports the one `BOOTSTRAP_ACTIONS`, or an AST/grep guard).
- **[MINOR] WP01 is a re-verification checkbox** (ADR already authored/committed/registered) — honest terminal state, closes `fixed` no-diff; flag for issue-verdict. No `deferred-with-followup` warranted anywhere.
- **[MINOR] Line-anchor drift** (ledger test `:95` not `:104`; worktree `:264` not `:263`) — WPs cite by NAME (robust); refresh FR-016 line numbers if ADR touched.
- Concession: didn't execute the 4 tests (verified names/locations + intent, not bodies).

### python-pedro (feasibility + markers) — VERDICT: WP04 not landable as written; others feasible once markers added
- **[BLOCKER] WP04 TYPE_CHECKING-only import CRASHES at runtime.** `ManifestRegistry.load_manifest` calls `ExpectedArtifactManifest.model_validate` at runtime (`manifest.py ~356`); TYPE_CHECKING-only → `NameError`. mypy passes (green), runtime reds, WP04 DoD excludes `tests/dossier/`. → **Use a lazy function-local runtime import (the existing `_doctrine_repository()` `# noqa: PLC0415` pattern) / PEP 562 `__getattr__` re-export — NOT TYPE_CHECKING-only. Add `tests/dossier/` to WP04 DoD.**
- **[BLOCKER] WP04 owned_files gaps (convergent w/ renata):** must edit `src/doctrine/missions/__init__.py` (`__all__` export), `src/specify_cli/acceptance/__init__.py` (accept triple `SPEC_FILE` ~`:998`), `src/specify_cli/cli/commands/agent_retrospect.py:247` (retrospect precondition) — none owned. → Add to owned_files.
- **[MAJOR] All 8 new test files need a routed `pytestmark`.** CI routing is marker-driven; a marker-less file is collected by NO job → reds `tests/architectural/test_ci_collection_completeness.py::test_every_test_node_is_collected_on_a_push_to_main` (arch-adversarial + fast-tests-core-misc) — NOT in any WP's local `pytest tests/<dir>/` DoD. → **Mandate pytestmark on each new file.** REQUIRED forms: `tests/charter/…` → `[pytest.mark.fast, pytest.mark.unit]` (or `pytest.mark.doctrine` if it reads the built-in DRG; + `pytest.mark.corpus` if it reads `packs/built-in/**`); `tests/specify_cli/runtime/…` + `tests/runtime/next/…` → `[pytest.mark.unit, pytest.mark.fast]`; `tests/doctrine/missions/…` → `[pytest.mark.fast, pytest.mark.doctrine]`.
- **[MINOR] WP04 new module needs `__all__`** (C-007 binding) + doctrine public-surface enrollment (`test_doctrine_public_surface.py`/`test_no_dead_symbols.py`).
- **[MINOR] WP02/WP03 conditional `corpus` marker** if a test reads `packs/built-in/**` (WP02 DRG tests likely; WP03 tmp_path likely exempt).
- **[MINOR] WP02 NFR-001** depends on `_resolve_action_bundle`'s internal `load_validated_graph` count — verify it loads once.
- **[MINOR] WP05/WP06 share the `gather_artifact_presence` contract** (WP05 callee, WP06 caller; `mission_family` already a param) — no file overlap; keep the signature call-compatible.
- **Baseline note:** no `marker_baseline`/`_baselines.yaml`/`slow-tests-nodeids.txt` edit needed (no @slow/@stress/@quarantine moves). Live gates: `test_ci_collection_completeness.py` (routing) + doctrine public-surface/dead-symbol (`__all__`). The `.pyc` `test_pytest_marker_convention` is stale from another checkout.
- Concession: static read only (no suite/mypy run); didn't trace `_resolve_action_bundle` internal load count; inferred marker-gate red from 100% sibling adoption.

## POST-TASKS CONVERGENCE (fix before implement)
1. **WP04 owned_files += `src/doctrine/missions/__init__.py`, `src/specify_cli/acceptance/__init__.py`, `src/specify_cli/cli/commands/agent_retrospect.py`, `tests/dossier/test_manifest.py`, `tests/dossier/test_manifest_guard_parity.py`, `tests/sync/test_dossier_pipeline.py`.** Import mechanism → lazy runtime / `__getattr__` (NOT TYPE_CHECKING-only). New module gets `__all__`. AC-9 extends to accept-triple + retrospect sites. DoD += `tests/dossier/`.
2. **WP02 += retrospect red-first** (`documentation`/`research` deliver) **+ AC-7 single-definition structural guard** + `corpus` marker if reading built-in DRG + verify `_resolve_action_bundle` single internal load.
3. **All new test files carry a routed `pytestmark`** (forms above) — add the mandate to every WP.



## POST-PLAN CONVERGENCE (act on these)
1. **Split WP04 → WP04a (green refactor: relocate+resolver+call-sites, NFR-003) + WP04b (behavioral reds: live gate AC-10, third-kind pins AC-12, stray-touch delete AC-11).**
2. **No memoization** — the `.merged` carrier guarantees single-load; drop the hedge (resolves the memoization ambiguity).
3. **Custom-family gate = data-driven presence via `path_pattern`** (AC-10 present/absent) + **retained `UnregisteredMissionFamilyError` strict-raise** for guard-table dispatch.
4. **WP02 gains a third `BOOTSTRAP_ACTIONS` site** (`specify_cli/cli/commands/charter/context.py:199`, display header on `mode==bootstrap`).
5. **C-001 relocation: 4 consumers + `TYPE_CHECKING` isolation** (WP04a subtask 1).
6. **FR-001 predicate carries a `None` guard.**
7. **WP04 CONSUMES `get_expected_artifacts` read-only** (zero repository.py edit → M4 conflict evaporates).
8. **Fidelity fixes:** file the `_KNOWN_ACTIONS` ticket + issue→WP map (WP01/ADR owns none); WP05→WP01 sequencing-only; FR-015 WP02 acceptance note; ADR labels stray-spec.md a 3rd (bugfix) change; fix stale spec fork-e lines; citation tightening.

---

### debugger-debbie (live-evidence / NFR-001) — VERDICT: red-first feasible but NOT uniform
- **[BLOCKER] NFR-001 guard mis-cite.** `test_charter_import_time_io.py` only spies `MissionTypeRepository.default` at IMPORT time (≤1 dir read); never invokes `build_charter_context*`, never counts `load_validated_graph`, never times the FSM path. `test_context.py` patches `load_validated_graph` but makes ZERO call-count assertion (the ":173 exactly-once" comment is about fragment-merge, not invocation). `test_chokepoint_overhead.py` measures a different chokepoint. → A runtime DOUBLE graph load keeps every cited test GREEN. **NFR-001 must name a NEW red-first test: patch `charter._drg_helpers.load_validated_graph` with a counter, assert `build_charter_context_json(action="tasks", mission_type="software-dev")` triggers exactly ONE load.**
- **[MAJOR] FR-001 new unbudgeted graph load.** Today the non-bootstrap path is graph-FREE (`build_non_bootstrap_context_result` context_result_builders.py:64-89 renders compact with no bundle; gates short-circuit BEFORE `_resolve_action_bundle` :272/:503). FR-001 forces a bundle resolve (→ `load_validated_graph` action_doctrine_bundle.py:192) before the mode decision → AC-2 `custom-action` now pays a full load to be told "compact." Threading IS feasible: `f"action:{type}/{action}" in bundle.merged.node_urns()` (models.py:413) — single load. → **Verify `load_validated_graph` is process-memoized (else FSM pays per step); state accepted new cost for undeclared actions in ADR.**
- **[MAJOR] AC-10 no named entry point.** `_PRESENCE_FILE_TAGS` is a closed 10-tuple; `evaluate_guards_strict` RAISES `UnregisteredMissionFamilyError` for families absent from `_GUARD_TABLES` (only research/documentation/software-dev/plan). AC-10's red is an absence with no callable. → **Pin exact seam: `gather_artifact_presence(feature_dir, mission_family='<custom>', step_id=...)` snapshot omits `<custom>.md` today → carries it after; DECIDE whether a custom family gets a `_GUARD_TABLES` registration or the strict raise is the intended gate.**
- **[MAJOR] AC-13 latent/synthetic.** Alias real but `_check_cli_guards` never reads mission type today; a plan mission doesn't naturally carry `tasks/WP*.md` at a review-step WP guard; runtime callers (:1641/:1659) gate on `_is_wp_iteration_step` so the natural path doesn't repro. → **Label AC-13 a latent-defect pin; red test seeds `meta.json mission_type: plan` + unapproved WP, asserts routing at the `_check_cli_guards` seam.**
- **[MINOR] AC-6 patch target vanishes.** The reversed test patches `_project_has_doctrine_overrides`→True, but FR-005 DELETES that probe; rewrite must seed real per-type `governance-profile.yaml` id-matches. AC-5 org/built-in tolerance is NET-NEW (today only project inspected) → layered fixtures. AC-4 red clean (`resolve_mission_type_context(repo,"softwaer-dev")` silent→raises `UnknownMissionTypeError` :805).
- **[MINOR] AC-9/AC-12/AC-14/AC-2 are GREEN characterization pins, not red-first — label them so.** Correctness note (convergent w/ daphne): `resolve_context` does NOT raise on missing action node (query.py:139-160 returns empty); the current bundle already degrades undeclared→empty grain, so **the FR-001 predicate MUST test `node_urns` membership, NOT empty-grain**, else a declared-but-empty node is misclassified undeclared.
- Concession: traced statically, did NOT execute tests; implementer confirms each red empirically (esp. AC-13 `get_wp_lane` fixture, AC-3 real-graph delivery vs `project` fixture).

## CONVERGENCE (survived independent scrutiny — act on these)
1. **NFR-001 predicate = `node_urns()` membership** (daphne + debugger, strong). Declared-but-starved node → bootstrap+empty is legitimate; document it. New load-count red-first test required.
2. **Fork (e) REVERSED → `expected-artifacts.yaml` `path_pattern` single authority** (paula, source-adjudicated; aligns w/ debugger AC-10 custom-family need). No `artifact_file` on MissionStepTemplateRef.
3. **AC-10 fail-closed both-directions + named entry point `gather_artifact_presence`** (renata + debugger).
4. **Enumerate red-by-design tests for FR-016**: `test_every_load_delivery.py:197`, `test_mission_type_profiles.py:260`, `test_context_schema_version_ledger.py:104`, `test_worktree.py:263` (renata + debugger).
5. **AC-13 = latent-defect pin**, AC-9/12/14/2 = green characterization pins — label (debugger).
6. **FR-015 carrier = direct URN, not mission_type edge** (daphne).
7. **FR-012 scope to the `else: spec_file.touch()` branch** (~609-610), not the whole 595-610 block (renata).
8. **FR-007/FR-008**: keep fold, name constant for fast-path role, interview consults it + declared-node source (daphne).
