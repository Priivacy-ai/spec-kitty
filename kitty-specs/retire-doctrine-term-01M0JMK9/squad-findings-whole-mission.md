# Adversarial Squad Findings — Whole Mission (post-merge, pre-PR-update)

**Point-cut**: whole-mission review after `spec-kitty merge` (WP01–WP05 `done`, mission review PASS WITH NOTES), before
the PR body is rewritten. **Date**: 2026-08-23. **Orchestrator**: Claude (fork agents, profile-loaded via
`spec-kitty agent profile show <id>` + `charter context --action review --json`). Historical squad documents
(`squad-findings-post-spec.md`, `-post-plan.md`, `-post-plan-coverage.md`, `-post-tasks-reroll.md`) remain untouched
snapshots.

**Sharp question**: can a downstream team execute M1→M6 from these merged artifacts alone, reach an honest I6 (zero
`doctrine` occurrences/pathnames in `HEAD` outside the single fixed exclusion root `kitty-specs/`), and never touch
`kitty-specs/` history — or do the artifacts rest on topology/evidence/planning claims the live repository contradicts?

## Lenses and verdicts

| Lens | Profile | Verdict (own lens) | One-line reason |
|---|---|---|---|
| Architecture / seams / topology | `architect-alphonso` | **FAIL** | wave partition is by directory prefix, not seam: M2's "`src/doctrine/` absent" gate collides with M4-owned `src/doctrine/skills/**` package data; M2-owned `.kittify/doctrine` code literals collide with M3-introduced CR-07; CR-01 sourced to `config.yaml` rows that are the CR-04 seam while `governance.doctrine` lives in `charter.yaml`; ADR promises the offer/activate boundary "survives" while prescribing a merge whose collision set is exactly the gated boundary modules |
| Live evidence / coverage | `debugger-debbie` | PASS-WITH-FINDINGS | inventory regenerates byte-identically and fails closed, but the terminal gate run from any token-free subdirectory reports zero on the real repo (CRITICAL, reproduced); guard fingerprint is tree-OID-keyed (vacuous across trees) and coordinate-keyed (spuriously red under churn); archive gate as a whole-range diff is unimplementable; symlink-target and homoglyph escape classes unpopulated today |
| Scope / sequencing / executability | `planner-priti` | PASS-WITH-FINDINGS | arithmetic honest and reproducible; but M1 "zero decisions" rests on writers that cannot emit token-free `charter.yaml` until M2/M4 land; guard-store/migration-script literal placement and the parity predicate unstated; M2 is one 13k-row PR; M6 lacks a release row; PR body stale |
| Anti-laziness / contract-vs-artifact | `reviewer-renata` | PASS-WITH-FINDINGS | load-bearing "set-equal / byte-identical / hash-pinned" claims are contract-true and reproduced; approvals recorded as `actor: user` note strings without `review_ref`; SC-001 independence verified three times under one profile; "exact member IDs" realised as interleaving spans |

## Convergence table and dispositions

FOLDED = amended in this commit · DEFERRED-DM = operator decision opened/deferred in the ledger · HANDED-OFF = recorded as
M1–M6 input/obligation in the stacked plan · UPSTREAM-GAP = tooling issue, not a mission defect · NOT-ACTED = reason given.

| # | Finding | Lenses | Severity (max) | Disposition |
|---|---|---|---|---|
| 1 | Terminal audit is cwd-relative: run from a token-free subdirectory it reports zero/exit 0 (`inventory.md` §8 `--cwd`; `contracts/inventory-schema.md` argv; `spec.md` terminal contract) | debugger | CRITICAL | FOLDED — toplevel-only precondition (`git rev-parse --show-prefix` empty → else audit error), `:(top)`-anchored pathspec `':(top)' ':(top,exclude)kitty-specs/'` (verified live: identical counts from root and subdir), `ls-tree --full-tree`, `mutation_subdir_cwd_cannot_pass_zero`; in contract, spec, ADR, methodology §3.4, stacked-plan M6 |
| 2 | `src/doctrine/skills/**` (OC-41 pathnames M4; OC-09 content M4) vs M2 gate "`src/doctrine/` absent" — package data resolved by `src/specify_cli/skills/registry.py:44,60,67`, shipped by `pyproject.toml:180-185`, counted by `release.yml:219-243` | architect (CRITICAL), planner (HIGH) | CRITICAL | FOLDED — OC-41 owner M4→M2 as `relocate` with registry/wheel/release-gate map rows; OC-09 stays M4; sums re-derived (M2 13,344 · M4 564) in methodology §1.2, stacked-plan §0/§1/§2 |
| 3 | `.kittify/doctrine` code literals (66 files; M2-owned OC-17/19/20) vs CR-07 introduced by M3 — M2 cannot close or breaks I2 | architect | HIGH | FOLDED — CR-07 introduction M3→M2 (dual-root reader/migrator with the renamed literals); M3 moves data and exercises CR-07; methodology §1.3/§2.2, stacked-plan §1 M2/M3, §2.2 |
| 4 | CR-01 sourced to `.kittify/config.yaml` rows (OC-03), but those rows are the `doctrine.org.packs` block (CR-04 seam, reader `src/doctrine/drg/org_pack_config.py:577`); `governance.doctrine` is `charter.yaml:2,19` (OC-02) | architect | HIGH | FOLDED — CR-01 re-sourced to OC-02; OC-03 owner M1→M2 (CR-04 seam); M1 302 · M2 13,344; `inventory.md` §4 amendment note says the stacked plan supersedes the TSV annotation (TSV/hash unchanged) |
| 5 | ADR asserts the offer→activate→consume boundary "survives unchanged" while prescribing a merge of `src/doctrine/` into `src/charter/` whose collision set is exactly the gated boundary modules (`__init__.py`, `pack_paths.py`, `provenance.py`, `resolver.py`, `template_catalog.py`, `versioning.py`, …; `Directive`, `DoctrineService`, `canonical_yaml`); no canonical sub-package named; `src/charter` imports `doctrine` 142×, reverse 0× | architect | HIGH | FOLDED — ADR M2 paragraph + `contracts/operator-surface-map-schema.md` + `adr-content-contract.md` §4: one named offer-side sub-package inside `src/charter/` (name fixed by M2's map gate), one-way import rule and boundary gates renamed, facade/implementation never merged, collision set enumerated as mandatory rows; dormant `spec-kitty-doctrine` manifest delete-vs-rename row |
| 6 | M1 "zero decisions" contradicted: generated `charter.yaml` `catalog` summaries come from M2-owned `src/charter/compiler.py:1449`, `activated_directives: 018-doctrine-versioning-requirement` ID is created by M4/CR-08, pack-source summaries are M4 — M1's "occurrence map empty" gate unsatisfiable without editing M2/M4 surfaces | planner (HIGH), architect (HIGH, generated manifests) | HIGH | FOLDED — wave-gate semantics restated (no row owned by this or an earlier wave remains; later-wave rows carried forward and listed); I1/I2 reading rule in methodology §1.4; M1/M2/M4 entries list carried-forward rows; ADR M1 paragraph |
| 7 | Transition guard: `match_sha256` embeds the tree OID (no cross-tree match — "hash" clause vacuous); `(line,column)` coordinates shift under unrelated edits (spuriously red; live: 99 moved / 50 absent between base and HEAD with nothing retired) | debugger | HIGH | FOLDED — guard re-keyed tree-independently (per path: count + multiset of `(match, SHA-256 of containing line)`; shrink = no new path, per-path count non-increase, no new line-pair); ten mutations rephrased; methodology §2.1, stacked-plan §0/M1/§3.1 |
| 8 | Archive gate as a base→result whole-range diff trips on unrelated missions mutating their own archive dirs (live: 38 M/D/R entries in the last 30 main commits) | debugger (HIGH), planner (MEDIUM), mission-review R-1 | HIGH | FOLDED — test form `git diff --name-status $(git merge-base <target> <tip>) <tip> -- kitty-specs/` → only `A` entries under the wave's own mission dir; methodology §3.5, stacked-plan §0/M5 |
| 9 | PR #3664 body stale (user-facing scope, S1–S9, wrong slugs, "ADR not in this PR") | planner | HIGH | HANDED-OFF to the orchestrator's PR-body rewrite (the body is not a repository artifact) |
| 10 | Guard-store location and `scripts/migrate_charter_interview_answers.py` token literals are new token-bearing files unless placed/constructed deliberately; parity predicate undefined; `charter generate` entry path (`compiler.py:515-516` section update vs `:725` whole-document save) unpinned; 43 referrers of `docs/context/doctrine.md` not in M1's map | planner (MEDIUM ×3), architect (LOW) | MEDIUM | FOLDED — M1 entry + ADR M1 paragraph + §3.1 dry-run rows: store untracked or in M1's own mission dir; script token-literal-free; parity predicate fixed (term set + definitions + aliases keyed by ID across three authorities + link closure); entry path pinned; referrers re-pointed (generated via generators) |
| 11 | M2 realism: 13k rows / one landing gate; no landable sub-invariant; slicing would need declared OC partitions | planner | MEDIUM | NOT-ACTED — the stack schema fixes six missions; dependency-slice editing/rollback already stated; re-sizing M2 is an operator planning choice recorded here for the M1 adoption decision, not a contract defect |
| 12 | Serialized runtime records keyed to archive slugs / retired profile IDs (`.kittify/migrations/mission-state/quarantine/**`, `kitty-ops/*.jsonl`, `.kittify/missions/**/retrospective.yaml`) cannot be "re-cited" without breaking linkage | architect | MEDIUM | DEFERRED-DM — `DM-01M0P6C8C7Q6SPBT412V39RPN0` opened and deferred (operator call: exclude alongside `kitty-specs/` / untrack after backup / schema-aware rewrite); recorded as M5's only `local_design_questions` entry |
| 13 | OC-22 lumps live architectural baselines/allowlists (`tests/architectural/_baselines.yaml`, `charter_path_literal_allowlist.yaml`, `_exemptions/doctrine.txt`) with terminology controls; M6 "deletes control machinery" would remove unrelated enforcement | architect | MEDIUM | FOLDED — M2 retargets live baselines; M6 deletes only the program's own transition store/controls; methodology §2.1/§3.4, stacked-plan M6, operator-surface-map mandatory rows |
| 14 | CR-05 sources mislabelled (`src/charter/drg.py` has no `doctrine:` literal; real producer `doctrine_synthesizer/apply.py:409,663`; external `spec_kitty_events` fixture consumer) | architect | MEDIUM | FOLDED — CR-05 re-sourced; external consumer named as M2 map row with owner/milestone |
| 15 | CR-06 shim as a package named `doctrine` breaks `importlib.resources.files("doctrine")` resolution (`src/charter/catalog.py:19`, `registry.py:60`, `hatch_build.py:12`) | architect | MEDIUM | FOLDED — shim module-only; every `files("doctrine")` site mapped before the shim lands |
| 16 | CLI map says "eight subcommands"; live group has 4 direct commands + `pack`/`org`/`mission-type`/`asset` subgroups + 3 `validate` routes; `charter mission-type` collision unresolved | architect | MEDIUM | FOLDED — operator-surface-map mandatory rows + ADR M2 paragraph + stacked-plan §3.2 |
| 17 | OC-30 (27,990 rows, 80% of M5) is rename-in-place of frozen evidence, not regeneration; `tests/architectural/test_marker_job_completeness.py:82-85` reads it; link check cannot close into the archive | planner (MEDIUM), debugger (LOW), mission-review R-5 | MEDIUM | FOLDED — M5 entry/outputs/tests; link check scoped `:(exclude)kitty-specs/` |
| 18 | M6 release step unsequenced (CI workflow host, required-check registration, 4.0 bump, CHANGELOG breaking entry, dormant-manifest fate) | planner | MEDIUM | FOLDED — M6 `outputs` release row + §2.4 M6→release row + §3.3 |
| 19 | Symlink targets invisible to `git grep` (5 symlinks in HEAD today, none token-bearing); homoglyph/zero-width/soft-hyphen splits satisfy a byte-literal gate | debugger | MEDIUM | FOLDED — third and fourth checked steps (`test_symlink_target_audited`, `test_no_homoglyph_or_format_char_evasion`) in contract/spec/ADR/methodology/stacked-plan |
| 20 | Paths containing LF rejected as malformed (fail-closed but un-runnable); fixture lacks the newline case | debugger | MEDIUM | FOLDED — structural record parsing; newline fixture required; frozen-base run unaffected (HEAD has no LF paths) |
| 21 | Rows created after the frozen base (this program's ADR, ~48 hits) appear in no OC/assignment table | debugger | MEDIUM | FOLDED — §0: every wave's opening audit re-derives ownership by the rule tables; M1 baseline = M1's own opening manifest |
| 22 | M6 entrypoint interface (`--commit … --json -`) and commit-OID binding not present in the WP02 script (`--base`, stores `"HEAD"`) | debugger (LOW), mission-review R-4 | LOW | FOLDED in contract wording (resolved commit OID, toplevel, git version, stdout/stderr hashes; usage errors ≠ exit 2); the WP02 script is frozen planning evidence, the M6 entrypoint is M6's deliverable |
| 23 | Approvals recorded as `actor: user` note strings without `review_ref`/reproduction command; WP05 independence only by shell pid; SC-001 verified three times under `reviewer-renata` | renata (MEDIUM ×2), mission-review R-6 | MEDIUM | UPSTREAM-GAP (approval events should carry claim `policy_metadata` + `review_ref`; to be filed) + recorded in `tracer-tooling-friction.md`; independence note added to the mission-review addendum |
| 24 | "Exact member IDs" realised as interleaving ID spans (25 of 44 OCs over-cover) | renata | LOW | FOLDED — contract wording: member set = TSV rows with that `occurrence_class_id`; span is orientation (`inventory-schema.md` item 3, `stacked-plan-schema.md` table 1) |
| 25 | Attestation field list and "no earlier result authorizes" sentence drift between contract, ADR, spec, stacked-plan | renata | LOW | FOLDED — harmonised in all four |
| 26 | `issue-matrix.json` #2727 tracker-thin (`fr/sc/scope` null); the live issue is about `src/glossary/` (M2 OC-21), only the glossary-authority slice is bound into M1 | planner | LOW | FOLDED — `fr: FR-003`, `sc: SC-003`, `scope` filled |
| 27 | `acceptance-matrix.json` unfilled scaffold despite `accept` passing (95 of 237 missions share this) | planner, renata, mission-review D-1 | LOW | UPSTREAM-GAP — `spec-kitty accept` does not populate/validate the matrix; not a mission defect |
| 28 | `retrospective.yaml` is generator boilerplate (`gaps: []`, `proposals: []`) | planner | LOW | NOT-ACTED — runtime-owned record; the tracer close paragraphs carry the hand-offs; `spec-kitty agent retrospect synthesize` is the sanctioned way to add proposals |
| 29 | `spec.md` `Status: Draft`; US1 step 2 omits Charter Pack; "45 OCs" tracer prose | planner, renata | INFO | FOLDED (status, US1) / NOT-ACTED (tracer prose is dated log) |
| 30 | `git --version` not pinned per wave (mission-review R-3) | planner | LOW | FOLDED — attestation and every reproduction record `git --version` |

## Re-derived arithmetic after the fold

M1 302 (OC-01, 02, 40) · M2 13,344 (OC-03, 12–25, 27, 28, 41–44, 48) · M3 111 (OC-04, 08, 45) · M4 564 (OC-06, 07, 09,
10, 11, 46) · M5 34,729 (OC-26, 29–35, 47, 49) · M6 0 = **49,050**; 3 + 22 + 3 + 6 + 10 = 44 populated OCs, each once.
CR introduction: M1 {01}; M2 {02, 03, 04, 05, 06, 07}; M3 {}; M4 {08}; removal M6 {01–08}. The frozen-base TSV
(`3631531b…`, 49,050 rows) and its hash are unchanged; WP05's and the mission review's recorded figures (304 / 13,259 /
111 / 647) are the pre-fold partition of the same rows.

## Synthesis

No lens disputed the evidence core: the inventory regenerates byte-identically, the hashes recompute, the self-tests
fail closed, the scope is planning-only, and the three operator decisions are honoured. Every structural FAIL was a
**partition-by-directory** defect (ownership assigned by path prefix where the live seam crosses a directory) or a
**contract hole** (cwd-relative audit, tree-keyed guard, whole-range archive diff). All are re-derivations or contract
tightenings, not re-planning; they were folded in this commit so the downstream program starts from artifacts the
repository does not contradict. One genuine operator decision (serialized historical records) is deferred in the ledger
and is M5's only open question. The architect lens's FAIL therefore stands as a finding against the pre-fold artifacts
and is resolved by this fold; the program's terminal rule and cardinality are untouched.
