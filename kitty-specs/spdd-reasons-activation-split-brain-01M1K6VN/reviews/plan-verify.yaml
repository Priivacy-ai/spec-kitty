schema: review-verify/v1
complete: true
phase: plan
mission: spdd-reasons-activation-split-brain-01M1K6VN
verifications:
  - id: PLAN-GOV-001
    status: resolved
    evidence: "plan.md § Project Structure > Source Code (line 105) now lists tests/charter/test_action_bundle_delivery.py explicitly, and § (a) item 3 (lines 246-262) plus Phasing WP2 (lines 575-579) name the file in WP2 scope. The fix commits to updating the four direct _classify_artifact_urns(..., set()) call sites to pass None instead, explicitly distinguishing that from FR-014's own new explicit-empty(frozenset()/set())-excludes-everything test so the two fixture meanings are not conflated. Re-verified against the live test file: `grep -n _classify_artifact_urns tests/charter/test_action_bundle_delivery.py` confirms bare `set()` literals appear at exactly lines 88, 190, 330, 348 (a 5th call at line 115 passes a non-empty set and is unaffected) — matching the plan's citations exactly. Substance change, not a restatement."
  - id: PLAN-ARCH-001
    status: resolved
    evidence: "plan.md § (b) 'What it compares' (lines 287-303) replaces the x-or-set() oracle with `pack_context.activated_paradigms is None or PARADIGM_ID in pack_context.activated_paradigms`, the analogous `is None or bool({...} & ...)` form for tactics, and `is None or any(...)` for directives — an absent per-kind key now evaluates as selector-satisfied, matching § (a) item 1's stated semantics and the codebase-wide None='all built-ins' contract it cites (pack_context.py's _read_list_key, drg_activation.py's _node_is_activated). This is the exact fix the finding's remediation specified, not a paraphrase — the contradiction between section (a) and section (b) is gone."
  - id: PLAN-ARCH-002
    status: resolved
    evidence: "plan.md § (a) item 1 (lines 171-199) adds an explicit, evidence-based carve-out: it now states the replication deliberately tracks PackContext.from_config's raw per-kind semantics, not compile_charter's project_configured-gated authority (_resolve_config_activated_roots), and gives the reasoning/citation for why this matches the spec's actual scope. Re-read spec.md's Decision Record 1 (lines 77-135) and FR-001(d) (line 490) myself: FR-001(d)'s literal text is 'applies the existing three-state per-kind semantics... verbatim, matching PackContext._read_list_key's contract' — no mention of project_configured/FR-018/T038 anywhere in spec.md (grep confirms zero hits). spec.md's own Summary and DR1 name PackContext.from_config's raw resolution, not compile_charter's gated one, as the authority to match. So the plan's chosen carve-out is faithful to spec.md's actual FR-001(d)/DR1 language, not a contradiction — plan.md accurately uses DR1's activated_kinds carve-out as an analogous precedent (correctly quoting DR1's own phrase 'faithful to the authority this spec's Summary actually cites') without misattributing project_configured awareness to DR1 itself. Verified compiler.py:102-110 (_CONFIG_ACTIVATION_FIELDS), compiler.py:153-181 (_resolve_config_activated_roots's project_configured gate, 'if value is None and project_configured: return frozenset()'), and compiler.py:360-375 (docstring) all match the plan's citations."
  - id: PLAN-VERIFY-001
    status: resolved
    evidence: "plan.md § (f) Gate set, Bandit row (line 401) now reads 'Yes — a real, blocking gate' and correctly explains that continue-on-error:true on the bandit step only lets later artifact-upload steps run, while a separate '[ENFORCED] Fail job if security checks failed' step reads steps.bandit.outcome and exits 1, and that quality-gate's needs: includes lint so a Bandit finding blocks quality-gate/merge. Re-verified against the live .github/workflows/ci-quality.yml: id: bandit + continue-on-error: true at lines 918/920, the dedicated 'Fail job if security checks failed' step at line 979 does `if [ \"${{ steps.bandit.outcome }}\" != \"success\" ] ... exit 1`, and quality-gate's needs: list includes lint at line 4280. All three code citations check out; the row's substantive conclusion (no Bandit-flaggable pattern expected in this mission's diff) is preserved but no longer rests on the false 'informational' premise."
  - id: PLAN-VERIFY-002
    status: resolved
    evidence: "plan.md § (a) item 3 (lines 210-262) now names all three consumption sites explicitly: delivery_table.py's start_urns comprehension (line 213), the exclusion guard (line 238), and action_doctrine_bundle.py's roots tuple construction (lines 230-234) — re-verified against the live files: grep confirms start_urns at delivery_table.py:213, the guard at delivery_table.py:238, and roots construction beginning at action_doctrine_bundle.py:230 with the directive comprehension at line 232, matching the plan's citations. The plan commits to a concrete conversion pattern: branch on activated_directives is None once, at the point project_directives is assigned in action_doctrine_bundle.py, converting None to a concrete value before any of the three sites iterates it — mirroring resolver.py's _resolve_directive_base early-conversion pattern the plan cites approvingly. A new WP2 test fixture with activated_directives absent (None, not []) exercising both functions end-to-end is added (lines 241-245), distinct from FR-014's explicit-empty fixture. This directly closes the TypeError crash path the finding identified."
