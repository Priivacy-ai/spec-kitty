# Mission-type governance profile — plan
#
# Pinned by `tests/missions/test_mission_type_profile_resolution.py`.
# Schema: `charter.mission_type_profiles.MissionTypeProfile`
# Contract: kitty-specs/charter-mediated-doctrine-selection-01KRTZCA/contracts/mission-type-profile.md
#
# `id` is the overlay identity for the `doctrine/base.py` builtin → org →
# project stack (it keys overlays on `id` and skips id-less files, base.py:249).
# INVARIANT (WP05): `id == mission_type` for every governance-profile.yaml —
# shipped or a project override at `.kittify/doctrine/mission_types/<type>/`.
#
# `template_set: null` keeps `software-dev-default` from leaking into a plan
# mission (FR-011 / journey 4): plan missions are decomposition/design/decision
# work, not code change, and MUST resolve zero software-dev doctrine.
#
# FR-005 — the plan domain (decomposition + design + decision capture) is wired
# here by *reference only* (no net-new artifacts). These selections are the
# type-grain defaults resolved for every step of a plan mission. Action-grain
# indices under `actions/` are intentionally sparse/empty (FR-004): plan
# governance is type-wide, so per-action grains carry no planning-specific
# additions. Type-grain and action-grain are kept disjoint (FR-013).
id: plan
mission_type: plan
template_set: null
selected_directives:
  - 031-context-aware-design
selected_tactics:
  - problem-decomposition
  - bounded-context-identification
  - deepening-opportunity-assessment
  - moscow-scoping-lens
  - eisenhower-prioritisation
  - adr-drafting-workflow
  - traceable-decisions
  - decision-marker-capture
  - premortem-risk-identification
selected_paradigms:
  - domain-driven-design
  - deep-module-design
  - c4-incremental-detail-modeling
selected_styleguides:
  - planning-and-tracking
selected_toolguides: []
selected_procedures: []
selected_agent_profiles: []
selected_mission_step_contracts: []
available_tools: []
activations: []
