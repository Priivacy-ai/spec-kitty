# Mission-type governance profile — research
#
# Pinned by `tests/missions/test_mission_type_profile_resolution.py`.
# Schema: `charter.mission_type_profiles.MissionTypeProfile`
# Contract: kitty-specs/charter-mediated-doctrine-selection-01KRTZCA/contracts/mission-type-profile.md
#
# Research missions focus on investigation and evidence gathering rather than
# code change (FR-005: decision/evidence + investigation). The type-grain set
# below carries the whole-mission investigation doctrine; per-step doctrine
# (decision-documentation, requirements validation, premortem, glossary
# curation) lives at the action grain under `actions/*/index.yaml`.
#
# `id` is the overlay identity for the `doctrine/base.py` builtin → org →
# project stack (it keys overlays on `id` and skips id-less files, base.py:249).
# INVARIANT (WP05): `id == mission_type` for every governance-profile.yaml —
# shipped or a project override at `.kittify/doctrine/mission_types/<type>/`.
# A project override MUST repeat `id: research` or field-merge would skip it.
#
# FR-013 disjointness: no artifact below appears in any `actions/*/index.yaml`.
# The type grain and action grain are unioned and de-conflicted on canonical
# URN; a double declaration is a construction-time error, not a de-duplication.
id: research
mission_type: research
template_set: null
selected_directives: []
selected_tactics:
  - dialectic-research
  - safe-to-fail-experiment
  - reverse-speccing
  - forensic-repository-audit
selected_paradigms: []
selected_styleguides:
  - research-citation-discipline
selected_toolguides: []
selected_procedures:
  - situational-assessment
  - event-storming-discovery
  - domain-aware-decision-interview
  - spike-timebox-policy
selected_agent_profiles: []
selected_mission_step_contracts: []
available_tools: []
activations: []
