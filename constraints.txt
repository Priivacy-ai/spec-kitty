# Dependency override constraints for spec-kitty-cli
#
# spec-kitty-runtime 0.4.x declares a transitive requirement on
# spec-kitty-events<4.0, but this repo vendors spec_kitty_events 4.0.0
# directly under src/specify_cli/spec_kitty_events/ and pins the
# public package to ==4.0.0.  The vendored copy is the actual runtime
# surface; the runtime package's transitive events pin is not enforced
# at import time.
#
# Use:  pip install -e ".[dev]" -c constraints.txt
#
# Fixes mission-review DRIFT-1 (01KPWT8P).
spec-kitty-events==4.0.0
