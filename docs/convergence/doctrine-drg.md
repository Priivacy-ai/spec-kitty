| sha | kind | disposition | evidence | one-line rationale |
|---|---|---|---|---|
| 358c081c4 | feat | applies | Current line has no `src/doctrine/agent_profiles/operating_procedures.py` and no `resolve_operating_procedure_entries` symbol (`rg` finds none); `src/doctrine/drg/migration/extractor.py:333` and `:469` still hand-pin `agent_profile:doctrine-daphne` and `agent_profile:researcher-robbie` procedure edges while `src/doctrine/agent_profiles/profile.py:161` still accepts `operating_procedures`. | The operating-procedures field is still not validated/data-driven into the DRG, so the old validate-and-derive fix targets a live defect. |
| 41e8333fa | feat | applies | `src/charter/cascade.py:87-89` defines `REFERENCE_RELATIONS` as only `REQUIRES`, `SUGGESTS`, and `REFINES`; `src/charter/cascade.py:244-259` returns every artifact-kind reachable candidate without filtering through `CHARTER_ACTIVATABLE_KINDS`. | Mission-type cascade still cannot follow the `mission_type -> action -> scope/instantiates` hop and can still offer non-activatable artifact kinds. |
| f28b6c94b | fix | applies | `src/doctrine/directives/models.py:30-37` keeps `DirectiveReference` to only `type` and `id`, so `reason` remains schema-forbidden; `src/doctrine/drg/migration/hand_authored_overlay.py:18-35` still documents extractor-regeneration overlay dependence; `src/doctrine/drg/migration/extractor.py:669` still describes directive references as `{type, id, when?}`. | Residual orphan wiring and reference rationale still cannot move losslessly from overlay edges into directive frontmatter. |
| e2ccc391f | fix | applies | Current `src/doctrine/drg/migration/extractor.py:1066-1220` discovers mission types and emits `mission_type --requires--> action` edges, but has no `_GOVERNANCE_PROFILE_SCOPE_FIELDS` or `extract_governance_profile_scope_edges`; `src/charter/mission_type_profiles.py:971-1017` reads expected artifacts but not DRG projection of type-wide governance. | Type-wide `governance-profile.yaml` selections still do not project into the DRG, and the old projection/binding change remains the right shape. |
| 1763bf2ae | fix | applies | `src/doctrine/missions/repository.py:362-383` catches `YAMLError` in `get_expected_artifacts` and returns `None`, the same return used when `_expected_artifacts_path` is absent at `:371-373`; there is no `MalformedManifestError` symbol in `src/`. | A present malformed `expected-artifacts.yaml` is still indistinguishable from an absent manifest. |
| d8beee276 | fix | applies | Current extractor has no `assert_governance_scope_edges_resolve` symbol, and its graph-generation path lacks any check between mission-governance edge extraction and node minting; the current governance-profile fields are only resolved in charter readers such as `src/charter/mission_type_profiles.py:1197-1316`. | Built-in governance-profile selections still lack a DRG-generation fail-loud guard for unresolved targets. |
| a03a39c7f | fix | applies | `src/doctrine/missions/mission_type_repository.py:209-253` defines `_inject_projected_fields` without a `pack_context` parameter and hardcodes `resolve_all_for_mission_type(..., pack_context=None)` at `:243-246`, while layered consumers pass real pack contexts elsewhere (`src/charter/mission_type_profiles.py:1068-1071`). | Org/project mission-type step projection still drops the caller's pack context at the repository injection seam. |
| dc8a88218 | feat | applies | `src/charter/synthesizer/topic_resolver.py:36-47` hand-restates `_DRG_NODE_KINDS` and omits live `NodeKind` values from `src/doctrine/drg/models.py:43-76`, including `mission_step_contract`, `template`, `asset`, `glossary`, `glossary_pack`, `mission_type`, and `anti_pattern`. | DRG topic resolution still has a drift-prone, incomplete node-kind gate instead of deriving from `NodeKind`. |
| 60e021ed4 | feat | applies | There is no `src/doctrine/drg/org_governance.py`; `src/doctrine/drg/org_pack_loader.py:495-520` loads `drg/fragment.yaml` plus augmentation projection only, and `rg` finds org-tier `governance-profile.yaml` handling only in charter/profile readers and tests, not in DRG org-pack projection. | Org-tier governance-profile selections still never become DRG `scope` edges, so unresolved org selections cannot fail through graph validation. |
| 0d8b49d9a | fix | applies | `src/charter/action_doctrine_bundle.py:187` calls `load_validated_graph(repo_root, org_root=org_root, org_roots=org_roots)` without `org_fragments`; `src/specify_cli/mission_step_contracts/executor.py:344` and `:362` do the same, while `load_org_drg` exists and tests cover `drg/fragment.yaml` packs elsewhere. | The action-doctrine bundle and step-contract executor still drop org `drg/fragment.yaml` contributions at their DRG load seams. |
