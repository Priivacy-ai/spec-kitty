---
doc_status: active
updated: '2026-08-27'
---

| sha | kind | disposition | evidence | one-line rationale |
|---|---|---|---|---|
| 9248683b5 | fix | obsolete | Current tree has no `src/specify_cli/cli/commands/sync.py`; 2.0 decision `HIC-EPHEMERAL-TEAM-STATUS-2026-08-25.md` deletes CLI-to-SaaS sync transport. | The tracker-egress doctor renderer lived only in the deleted sync command surface. |
| 3bacb5b98 | fix | applies | Current `src/specify_cli/cli/commands/agent/status.py:239-326` exposes `--review-ref` and `--evidence-json`, but no `--review-result-json`; current `src/specify_cli/orchestrator_api/commands.py:1311-1342` keeps the review-result parser private to orchestrator. | Agent status emit still cannot carry the structured `ReviewResult` path that the status FSM requires for `in_review` exits. |
| 9115e9184 | fix | applies | Current `src/specify_cli/orchestrator_api/commands.py:1271-1308` enforces the for_review commit gate, and calls it at `src/specify_cli/orchestrator_api/commands.py:1426-1427`; `src/specify_cli/cli/commands/agent/status.py:326-358` transitions without any `for_review` gate call. | The same ungated agent-status path still lets a lane WP reach `for_review` without the orchestrator's commit check. |
| 625f8547c | fix | superseded | Current `src/specify_cli/migration/mission_state.py:673-681` calls `enforce_primary_write_ownership(Path.cwd(), resolved_repo_root)` inside `repair_repo` before selecting or writing mission dirs. | The repair entry point is already self-protecting against foreign-lane invocation. |
| 53a731240 | fix | superseded | Current `src/specify_cli/dossier/manifest.py:104-154` defines `ManifestSchemaError`, and `ManifestRegistry.load_manifest` raises it on built-in and org schema failures at `src/specify_cli/dossier/manifest.py:304` and `src/specify_cli/dossier/manifest.py:340`; old sync consumers are deleted. | The live manifest boundary already uses a domain exception, and the sync misattribution consumer no longer exists. |
| 9818657a6 | fix | obsolete | Current `src/specify_cli/cli/commands/_doctrine_collect.py` has no `_run_operating_procedures_check` or `collect_operating_procedure_entries` call, and `rg 'built_in_root\(\) / "agent_profiles"' src/specify_cli/cli/commands/_doctrine_collect.py` returns no matches. | The exact local built-in agent_profiles path composition this fixed is gone. |
| a87fcdc84 | fix | superseded | Current `src/runtime/next/runtime_bridge.py:1666-1673` scopes the CLI guard pre-check with `mission_type == MISSION_TYPE_SOFTWARE_DEV`. | The fail-open non-software-dev guard routing is already prevented by the current mission-type check. |
| 4c6eef424 | fix | obsolete | Current tree has no `src/specify_cli/sync/admission_negotiation.py`; 2.0 decisions replace the sync admission handshake with team admission plus live relay capability minting. | The cached sync admission handshake hook no longer exists on this line. |
| b0edb11a3 | fix | obsolete | Current tree has no `kitty-specs/rc3-canonical-mission-type-reader-01M0GGWM/meta.json`. | The dogfood mission artifact that needed `status_phase` stamping is not present in the EXPERIMENTAL line. |
| 22fa2123b | fix | applies | Current `src/specify_cli/cli/commands/agent/mission_setup_plan.py:387-388` still documents a supported legacy `mission` field, and the error at `src/specify_cli/cli/commands/agent/mission_setup_plan.py:416` still says `mission_type` or legacy field `mission`. | The operator-facing plan-template error still advertises the retired legacy `mission` field. |
| 8ad6c8182 | fix | superseded | `rg 'SPEC_KITTY_CMD_OUTPUT_FILE=/tmp/x\|/tmp/x' tests/review/test_baseline.py tests/architectural` returns no matches; the current guard is `tests/architectural/test_no_tmp_paths_in_tests.py:62`. | The shared `/tmp` literals are already absent from the live baseline tests. |
| 1121b2385 | fix | superseded | Current `src/mission_runtime/__init__.py:73-78` imports `ReadDegradeStrategy`, `ReadDirDecision`, and `resolve_read_dir_or_degrade`, and exports them at `src/mission_runtime/__init__.py:95-119`; `rg 'from mission_runtime\.read_dir_degrade' src/specify_cli src/runtime` returns no matches. | The read-dir degrade API is already available through the mission_runtime root for external consumers. |
| e6e3b7357 | fix | obsolete | Current tree has no `kitty-specs/rc3-lane-allocation-single-seam-01M0GGX8/status.events.jsonl`. | The malformed dogfood status log with `WP1` through `WP5` annotation rows is not present in this line. |
| c113d3746 | fix | applies | Current `src/specify_cli/runtime/agent_commands.py:315-318` and `src/specify_cli/runtime/agent_commands.py:335-338` still perform `chmod`, `write_text`, then `chmod` in place, with no symlink refusal and no atomic temp-then-replace writer. | Generated command writes still have the same non-atomic, symlink-following pattern after the old writer module was deleted. |
| 3bce08000 | fix | applies | Current no-migrations JSON path folds worktree upgrade errors into `worktree_warnings` at `src/specify_cli/cli/commands/upgrade.py:842-845`, but `upgrade_failed` ignores them at `src/specify_cli/cli/commands/upgrade.py:885` and JSON `errors` is built only from mission activation and surface drift at `src/specify_cli/cli/commands/upgrade.py:891-893`. | A worktree-stamp failure can still be reported as warnings while `success` remains true and `errors` remains empty. |
| d060cff9a | fix | superseded | Current `src/specify_cli/core/mission_creation.py:721-738` and `src/specify_cli/core/mission_creation.py:760-777` wrap `_commit_feature_file` and raise `RuntimeError(f"meta.json commit failed: {exc}")`; `tests/core/test_mission_create_checkout_restore.py:253-317` pins the message. | Meta.json commit failures already name the failed step and preserve the underlying git error. |
| ed5321d04 | fix | obsolete | Current `src/specify_cli/mission_step_contracts/executor.py:20-32` imports DRG helpers through `charter.drg` and has no `load_org_pack` or `OrgPack*Error` imports; `src/charter/drg.py:102-105` already re-exports `load_org_pack` and `OrgPackMissingError`. | The direct executor import path from `doctrine.drg.org_pack_loader` is gone. |
| d76aa3f5d | fix | superseded | Current up-to-date upgrade path calls `_print_dry_run_provisioning_notice(project_path, dry_run=dry_run, json_output=json_output)` at `src/specify_cli/cli/commands/upgrade.py:882-884`; the helper is defined at `src/specify_cli/cli/commands/upgrade.py:526-540`. | Up-to-date dry runs already preview missing `mission_type_activations` provisioning. |
