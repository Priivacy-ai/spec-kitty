---
doc_status: active
updated: '2026-08-27'
---

| sha | kind | disposition | evidence | one-line rationale |
|---|---|---|---|---|
| d56f24e11 | fix | applies | Current `src/charter/kind_vocabulary.py:240-252` documents a live residual: nested org styleguides load at runtime but are not resolved by charter activation, and says the correct fix is a single recursion authority. | The recursion-parity defect survives on this line even though parts of the kind vocabulary were later consolidated. |
| 7062c6740 | feat | applies | Current `src/charter/_drg_helpers.py:136-145` states cascade reads root-level `*.graph.yaml` only and that edges authored only in `drg/fragment.yaml` are invisible; `src/specify_cli/doctrine/pack_validator.py:118-123` flags only `drg/*.graph.yaml`, not `fragment.yaml`. | Org `drg/fragment.yaml` edges still do not enter the charter cascade and the validator still misses the fragment-only shape. |
| 3b87cc843 | fix | applies | Current `src/charter/synthesizer/project_drg.py:45-49` maps only `directive`, `tactic`, and `styleguide`; `src/charter/synthesizer/project_drg.py:172-177` skips every unsupported target kind, and `src/doctrine/drg/project_scan.py` is absent. | Project-tier `agent_profile` artifacts still are not emitted as cascade-reachable project DRG nodes. |
| 4b03c48f2 | fix | applies | Current `_ActionDoctrineBundle` has no glossary-pack slot at `src/charter/action_doctrine_bundle.py:54-60`; `src/charter/context_renderers/delivery_table.py:113` maps `NodeKind.GLOSSARY_PACK` to `slot=None`; `src/charter/context.py:486-494` omits glossary packs from JSON delivery. | The loaded-doctrine delivery gap survives for glossary packs even though procedures/assets and several renderer seams have moved on. |
| b8d66d95b | fix | applies | Current JSON delivery calls `build_disclosure_payload` with only directives, tactics, styleguides, toolguides, procedures, and assets at `src/charter/context.py:486-494`; there is no `glossary_pack_ids` carrier on `_ActionDoctrineBundle` at `src/charter/action_doctrine_bundle.py:54-60`. | Delivered glossary packs still cannot appear in `charter context --json` `references[]`. |
| 7bc586dd3 | fix | applies | Current `build_charter_context_include` gates only `agent_profile` through `_build_activation_aware_doctrine_service` at `src/charter/context.py:353-367`; `_render_doctrine_artifact_include` has no `glossary_pack` renderer at `src/charter/context_renderers/template_include.py:228-265`. | `--include glossary-pack:<id>` still neither routes through the activation gate nor has a renderer. |
| 5d70ff380 | fix | obsolete | Current `_build_activation_aware_doctrine_service` signature has no `agent_profile_overlay_dir` parameter at `src/charter/doctrine_service_builder.py:159-201`, and current raw `DoctrineService.__init__` accepts no such parameter at `src/doctrine/service.py:25-30`. | The monkeypatch seam this fixed no longer exists in the current builder/API shape. |
| ee43f2d95 | feat | applies | Current `BOOTSTRAP_ACTIONS` remains a hard-coded four-token set at `src/charter/context.py:113`; non-bootstrap actions return compact before resolving action-node membership at `src/charter/context.py:253-261`, while action bundle resolution is only reached afterward at `src/charter/context.py:268-277`. | Action delivery is still gated by the four-token vocabulary, not by declared DRG `action:<type>/<action>` nodes. |
| 25fdb08ca | fix | applies | Current `resolve_mission_type_context` still uses project-wide `_project_has_doctrine_overrides(repo_root)` at `src/charter/mission_type_profiles.py:633-656`; `src/charter/mission_type_profiles.py:1238` retains that helper instead of a per-type governance-profile probe. | Unknown mission-type tolerance is still project-wide rather than tied to a matching project/org `governance-profile.yaml`. |
| 968ff863b | feat | applies | Current `FetchResult` has no `unchanged` or `etag` field at `src/specify_cli/doctrine/sources/protocol.py:30-50`; `HttpsBundleSource._headers()` sends only authorization at `src/specify_cli/doctrine/sources/https_source.py:150-157`; `fetch()` treats any status `>=400` as error and never handles `304` at `src/specify_cli/doctrine/sources/https_source.py:62-81`. | HTTPS doctrine fetches still cannot perform ETag conditional refresh or report an unchanged snapshot. |
| fcb45d66e | fix | applies | Current HTTPS fetch sets `pack_version = self.ref or response.headers.get("ETag")` at `src/specify_cli/doctrine/sources/https_source.py:139`, but there is no persisted `etag` manifest field in `src/specify_cli/doctrine/snapshot.py:191-197` and no stored-ETag forwarding before `source.fetch()` at `src/specify_cli/doctrine/snapshot.py:90-97`. | The conditional-refresh hardening is still absent because ETag state is not stored separately or replayed into the next request. |
| 48e3c7fd4 | fix | hostile | Current `compute_pack_manifest_hash` is exported from `src/specify_cli/doctrine/pack_manifest.py:282-295`; `tests/architectural/test_no_dead_symbols.py:185-190` records the unified pack-manifest hash surface as a declared public API. | Porting the demotion would remove a public manifest-hash API the current line intentionally preserves. |
| 77abd0557 | fix | superseded | Current `src/specify_cli/doctrine/pack_manifest.py:131-142` defines `compute_pack_manifest_hash`, `src/specify_cli/doctrine/pack_manifest.py:145-156` uses it, and `src/specify_cli/doctrine/pack_manifest.py:282-295` exports it. | The public manifest hash API already exists on this line. |
| 0988e70ab | fix | applies | Current `resolve_org_expected_artifacts` looks for `org_root / mission_type / expected-artifacts.yaml` at `src/charter/org_expected_artifacts.py:51-57` and `src/charter/org_expected_artifacts.py:80-83`, not under `missions/<type>/`. | Org-tier expected-artifacts are still anchored at the wrong path for the current pack layout. |
